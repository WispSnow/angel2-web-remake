# 项目推进与 AI 协作使用指南

本文件回答三个日常问题：下一步如何决定、什么时候运行哪些命令、什么时候值得使用多个
Agent。当前里程碑和冻结边界始终以 [`planning/STATUS.md`](planning/STATUS.md) 为准；本文件
只描述长期稳定的使用方法，不记录容易过期的当前关卡状态。

下一款逆向复刻项目的完整方法论见
[`ref/next-project/README.md`](ref/next-project/README.md)。

## 1. 项目不是由命令推进的

项目由“用户决定 + 里程碑合同”推进，`pnpm` 命令只负责运行、生成和验证。通常可以直接用
自然语言给 Codex 下达任务，不需要用户亲自组合命令。

```text
用户确定方向或接受结果
→ Agent 读取状态、证据和合同
→ Agent 完成获授权工作
→ Agent 运行相称的生成／测试／构建
→ 用户从普通入口试玩并接受，或登记反馈
```

`pnpm docs:check` 不会自动修改状态、授权关卡或生成规格。它只检查现有文档是否彼此一致。

## 2. 每次开始任务的标准入口

1. 阅读 [`AGENTS.md`](AGENTS.md)；
2. 阅读 [`planning/STATUS.md`](planning/STATUS.md) 的当前焦点、下一步和冻结边界；
3. 阅读 STATUS 指向的当前里程碑；
4. 根据任务读取对应原版证据、复刻设计和系统／关卡合同；
5. 运行 `git status --short --branch`，保留现有工作；
6. 只处理用户授权范围，完成后运行最低充分验证。

不要依据完成百分比或代码是否已经存在决定范围。`specified` 不等于授权实现，实验室开放
某种表现也不等于相应战役系统已经解冻。

## 3. 里程碑状态如何使用

```text
frozen → specified → authorized → in-progress → verified → accepted
```

| 状态 | 含义 | 谁能推动 |
| --- | --- | --- |
| `frozen` | 尚未授权实现，只能取证、补规格或提出拆分 | 用户允许研究即可 |
| `specified` | 纸面合同闭合，但仍不能写运行时代码 | Agent 完成规格 |
| `authorized` | 用户明确授权指定范围 | 用户决定 |
| `in-progress` | 已授权范围正在实现 | Agent 执行 |
| `verified` | 自动技术门禁通过 | Agent 提供验证证据 |
| `accepted` | 必要的普通入口试玩／产品确认通过 | 用户决定 |

每个里程碑文件头的 `状态` 是该里程碑的状态真值。状态变化时还要同步 STATUS、ROADMAP、
设计验收和直接相关文档，然后运行：

```bash
pnpm docs:check
```

## 4. 推进一个新关卡

### 4.1 先建立纸面合同

在没有实现授权时，可以要求：

> 先取证并建立下一关纸面合同，不实现运行时代码。闭合地图、阵容、目标、剧情、事件、
> 存档、路由、UI、音画和验收场景；保留所有 TBD 和冲突等待决定。

纸面阶段应产生：

- 原版机器证据与 Evidence ID；
- 关卡垂直切片；
- 新系统合同；
- 原版怪癖与复刻决定；
- 存档版本和冻结终点；
- 可执行验收场景；
- 无实施必需 `[TBD]` 的结论。

纸面完成后运行 `pnpm docs:check`。该命令通过只表示文档一致，不自动获得实现授权。

### 4.2 明确授权后实现

用户可以说：

> 我接受纸面合同，授权按该合同有界实现；不要解冻下一关或未被本关消费的系统。

实现阶段通常依次完成：

1. 从机器证据生成稳定运行时内容；
2. 扩展关卡 manifest、模拟、存档和调试场景；
3. 接入 Phaser、DOM UI、音频和剧情表现；
4. 添加或更新定向单元测试；
5. 添加或更新定向 Playwright 验收；
6. 查看代表性截图／音画结果；
7. 运行 `git diff --check` 和最终工作区检查。

### 4.3 自动验证后人工接受

自动测试通过后，由用户从普通 `/` 使用玩家可见控件试玩。可以回复：

> 本关接受，请关闭人工门禁并同步状态。

或者提供具体反馈。反馈应尽量包含玩家看到的现象、触发步骤、预期行为和是否来自原版记忆；
Agent 负责复核证据，不能仅因反馈看起来合理就改写 `[OF]`。

## 5. 常用命令

| 命令 | 用途 | 使用时机 |
| --- | --- | --- |
| `pnpm dev` | 从普通入口运行游戏 | 人工试玩和真实流程验收 |
| `pnpm dev:debug` | 打开战役调试中心 | 定向进入关卡状态和复现问题 |
| `pnpm dev:arena` | 打开全地形竞技场 | 组合验证职业、AI 和技能 |
| `pnpm dev:combat` | 打开全景战斗实验室 | 动画、方向、声音和时间轴检查 |
| `pnpm dev:techniques` | 打开地图技能实验室 | 技能规则与表现检查 |
| `pnpm dev:portraits` | 打开肖像实验室 | 眨眼、口型和布局检查 |
| `pnpm dev:promotions` | 打开转职实验室 | 阈值、候选和身份同步检查 |
| `pnpm docs:check` | 检查里程碑状态、索引和文档链接 | 修改规划、规格和状态后 |
| `pnpm content:stageN` | 从证据重新生成指定关卡内容 | 来源、生成器或生成内容变化后 |
| `pnpm exec vitest run <file>` | 定向规则测试 | 修改模拟、内容、AI、PRNG 或存档后 |
| `pnpm exec playwright test <file>` | 定向浏览器验收 | 修改 UI、输入、剧情或玩家流程后 |
| `pnpm test:e2e:visual <file>` | 生成显式视觉审计证据 | Canvas、动画、HUD 或布局改变后 |
| `pnpm build` | TypeScript 与生产构建 | 源码、类型、资源引用或打包改变后 |
| `pnpm build:release` | 重建只含玩家入口的 `release/` | 用户确认形成发布候选后 |
| `pnpm check` | 完整覆盖率、构建和 E2E 门禁 | 发布候选或用户明确要求全量验证时 |

日常任务默认运行定向测试，不把每次小改动都升级成完整 `pnpm check`。具体源码到测试文件的
责任映射见 [`tests/README.md`](tests/README.md)。Cloudflare Pages 的发布授权、release 预览、
上传、线上验证与回滚见 [`docs/release-and-deployment.md`](docs/release-and-deployment.md)。

蓝图中出现的 `pnpm gate:spec`、`gate:implement`、`gate:accept` 和 `milestone:set` 是未来候选，
当前项目没有实现这些命令。

## 6. 多 Agent 的默认策略

当前项目不需要长期运行多个独立写入 Agent。默认使用：

```text
1 个主 Agent：范围控制、整合和唯一写入负责人
＋
按需使用 1–2 个只读子 Agent：取证或 QA
```

主 Agent 负责：

- 读取用户授权并维护当前任务边界；
- 整合证据和产品决定；
- 修改共享规格、代码和状态；
- 选择验证范围并交付最终结果。

只读取证 Agent 负责：

- 独立检查二进制、机器 JSON、原版录像和专题笔记；
- 返回 Evidence ID、结论、冲突和未知项；
- 不修改复刻规则或运行时代码。

只读 QA Agent 负责：

- 审查改动路径和测试缺口；
- 运行定向测试、浏览器流程和视觉检查；
- 报告玩家所见、复现步骤、影响和责任子系统；
- 不直接把自己的发现改成产品决定。

## 7. 什么时候使用并行 Agent

适合并行：

- 新关卡的地图／阵容、剧情／表现、存档／路由可以独立取证；
- 大型改动需要分别审查模拟、表现和测试缺口；
- 全职业、全技能或大量资源需要矩阵核验；
- 日志、截图和大型文档需要分片分析。

不适合并行：

- 小型 bug 或单文件修改；
- 多个 Agent 会同时修改控制器、存档 schema、STATUS 或同一规格；
- 产品决定尚未确定；
- 后一步依赖前一步结论；
- 协调成本高于实际工作量。

并行读操作可以共享当前工作区。确需并行写入时，必须使用独立 worktree／分支，明确各自允许
路径和验收命令，最后由主 Agent 合并；不要让多个 Agent 在同一工作区修改同一真值文件。

## 8. 可直接使用的委派提示

### 下一关取证与规格

> 主 Agent 负责整合并保持代码冻结。委派两个只读子 Agent：一个核查地图、部署、阵容、
> 目标和 AI；另一个核查剧情、事件、音画与路由。等待两者完成后建立纸面合同，列出冲突和
> TBD，不实现运行时代码。

### 已批准合同的实现

> 只使用一个写入 Agent 按已批准合同实现。可以另用一个只读 QA Agent 准备验收矩阵和审查
> 测试缺口。不得修改下一关或合同外系统。

### 大型改动复核

> 使用只读子 Agent 分别审查：确定性模拟与存档、Phaser／DOM／音画表现、测试与文档追溯。
> 主 Agent 汇总真实问题，避免多个 Agent 同时修复同一文件。

### 小型试玩反馈

> 先复现并定位反馈，核对原版证据和当前合同；只实现有界修复和回归测试，不启动并行写入
> Agent，不扩大到相邻系统。

## 9. 一次交付应说明什么

每次 Agent 交付至少区分：

- 已完成；
- 已运行并通过的命令；
- 没有运行的验证及原因；
- 仍冻结的内容；
- 需要用户试玩或决定的事项；
- 最终工作区是否包含意外生成物。

自动测试、调试场景和实验室不能代替普通入口人工接受；人工接受也不能代替规则、存档和
确定性测试。两者同时成立，里程碑才真正闭环。
