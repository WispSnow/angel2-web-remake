# M13：第 8 关纸面合同

状态：`specified`；纸面门禁完成，已转入 M14 有界实现

更新日期：2026-08-09

## 目标

在实现 `stage-08` 前，闭合“營地遭到偷襲”的三背景关前剧情、固定八人阵容、手动／自动
友军拆分、全灭目标、蘇蘭達保护条件、第 1 回合 SAY 156、原生 SAY 157 调用遗漏、普通
保存、v22 和 `stage-09` 冻结边界，使实现不需要从错误目标原文或行为号猜规则。用户试玩后
以 `REMAKE-032` 将 SAY 157 漏播判定为原版 bug；该后续产品修正由 M14 v23 实施，不改写
本里程碑记录的原版事实。

## 输入

- 关卡模板：stage 8 `B/0017`；
- 机器目标：全部 side 2 缺席胜利，side 1 槽 `8` 缺席失败；
- 角色身份：side 1 槽 `8/17/18` 为蘇蘭達、阿曼妮、雷伊拉；
- 剧情／路由：模块 25 SAY `21`、`BK/7→BK/6→BK/8`，第 1 回合 SAY `156`，完成后
  module 27 / stage 9；
- M12 已验证的 runtime manifest、固定战斗、友军自动阶段、剧情背景、胜利保存和调试合同。

## 纸面产物

1. [`stage-08.md`](../../design/remake-gdd/vertical-slices/stage-08.md)：完整时间轴、固定阵容、
   三军团、胜负、剧情、原生遗漏、保存、规则集和 `S08-A–K`；
2. `REMAKE-031`：默认目标按机器条件显示全灭与保护蘇蘭達，不沿用 SAY/0090；
3. v22 与 `stage-09` 只作为 M14 实施合同，不改写历史 v21 含义。

## 模块化约束

- Stage 8 只能通过 runtime manifest 与生成内容登记；控制器、UI、Phaser 和存档层不得按
  关卡号、槽 `8` 或姓名分派目标、军团、剧情或路由；
- 行为 `2` 只进入关卡军团定义，不由控制器动态推断控制权；
- 原版 SAY 157 的不播放由事件证据明确表达；后续 `REMAKE-032` 通过共享胜利剧情事件补播，
  不在控制器新增特判；
- v22 原合同只在普通现场胜利后提供编号保存；M14 现以 v23 承接胜利剧情事件与 v22 迁移，
  完成档仍停在不可玩的 `stage-09` 边界。

## 完成门槛

- [x] 地图、固定阵容、目标、行为、事件、音乐、剧情与路由均链接机器证据；
- [x] SAY 90 与机器目标冲突由 `REMAKE-031` 明确处理；
- [x] SAY 21、156、157 的调用顺序与遗漏边界闭合；
- [x] v22、失败重试、普通保存、完成档和 loaded `1000` 不重播边界明确；
- [x] `S08-A–K` 覆盖确定性、调试场景、视觉验收与无硬编码；
- [x] 无实施必需 `[TBD]`。

## 下一门槛

用户于 2026-08-09 确认第 7 关测试成功并完成，要求继续推进第 8 关，因此 M12 人工门禁与
M14 有界实施授权同时闭合。实现、自动验收和人工接受记录归入
[`M14-stage-08-implementation.md`](M14-stage-08-implementation.md)；第 9 关继续冻结。
