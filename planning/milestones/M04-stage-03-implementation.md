# M04：第 3 关实现

状态：`verified`

授权日期：2026-08-04

授权来源：用户确认第 2 关验收完成，并明确要求继续推进第 3 关

## 目标

从第 2 关完成战役状态真实进入“通過力場”，以证据生成的固定 `13 vs 12` 阵容验证
六名玩家手动角色、七名自动友军、行为 `3/4` 编队跟随、双保护目标、僧侣 `1H/1I`、
两段剧情、双方音乐、v14 战中恢复／入关快照和 `stage-04` 边界。

## 授权范围

1. 从机器产物生成第 3 关地图、地形、固定阵容、目标、剧情、事件、音乐和内容身份；
2. 在通用我方自动调度中接入本关消费的行为 `2/3/4` 与领队／跟随关系；
3. 复用 `1H` 并新增初級回復 `1I` 的玩家、AI、规则、地图表现、声音与存档合同；
4. 修正原版本关目标文字与机器槽位不一致，默认 UI 显示希蜜／黛西双保护条件；
5. 开放第 2 关到第 3 关普通路由，完成失败重试、胜利保存和 `stage-04` 边界；
6. 建立 v14、v2–v13 确定迁移、第 3 关战中恢复及调试／Playwright／视觉验收。

## 继续冻结

- `stage-04` 战斗与后续关卡；
- `2I/3I` 规则及未被第 3 关消费的中高阶技术、状态和职业 AI；
- 全战役批量生成、全 AI 泛化、Mod 系统与发布部署；
- 任何改变第 0–2 关已接受模拟、PRNG、存档语义或表现的重设计。

## 输入合同

- [`stage-03.md`](../../design/remake-gdd/vertical-slices/stage-03.md)；
- [`techniques-stage3.md`](../../design/remake-gdd/systems/techniques-stage3.md)；
- [`turn-action-system.md`](../../reverse/notes/turn-action-system.md)；
- [`ai-decision-system.md`](../../reverse/notes/ai-decision-system.md)；
- [`stage-events-and-campaign-routing.md`](../../reverse/notes/stage-events-and-campaign-routing.md)。

## 实施顺序

### P1：证据生成与关卡合同

- 固化 B/0007、目标、SAY 12/13、音乐 `9→8 / 37→36` 与来源哈希；
- 生成 2,500 格地形、13/12 固定阵容、行为值和稳定语义身份；
- 登记 `REMAKE-017`，目标面板使用实际双保护槽位。

### P2：编队与僧侣技术

- 自动友军保持在手动阶段后执行，行为 `3/4` 先应用领队／跟随规则；
- `1I` 完成选择、范围治疗、经验、AI 池、情境对白、255 tick 表现和原子提交；
- 冰封、表现设置与 PRNG 边界继续遵守已接受合同。

### P3：路由、重试与存档

- 第 2 关完成路由真实建立第 3 关；失败／撤退从不可变快照重建；
- v14 严格保存第 3 关战况与入关快照，并确定迁移 v2–v13；
- 现场 `999` 播放 SAY 13，完成档 `1000` 直达 `stage-04`。

### P4：调试与完整验收

- 调试中心登记关前、固定准备、玩家回合、胜利准备和完成路由；
- 按 `S03-A–K` 增加内容、模拟、存档、控制器、E2E 和视觉覆盖；
- 运行 `pnpm check`、逆向证据校验和 `git diff --check`。

## 退出条件

`S03-A–K` 全部通过、普通 `/` 可从第 2 关完成状态进入第 3 关并到达 `stage-04` 明确
边界、旧档迁移与第 0–2 关零回归后，M04 转为 `verified`；用户人工通关接受后转为
`accepted`。

## 验证结果

- `pnpm check`：323 条单元／覆盖率测试、生产构建与 118 条 Chromium 验收全部通过；
- `node reverse/tools/angel2-phase1-verify.mjs`：证据基线通过，实施必需未知项为 0；
- `S03-A–K`、第 2 关续入、第 3 关胜败／保存／`stage-04` 边界及代表性视觉截图已验证；
- 用户试玩发现的“妮雅缺席仍授职”和“范围回复全屏反馈”已按 `REMAKE-018/019` 修复并回归；
- M04 当前为 `verified`，仍需用户从普通 `/` 人工通关后才能转为 `accepted`。
