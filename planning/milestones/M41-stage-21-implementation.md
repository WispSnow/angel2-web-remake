# M41：内部第 21 关有界实现

状态：`accepted`

更新日期：2026-08-11

## 范围

按 M40 实现内部 stage 21“焦土森林村莊外”：证据生成内容、SAY `42–44`、四名斥候
生成与脚本移动、直接完成路由、v43 迁移及调试／验收场景。

## 门禁

- [x] 生成器校验 B/0043、标题、剧情、空模板、事件、音乐、五条动态生成通道与工兵 token；
- [x] 妮雅、希蜜、葛蒂拉斯、蘇蘭達按战役 roster 继承职业／生命／经验并按原版顺序移动；
- [x] 过场结束直接路由 stage 22，不进入玩家回合、普通胜利反馈或存档提示；
- [x] v43/v42 与 stage 21 完成档严格校验；
- [x] 关前与完成路由调试场景接入；三套 debug roster 延续第 20 关成长与必经职业基线，
  不把四名斥候回退为士兵；
- [x] SAY/0043 点名段保留原版“只开肖像、不开文字窗”的五个检查点；共用故事编译器与
  对白层同步修正，第 20 关 SAY/0074 的龍王石像因此一并恢复；
- [x] 四名斥候不带 `80h` 行动位，过场中不显示已行動徽章；写入结束按原版
  `focusPortraitResource 46` 把右栏停在妮雅；
- [x] `initialOrigin` 写成 `originBounds` 允许的实际值，不依赖相机钳制；
- [x] 全量 Vitest、全量 Chromium、四张正式截图视觉审计与生产构建通过；
- [x] 用户于 2026-08-11 确认第 21 关测试通过并授权推进第 22 关。

## 遗留

- stage 22 已由 M42/M43 授权；stage 23 及后续内容保持冻结；
- 不把本关的一次性四人写入外推为通用增援或可交互战斗授权；
- `story-reinforcements` 的 `actionSpent` 默认仍为已行动，第 6/20 关沿用原状；只有
  确认不进入任何阵营阶段的过场才应关闭它。
