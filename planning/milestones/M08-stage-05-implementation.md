# M08：第 5 关有界实现

状态：`accepted`；用户于 2026-08-08 确认测试成功并要求推进第 6 关

更新日期：2026-08-08

## 授权与目标

用户于 2026-08-08 明确确认“第4关接受，推进第5关”。本里程碑按
[`stage-05.md`](../../design/remake-gdd/vertical-slices/stage-05.md) 的 `S05-A–N`，实现
“遭遇丁塔琪”与原版场景 42“异世界之门”的连续闭环，并只把第 6 关登记为冻结路由终点。

## 已完成范围

1. 从 B/0011、B/0085、SAY `9/10/11/18/20/19`、音乐目录和 side 2 角色表生成稳定运行时
   内容、地图、缩略图、动态地形与音频资产；
2. 通过唯一 runtime manifest 接入第 5 关直接部署、固定 1 + 可选 7／最多 6 人、十四名
   敌军、双首领任一离场胜利与妮雅离场失败；
3. 以通用事件效果串联场景 42 的两次脚本移动、四段剧情、正式 `4L` 表现／结算、琴斯与
   女帝非死亡离场及 `stage-06` 路由；
4. 保存 schema 升至 v19，冻结 v18 含义并增加 v18→v19 迁移；第 5 关普通胜利可保存，
   场景 42 不出现第二次保存提示；
5. 调试中心登记部署、玩家回合、两名首领近胜、妮雅近败、胜利准备、第 5 关完成路由、
   场景 42 现场与完成路由；
6. 减少动态的落雷使用三段可辨识时间线，保留 304 native tick 语义、相同伤害、事件、
   roster、PRNG 与路由；
7. 控制器、UI、Phaser 与存档层不按第 5 关编号、场景 42 编号、首领姓名或剧情槽位分派。

## 自动验收

- [x] 内容生成、部署、阵容、胜负、AI 优先级和场景 42 台阵通过定向 Vitest；
- [x] v19 当前 schema、v18 迁移和第 5 关／场景 42 保存语义通过定向 Vitest；
- [x] `stage5.spec.ts` 覆盖 `S05-A–L`，包括双首领独立胜利、保存、304 tick 落雷、非死亡
  离场、减少动态与 `stage-06` 冻结边界；
- [x] 第 4 关进入第 5 关部署、调试中心目录、类型检查、生产构建与证据校验通过；
- [x] 部署、SAY 9、正常落雷、离场剧情与减少动态代表性截图完成人工视觉检查；
- [x] `git diff --check` 与最终工作区范围检查通过。

## 人工接受门槛

- [x] 用户从普通 `/` 完成第 5 关并确认：部署、目标、SAY 9／10、战后保存、异世界之门
  过场与最终第 6 关边界符合预期，或登记有界反馈。

## 试玩反馈

- `M08-FB-01`：复核用户报告的“第 5 关每回合生命减半”。原版 stage 5 事件处理器的
  `specialCallCounts` 为空，运行时也没有 `route-pulse`；新增回归锁定无中毒单位换回合生命
  不变。第 5 关若出现减半，只能来自单位明确持有的“中毒”状态，不继承第 4 关力场。
- `M08-FB-02`：修复自动魔弓射击把同码 `1I` 误投影为初級回復生命字幕。AI 字幕现在先
  校验动作种类，弓兵、弩兵、魔弓兵三种射击均不进入技术宣言分派。同步复核发现敌方
  弓兵系未进入射击规划、会退化为普通近战；现已让敌我自动行动统一使用三种正式射击。

## 继续冻结

M08 完成时第 6 关仍未属于本里程碑；用户接受 M08 后另行授权的第 6 关工作记录在 M09/M10。
