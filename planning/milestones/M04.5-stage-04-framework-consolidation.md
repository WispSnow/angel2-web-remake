# M04.5：第 4 关前框架收口

日期：2026-08-04

状态：`verified`

## 授权与范围

用户在第 3 关人工验收后要求先全面检查框架，并于 2026-08-04 确认“按建议推进”。本里程碑
只重构第 0–3 关已经接受的装配、存档、固定关卡构造、动作表现与开发入口，不实现
`stage-04` 战斗、内容、新存档状态或仍冻结系统。

## 已完成

- 以 `stage-runtime.ts` 建立唯一可玩关卡装配清单，统一延迟加载、进入、部署适配、恢复、
  重试能力、完成路由、资产、动作表现能力和当前存档逐关元数据；
- `GameController` 的普通固定关卡进入、恢复、重试、完成写档和调试直通不再增加逐关分支；
  第 0 关跳过关前剧情的重试通过有类型能力保留；
- 当前 v14 schema、v2–v13 冻结迁移链和 20 槽 repository 分目录维护；历史迁移语义未合并；
- 第 2、3 关共用纯模拟固定关卡单位／scenario 构造器；
- AI 动作与规划类型移入无实现依赖的 `ai-contracts.ts`，非生成 TypeScript 依赖环归零；
- 共用动作 API 改为语义目录，Phaser 按关卡声明的可达动作加载地图表现；地图单位资产改为
  `side-classId` 键值表；
- 调试中心顺序读取关卡清单，场景建立使用完整 factory map；根内容生成命令由单一生成清单编排；
- Stage 4 冻结终点卡读取当前关卡完成元数据，不再手写第 1／2／3 关映射。

## 验证

- `pnpm content`：通过，全部生成器重跑且无生成内容差异；
- `pnpm check`：通过；最终 339 项 Vitest、覆盖率门槛、生产构建与 121 项 Chromium 验收全部通过；
- `node reverse/tools/angel2-phase1-verify.mjs`：证据基线通过，实施必需未知项为 0；
- 人工查看 `debug-hub.png` 与 `stage3-stage4-boundary.png`，调试目录和冻结终点构图正常；
- `git diff --check`：通过。

## 继续冻结

- `stage-04` 关卡事实、纸面合同、阵容、规则、剧情、AI、素材与可玩入口；
- 本次没有关卡需求消费的射击、状态、深层转职和其他全战役能力；
- 存档仍为 v14；只有新增持久化语义时才允许新建版本和迁移。
