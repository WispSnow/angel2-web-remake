# M09：第 6 关纸面合同

状态：`specified`；纸面门禁完成，已转入 M10 有界实现

更新日期：2026-08-08

## 目标

在实现 `stage-06` 前，闭合“過異世界之門”的反向关前流程、九人部署、九名敌军、
西艾蕾首领目标、胜利后的游骑兵动态台阵、普通保存、v20 和 `stage-07` 冻结边界，使实现
不需要从槽位、姓名或 DOS 事件顺序中猜规则。

## 输入

- 关卡模板：stage 6 `B/0013`；
- 机器目标：side 2 槽 `19` 缺席胜利，side 1 槽 `0` 缺席失败；
- 角色身份：side 2 槽 `19`“西艾蕾”，side 1 槽 `17`“阿曼妮”；
- 剧情／路由：部署后模块 25 SAY `14`，开战 SAY `15`，现场胜利 SAY `16`、动态台阵、
  槽 `17` 移动、SAY `115`，完成后 stage 7；
- M08 已验证的 runtime manifest、通用部署、剧情、脚本移动、胜利保存和调试中心合同。

## 纸面产物

1. [`stage-06.md`](../../design/remake-gdd/vertical-slices/stage-06.md)：完整玩家时间轴、部署、
   阵容、胜负、双背景剧情、台阵、保存、规则集和 `S06-A–L`；
2. `REMAKE-028`：默认目标按机器槽显示“擊敗西艾蕾”，不沿用 SAY/0088 的“愛”；
3. v20、动态剧情增援投影与 `stage-07` 只作为 M10 实施合同，不改写历史 v19 含义。

## 模块化约束

- Stage 6 只能通过 runtime manifest 与生成内容登记；控制器、UI、Phaser 和存档层不得按
  关卡号、槽 `19/17` 或姓名分派目标、台阵、移动或路由；
- 关前背景切换来自对白页的证据元数据，属于表现状态，不进入模拟或存档；
- 台阵单位使用剧情派生 ID，不替换当前部署单位，不写回战役 roster，也不参与行动、胜负、
  经验、转职或状态结算；
- v20 只在完整现场胜利演出后提供普通编号保存；完成档停在不可玩的 `stage-07` 边界。

## 完成门槛

- [x] 地图、部署、阵容、目标、逐槽行为、事件、音乐、剧情与路由均链接机器证据；
- [x] SAY 88 与机器目标冲突由 `REMAKE-028` 明确处理；
- [x] SAY 14 的 `BK/5→BK/31` 切换及现场胜利台阵、移动顺序闭合；
- [x] v20、失败重试、普通保存、完成档和 loaded `1000` 不重播边界明确；
- [x] `S06-A–L` 覆盖确定性、调试场景、视觉验收与无硬编码；
- [x] 无实施必需 `[TBD]`。

## 下一门槛

用户于 2026-08-08 确认第 5 关测试成功并要求继续推进下一关，因此 M08 人工门禁与第 6 关
有界实施授权同时闭合。实现、自动验收和人工接受记录归入
[`M10-stage-06-implementation.md`](M10-stage-06-implementation.md)；第 7 关继续冻结。
