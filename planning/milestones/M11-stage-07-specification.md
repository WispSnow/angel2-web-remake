# M11：第 7 关纸面合同

状态：`specified`；纸面门禁完成，已转入 M12 有界实现

更新日期：2026-08-09

## 目标

在实现 `stage-07` 前，闭合“來到異世界”的双背景关前剧情、双固定单位部署、十一名敌军、
萊莉首领目标、普通保存、v21 和 `stage-08` 冻结边界，使实现不需要从目标原文、槽位或
DOS 事件顺序中猜规则。

## 输入

- 关卡模板：stage 7 `B/0015`；
- 机器目标：side 2 槽 `18` 缺席胜利，side 1 槽 `0` 缺席失败；
- 角色身份：side 2 槽 `18`“萊莉”，职业 `13`“陸戰騎士”，肖像 `D/19`；
- 剧情／路由：模块 25 SAY `17`、`BK/6→BK/7`，部署后直接战斗，完成后 stage 8；
- M10 已验证的 runtime manifest、通用部署、剧情背景、胜利保存和调试中心合同。

## 纸面产物

1. [`stage-07.md`](../../design/remake-gdd/vertical-slices/stage-07.md)：完整玩家时间轴、部署、
   阵容、胜负、双背景剧情、保存、规则集和 `S07-A–K`；
2. `REMAKE-030`：默认目标按机器槽 `18` 判定胜负。其显示文案条款已由 `REMAKE-089` 取代——
   `REMAKE-051` 证实本关原文是 SAY/0109 而非 SAY/0089，目标面板现逐字引用该原文；
3. v21 与 `stage-08` 只作为 M12 实施合同，不改写历史 v20 含义。

## 模块化约束

- Stage 7 只能通过 runtime manifest 与生成内容登记；控制器、UI、Phaser 和存档层不得按
  关卡号、槽 `18` 或姓名分派目标、部署、剧情或路由；
- 关前背景切换来自对白页证据元数据，属于表现状态，不进入模拟或存档；
- 敌军身份与职业必须分别来自角色目录和 B/0015，不以原文“妖龍”覆盖机器单位；
- v21 只在普通现场胜利后提供编号保存；完成档停在不可玩的 `stage-08` 边界。

## 完成门槛

- [x] 地图、部署、阵容、目标、逐槽行为、事件、音乐、剧情与路由均链接机器证据；
- [x] SAY 89 与机器目标冲突由 `REMAKE-030` 明确处理；
- [x] SAY 17 的 `BK/6→BK/7`、部署前顺序与无开战／胜利剧情边界闭合；
- [x] v21、失败重试、普通保存、完成档和 loaded `1000` 不重播边界明确；
- [x] `S07-A–K` 覆盖确定性、调试场景、视觉验收与无硬编码；
- [x] 无实施必需 `[TBD]`。

## 下一门槛

用户于 2026-08-09 确认第 6 关已试玩并接受，要求继续推进第 7 关，因此 M10 人工门禁与
第 7 关有界实施授权同时闭合。实现、自动验收和人工接受记录归入
[`M12-stage-07-implementation.md`](M12-stage-07-implementation.md)；第 8 关继续冻结。
