# M75：隱藏 stage-38 有界实现

状态：`verified`，等待用户从普通入口试玩

M75 承接已接受的 M74 合同，只实现主线结局后的最终隐藏战与模块 46 终端，不解冻其他
冻结内容，也不改变 v73 存档 schema。

- [x] B/0077 证据生成内容、stage runtime manifest、地图／小地图／构筑图块与 `MUSIC/33→32`、
  `MUSIC/5→4` 已接入；
- [x] 妮雅／希蜜固定槽、18 个开放格、44 名静态敌军、五通道无增援、全灭／妮雅失败优先与
  SAY/0164/0165 已接入正式模拟；
- [x] 44 名敌军按原版 side 2 描述符投影：槽 `2..7,15..24` 的 16 名历代角色显示原名与
  专属肖像，其余 28 名 `FFh` 描述符单位回退为职业名与职业肖像；
- [x] SAY/0164 前按原版聚焦妮雅；44 名静态敌军在悼词前已存在，调试中心提供独立开场入口；
- [x] stage 49 边界只通过玩家可见控件进入 stage 38；完成后使用 `stage-39` 终端标识，
  只恢复模块 46，不把不可达 `module27/stage38` 写入当作重玩路由；
- [x] 片尾由证据生成器固化 `B/88`、`C/33`、`UN/54`、`UN/55` 与 `ending-presentations.json`：
  八次连续 400 步转场自动执行，忽略点击／Enter／Space，`The End` 按 1/400/1、10、80、
  10+10×10、10、10 native tick 顺序永久循环；片尾窗口与其它关卡页面一致水平居中；
- [x] v73／v72 相关保存、路由、调试成长档和普通 `/` 入口边界未新增字段；
- [x] 定向 Vitest、TypeScript、stage 38／stage 49 Chromium、调试中心 E2E、生产构建与视觉
  审计通过；
- [ ] 用户从普通 `/` 完成隐藏关并确认终幕可接受。
