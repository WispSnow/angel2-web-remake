# M57：第 29 个可玩关卡（内部第 30 关）有界实现

状态：`accepted`

更新日期：2026-08-13

## 目标

实现 stage 30 的三段剧情、固定战斗、确定性多形态链、最终女帝归队、v59、调试场景与
stage 31 冻结路由。

## 退出条件

- [x] 从机器证据生成地图、剧情、单位、形态、目标、音乐和内容身份；
- [x] 三名固定我方、开场女帝转士兵和共享专家 AI 接入；
- [x] 普通／反击／特殊致死的形态台词、重建与最终归队接入；
- [x] SAY/0057／0058／0059、胜负、失败重试与 stage 31 路由接入；
- [x] v59/v58/v57 严格存档与迁移接入；
- [x] 调试场景、两套成长档和测试说明接入；
- [x] 定向 Vitest／Playwright、截图、构建、文档、证据与 diff 门禁通过。

浏览器验收另发现并修复了对话来源从 SAY 页面切到 `battle-context` 时，逐字渲染键只含
阶段／页码而残留旧文字的问题；现在来源记录、等待号与地址也参与页面身份。M57 自动门禁
已通过。`M57-FB-01` 根据用户试玩纠正连续变形的角色画像：原生槽 27 描述符固定为 D/41，
职业记录变化不会触发 `FFh` 通用画像回退；实现、v59/v58 存档迁移、调试夹具、浏览器断言与纸面
合同均改为全形态保持女帝插图。`M57-FB-02` 针对“通关保存后对白中途跳转”反馈，
把 stage 29 通关写档后的 SAY/0057 十七个停顿全部纳入回归，且只允许在最后一句完成后
转入 SAY/0058；stage 30 的 SAY/0059 六个停顿、通关保存与从标题读档到 stage 31 边界亦已
通过。用户于 2026-08-13 明确确认测试通过，M57 转为 `accepted`，并授权 M58/M59
继续推进内部 stage 31。

## 边界

- 只实现 M56 合同；stage 31 的后续范围由 M58/M59 独立授权；
- 普通 `/` 不依赖调试夹具或测试查询参数。
