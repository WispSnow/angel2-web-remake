# M06：第 4 关有界实现

状态：`implemented`；自动门禁完成，等待用户人工试玩接受

更新日期：2026-08-04

## 授权与目标

用户于 2026-08-04 明确授权 M06。按已闭合的
[`stage-04.md`](../../design/remake-gdd/vertical-slices/stage-04.md) 与
[`force-field-escort.md`](../../design/remake-gdd/systems/force-field-escort.md)，实现第 4 关
“遭遇丁塔琪”的完整可玩闭环，并维持第 5 关冻结终点。

## 实施包

1. 由逆向机器产物生成 B/0009 地图、部署、阵容、剧情、音乐、事件和 `MAGIC/26` 表现目录；
2. 通过唯一关卡 runtime manifest 登记进入、恢复、重试、完成、资产与存档元数据；
3. 让第 1／4 关复用参数化部署投影与阵容构造，不在 UI、控制器或 Phaser 分派关卡号；
4. 新增有类型的 `route-pulse` 描述符，模拟层负责路线、安全区、生命变化、死亡和目标检查；
5. 由表现键消费准备结果，播放 44 native tick 力场效果；速度、减少动态与跳过不改变结算；
6. 将当前存档升级为 v15，补齐 v14 迁移、stage 4 战中／完成恢复与 stage 5 冻结路由；
7. 登记关前、部署、玩家回合、首轮力场、近胜（胜利准备）和完成路由调试场景；
8. 完成 `S04-A–L` 单元、构建、Chromium、普通入口与代表性截图验收。

## 模块化门禁

- 内容参数是槽位、路线、范围、效果和表现的唯一来源；
- 每个单位恰好属于一个显式军团，行为号不推断控制权；
- 控制器只编排通用准备／表现／提交协议；Phaser 不重新计算安全区或受影响单位；
- DOM 部署和目标 UI 只消费 runtime 投影，不读取 `stage-04`、槽 `24` 或角色名；
- v14 和更早 schema 保持冻结，通过独立迁移进入 v15；
- `campaign-route/stage-05` 只显示冻结终点，不加载或实现第 5 关。

## 完成门槛

- [x] 第 4 关内容生成可复现且源哈希、运行时引用完整；
- [x] 部署、三军团、到达目标、保护失败和结界结算通过纯模拟测试；
- [x] `route-pulse` 结算只发生在模拟层的准备／提交边界，表现速度、减少动态和音频不参与结算或 PRNG；
- [x] v15、v2–v14 迁移、战中恢复、失败／撤退重试和完成路由通过；
- [x] 普通 `/` 入口、调试中心流程、键鼠、窄屏和代表性截图通过；
- [x] `pnpm check`、证据校验与 `git diff --check` 通过；
- [ ] 用户完成第 4 关人工试玩并接受。

## 试玩反馈

- `M06-FB-01`（2026-08-04）：第 4 关首次试玩发现葛蒂拉斯使用了未装载的 `ally-magician`
  纹理，同时 `debug.html` 的关卡标题存在重复手写与对齐风险。生成器现固化并复制原版魔术士
  图形，关卡 manifest 显式声明运行时资源；调试列表和会话工具栏统一从关卡 manifest 派生
  标题，并以固定标题列保持第 0–4 关对齐。专项单元、Chromium 与实际截图复验通过。

## 继续冻结

第 5 关内容、后续技术／状态／转职能力和未被本关消费的通用系统不属于 M06。
