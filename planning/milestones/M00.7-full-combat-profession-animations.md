# M00.7：全职业普通全屏战斗动画

状态：`accepted`
范围：只复现普通全屏战斗表现，不解冻第 1 关、部署或后续关卡内容。

自动验收日期：2026-08-01
用户接受日期：2026-08-01

## 顺序门禁

职业按原生记录 `0 → 38` 严格推进。每个记录必须同时满足以下条件，才可把
`src/game/content/full-combat-acceptance.ts` 中的状态改为 `accepted`：

1. 左右职业块的六组命令流已由
   `reverse/parsed/native/combat-presentations.json` 完整导出；
2. direct／`class+50` 原版素材、逐帧 x 锚点和 y-offset 已进入生成内容；
3. 正式运行时按原生命令拆分人物本体与 `G1..G5` 辅助通道，并解释离散镜头、
   `YD/ND` 显示位移和 `EY/NE/UE` 原版通用轨迹；
4. Vitest 逐渲染子步覆盖左右人物、辅助通道、坐标、镜头、显示位移、通用轨迹、
   重复声音槽、格挡、重伤和 24 子步死亡流，并拒绝任何图形帧越界；
5. Playwright 在动画实验室生成攻击、格挡、重伤、死亡四张截图，并人工查看；
6. 窗底左右生命条按原生 210 像素分层槽显示，在先攻／反击命中点更新而不改写冻结的状态框数字；
7. 动画实验室可独立设置双方生命，并以当前职业组合切换格挡／重伤／死亡，不用固定职业专项按钮作为通用入口；
8. 当前记录全部通过后，才开始实现下一个记录。

记录 `36`「龍」、`37`「頭」、`38`「手」保留在 39 项总账中。**2026-08-01 更正**：早先
把它们标为 `not-applicable-original` 是错误结论——当时只检查了 side 1 描述符。原版
这三条记录的 side 2 表现块完整有效（step-count、五槽音效、逐帧锚点、六个命令指针
齐备），右侧图形 `Y_00/36..38` 与 `Y_00/86..88` 也全部存在；缺的只有 side 1，因为
它们永远不会作为 side 1 单位出战。证据见
`reverse/notes/ordinary-combat-presentations.md` 的「记录 36–38 只有 side 2，但
side 2 完整」一节。

这三条与 `0..35` 的区别是**单向**：只有右侧可达，所以按女帝的 `rightOnlyOriginal`
口径验收——只出右侧四状态截图，不做左右双向比对，也不得据左侧空占位创作左侧画面。
2026-08-01 用户授权后已实现并自动验收。

用户随后完成最终手动验收，接受 0–38 的职业表现、格挡／重伤／死亡、方向限制、
音效节奏和原生分层生命条。0–34 为双向记录；35–38 只按原版可达的右侧验收。

同日后续反馈形成的 `M00.7-V11` 已把换攻前与最终非致死收场前的表现保持统一为
`667 ms`；用户复验节奏无误，人工门禁关闭。该调整只影响表现等待，不改变模拟、音效
顺序、PRNG 或存档语义。

## 当前进度

| 原生记录 | 职业 | 状态 | 说明 |
| ---: | --- | --- | --- |
| 0 | 士兵 | 已自动验收 | 原生命令、左右锚点、格挡／重伤／死亡与四张截图已通过 |
| 1 | 魔劍戰士 | 已自动验收 | 人物本体与 G1 魔剑效果分层同步，左右语义与四张截图已通过 |
| 2 | 叢林戰士 | 已自动验收 | 原生跃起／俯冲、入场、受击恢复、左右语义与四张截图已通过 |
| 3 | 魔祭師 | 已自动验收 | 人物、目标与 G1 法阵分层同步，左右语义与四张截图已通过 |
| 4 | 祈導師 | 已自动验收 | 原生跑动、跃起、击后离场、左右语义与四张截图已通过 |
| 5 | 咒術師 | 已自动验收 | 延迟 G1 命中特效、目标声音、左右语义与四张截图已通过 |
| 6–19 | 魔術士～巨龍騎士 | 已自动验收 | 逐记录左右命令流、G1/G5、声音、四状态截图串行通过；巨龍騎士宽帧按真实位图边界裁剪，击后不再提前突隐；命中音改为逐记录重放击后流声音记号，妖龍騎士格挡分支按原生保持无声 |
| 20 | 弓兵 | 已自动验收 | 左右放箭、底部锚点/y-offset、命后 G1、四状态截图通过 |
| 21 | 弩兵 | 已自动验收 | G1 上升与巨箭下坠通道、人物通道 `:S` 绝对底部锚点、左右原生落点命中及四状态截图通过 |
| 22 | 騎兵 | 已自动验收 | 投枪逐帧底部锚点、手部释放高度、抛物线、双段受击与左右四状态截图通过 |
| 23 | 飛馬戰士 | 已自动验收 | 飞马冲锋、左右语义与四状态截图通过 |
| 24 | 修女 | 已自动验收 | 法球主通道、原生目标锚点、左右命中位图接触与四状态截图通过 |
| 25 | 僧侶 | 已自动验收 | 攻击/受击 G1 通道、左右语义与四状态截图通过 |
| 26 | 水戰士 | 已自动验收 | 水流效果贴地高度、左右语义与四状态截图通过 |
| 27 | 神劍戰士 | 已自动验收 | 突进剑击、左右语义与四状态截图通过 |
| 28 | 戰士 | 已自动验收 | 左右锚点、跑跳落地、击后专用收势出窗与四状态截图通过 |
| 29 | 鋼甲戰士 | 已自动验收 | 宽幅防御/冲击效果、左右语义与四状态截图通过 |
| 30 | 祭司 | 已自动验收 | 贴地法术光点、左右语义与四状态截图通过 |
| 31 | 魔導士 | 已自动验收 | 施法者本体、左右语义与四状态截图通过 |
| 32 | 魔法師 | 已自动验收 | 宽幅像素帧、左右语义与四状态截图通过 |
| 33 | 邪劍戰士 | 已自动验收 | 剑光/人物叠层、左右语义与四状态截图通过 |
| 34 | 工兵 | 已自动验收 | 箭形帧底部锚点、左右语义与四状态截图通过 |
| 35 | 女帝 | 已自动验收 | 原版左侧为空、右侧逐字节复用士兵图形；实验室锁定唯一可达方向并提示，右侧四状态截图通过 |
| 36 | 龍 | 已自动验收（仅右侧） | 先攻 `5,5,12,9`、`:X` 交替 `Y_00/86` 两帧、右侧四状态截图通过 |
| 37 | 頭 | 已自动验收（仅右侧） | 先攻 `4,9,5,8,6,3,1,1,1`、`Y_00/87` 7 帧走到帧 6，帧 4–6 的 y-offset `45` 使镰刀收势按原版沉到地面线下并由战斗窗裁剪；右侧四状态截图通过 |
| 38 | 手 | 已自动验收（仅右侧） | 先攻 `6,10,4,4,4,4,4,4,4`、`Y_00/88` 两帧巨拳、`:J/NE` 与三次 `V5` 上下冲击、右侧四状态截图通过 |

连续验收前缀已达到 `0..38`。`0..35` 的既有验收不受影响：重跑导出器后这些记录的导出
结果逐字节不变，只新增了 36–38 的 side 2 命令流与逐帧锚点。全量单元测试、生产构建、
完整 E2E、运行时素材存在性、证据校验均已通过。

实现要点（36–38）：

- 生成器按 `side2.available` 逐侧输出，右侧限定记录不产生 `left` 资产与命令流；
  `reach` 字段进入运行时 profile，左侧访问改为显式抛错而不是静默 `undefined`。
- 移除了三处把记录截断到 `<= 35` 的硬编码：`fullCombatClass` 曾把 36–38 静默降级
  为士兵（记录 0），`ui.ts` 的资产查找曾直接拒绝 `nativeRecord > 35`。
- 实验室的右侧锁定从女帝一条推广为按 `reach` 判定，并同时覆盖守方：右侧限定职业
  当攻方时锁 `side=right`、当守方时锁 `side=left`，攻守同时右侧限定则守方让位为士兵。
  这一并修掉了女帝当守方且 `side=right` 时会被摆到左侧的既有漏洞。
- `E/57` 已随生成器进入 `public/assets/original/audio/e/`，它只被 37/38 引用。

仍未闭合：

- 36–38 的反击（`mainLeftOrAttacker` +50 流）可达性仍是 `[H]` 推论，未实机复验；
  实验室里该流是按“右侧攻方”重放的，不依赖这条推论。
- 场景 20/22/37 本身仍被冻结，本次只做全屏表现，不涉及这些关卡的规则或编队。
