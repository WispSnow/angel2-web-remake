# M07：第 5 关纸面合同

状态：`specified`；纸面门禁完成，已转入 M08 有界实现

更新日期：2026-08-08

## 目标

在不实现 `stage-05` 的前提下，闭合“遭遇丁塔琪”的部署、十四名敌军、双首领任一击破、
普通战后保存、原版场景 42 传送门桥接、v19 和 `stage-06` 冻结边界，使后续实现不需要从
逆向资料或关卡专用分支中猜规则。

## 输入

- 关卡模板：stage 5 `B/0011` 与传送门场景 42 `B/0085`；
- 机器目标：side 2 槽 `25/26` 任一缺席胜利，side 1 槽 `0` 缺席失败；
- 角色身份：side 2 角色表槽 `25`“汀塔琪”、槽 `26`“萊茵”；
- 剧情／路由：SAY `9/10`，stage 5 完成后模块 27 / stage 42；桥接 SAY `11/18/20/19`
  后转 stage 6；
- M06 已验证的关卡 manifest、部署、职业／技术、存档分层、动作表现与调试中心合同。

## 纸面产物

1. [`stage-05.md`](../../design/remake-gdd/vertical-slices/stage-05.md)：完整玩家时间轴、部署、
   阵容、胜负、UI、保存、传送门桥接、规则集和 `S05-A–N`；
2. `REMAKE-027`：默认目标按双首领机器条件显示，并保留标题“丁塔琪”与角色身份“汀塔琪”
   的原版文本差异；
3. `campaign-roster.json` semantic version 3：同时导出并签名 side 1 / side 2 各 60 项角色表，
   对敌方槽 `15/25/26` 建立生成期硬断言；
4. v19、stage 42 与 `stage-06` 只作为后续实施合同，不在 M07 修改存档或开放路由。

## 模块化约束

- Stage 5 和桥接场景只能通过 runtime manifest 与生成内容登记；控制器、UI、Phaser 和存档层
  不按关卡号、槽位或姓名分派目标、身份、落雷、离场或路由；
- 敌方身份从 side 2 角色机器目录生成，职业、地图图形和 AI 继续来自 B 模板与职业目录；
- 双首领条件使用通用目标描述符；军团由内容显式声明，不从行为 `0/1` 反推控制权；
- stage 42 复用正式地图、脚本移动、剧情和 `4L` 规则／表现原语；304 tick 后先提交标准
  落雷伤害，槽 `7/23` 的后续清除再建模为剧情离场，不走死亡或额外伤害链；
- v19 只在 stage 5 普通胜利提供编号保存；完成档重建现场 stage 42，桥接本身不显示第二次
  胜利保存提示；
- Stage 6 只保留冻结路由终点。

## 完成门槛

- [x] 原版地图、部署、阵容、目标、逐槽行为、事件、音乐和双阶段路由均链接机器证据；
- [x] side 2 完整角色表进入生成器与机器规格，汀塔琪／萊茵身份不再手抄；
- [x] 原版 SAY 87 与机器目标冲突由 `REMAKE-027` 明确处理；
- [x] stage 42 的即时胜利、脚本移动、剧情、304 tick `4L`、非死亡离场与 stage 6 路由闭合；
- [x] v19、失败重试、stage 5 完成档和 stage 42 `1000` 重播边界明确；
- [x] `S05-A–N` 覆盖普通入口、双目标、失败、保存、过场、表现确定性、调试场景与无硬编码；
- [x] 无实施必需 `[TBD]`；M07 未创建 Stage 5 运行时内容、存档 schema 或玩家可达路由；
  side 2 证据表扩展只机械重生成 Stage 4 来源指纹，玩法数据保持逐字节等价。

## 下一门槛

用户于 2026-08-08 明确表示“第4关接受，推进第5关”，因此 M06 人工门禁与第 5 关有界
实施授权同时闭合。后续实现、自动验收和人工接受记录归入
[`M08-stage-05-implementation.md`](M08-stage-05-implementation.md)；第 6 关继续冻结。
