# M14.7：魔弓完整箭道控制

状态：`verified`

更新日期：2026-08-09

## 授权与目标

用户指出原版魔弓箭道完全随机，难以发挥线形多目标价值，并明确授权按建议推进
`REMAKE-035`：玩家先选主目标，再指定一条完整合法箭道；专家敌方 AI 直接选择最高价值
确切线路。此能力适用于已开放关卡和竞技场，不解冻第 9 关。

## 实施范围

1. 模拟层按原生范围梯度和方向序枚举全部最短、可传播箭道，不读取玩法 PRNG；
2. 多线路进入独立选择层，单线路直接提交；取消返回主目标，线路成为正式动作意图参数；
3. 战场显示当前实线、其他候选虚线及主／副目标轮廓，底栏提供循环和发射按钮；
4. 键盘方向键或 `Q/E`、鼠标滚轮／按钮、手柄肩键均可切换线路；
5. 专家 AI 对每条确切线路分别估值；独立友军只在既有目标策略内确定性取线路目录首项；
6. 保存身份升级至 v26 / `directed-magic-arrow-1`，v25 战中档和完成档无损迁移。

## 验证

- [x] 线路枚举、显式命中、非法线路拒绝、路径零 PRNG 和专家 AI 确切线路有专项 Vitest；
- [x] v26 当前 schema 与 v25 无损迁移通过保存层定向 Vitest；
- [x] 竞技场覆盖目标→线路→取消→重选→提交，并断言只命中所选线路；
- [x] 代表性线路预览截图人工检查，生产构建与文档门禁完成。

## 继续冻结

不改变射程、伤害、分段防魔、经验、迅龍闪避或表现时序，不重制独立友军的整体目标策略，
不实现 `legacyStrict` 开关，也不解冻第 9 关。未来改变线路合法性或效用权重必须更新规则身份。
