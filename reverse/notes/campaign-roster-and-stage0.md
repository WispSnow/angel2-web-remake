# 战役角色显示身份与第 0 关初始阵容

日期：2026-07-27

## 结论

第 0 关六名我方单位的初始职业与玩家向身份现已闭合：

| 地图顺序 | 槽 | 坐标 | 初始职业 | 原版玩家向单位名 | 肖像记录 | 身份来源 |
| ---: | ---: | --- | --- | --- | ---: | --- |
| 1 | 0 | `(10,23)` | 士兵 | 妮雅 | 46 | 具名角色描述符 |
| 2 | 43 | `(27,25)` | 士兵 | 士兵 | 47 | 当前职业回退 |
| 3 | 42 | `(21,27)` | 士兵 | 士兵 | 47 | 当前职业回退 |
| 4 | 1 | `(28,29)` | 士兵 | 希蜜 | 45 | 具名角色描述符 |
| 5 | 40 | `(27,32)` | 士兵 | 士兵 | 47 | 当前职业回退 |
| 6 | 41 | `(22,37)` | 士兵 | 士兵 | 47 | 当前职业回退 |

所以原版战场 HUD 的身份行分别显示“士兵／妮雅”“士兵／希蜜”，以及四个“士兵／士兵”。后四个不是四名尚未解码的具名角色，也不应由对白擅自命名为宫殿卫士。

第 0 关唯一骑兵是 side 2 槽 15 的具名角色“哈釘”，肖像记录为 15，不是通用骑兵。其职业仍为记录 22“騎兵”，因此地图棋子和职业战斗动作沿用骑兵资源，右栏身份则显示“騎兵／哈釘”。

机器规格位于 [`../parsed/native/campaign-roster.json`](../parsed/native/campaign-roster.json)。

## 新战役文件初值与开战前修正

`GO.EXE` 的 Borland 调试符号把 `ME_DATA` 与 `ME_EXP` 定位在 `046A:0112` 和 `046A:01A8`。两者各为 75 个 16 位字，发布文件中的 150 字节初值全部为零：

- `ME_DATA[0..74] = 0`：所有战役槽起始职业为记录 0“士兵”；
- `ME_EXP[0..74] = 0`：所有战役槽起始累计经验为 0。

第 0 关生成后的 `JUST.TST` 又独立确认六个实际出场槽的职业记录均为 0。因此这里不是仅凭全局默认值推测：新游戏文件初值与逐关生成结果一致。但这些值还不是玩家第一次取得控制时的最终经验和属性。

模块 27 主入口在准备 `JUST.TST` 前调用 `0000:0493`。DS:`0028h` 是无父接口的独立运行哨兵：值为 `4Eh` 时函数直接返回；正常由 GO 父块启动、该值不为 `4Eh` 时，函数遍历 58 个导入的 side-1 槽。职业记录为 0、角色描述符肖像字节不是 `FFh`，且累计经验不高于 299 的单位，会被写为 299 经验。第 0 关因此分成两组：

| 槽与身份 | 开战前经验 | 等级 | 攻击 | 防御 | 生命 | 移动 | 下一级阈值 |
| --- | ---: | ---: | ---: | ---: | --- | ---: | ---: |
| 0 妮雅、1 希蜜 | 299 | 3 | 45 | 27 | `180/180` | 4 | 300 |
| 40–43 四名通用士兵 | 0 | 1 | 39 | 21 | `160/160` | 4 | 100 |

槽 0/1 的具名描述符分别有肖像 46/45，满足经验下限条件；槽 40–43 的描述符肖像为 `FFh`，因此跳过并保留 0 经验。模块 29 的 `0000:536B` 随后按各自累计经验重建属性，并把当前生命写为最大生命。原始动态状态中按槽出现的 `105/108/104/105/106/107` 等当前生命初始化字仍会被覆盖，不能解释为最终值。

这不是姓名直接提供一套角色属性，也不是难度对我方加成；差异来自模块 27 以职业、具名描述符和经验上限共同门控的开战前经验修正。四档难度下这六名我方的上述初值不变。原版 HUD 实机在第 0 关首次控制时也显示妮雅为等级 3、经验 `299/300`、攻 45、防 27、生命 `180/180`，与静态控制流结论一致。

## 具名角色与职业回退

模块 29 `0000:5087` 为 side 1 选择角色显示描述符表 DS:`362Ch`。每项描述符的第 0 字节是肖像记录，第 `+5` 字节开始是 `$` 结尾的 Big5 名称：

- 槽 0 → DS:`36A6h` → 肖像 46、名称“妮雅”；
- 槽 1 → DS:`36B3h` → 肖像 45、名称“希蜜”；
- 槽 40–43 → DS:`38A2h..38C9h` → 肖像 `FFh`、内部占位名 `xxxx18..xxxx21`。

当肖像为 `FFh`，`0000:51B9` 会以当前职业短码扫描 DS:`39A6h` 的“职业短码→通用显示描述符”表，并同时替换肖像和单位名。职业记录 0 的 side-1 短码为 `0A`，对应 DS:`3A44h`：肖像 47、名称“士兵”。因此内部 `xxxxNN` 从不会成为正常玩家向显示名。

这个回退规则也解释了未来转职后的通用槽：其玩家向肖像与单位名跟随当前职业，而不是固定为士兵。

## 第 0 关具名敌方骑兵

模块 29 `0000:5087` 在 side 2 分支把角色显示描述符表设为 DS:`32ADh`。槽 15 的表项位于 DS:`32CBh`，指向描述符 DS:`33E3h`；该描述符第 0 字节为 `0Fh`（肖像记录 15），从 `+5` 开始的 Big5 `$` 结尾名称为“哈釘”。第 0 关模板同时把 side 2 槽 15 的职业记录设为 22“騎兵”，位置为 `(23,32)`。

因此该单位的身份与职业应分开解析：角色肖像/姓名来自 side-2 槽描述符，地图棋子、数值和战斗动作来自当前职业。将其显示为通用“騎士團騎兵”或套用通用敌兵肖像都会丢失原版身份。

## 第 0 关开场镜头

逐关事件让槽 0 妮雅从 `(10,23)` 脚本移动到 `(29,26)`。原版脚本移动每走一格都重画并按常规 `(x-4,y-3)` 偏移重新居中；地图边界允许时，终点对应的 `10×7` 视口原点是 `(25,23)`，可见范围为 X=`25..34`、Y=`23..29`。随后 `SAY/0001` 继承这个战场焦点，剧情结束恢复同一战场层。

因此玩家首次取得控制时的规范状态为：

- 妮雅已在 `(29,26)`；
- 焦点仍指向妮雅；
- 视口原点为 `(25,23)`；
- 回合为第 1 回合；
- 标准单位详情面板在玩家保持聚焦四个更新 tick 后才出现。

等价最短路径的中间格可能受 PIT 余数影响，但最终棋盘、焦点与视口不变。完整逐格移动和聚焦规则见 [`stage-event-presentations.md`](stage-event-presentations.md)。

## 复现命令

```sh
node reverse/tools/angel2-campaign-roster.mjs --extract \
  ref/ANGEL2/GO.EXE \
  reverse/parsed/debug/GO-symbols.json \
  reverse/unpacked/lzexe-modules/raw/0029-unpacked.bin \
  reverse/parsed/saves/TST-decoded.json \
  reverse/parsed/native/unit-descriptors.json \
  reverse/parsed/native/campaign-roster.json
```

生成器校验 `GO.EXE` 与符号表来源哈希、两段 75 字文件初始数组、模块 29 角色加载代码、60 项 side-1 角色指针表、职业视觉回退表、`JUST.TST` 的六个槽/职业/坐标，以及最终显示名与肖像。开战前经验修正及最终属性由 `angel2-battle-lifecycle.mjs` 校验模块 27 `0000:0493` 并写入 `battle-lifecycle.json`。当前仍保持 `implementationFrozen=true`。
