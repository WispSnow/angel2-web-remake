# Web 复刻规则决策记录

日期：2026-07-15  
状态：设计评审中，`implementationFrozen=true`  
原版事实基线：`original-gdd.md` Draft 0.68

## 规则分层

本文件只记录 Web 复刻的产品选择，不改写原程序事实。后续实现应把规则分成三层：

1. `originalFacts`：原程序代码、数据和资源实际表现，作为不可变取证基线。
2. `stableRemake`：默认游玩规则；修复已确认的原版 bug，并保证存档与随机流稳定。
3. `legacyStrict`：可选的原版怪癖兼容模式；只复现安全且确定的差异，不模拟越界写内存等未定义行为。

Mod 规则只能覆盖 `stableRemake` 的显式配置项，不能修改 `originalFacts` 的证据记录。

## 已确认决策

### REMAKE-001：转职属性刷新但不治疗

原版的 `0744h` 提交函数只写新职业并把累计经验清零；紧接着的常规装载再按“新职业 + 经验 0”派生等级、攻击、防御、最大生命、移动和可用行动。因此“提交函数没有立即重算”是低层时序事实，玩家可见结果仍是转职完成后职业属性更新。

`stableRemake` 将这两步合成一个不可观察中间态的原子事务：

- 写入目标职业；
- 当前经验归零；
- 立即按目标职业零经验行重算等级、攻击、防御、最大生命、移动和职业行动；
- 当前生命保持原值，不按新最大生命补满，也不增加生命。

全部 31 条可达转职边中，目标职业首行最大生命均不低于来源职业转职前的第三行最大生命，因此原版数据不会产生“当前生命高于新最大生命”的额外钳制问题。

### REMAKE-002：普通射手原地最优时仍应射击

原版普通射手 AI 会在敌人曼哈顿距离 2 的八个候选落点中选择地形防御最高者，行动者当前格也可参选。若当前格胜出，重定位函数因为没有实际换格而返回失败，外层随即跳过射击；行为 1 则有独立的原地射击路径。

`stableRemake` 把“当前格就是已验证的最佳射击位”视为重定位成功，直接从原地射击；`legacyStrict` 保留跳过射击。该修正不改变玩家射程、候选评分或伤害公式。

### REMAKE-003：远追应对称考虑敌人四邻并使用行动方语义

原版默认远追只检查 `enemyCell + 50`，即 50 列棋盘中敌人下方的一格，完全漏掉上、左、右。行为 2 在追逐前还固定以模式 `A` 建图；side 2 单位因此使用了 side 1 的占用语义作预检查。

`stableRemake`：

- 对称考察敌人上、下、左、右四个合法空邻格；
- 使用当前行动方的阵营/占用语义构建预检查与移动图；
- 保留“本次只追逐移动、不追加攻击”的行为边界；
- 在合法候选之间继续使用明确、可测试的范围梯度与固定平局顺序。

`legacyStrict` 则保留 `+50` 偏向与行为 2 的模式 `A` 门控。

### REMAKE-004：毒不能把生命降到 0

原版状态链执行 `floor(currentLife / 2)`，所以序列可能为 `7→3→1→0`；到 0 后又不调用死亡移除器。这是确认过的代码行为，但作为默认规则属于 bug。

`stableRemake` 使用：

```text
currentLife = max(1, floor(currentLife / 2))
```

毒仍在原有状态 tick 时点结算并递减持续时间，但不会杀死或移除单位。`legacyStrict` 可保留原版“0 生命仍占格”的安全确定行为。

### REMAKE-005：普通物理伤害不受防魔影响

原版由战斗动画开关分成两条普通伤害路径：地图逐点路径会错误读取防魔，全屏路径不读取。该分歧保留在 `originalFacts`，但不是默认规则。

`stableRemake` 规定：

- 普通攻击与反击都是物理伤害，无论使用地图表现还是全屏表现，都忽略防魔；
- 表现设置不得改变伤害、状态或随机规则；
- 每个伤害事件必须显式标记为物理或魔法；所有魔法伤害都读取防魔并被其阻挡，不能因使用地图或全屏表现而绕过；
- 原版炎暴“伤害不检查防魔、事后却清防魔”的路径在 `stableRemake` 中按魔法伤害修复为先阻挡再消耗；
- 冰雪等不造成伤害的魔法效果另用 `effectType` 表达，其位移/免疫规则不冒充伤害类型；
- 魔弓的两段伤害、防魔消耗时点，以及 `WD` 等特殊魔法是否在阻挡后消耗防魔，仍保留各动作已经确认的多段结算顺序。换言之，统一的是“魔法伤害必受防魔检查”，不是把所有动作强行压成同一个单段处理器。

### REMAKE-006：保留魔弓的双半伤结构

魔弓选中目标承受两次半伤。防魔阻挡第一次并被消耗，第二次仍会结算；其他受线形效果波及且有防魔的单位挡住本次半伤并失去防魔。默认复刻保留该规则。

### REMAKE-007：Web 存档保存确定性随机状态

原版 `.TST` 是玩法快照，不保存 PIT 相位、随机状态或输入日志，因此同一存档后重复同样操作，不保证得到相同伤害、AI 选择或等价路径。这不表示存档内容不稳定，只表示它不是确定性重放检查点。

`stableRemake` 规定：

- 模拟层使用固定、版本化的伪随机算法，不直接使用墙钟、帧率或音频完成时点；
- Web 原生存档保存 PRNG 算法版本、完整状态和调用序号；
- 同时保存规则版本及有序 Mod/内容哈希；
- 需要战局重放时另存语义动作日志，普通存档不强制携带完整日志；
- 导入原版 `.TST` 时不伪造不可恢复的 DOS 随机未来，而是从规范化 legacy payload 哈希确定性初始化 Web PRNG；同一原版存档在相同规则/Mod 下将产生稳定的 Web 后续结果；
- 渲染、动画和声音不得消费模拟层随机数。

### REMAKE-008：保留无消费者兼容字段

`DATA.field5`、单位动态记录 `04h/06h` 和其他已确认无规则消费者的兼容字段，在导入、保存和导出时原样保留，但默认不赋予战斗效果。Mod 若使用这些字段，必须声明自己的 schema 与版本，不能声称是原版语义。

### REMAKE-009：同一动作不因阵营或控制方式改变规则参数

用户于 2026-08-02 明确决定：Web 复刻默认规则不保留原版“同一技术由 AI 使用时另取一套
范围或数值参数”的不对称。同一个语义 action ID 应代表同一套能力；玩家手动、我方自动
和敌方 AI 的差异只属于决策策略，不属于动作本身。

原版两张技术表的选择范围参数并不一致：

| 动作族 | 玩家表 | AI 表 | 原版差异 |
| --- | --- | --- | --- |
| 落雷 `1L..4L` | `4/5/6/7` | `3/4/5/6` | AI 全部少 1 |
| 炎暴 `1F..4F` | `5/6/7/7` | `4/5/6/6` | AI 全部少 1 |
| 冰雪 `1C..4C` | `2/3/3/4`（自中心玩家路径不读） | `2/3/4/5`（AI 候选门） | 都不改变固定施法者中心；后两阶只影响 AI 是否找到候选 |
| 治疗 `1H..3H` | `5/6/7` | `5/6/7` | 无差异 |
| 回復 `1I..3I` | `4/5/6` | `4/5/6` | 无差异 |

其余具有玩家/AI 同名入口的技术也已逐项比对：`1V..3V`、`1D..3D` 和八项状态动作的
选择范围完全相同；`WD` 只有 AI 专用入口，不属于同名动作对照。因此没有遗漏其他
“AI 范围少 1”的普通技术族。

射击最大距离本身没有敌我差异，但原版 AI 的两项伤害区间被单独削弱：弩兵玩家为
`70..89`、AI 为 `50..89`；魔弓兵玩家基础 roll 为 `50..69`、AI 为 `50..59`。弓兵
双方均为 `30..49`。

`stableRemake` 规定：

- 同一 action ID 的选择距离、最小距离、效果半径、范围形状、目标合法性、伤害/治疗/
  状态公式、防魔消费、多段结构和提交时点均与阵营及人工/AI 控制无关；
- 以玩家可见动作表和已经建立的语义 action definition 作为统一参数来源，不取敌我两表
  的较大值，也不按阵营动态加减；因此敌方 `1F` 使用与玩家相同的范围 `5`，`1H` 仍为
  双方原本就相同的范围 `5`；
- 落雷和炎暴使用上表玩家侧参数；冰雪不开放目标格选择，玩家和 AI 都固定以施法者
  当前格建立效果。AI 表的距离只用于施放前的敌方候选门，不成为技能 action 的中心参数；
- 弩兵和魔弓兵由 AI 使用时采用与玩家相同的伤害区间；魔弓仍保留 `REMAKE-006` 的双半伤
  结构，只统一基础 roll；
- AI 仍可在技能池、选技能、选目标、移动、评分、平局顺序和失败兜底上与人工操作不同；
  难度对单位属性的既有修正也不由本条取消；
- 没有玩家同名入口的 Boss、部件和逐关脚本动作可保留其专用规则，但必须使用独立 action
  ID 或明确的内容效果，不能暗中覆盖普通动作定义。

`legacyStrict` 保留原版玩家/AI 分表和射击数值差异；冰雪在两种规则集都保持原版的
施法者自身中心。Mod 若改变普通动作
参数，默认同时作用于双方；需要阵营专属能力时必须声明新的语义 action ID 和规则身份。

本决策在 M02 只直接改变第 1 关敌方修女的 `1F` 范围来源；`1H` 数值不变。落雷/冰雪的
敌方入口、弩兵和魔弓兵当前不在 M02 实现范围，以上内容先作为后续切片的设计真值记录，
不据此提前解冻。

### REMAKE-010：校正 D/59 弓兵的休眠眼部坐标错误

原版表 C 的 `DS:0C84` 为 `D/59` 弓兵记录眼部原点 `(40,24)`、口部原点 `(64,32)`。
资源 frame 1 是 `16×16` 的睁眼覆盖片：它在 `(40,24)` 与主肖像不匹配，在 `(56,24)`
则逐像素完全一致。原版剧情语料直接引用的 50 个肖像 id 不含 `59`，单位详情又只绘制
frame 0，因此该错误在发布版可达流程中保持休眠；全角色复刻动画首次让它变成可见错位。

`stableRemake` 将 `D/59` 的眼部原点改为 `(56,24)`，三张眼部覆盖片统一使用该位置；
口部仍保持原版 `(64,32)`。生成目录必须同时记录原始坐标、应用坐标和本决策 ID，
不得修改 `reverse/` 中的原始事实，也不得把这条例外推广到其他肖像。

### REMAKE-011：第 1 关出击准备采用现代构图

用户于 2026-08-02 明确决定：第 1 关出击准备页采用三列五行人物卡、战场预览小地图、
人物详情和底部主行动组成的现代构图，不再以原版名单热区和像素坐标作为实现约束。
名单仍保留原版列优先键盘拓扑，固定／出场／待命状态必须明确可见；小地图公开敌军起始
位置，并使用我方蓝、敌方红、空落点白三色标记，当前落点在白与蓝之间闪烁。

原版构图、热区、坐标和调色板继续保留为 `[OF]` 证据；现代表面是 `[DD]`，不得反写
原版事实。完整布局、输入、窄屏、减少动态与验收合同见
`design/remake-gdd/systems/deployment-stage1.md`。

### REMAKE-012：用语义激活状态替代原版逐槽 AI 行为

用户于 2026-08-02 明确决定：`stableRemake` 的敌方 AI 不再以原版逐槽行为编号作为运行时
策略。原版职业行动分派和行为 `0..12` 继续完整保留在 `originalFacts`，默认复刻关卡则
使用适合现代 SRPG 的语义状态机：守卫、军团警戒和追击。

`stableRemake` 规定：

- 守卫不主动离开当前格，但当前位置有合法攻击或技术目标时可以行动；
- 军团警戒在敌方阶段开始时检测成员本回合能否造成伤害，以及此前被我军攻击／敌对技术
  成功提交的事件；普通攻击计入合法移动落点，伤害技术只计当前位置，治疗不触发，军团
  外单位的范围也不触发；任一成员触发后整团同阶段、不可逆地进入追击；
- 追击允许先移动再普通攻击；技术只能从回合开始位置施展，当前位置没有合法技术时可
  改选移动后普通攻击或只移动推进；射击继续服从其独立动作合同；
- AI 不预看 PRNG，按确定击杀、紧急治疗、有效伤害、一般治疗、推进的固定优先级选择；
- 生命低于 `40%` 且没有确定击杀或紧急治疗时可以原地休息；不实现远离敌军的撤退移动；
- 激活军团和延迟行动回合属于模拟真值，必须保存并确定性恢复；表现只投影结果。

第 1 关的首个关卡配置为：槽 `45/46` 组成第一军团 `forward-patrol` 并开局追击；槽
`40/41/42/43` 组成第二军团 `castle-guard` 并以警戒态开始；芳（槽 `16`）不属于两个
军团，以主将守卫开始。第一军团或芳能够攻击我军时不会连带解除第二军团警戒。若第二
军团在回合 `N` 解除警戒，芳在回合 `N` 仍守卫，从回合 `N+1` 的敌方阶段起追击，以
避免主将先于卫队冲到前线。该延迟在军团激活后不可撤销。

`legacyStrict` 可继续使用原版逐槽行为和职业随机池，但不得把其结果写回
`originalFacts`。Mod 可以声明新的激活策略、军团与评分器；改变这些配置必须改变规则／
内容身份。

2026-08-09 应用户要求再次复核并在设计 GDD 明列两套优先级：原版普通近战只最大化
攻击者落点地形防御，法师系先从职业／成长层池抽一个动作再选目标；`stableRemake` 仍按
本决策的确定性效用顺序运行。此次记录没有撤销 `REMAKE-012`，若要把原版排序改为默认
复刻规则，须另立决策。

### REMAKE-013：冰封不可被攻治、外圈不外推，破邪可解除

用户于 2026-08-03 明确决定：原版冰雪造成的大范围行动禁用缺少反制窗口，目标在跳过
本阵营阶段期间仍能被普通攻击、射击和技术持续伤害，使其只能被动挨打。该原版行为继续
保留在 `originalFacts`；`stableRemake` 将冰雪写入的逐单位行动禁用位命名为玩家可见的
“冰封”状态，并在同期间将单位从攻击与治疗的合法目标中排除。

`stableRemake` 规定：

- 冰封持续到目标跳过紧接着的一次本阵营阶段后解除；它不占八槽普通状态；
- 冰封不能叠加。再次命中已经冰封的单位不移动、不刷新或延长解除时点，也不消费其
  防魔；
- 原版位移比较把范围值 `0` 也视作比最外圈的 `1` 更低，因此会把最外圈单位推出技能
  范围。默认复刻要求目的格范围值仍大于 `0`：内圈单位仍可被推到最外圈，已经位于
  最外圈的单位只冰封、不移动，也不触发位移经验或随机调用；
- 冰封单位不能选择、移动、攻击、反击、射击、施术、休息或进入 AI 行动队列；
- 冰封单位不能成为普通攻击、射击、单体伤害技术或治疗的合法目标；玩家选目标、我方
  自动与敌方 AI 使用同一门禁，拒绝的动作不消费行动或 PRNG；
- 冰封单位若被以其他目标为中心的范围伤害或范围治疗覆盖，伤害与治疗均为 0；不消费
  防魔、不改变生命、不触发死亡、击杀奖励或治疗经验。未来的持续伤害也必须跳过冰封；
- AI 不能用冰封单位触发军团“本回合可造成伤害”的警戒解除条件；
- 唯一可以直接选中冰封单位的恢复类动作是“破邪” `TR`。原版 `TR` 选择距离为 7，
  会清防御下降、攻击下降、混乱、施毒与禁咒，但不读取冰雪的独立禁用数组；
  `stableRemake` 在保留这五项净化的基础上额外清除冰封。表现完整结束并原子提交后冰壳
  消失，尚未行动的目标可在当前本阵营阶段恢复行动；
- 地图上的原版冰壳持续投影整个冰封状态，直到自然解除或破邪提交，其他范围演出不能
  暂时隐藏它。

`legacyStrict` 保留原版“行动禁用但仍可受伤／受治疗、最外圈可被推出范围、破邪不能
解除”的结算。Mod 若改变持续时间、可叠加性、位移边界、目标门禁、范围免疫或净化
关系，必须声明新的规则身份。

### REMAKE-014：敌方技术提示保留原文并对焦效果中心

用户于 2026-08-03 试玩第 1 关后确认：敌方施术缺少可辨识提示且镜头不在释放点时，
无法可靠判断敌军做了什么。原版“ＡＩ對話”默认开启，成功施术会用行动者肖像显示动作组
情境原文；第 1 关 `1F/1H` 分别为“看我的火球魔法.”与“生命單.”。原版随后只重绘
当前视口，没有调用目标居中原语。

`stableRemake` 恢复这两条原文，并在说话者栏补充语义技术名。敌方技术顺序固定为
“准备结果 → 将相机平滑夹取到 `prepared.result.target` → 情境提示 → 原版技术表现 →
原子提交”。相机对焦是明确的 Web 可读性优化，不回写成 `[OF]`；它只消费已准备的
语义中心，不重算目标或结果。

“ＡＩ對話”作为持久表现设置默认开启；关闭时只省略提示窗，对焦仍执行。提示时长、
逐字音、动画加速和对话开关都不能改变生命、状态、行动位、胜负、PRNG、规则身份或
存档语义。尚无取证原文的后续敌方技术不能虚构台词；在补齐动作组证据前只保留对焦与
既有技术表现。

用户于 2026-08-03 进一步要求把同一机制扩展到所有后续技能及友方 NPC。原版 33 项 AI
参数行已全部绑定到 `presentationGroup 10..23` 的 14 条情境原文；运行时目录由该机器
证据生成，并以动作定义的原生短码查询。side 1 自动单位沿原版使用上窗，side 2 敌军
使用下窗；两侧都执行同一“对焦 → 提示 → 表现 → 提交”边界。这样新增已有原生短码的
技能时无需另补对白。

`1V..3V` 虽登记原版火球动作组，但继续保持无发布版 AI 池生产者；`SM/FM/OJ` 没有可
证实的 AI 参数行对白，目录必须返回无提示而不是按名称猜测。本次只解冻情境对白取证、
生成目录和已实现自动行动的投影，不解冻中高阶技能规则、后续关卡 AI 或战役内容。

### REMAKE-015：空地点击显示职业相关地形特性

用户于 2026-08-04 明确要求把空白地形检查作为 remake 信息增强。原版没有逻辑地形槽的
可见名称消费者，但 23 槽职业移动 profile、地形防御百分比和普通攻击公式均已有原生证据。

`stableRemake` 规定：

- 玩家阶段中性态点击无单位格时，在右栏上半部显示该格的复刻地形分类名、棋盘坐标、进入损耗、
  攻击加成和防御加成；下半部实时小地图继续可见，不用战场中央浮窗遮挡棋盘；
- 移动损耗和地形防御必须按最近一次明确焦点单位的当前职业 profile 计算，并显示参照单位；
  防御同时显示百分比与按该单位当前防御折算的整数点数；
- 原版地形不提供攻击加成，因此明确显示“无”，不得为了补齐面板而虚构数值；职业不可进入
  的地形显示“不可进入”，也不显示无法合法占据后的防御折算；
- 逻辑槽没有原版显示名证据。用户于 2026-08-04 追加要求玩家界面不显示“槽 N”，改用
  原版战场图块外观归纳出的 `[DD]` 复刻分类；数值槽仍是规则、存档和调试的规范身份，
  分类名不得反写为 `[OF]`。由于同一规则槽在不同图块组中可承载近似但不完全相同的材质，
  显示名先查已校订的关卡级名称，再以全局上位分类兜底；
- 地形检查只读且不阻断战场：再次点击空地更新目标，点击单位转入正常单位交互，次操作、
  面板关闭按钮或打开其他按需界面时收起；移动/目标选择中的空格点击仍优先服从当前动作，
  不打开检查面板；
- 检查、关闭和切换参照不得改变单位、行动位、镜头、胜负、PRNG 或存档语义。

该增强适用于已开放及后续复用同一战场表面的关卡；它只开放地形信息投影，不解冻新的
地形构造技术、职业、关卡或规则实现。

44 张原版战场模板的逐槽图块复核给出以下全局兜底分类；槽 17、22 在现存模板中没有配置
引用，保持“未分類地形”，不从数值 profile 反猜外观：

| 逻辑槽 | `[DD]` 全局显示分类 | 主要视觉依据 |
| --- | --- | --- |
| 0 | 地圖邊界 | 黑区、不可通行边界及部分墙体装饰 |
| 1 | 沙地 | 土路、沙土平面 |
| 2 | 平原 | 草地与普通开阔地 |
| 3 | 森林 | 树林覆盖格 |
| 4 | 山地 | 山石、瓦砾等崎岖地面 |
| 5 | 陡坡 | 山脊与陡峭坡面 |
| 6 | 橋樑 | 跨水或跨沟通道 |
| 7 | 海域 | 开放水面 |
| 8 | 沙漠 | 沙漠普通地面 |
| 9 | 流沙與陷阱 | 流沙、尖刺等危险地面 |
| 10 | 石砌地面 | 道路、城内石地与普通硬质地面 |
| 11 | 城牆與岩壁 | 城墙、岩壁及高防护硬质边缘 |
| 12 | 深水與斷崖 | 不可通行水域、断崖与空洞边界 |
| 13 | 室內地面 | 宫殿、室内与普通建筑地面 |
| 14 | 階梯 | 楼梯、台阶与平台边缘 |
| 15 | 要地 | 王座、旗帜等关卡显著地点 |
| 16 | 通道 | 红毯等室内主通道 |
| 17 | 未分類地形 | 现存模板无配置引用 |
| 18 | 木板地面 | 木制甲板与室内木地面 |
| 19 | 水井 | 水井格 |
| 20 | 場景設施 | 箱柜、残骸等场景设施 |
| 21 | 冰面 | 冰雪地面 |
| 22 | 未分類地形 | 现存模板无配置引用 |

当前开放关卡再使用更贴近本关图块的名称：第 0 关为“城牆與邊界／宮殿地面／宮殿階梯／
王座／紅毯”；第 1 关为“地圖邊界／沙地／平原／森林／山地／橋樑／石砌道路／城牆／
河流”；第 2 关为“地圖邊界／平原／森林／土坡／橋樑／石砌地面／城牆／河流”；第 3 关为
“地圖邊界／沙地／平原／森林／山地”；内部第 27 关为“地圖邊界／岸邊沙地／草地／淺水／
障礙／石砌街道／城牆／深水／鐵板”。

内部第 27 关是首个玩家可操作工兵的关卡，它的逻辑槽 `9` 与 `21` 在基础棋盘上只出现在
`(16,26)` 与 `(16,25)` 两个构筑源格，因此这两个名称同时就是 `2K/障礙` 与 `1K/鐵板`
构筑后的地形名；沿用通用表的“流沙與陷阱／冰面”会在工兵构筑后明显读错。未配置逐关
名称的关卡继续回退到上表。

### REMAKE-016：失败与撤退回滚到不可变入关快照

用户于 2026-08-04 明确决定修正原版的同关无限练级路径。原版失败与主动撤退都返回
`module 27 / currentStage`，但不会回滚上层战役职业与累计经验；因此本次尝试取得的经验、
升级和转职会被下一次同关部署继续使用。该行为保留为 `[OF]`，默认复刻不沿用。

`stableRemake` 规定：

- 每次从上一关完成状态或新游戏首次进入一关时，在任何本关部署、剧情事件、战斗动作和
  PRNG 消费之前建立不可变的“入关快照”；同关重试不会更新该快照；
- 快照包含关卡 ID、规则身份、难度、75 槽战役 roster 的职业／经验／生命，以及完整
  PRNG 状态与调用序号；表现设置、镜头和 UI 状态不属于快照；
- 战败确认或“全面撤退”确认后，丢弃本次尝试的单位、回合、行动位、状态、生命、经验、
  升级、转职、事件消费和 PRNG 进度，完全从入关快照重建同关；
- 有部署页的关卡回到部署，并允许玩家重新选择当次出场阵容；无部署页或固定阵容关卡
  重建固定战斗。两者都不能把本次尝试的成长带回准备阶段；
- 读取战中档先恢复该档的当前战况，但存档必须同时保存原始入关快照；随后发生失败或撤退
  时仍回到原始快照，不能把“读档时的当前状态”误当成新入关状态；
- v12 及更早的旧战中档没有这份历史信息，无法反推出真正的入关状态。迁移到 v13 时将
  旧档当前战役状态一次性采纳为基线；迁移后新取得的成长照常回滚。完成档不需要额外
  快照，其当前 roster 与 PRNG 在进入下一关时自然成为下一关的新快照；
- 正常胜利仍保留本关最终成长并带入下一关。只有失败与主动撤退触发回滚。

`legacyStrict` 保留原版失败／撤退继续积累成长的行为。Mod 若改变快照边界、回滚字段或
旧档迁移策略，必须声明新的规则身份与存档版本。

### REMAKE-017：第 3 关按机器槽位显示双保护目标

第 3 关 SAY/0085 的可见目标文字写“「妮雅」戰敗”，但 B/0007 没有 side 1 槽 0
妮雅；模块 29 的胜负处理器实际依次检查 side 1 槽 1“希蜜”和槽 3“黛西”，任一缺席
立即失败。开场与胜利 SAY/12、13 也明确由希蜜、拉朵那和黛西参与当前救援战。

`stableRemake` 以机器胜负条件为玩法真值，并把目标面板、提示、失败状态和调试夹具统一
显示为“希蜜或黛西戰敗”。原版 SAY/0085 文字及冲突继续保留在证据层，不改写成原版
正确文本。`legacyStrict` 可以逐字显示“妮雅戰敗”，但不得因此把实际失败槽位改为 0；
Mod 若同时修改文字与胜负条件，必须声明新的内容／规则身份。

### REMAKE-018：妮雅缺席时由当前关卡主将授职

用户于 2026-08-04 试玩第 3 关后明确指出，妮雅未出场时不应继续作为转职授予者。
原版模块 29 `0000:0487` 实际把授予者硬编码为肖像记录 `002Eh` 妮雅：非妮雅单位先在
下窗请求，再由 `MOV CX,002Eh` 切换妮雅到上窗回应；这不是原版自动选择在场主将。

`stableRemake` 保留三段原文、窗口顺序、自动推进和规则提交时点，但要求授予者必须是
当前战场的在场主将：妮雅在场时仍由妮雅授职；第 3 关妮雅缺席时由 side 1 槽 1 希蜜
授职。若达标单位就是当前授予者，则以其姓名／肖像走单段自问分支，不生成自己向自己
请求和回应的双窗口。该决定只改变表现身份，不改变资格、候选、经验、PRNG 或存档。

`legacyStrict` 保留原版固定妮雅表现。Mod 若改变关卡主将，必须同时提供稳定主将身份或
明确采用原版固定授予者。

### REMAKE-019：范围回复只标记实际效果范围

原版 `1I..3I` 的 `MAGIC/20` 表现会在 17 个阶段中把图形画到全部同阵营占用格，随后
`1000:6FFE` 才按范围图结算生命。因此原版会让范围外单位也出现回复图形，虽然生命不会
变化。用户于 2026-08-04 试玩第 3 关时确认这种反馈会被理解为全屏治疗。

`stableRemake` 保留 `MAGIC/20` 描述符顺序、`E/36`、17 阶段与 255 native tick，但只在
准备结果的 `affectedUnits` 上绘制回复图形；范围外单位不显示回复反馈。实际治疗范围、
外／中／内数值、封顶、冰封门禁、经验、原子提交与 PRNG 均不改变。

`legacyStrict` 保留原版全同阵营占用格绘制。Mod 若修改效果范围或表现范围，必须分别声明，
不得用扩大图形范围暗示扩大规则结算。

### REMAKE-020：第 3 关采用防御区救援 AI 与双军团目标

用户于 2026-08-04 试玩第 3 关后指出，高难度下七名自动友军会主动离开有防御加成的
森林／山地并快速阵亡，使“救援”目标过度受不可控单位影响。`stableRemake` 因此为本关
采用以下明确的关卡 AI；它不是原版行为 `0/2/3/4` 的事实，也不回写到逆向证据：

- “自动友军”只指 side 1 行为 `2/3/4` 的七个固定槽，不包含玩家控制的六名救援单位；
- 第 3 关逻辑地形槽 `3`（森林）与槽 `5`（山地）组成防御区。开局位于区外的自动友军
  在第一个自动友军阶段把移入可达、未占用森林作为最高优先级，并为此消耗本次行动；若
  暂时没有合法森林格，后续阶段继续尝试。单位一旦处于防御区，移动路径上的每一格及
  终点都必须仍为森林或山地，攻击、射击和跟随领队也不能越界；
- 自动友军生命严格低于最大生命的 `50%` 时，在能够攻击或追击之前休息。自动友军中的
  修女先尝试对防御组使用“初級治療”：低于 `50%` 的目标优先，其次才是其他受伤目标；
  没有合法治疗目标时再处理自身休息、进攻或等待。治疗只在既有技能范围、行动位、冰封
  与禁咒门禁内规划，不增加范围、不预看 PRNG；
- 敌军第一军团固定为地图最上方的士兵槽 `42/41/40`、左侧修女槽 `43` 与主将莎槽
  `17`，其进攻动作优先把七名自动友军作为目标；第二军团固定为其余槽
  `44/45/47/46/50/48/49`（包含骑兵槽 `50`），优先攻击六名玩家救援单位；
- 军团目标只约束普通攻击、进攻技术和追击，不阻止修女／僧侣治疗同阵营单位。指定目标
  组仍有存活单位时不得跨组挑选较近目标；指定组全灭后才退回攻击另一组，避免单位永久
  空转；
- 这套策略在第 3 关全部难度统一生效。难度继续只改变既有敌军数值，不建立同一规则身份
  下按难度分叉的目标或路径语义。

`legacyStrict` 保留原版逐槽行为与低生命门槛。Mod 若改变防御区、回血阈值、治疗对象、
军团成员或目标切换条件，必须声明新的规则身份。

### REMAKE-021：第 3 关集团命令固定由希蜜统率玩家救援队

用户于 2026-08-04 试玩第 3 关第 2 回合时确认：妮雅缺席后，集团命令的主将身份应为
希蜜，不应因上一阶段的自动行动焦点回退为阵容首位爱欧里雅；集团命令也必须先作用于
六名玩家控制的救援角色，不能被七名固定自动友军截走。

`stableRemake` 规定：

- 第 3 关 side 1 槽 `1` 希蜜是固定集团命令主将。每个新玩家回合把战斗焦点、光标与
  镜头恢复到仍在场的希蜜；“全部休息／跟随主将／自由行动”均由希蜜姓名与肖像显示
  原文命令；
- “跟随主将”不再从当前光标或上一个 AI 焦点临时选择本关主将；只要希蜜尚未行动且未
  冰封就保持可用，执行时先提交希蜜，再让其他尚未行动的玩家救援角色以希蜜为凝聚目标；
- “全部休息”只批量结算尚未行动的行为 `0` 玩家角色。行为 `2/3/4` 自动友军不被批量
  休息，仍进入其正常自动阶段；
- “跟随主将／自由行动”先把剩余行为 `0` 玩家角色交给通用我方 AI，全部完成后才调度
  行为 `2/3/4` 自动友军。后者继续使用 `REMAKE-020` 的防御区策略，不继承希蜜的临时
  凝聚目标，也不接收“自由行动”；其随后行动是独立友军军团的正常阶段，不是集团命令
  对 NPC 生效；
- 这只修正第 3 关主将身份与同阵营两类控制权的队列边界，不改变攻击、恢复、经验、
  PRNG、敌方 AI 或第 0–3 关已接受的集团命令语义。

`legacyStrict` 保留原版按当前 side 1 焦点选择临时主将和原生统一调度顺序。Mod 若改变
本关集团主将、手动／自动角色划分或接管队列，必须声明新的规则身份。

### REMAKE-022：控制权、军团归属与 AI 策略使用显式内容合同

用户于 2026-08-04 完成第 3 关上述修正测试后明确要求：独立友军与敌方多军团能力必须
沉淀为未来关卡可复用的框架，不能继续用关卡槽位分支或原版行为号推断控制权和目标组。

`stableRemake` 规定：

- 每个已迁移关卡在模拟层显式声明稳定军团 ID、阵营、成员、`player/independent-ai`
  控制权、可选主将、AI 策略和可选优先目标军团；同一单位不能属于多个军团；
- 原版逐槽行为号继续作为策略的警戒／移动输入，但不再决定玩家是否能控制该单位。
  因此未来即使独立友军的原版行为号为 `0`，也不得进入玩家选择、全体休息或集团命令；
- 集团命令主将必须属于玩家军团。控制器、UI 与 Phaser 只消费模拟层给出的控制权和
  军团投影，不按槽位、角色名或行为号重新划分队列；
- 每个军团独立选择有类型的 AI 策略。第 3 关第四军团使用 `terrain-hold`；`REMAKE-037`
  后其余军团的默认策略统一为 `expert`。未来新增策略须扩展通用策略合同和规划器，不在
  逐关控制器中新增槽位分支；
- 军团可以优先一个或多个敌对军团。优先组尚有存活单位时，进攻技术、普通攻击和追击
  共用该过滤器；全灭后只能执行内容声明的“全体敌军”或“等待”兜底；治疗不跨入敌军
  目标过滤器，并服从策略声明的友军范围；
- 构造战斗时必须验证 ID、成员、阵营、主将、目标关系和策略参数。军团定义是版本化关卡
  内容，不重复写入战中档；可变警戒／激活态仍按既有存档合同保存。

`legacyStrict` 可把原版行为号映射为军团策略，但 Web 控制权仍须显式声明，避免同阵营
NPC 被浏览器输入接管。Mod 可以改变军团、策略与目标配置，但必须改变内容／规则身份。
完整字段、顺序和验收见 `design/remake-gdd/systems/force-ai-groups.md`。

### REMAKE-023：第 4 关按机器规则显示护送目标与结界风险

第 4 关 SAY/0086 的可见目标文字写“打敗敵人首領『倩』／『妮雅』戰敗”，但 B/0009
只有 side 2 槽 `40/41` 两名士兵，没有“倩”。模块 29 的胜利处理器实际要求 side 1
槽 `24` 葛蒂拉斯进入线性格号 `0..174`；失败处理器依次检查槽 `0` 妮雅与槽 `24`
葛蒂拉斯，任一缺席即失败。SAY/7、8、174 也一致描述葛蒂拉斯以小型结界带队穿过力场。

原版还允许玩家把可选单位部署在六个格中的任意格，但行为 `12` 第一次效果以葛蒂拉斯
初始位置为中心时，`(23,40)` 与 `(27,40)` 已在安全区外，会立即令对应单位当前生命
减半；另外四个部署格与两名固定单位在首轮安全区内。该差异是有证据的战术选择，不是
应由复刻版静默禁止的错误。

`stableRemake` 规定：

- 机器目标是唯一玩法真值；目标面板、失败反馈、调试夹具和可访问文本统一显示
  “葛蒂拉斯抵达力場盡頭；妮雅或葛蒂拉斯戰敗则失败”，击败两名士兵不触发胜利；
- 六个部署格全部保持合法，不自动换位、不阻止确认，也不改变首次结算；部署地图显示首轮
  结界轮廓，在两个区外格明确提示“首輪力場區外：目前生命減半”；
- 战斗中选择葛蒂拉斯、打开目标详情或播放其独立行动时，可查看以她当前格计算的安全区与
  “行动后区外我方当前生命减半”说明；该信息来自剧情已明确说明的结界，不公开内部路线
  目标格、搜索预算或未来路径；
- 安全区预览、文字、减少动态和动画速度都是表现投影，不改变行为 `12` 的路线、每次尝试
  触发、目标集合、向下取整、防魔边界、死亡、胜负或 PRNG。

`legacyStrict` 可逐字显示 SAY/0086 且不提供现代安全区预览，但机器胜负和力场结算仍保持
原版。Mod 若修改目标区、保护槽、部署合法性、效果范围或生命比例，必须分别声明内容／
规则身份；只修改提示样式不得进入模拟语义。

### REMAKE-024：逐关职业记录只初始化未成长角色，不覆盖已转职职业

原版模块 27 进入普通战斗时先导入战役 roster，再把 B 模板 side 1 的非零职业记录写回
对应槽位。native stage `1/2/4` 的 B 模板都为槽 `24` 葛蒂拉斯写入职业记录 `6`
“魔術士”；因此原版即使已经把她转职为邪法師、魔法師或巫師，进入这些关卡仍会把职业
改回魔術士，同时保留累计经验。这是原版装载顺序的确定事实，不是 Web 内容声明应继续
制造的角色成长回退。

`stableRemake` 把该非零记录解释为角色首次进入战斗时的职业基线：只有战役槽缺失，或仍
保持新战役默认的士兵／零经验状态时，才把葛蒂拉斯初始化为魔術士；一旦 roster 已保存
实际成长状态，后续关卡必须完整继承职业与累计经验，当前生命仅在新职业最大生命以下
钳制。由魔術士到邪法師、魔法師或巫師的合法转职都可跨第 2、4 关保存、部署和恢复，不得
因 B 模板、调试夹具或存档校验退回魔術士。地图单位图形按继承后的语义职业从共享目录
加载，不能把未登记的高阶职业静默画成士兵或缺失纹理。

v13–v15 战中档保存了不可变入关 roster；若旧运行时已经把当前 roster 与战场单位都错误
写成魔術士，v16 迁移从该快照恢复原职业，同时保留当前累计经验和当前生命（仍受恢复职业
最大生命钳制）。完成档没有入关快照，无法在邪法師、魔法師、巫師之间无歧义反推，不做
猜测性迁移。

`legacyStrict` 将来可按原版顺序强制写回记录 `6`，但必须与 `stableRemake` 使用不同规则
身份。Mod 若改变角色首次职业或允许其他模板覆盖，必须显式声明初始化条件与存档语义。

### REMAKE-025：工兵构筑使用可达玩家路径，并把动态地形纳入规则与存档

模块 29 的玩家 `1K/2K` 分支在选定空格后先沿 mode `M` 的路径移动工兵，再以其新位置
为中心按 `+50/-50/+1/-1` 调用邻格助手。助手只跳过逻辑地形槽 `0`，其余邻格写入该关
配置的构筑源 token；中心格不变。动作表中另有一组 `1K/2K` 包装器会横向写入五个连续
token，但正常玩家分支在 `0000:761A..762F` 直接跳到 `0000:76C0`，不会调用这些包装器；
普通 AI 类别也没有产生构筑动作的技术池。复刻版不能把不可达包装器误作玩家效果。

原版邻格助手使用线性偏移，在地图左右边界可能跨行；更坏的越界结果取决于相邻 DOS
内存，不是可移植的关卡规则。原版地图重绘直接读取已修改的地形缓冲，而当前 Web 地图
底图是静态图片；若只覆盖画面、不更新模拟和存档，会令移动、地形防御、AI 与读档互相
矛盾。

`stableRemake` 规定：

- `1K` 复现正常玩家可达流程：固定 seed `5` 的 mode `M` 选择空格、播放普通移动，随后
  同时把几何上的下、上、右、左四个邻格中非槽 `0` 的格改成该关 `iron-plate` 动态地形；
  中心格、单位、生命、经验、状态与 PRNG 不变，行动位在成功后消费；
- 邻格必须同时满足 `0 <= x < width`、`0 <= y < height`；越界方向直接跳过，不跨行、
  不访问地图外内存。处于合法邻格上的单位不阻止铺设，后续立即按新地形计算；
- 动态地形是模拟状态。地形检查、移动代价、合法范围、地形防御、攻击、AI 规划和地图
  覆盖图统一读取同一份覆盖表；战中档保存有序的语义覆盖 `{position, kind}`，恢复时验证
  边界、唯一性和关卡支持的 kind，不能保存 DOS token 或浏览器贴图路径作为领域身份；
- 原版没有独立构筑图形、声音、描述符或等待。原版表现模式只播放已有的逐格移动，提交
  后立即以该关原始 40×44 铁板 tile 覆盖邻格；不得添加烟尘、闪光或人为停顿。动画速度
  只能缩放移动墙钟，不能改变提交时点或地形结果；
- 工兵仍走原版 ordinary AI 类别，不自动使用 `1K/2K`。竞技场可以由玩家直接验证
  `1K`，但不能为了展示而给敌方工兵新增原版不存在的构筑决策。

`legacyStrict` 将来可另行提供线性 `+50/-50/+1/-1` 的行边界跨越语义，但仍必须把索引
限制在已分配的 `50×50` 地图缓冲内，不模拟相邻 DOS 内存破坏；休眠的横向五 token
包装器只能作为取证工具入口，不能成为正常玩家菜单动作。Mod 若改变 seed、邻格顺序、
跳过槽、动态地形种类或视觉 token，必须改变规则／内容身份。

### REMAKE-026：防魔 AI 孤项使用玩家动作定义安全修复

原版魔導師第三层 AI 池为 `3H/2I/AA/FM`，但 33 项 AI 动作参数表没有 `FM` 行。
查找失败返回值被忽略，后续可能读取零值或上一动作残留的处理器、选择器和距离；单次可见
结果属于原生未定义内存行为。玩家 `FM` 则完整闭合为友方目标、选择距离 7、写入
`+0C=8001h`，并与 `AA` 共用 `MAGIC/16 + UN/51` 的 300 tick 表现。

`stableRemake` 规定：

- 保留魔導師第三层池的原始顺序 `3H/2I/AA/FM`，抽到 `FM` 时直接使用玩家同名 action
  definition：从当前位置距离 7 内选择存活友军，写入一次性防魔并按原版取得 `10..13`
  经验；阵营、自动控制或敌方控制不改变范围、目标合法性、状态或提交时点；
- AI 使用已有友方技术的最大缺失生命选择器，完全同分取行优先扫描中较后的格；自己、
  满生命、已有防魔和冰封友军都合法。该排序是安全确定性修复，不倒写为原版参数行；
- `FM` 没有可证实的 AI 参数行或动作组对白。自动施术仍依 `REMAKE-014` 对焦目标，但不
  显示虚构台词；表现设置、动画和声音不得消费额外模拟随机数；
- `legacyStrict` 只在证据／兼容元数据保留原始孤项。若将来开放该规则集，抽到缺行项时
  必须安全视为特殊动作准备失败并进入普通 AI 兜底，不能读取残留字段、模拟越界内存或
  伪造一个所谓“原版结果”。

Mod 可以另选 `FM` 的 AI 排序或移除该池项，但必须改变规则身份。`SM` 仍是独立孤项，
不因本决定自动映射为 `OJ`。

### REMAKE-027：第 5 关按双首领机器条件显示目标

第 5 关 SAY/0087 的可见目标写“打敗敵人首領『麗』／『妮雅』戰敗”，但 B/0011 没有
名为“麗”的单位。模块 29 的机器胜利处理器依次检查 side 2 槽 `25` 与槽 `26`：只要任一
槽不在棋盘就立即胜利，而不是要求击败两者；失败处理器只检查 side 1 槽 `0` 妮雅。
side 2 角色表确认槽 `25` 为“汀塔琪”、槽 `26` 为“萊茵”，SAY/9、10 也把两人共同写成
遭遇、交战和战后恢复的剧情主体。标题 SAY/0123 的“遭遇丁塔琪”与角色描述符的“汀塔琪”
属于原版文本差异，不改变槽身份。

`stableRemake` 规定：目标面板、部署说明、失败反馈、调试夹具和可访问文本统一显示
“擊敗汀塔琪或萊茵任一人；妮雅戰敗则失败”。击败其他十二名敌军不触发胜利；任一首领
离场后按同一终端检查结束战斗，不再要求或允许继续击败另一人。标题保留原版“遭遇丁塔琪”，
单位身份和剧情对白保留原版“汀塔琪”，不得为了表面统一而改写任一证据来源。

`legacyStrict` 可逐字显示 SAY/0087 的“麗”，但机器胜负仍保持原版双槽任一缺席。Mod 若
修改目标槽、要求全灭双首领、改变保护目标或统一标题／角色名，必须分别改变内容或规则
身份；只修改目标文字排版不进入模拟语义。

### REMAKE-028：第 6 关按机器槽显示西艾蕾首领目标

第 6 关 SAY/0088 的可见目标写“打敗敵人首領『愛』／『妮雅』戰敗”，但 B/0013 的
side 2 活跃阵容没有名为“愛”的单位。模块 29 的胜利处理器实际检查 side 2 槽 `19` 是否
仍在棋盘；失败处理器检查 side 1 槽 `0`。side 2 角色表确认槽 `19` 为“西艾蕾”、肖像
`D/5`，B/0013 把该槽设为职业记录 `13`“陸戰騎士”，SAY/15、16 也始终由西艾蕾作为
追击队首领发言。

`stableRemake` 规定：目标面板、部署说明、失败反馈、调试夹具和可访问文本统一显示
“擊敗西艾蕾；妮雅戰敗则失败”。击败其余八名敌军不触发胜利；西艾蕾离场后立即进入
SAY/16 与现场游骑兵台阵，不要求清除剩余追击队。目标修正只改变玩家可见说明，不改变
槽 `19/0` 的机器胜负、事件顺序、经验、PRNG、保存时点或完成路由。

`legacyStrict` 可逐字显示 SAY/0088 的“愛”，但机器胜负仍保持槽 `19`。Mod 若修改目标槽、
要求全灭、改变保护目标或替换首领身份，必须分别改变内容或规则身份；只修改目标文字排版
不进入模拟语义。

### REMAKE-029：剧情对话使用无按钮点击推进与右键跳过确认

已开放关卡的剧情文本窗原先在正文右下角叠放“繼續／跳過”按钮；长行文本会进入同一视觉
区域，按钮因而可能遮挡正文。玩家已经可以点击对话层推进，常驻按钮不再提供独立能力。

`stableRemake` 规定：

- 剧情对话窗不显示“繼續”“跳過”或“跳過過場”等常驻按钮；左键点击对话层任意位置时，
  若逐字显示尚未完成则先显示完整本页，再次点击才进入下一页；
- 在剧情阶段点击鼠标右键，弹出使用既有像素文本窗和原版命令选框的确认菜单，询问
  “是否跳過劇情對話？”，只提供“是／否”，并默认选择“否”；
- 选择“是”只完成当前剧情对话记录，随后仍按正式事件时间线执行移动、技术表现、增援和
  后续剧情；选择“否”、再次右键或按取消键返回原页，不改变页码；
- 确认菜单打开时必须阻断对话推进和底层战场输入；方向输入可切换选择，确认输入提交当前
  选择。转职授予、集团命令和自动施术提示不属于可跳过剧情，不弹出该菜单；
- 打开、取消或确认菜单不得消费玩法 PRNG，不进入存档，不改变事件消费、模拟结果或路由。

该决定只改变 Web 对话 UI 和语义输入映射，不倒写为原版事实。Mod 若恢复常驻按钮、改变
跳过范围或默认选择，属于表现／输入规则覆盖；不得借此跳过模拟事件提交。

### REMAKE-030：第 7 关按机器槽显示萊莉首领目标

第 7 关 SAY/0089 的可见目标写“打敗妖龍／『妮雅』戰敗”，但 B/0015 的 side 2 活跃
阵容没有敌方角色槽 `28`“妖龍”。模块 29 的胜利处理器实际检查 side 2 槽 `18` 是否仍在
棋盘；失败处理器检查 side 1 槽 `0`。side 2 角色表确认槽 `18` 为“萊莉”、肖像 `D/19`，
B/0015 把该槽设为职业记录 `13`“陸戰騎士”。

`stableRemake` 规定：目标面板、部署说明、失败反馈、调试夹具和可访问文本统一显示
“擊敗萊莉；妮雅戰敗则失败”。击败其余十名敌军不触发胜利；萊莉离场后立即进入普通
胜利反馈和保存。目标修正只改变玩家可见说明，不改变槽 `18/0` 的机器胜负、经验、PRNG、
保存时点或完成路由。

`legacyStrict` 可逐字显示 SAY/0089 的“妖龍”，但机器胜负仍保持槽 `18`。Mod 若修改目标
槽、要求全灭、改变保护目标或替换首领身份，必须分别改变内容或规则身份；只修改目标文字
排版不进入模拟语义。

### REMAKE-031：第 8 关按机器条件显示全灭与蘇蘭達保护目标

第 8 关 SAY/0090 的可见目标继续写“打敗妖龍／『妮雅』戰敗”，但 B/0017 的固定阵容
既没有敌方角色槽 `28`“妖龍”，也没有 side 1 槽 `0` 妮雅。模块 29 的胜利处理器实际
扫描全部 2500 格，只在 side 2 完全不存在时返回胜利；失败处理器检查 side 1 槽 `8`。
side 1 角色表确认槽 `8` 为“蘇蘭達”、肖像 `D/10`，B/0017 把该槽固定为职业记录
`22`“騎兵”。

`stableRemake` 规定：目标面板、失败反馈、调试夹具和可访问文本统一显示“擊退龍塔
襲擊者；蘇蘭達戰敗则失败”。清除十名敌军仍不胜利，最后一名 side 2 离场才进入普通
胜利反馈和保存。目标修正只改变玩家可见说明，不改变机器全灭／槽 `8` 条件、经验、PRNG、
保存时点或 module 27 / stage 9 路由。

`legacyStrict` 可逐字显示 SAY/0090 的“妖龍／妮雅”，但机器胜负仍保持全灭／槽 `8`。
Mod 若修改目标、保护对象或控制权，必须分别改变内容或规则身份。

### REMAKE-032：第 8 关补播原版漏调渲染器的 SAY 157

第 8 关现场胜利处理器在聚焦单位后把 SAY `157` 写入 DS:`80B5h`，却没有调用战斗剧情
渲染器，直接写入下一模块与场景；完整处理器目录只在本关确认这种“写入记录号但不调用
渲染器”的控制流遗漏。SAY 157 本身是完整的一页蘇蘭達撤离对白，内容、肖像、窗口和输入
检查点均可正常解析，与全灭后前往会合的叙事顺序没有冲突。

用户于 2026-08-09 明确判断这是原版 bug，并要求复刻版补充。`stableRemake` 规定：最后一名
side 2 离场、`stageProgress=999` 提交后播放 SAY 157；对白完成或由玩家确认跳过后，才进入
普通胜利反馈、编号保存与 module 27 / stage 9 路由。补播只增加玩家可见的胜利剧情事件，
不得改变单位、经验、PRNG、胜负时点、保存选择或完成路由。完成档读取仍不重播该对白。

`legacyStrict` 保留原版漏播；Mod 可独立选择补播与否，但必须改变内容身份。复刻版实现以
`stage-08-victory-story` 语义事件表达，不在控制器、UI 或 Phaser 中按关卡号或 SAY 号特判，
并以 v23 从 v22 无损迁移第 8 关战中档和完成档。

### REMAKE-033：stableRemake 默认统一使用专家敌方 AI

用户于 2026-08-09 在复核原版普通攻击与法师系 AI 后明确决定：原版默认 AI 偏简单，
复刻版应按现代战棋的常见实践提高挑战；不增加单独的 AI 难度等级，`stableRemake` 在原版
四档战役难度下统一使用“专家”敌方规划器。

`stableRemake` 规定：

- `REMAKE-012` 的守卫／警戒／追击、剧情路线与 `REMAKE-022` 军团教义继续作为硬约束；
- 普通攻击、射击、技术、移动、休息和待命进入同一确定性效用比较；优先识别确定击杀、
  紧急救援、有效伤害／治疗、范围收益、控制与增益价值，同时扣除反击、暴露、重复状态、
  无效目标和偏离目标的成本；
- 规划只读取当前公开模拟状态。随机伤害用最小值判断确定击杀、用固定整数期望估值，
  不读取、克隆、推进或预看玩法 PRNG；真正动作提交仍按正式规则消费 PRNG；
- 每次敌方动作提交后，从尚未行动的敌军中重新选择当前最高价值行动者；死亡、治疗、状态、
  目标军团和占位变化即时进入下一次规划，不保存阶段开始时的过期整队计划；
- 四档原版难度仍只通过已确认的经验播种和属性改变公开状态；评分器、搜索边界、随机偏置
  和玩家可见信息完全相同，不新增“简单／普通／专家”AI UI；
- 精确权重与估值合同见
  `design/remake-gdd/systems/expert-enemy-ai.md`。改变权重、可读信息或搜索边界必须改变规则身份。
- 当前保存身份升级为 v24 / `expert-enemy-ai-1`；v23 战中档和完成档无损迁移，不保存候选、
  分数或追踪，迁移后尚未提交的敌方行动从公开状态按专家规则重新计算。

`legacyStrict` 可保留原版逐槽行为、职业 PIT 动作池和目标排序，但不得把结果写回
`originalFacts`。本决策不允许 AI 读取战争迷雾外信息、未来 PRNG 或玩家输入，也不解冻
第 9 关内容、玩家／独立友军 AI 重制或运行时机器学习。

### REMAKE-034：专家射手使用安全射程边缘，冰雪实际候选延后且纯冰雪残军禁用

用户于 2026-08-09 复核 `REMAKE-033` 后明确决定：射手不应机械选择最远格，而应在确认
击杀／伤害收益后优先安全的有效射程边缘；冰封仍可连续惩罚进入范围的玩家，但不能因为
敌方伤害者尚未行动或战场只剩冰雪职业而形成无伤害循环。

`stableRemake` 规定：

- 敌方射手先比较确定击杀、总期望伤害与目标威胁；同一战术收益下依次选择被较少敌方
  完整威胁来源覆盖、距目标较远、地形防御较高、路径较短的射击格。完整威胁近似包括
  普通移动攻击、移动后射击与当前位置的敌对技术；当前格同样优良时不移动；
- 魔弓主目标仍用固定整数期望估值；沿线副目标按原生前驱选择的全部合法线路均匀求期望，
  不读取或克隆 PRNG，不把某条未来随机线路当成已知事实；
- 每次选择下一敌方行动者时先重算各自最高候选。只有最高候选实际为 `1C/2C/3C/4C` 的
  行动者才进入延后组；只要尚有任何非冰雪候选，先按原专家分数执行非冰雪行动。巫师若
  因新状态改选击杀、其他技术、普通攻击、移动、休息或待命，立即恢复正常顺序；
- 若全部存活 side 2 单位都具备冰雪技术，则过滤所有冰雪候选；它们改选其他合法行动。
  本规则按职业能力判断，适用于一名或多名冰雪职业残军；
- 不新增“近期冰封”标记、抗性、冷却或控制减分。冰雪的伤害免疫、行动禁用、位移、防魔、
  自然解封与破邪规则保持不变，玩家仍须谨慎进入效果范围；
- 若玩家阶段开始时全部可控 side 1 单位均冰封，流程自动跳过该玩家阶段，并继续在既有
  敌方调度前时点解封，避免没有合法操作却停在地图输入；
- 当前保存身份升级为 v25 / `expert-ranged-control-ai-1`；v24 战中档和完成档无损迁移，
  不增加历史冰封或候选字段，迁移后的下一次规划按本规则重算。

`legacyStrict` 可保留原版射手落点与冰雪行动顺序。Mod 若改变威胁估值、射程边缘排序、
冰雪延后或残军门禁，必须改变规则身份。本决定不解冻第 9 关或改变玩家／独立友军 AI。

### REMAKE-035：魔弓兵由玩家指定完整箭道，专家 AI 选择最高价值确定线路

原版魔弓在主目标确认后，从目标沿范围梯度回溯射手；每遇到多个等价前驱格就读取 PIT
选择一个方向，因此同一目标的沿线副伤害不可预先控制。用户于 2026-08-09 明确判断这种
完全随机线路使职业难以发挥，并授权复刻版把完整箭道交给玩家指定。

`stableRemake` 规定：

- 玩家先选择合法主目标，再从规则层枚举的全部最短、可传播箭道中选择一条完整线路；
  只有一条时跳过第二步。确认后，线路作为动作意图的一部分进入正式准备／提交；取消线路
  选择只返回主目标层，不消耗行动或 PRNG；
- 战场以金色实线显示当前线路，以低透明虚线显示其他候选；主目标与沿线副目标使用不同
  轮廓。键盘方向键或 `Q/E`、手柄肩键、鼠标滚轮／按钮均可切换，确认发射、取消返回；
- 线路枚举和切换不读取玩法 PRNG。正式伤害、迅龍闪避和经验仍按既有动作定义消费确定性
  PRNG，因此存档恢复后的相同语义操作保持相同结果；
- side 2 专家魔弓对每条合法线路分别计算确定击杀、有效伤害、目标威胁和既有射手落点
  成本，直接提交最高价值线路；不再按原版随机线路的概率期望估值。平局按稳定线路键打破；
- 独立友军仍不重制整体目标策略；当既有策略决定使用魔弓射击时，从同一线路目录中确定性
  选择当前排序第一条，避免重新引入路径随机。
- 当前保存身份升级为 v26 / `directed-magic-arrow-1`；v25 战中档和完成档无损迁移。
  临时目标、候选线路、当前索引和表现帧都不保存，读取后尚未提交的行动重新选择。

`legacyStrict` 可保留原版 PIT 随机箭道。Mod 若改变箭道合法性、排序、输入提交层级或 AI
线路效用，必须改变规则身份。本决定不改变魔弓伤害值域、分段防魔、经验、射程或表现时序，
不重制独立友军的整体 AI，也不解冻第 9 关。

### REMAKE-036：冰雪外圈稳定外推，专家 AI 次优先命中巫師

用户于 2026-08-09 试玩专家 AI 与冰雪排程后指出：持续把目标留在冰雪最外圈会形成过强、
稳定的重复控制；同时巫師作为范围控制核心，应成为敌方明确可读的高仇恨目标。

`stableRemake` 规定：

- `1C/2C/3C/4C` 延续原版的行优先目标顺序和“下、上、左、右”目的格顺序。合法目的格
  只要求在地图内、可通行、无人占用且范围值严格低于当前格；不再要求范围值大于 `0`。
  因而值 `1` 最外圈目标也会尝试移动到值 `0` 格并离开本次冰雪范围；
- “稳定外推”不绕过地图、地形或占位。若四向没有合法的更低值格，目标仍留格并冰封；
  至少一名目标成功位移时仍只取一次该技术的位移经验随机，无位移时仍为零经验、零调用；
- `REMAKE-013` 的冰封不可行动／攻治、不可叠加／刷新、防魔与龍／頭／手免疫、自然解封
  和破邪解除规则保持不变；`REMAKE-034` 的实际冰雪行动末位排程与纯冰雪残军门禁也不变；
- 专家敌方 AI 使用严格优先级带：任何确定击杀高于一切非击杀；没有确定击杀时，任何能对
  `wizard/巫師` 产生有效敌对结果的候选高于紧急救援及其他普通策略。有效结果包括造成伤害、
  施加控制或负面状态、以及消费防魔；完全阻挡、免疫、重复状态或无效动作不取得巫師仇恨；
- 范围与线路动作只要有效命中至少一名巫師，就进入同一“巫師仇恨”优先级带；命中多名
  巫師不会越过确定击杀。带内仍按既有击杀数、伤害、控制、威胁、风险与稳定平局顺序比较；
- 当前保存身份升级为 v27 / `ice-counterplay-wizard-focus-1`；v26 战中档和完成档无损迁移。
  不保存 AI 分数、候选或冰雪临时位移，读取后从公开模拟状态重新规划。

`legacyStrict` 可保留原版 AI 优先级，但其冰雪最外圈外推与当前默认规则一致。Mod 若改变
冰雪目的格资格、巫師职业标签或两级优先级带，必须改变规则身份。本决定不增加 AI 难度
选项，不改变玩家／独立友军 AI，也不解冻第 9 关。

### REMAKE-037：双方默认自动行动共享专家 AI

用户于 2026-08-09 试玩第 8 关后发现，把魔術士交给“自由行动”时只会普通攻击，不会
使用技术。复核确认这是 Web 实现分成两套默认规划器造成的漂移：side 2 已使用专家候选与
效用排序，side 1 自动行动仍走旧的职业分支；原版则由同一套职业分派机制服务双方，阵营
只改变敌对目标方向。用户明确要求默认友军 NPC 与“自由行动”使用和敌军一致的 AI，只有
关卡或玩家命令显式声明的策略可以覆盖。

`stableRemake` 规定：

- side 1 玩家单位交给“自由行动”后、独立友军 NPC，以及 side 2 敌军，默认都使用同一
  “专家”动作候选、严格优先级带、效用权重、射手／魔弓规则、冰雪门禁与稳定平局顺序；
- 默认自动队列在每次动作提交后从尚未行动者重新规划，读取最新的死亡、生命、状态与占位；
  规划不读取玩法 PRNG，正式动作结算仍按原合同消费 PRNG；
- “跟随主将”先满足临时凝聚约束，第 3 关 `terrain-hold`、第 4／5 关路线脉冲以及关卡
  目标／警戒／军团目标仍是硬约束；这些显式策略不因共享默认评分器而被取消；
- 玩家手动阶段、玩家军团先于独立友军的阶段边界、独立友军控制权和四档难度数值不变；
  不增加单独 AI 难度选项；
- 当前保存身份升级为 v28 / `shared-automatic-expert-ai-1`；v27 战中档和完成档无损迁移。
  不保存候选、分数或行动队列，读取后从公开模拟状态重新规划。

`legacyStrict` 可继续使用原版逐槽行为与职业 PIT 池，但 Web 控制权仍须由军团内容显式声明。
Mod 若改变默认规划器、权重、行动者重选或显式策略优先级，必须改变规则身份。本决定不
解冻第 9 关或新增关卡内容。

### REMAKE-038：第 8 关八名友军全部由玩家控制

用户于 2026-08-09 在最高难度试玩第 8 关后指出：五名行为 `2` 的游骑兵由独立友军 AI
控制时会主动冲出阵地送死，玩家无法纠正，体验不佳。原版 B/0017 的八名 side 1 单位、
坐标、职业和行为值继续作为 `[OF]` 证据；本决定只改变 `stableRemake` 的控制权归属。

`stableRemake` 规定：

- 第 8 关 side 1 槽 `8/17/18/40/41/42/43/44` 全部归入
  `sulanda-ranger-command` 玩家军团，集团主将仍为槽 `8` 蘇蘭達；不建立独立友军军团；
- 八名单位均可被玩家点选并提交移动、攻击、射击、技术、休息或待命；行为 `2` 只保留为
  原版内容证据，不再触发 NPC 控制权；
- “全部休息／跟随主将／自由行动”作用于全部尚未行动的玩家单位。只有玩家主动选择
  “跟随主将”或“自由行动”时，剩余单位才交给 `REMAKE-037` 共享专家 AI；
- 全部八名手动行动完成后直接进入敌方阶段，不插入独立友军行动。四档难度使用相同控制权，
  胜负、固定阵容、剧情、PRNG、敌军 AI 与第 9 关冻结边界不变；
- 当前保存身份升级为 v29 / `stage8-all-player-control-1`；v28 战中档和完成档无损迁移。
  军团归属由当前关卡内容重建，不把控制权重复写入存档。

`legacyStrict` 可保留原版三名玩家加五名自动友军的控制方式。Mod 若改变第 8 关玩家军团
成员或集团主将，必须改变内容／规则身份。本决定不推广到“救援友军”等明确依赖独立友军
策略的其他关卡，也不解冻第 9 关。

### REMAKE-039：第 9 关目标面板按机器条件显示

原版 SAY/0091 写“妮雅到達死亡之谷的頂端／妮雅戰敗”，但场景 9 的胜负处理器实际要求
side 1 槽 9 多莉进入线性格 `0..933`，或 side 2 全部离场；失败则检查槽 0 妮雅和槽 9
多莉，任一缺席即失败。B/0019 的固定槽、SAY/0022 的保护请求与 SAY/0023 的登船对白也
都确认路线角色是多莉。原文、机器条件与剧情主体的冲突继续保留在证据层。

`stableRemake` 目标面板显示“護送多莉抵達死亡之谷頂端，或擊退全部敵軍”，失败显示
“妮雅或多莉戰敗”。机器条件、失败优先级、固定部署和全灭备选胜利保持原版。当前保存
身份随第 9 关升级为 v30 / `stage-09-death-valley-1`；v29 无损迁移。

`legacyStrict` 可显示 SAY/0091 原文，但不能据此改写机器槽位。Mod 若改变保护槽、到达范围
或全灭备选条件，必须改变内容／规则身份。

### REMAKE-040：第 9 关行为 12 终点安全结算

原版多莉行为 12 按格号分段前往 `1266→1134→884`，每次移动预算为 7；当行动开始时
所在格低于 `934`，代码写入胜利 999，却没有设置新的路线目标。随后路径器误把 VGA 复制
遗留的 `DI=3450` 当作地图格，可穿行相邻零缓冲、折回后额外移动，或超过 100 项覆写代码
段。该行为是原生未定义内存破坏，不是可移植的战术随机。

`stableRemake` 保留三段目标、预算 7、占位与地形寻路；多莉进入 `0..933` 后立即写入胜利
999并消费本次独立友军行动，不再调用陈旧路径器。胜利剧情和普通保存仍按原事件顺序发生，
不会产生终点后的额外移动或剩余敌方行动。原版缺陷轨迹只保留在逆向证据和诊断资料中。

`legacyStrict` 也不得在普通 Web 运行时模拟内存越界；若研究工具需要重放参考轨迹，必须
隔离为不保存、不影响战役的诊断模式。Mod 可改变安全路线，但必须改变规则身份。

### REMAKE-041：内部第 11 关显示真实护送目标与玩家标题

原版内部场景号不按玩家游玩顺序连续：场景 9 的处理器跳到场景 11，场景 11 再跳回场景
10。SAY/0128 对应场景 11 标题“拯救蘇蘭達”，SAY/0129 才对应后续场景 10
“飛船上遭遇敵人”。旧 Web 冻结边界曾把两个标题错配，v30 场景 9 完成档因而保存了错误
标题。该错误不改变原版路由事实，但会误导玩家对下一场内容的理解。

场景 11 的机器条件要求 side 1 槽 8 蘇蘭達进入线性格 `0..279`，失败条件是该槽离场；
SAY/0093 却写“打敗碧娜維姬／妮雅戰敗”，且 B/0023 既无碧娜維姬也无妮雅。SAY
24–27 的撤离剧情同样明确由蘇蘭達带队登船。

`stableRemake` 使用 SAY/0128 标注场景 11，并显示“護送蘇蘭達登上飛船／蘇蘭達戰敗”。
胜负槽位、`0..279` 范围、全灭不胜利和失败优先级保持原版；v31 迁移 v30 时只把指向
stage 11 的旧错误标题改为“拯救蘇蘭達”，不改战役、战果、PRNG 或 roster。后续 v32
补齐原版每轮增援并无损迁移 v31；该增援属于 BAT-056 原版事实，不是新的规则修正。

`legacyStrict` 可显示 SAY/0093 原文，但不能据此改写机器条件。Mod 若改变护送角色、范围
或 stage 9→11→10 路由，必须改变内容／规则身份。

### REMAKE-042：内部第 10 关显示机器真实歼灭目标

内部 stage 10 的 SAY/0094 写“妮雅回到瓦爾克麗城／妮雅戰敗”，但 B/0021 是飞船甲板
战场，没有返回城堡的到达格或路线角色；胜负处理器实际在妮雅仍在场时，以 side 2 是否
全部离场作为唯一胜利条件。SAY/0028 也明确要求先处理追上飞船的半龙与飞马部队。

`stableRemake` 显示“擊退全部追兵／妮雅戰敗”。机器胜负条件、失败优先级、固定妮雅、
部署格和敌军阵容保持原版。保存身份随本关升级为 v33 / `stage-10-airship-pursuit-1`，v32
无损迁移。

`legacyStrict` 可显示 SAY/0094 原文，但不能据此添加不存在的回城格。Mod 若改变全灭条件、
保护槽、部署或敌军阵容，必须改变内容／规则身份。

### REMAKE-043：内部第 12 关显示沼泽战的真实歼灭目标

内部 stage 12 的 SAY/0095 沿用了“打败攻击瓦尔克丽城的敌人／妮雅战败”，但 B/0025
是飞船坠毁后的沼泽战场，五个敌方根槽全为水戰士；逐关处理器和 SAY/0030–31 也只描述
水戰士出现、被击退及收服。机器胜负处理器实际检查妮雅仍在场且 side 2 全部离场。

`stableRemake` 显示“擊退全部水戰士／妮雅戰敗”。机器胜负条件、失败优先级、固定妮雅、
部署和五个水戰士根槽保持原版；水戰士受普通近战后产生的共享生命分裂身体属于既有职业
机制，不计为关卡增援。保存身份随本关升级为 v34 / `stage-12-swamp-water-warriors-1`，
v33 无损迁移。

`legacyStrict` 可显示 SAY/0095 原文，但不能据此添加不存在的瓦尔克丽城战场或攻击者。
Mod 若改变全灭条件、保护槽、部署、敌军根槽或水戰士职业机制，必须改变内容／规则身份。

### REMAKE-044：水戰士共享生命在普通战斗结束后统一投影到其他分身

水戰士多个身体共享同一模拟生命。此前 Web 在普通攻击提交时已正确同步模拟结果，但地图
表现只冻结直接参战者的战前生命；未被直接命中的同组分身会立即读取最终生命，造成它们在
命中／反击动画尚未结束时提前扣血。

`stableRemake` 保持战果、共享生命、PRNG 与存档提交时点不变，仅修正表现投影：地图模式中，
直接参战的身体按既有命中后逐点扣血节奏显示；其他同组分身在整段普通战斗表现结束前保持
战前生命，结束后一次显示共享最终生命。全景模式下，全景生命条仍按正式战斗脚本播放，但
底层地图上的所有同组身体保持战前生命，直到返回地图后再统一显示最终值。新产生的分裂体
仍在战斗表现结束后出现。

这是纯表现修复，不改变模拟、规则身份或存档版本；地图／全景开关、加速和减少动态均不得
改变结算结果。

### REMAKE-045：全景战斗期间右侧角色详情冻结战前快照

原版全景战斗会在入场时绘制两侧状态数字，并让这些数字在整段交锋中保持战前值；只有战斗
窗底部的彩色生命槽在命中时切到扣血后数值。Web 版的常驻右侧角色详情栏不属于原版全景窗，
此前却直接读取已经原子结算的模拟单位，导致生命、经验和状态在全景动画结束前提前显示最终
结果，与全景状态框及返回地图后的表现时点冲突。

`stableRemake` 将此视为原版构图在 Web 常驻 HUD 上的适配 bug：只要普通战斗仍处于 `full*`
阶段，右侧详情栏对当前参战焦点统一读取战斗入场快照，包括生命、经验、行动状态、增减益及
由其推导的有效数值；全景窗底部生命槽继续保留原版的命中时更新。全景层关闭、回到地图后，
详情栏一次切换到最终模拟状态。地图战斗模式的既有逐点生命表现不变。

这是纯 UI 投影修复，不改变战果、模拟提交顺序、PRNG、规则身份或存档版本；普通职业、水戰士
及后续职业必须共用同一规则，不得按职业另开详情时序分支。

### REMAKE-046：内部第 13 关显示机器真实的瑪西爾首领目标

内部 stage 13 的 SAY/0096 写“打败所有的敌人／妮雅战败”，但胜负处理器只扫描 side 2
槽 24 是否仍在棋盘；B/0027 的该槽是具名神劍戰士瑪西爾。其余八名守军即使仍在场，瑪西爾
离场也会立即满足胜利。关前 SAY/0032 只要求精锐小队突击龍塔，没有建立全灭语义。

`stableRemake` 显示“擊敗『瑪西爾』／『妮雅』戰敗”。机器胜负条件、失败优先级、部署、
九名开局敌军与无增援事实保持原版；不把文案的“所有敌人”反向写成歼灭规则。保存身份随
本关升级为 v35 / `stage-13-dragon-tower-marsiel-1`，v34 无损迁移。

`legacyStrict` 可显示 SAY/0096 原文，但不能据此改变槽 24 条件。Mod 若改为全灭、替换首领、
改变保护槽、部署或敌军阵容，必须改变内容／规则身份。

### REMAKE-047：内部第 14 关显示机器真实的芳首领目标

内部 stage 14 的 SAY/0097 写“叫醒沉睡中的維絲塔／妮雅战败”，但胜负处理器只扫描
side 2 槽 8 是否仍在棋盘；B/0029 的该槽是具名半龍戰士芳。其余六名守军即使仍在场，
芳离场也会立即满足胜利。SAY/0033 只建立攻略队闯入龍塔、芳下令阻挡的情境，没有維絲塔
在本关棋盘或逐关事件中出现的证据。

`stableRemake` 显示“擊敗『芳』／『妮雅』戰敗”。机器胜负条件、失败优先级、部署、七名
开局敌军与无增援事实保持原版；不为错误文案虚构唤醒事件。原版第 6 回合起持续清零敌方
逐槽行为保留为证据元数据；已接受的 `REMAKE-033/037` 共享专家 AI 不读取这些旧行为值，
因此默认规则不新增会改变模拟的运行时事件。保存身份随本关升级为 v36 /
`stage-14-dragon-tower-fang-1`，v35 无损迁移。

`legacyStrict` 可显示 SAY/0097 原文，但不能据此改变槽 8 条件。Mod 若加入維絲塔、改为
到达／唤醒目标、替换首领、改变保护槽、部署或敌军阵容，必须改变内容／规则身份。

### REMAKE-048：内部第 15 关显示机器真实的蘭首领目标

内部 stage 15 的 SAY/0098 写“打败敌将领娜米／妮雅战败”，但胜负处理器只扫描 side 2
槽 9 是否仍在棋盘；B/0031 的该槽是具名半龍戰士蘭，SAY/0034 也以蘭的画像 35 和台词
建立第二层守将身份。其余九名守军即使仍在场，蘭离场也会立即满足胜利。

`stableRemake` 显示“擊敗『蘭』／『妮雅』戰敗”。机器胜负条件、失败优先级、部署、十名
开局敌军与无增援事实保持原版；不把目标文本的错名反向写入模板身份或规则。原版第 6 回合
起持续清零敌方逐槽行为保留为证据元数据；已接受的 `REMAKE-033/037` 共享专家 AI 不读取
这些旧行为值，因此默认规则不新增会改变模拟的运行时事件。保存身份随本关升级为 v37 /
`stage-15-dragon-tower-lan-1`，v36 无损迁移。

`legacyStrict` 可显示 SAY/0098 原文，但不能据此改变槽 9 条件。Mod 若替换首领、改变保护槽、
部署或敌军阵容，必须改变内容／规则身份。

### REMAKE-049：落雷双相电波恢复两层，并修复单位标记层的高字节比较

原版落雷的电波每次绘制调用两个写入器，都写 `DS:58A5h` 的 `10×7` 地图特效 sprite 层：

- **扫描层** `0000:6599 → 0000:97DC → 0000:65A5`：对范围值非零且落在当前扫描带的格写入
  `范围值 − 当前阈值` 作为 sprite 码，`0000:7EDD` 按 `帧 = 码 − 1` 贴图、码 `0` 不画。
  不读取阵营，也不要求格上有单位。因为范围图中心值最高，画面表现为自中心向外扩散、
  每圈落后一次绘制的爆闪。
- **标记层** `1000:6E46`：对范围值非零、落在同一扫描带、且阵营等于 `DS:1EF6h` 的格，交替
  绘制逐级固定的两个 tile 码。`2L/3L/4L` 指向各自资源末两帧的“角色被电击”专用美术，
  `1L` 的 `MAGIC/31` 没有人物美术，改用序列中最盛的两帧。

逐级帧预算证实两层划分：`2L/3L/4L` 满足 `帧数 = 波宽 + 2`（`7=5+2`、`6=4+2`、`13=11+2`），
`1L` 为 `12` 帧、波宽 `9`。

`[OF]` 标记层在发行版里永远不会绘制：`1000:6E52` 用 `mov al,[es:di]` 读范围值时 `AH` 仍是
`DS:01A9h` 范围图段的高字节（`2FH`），随后 `1000:6E5F` 却做 `cmp ax,[0x5228]` 的字比较，
结果恒大于波宽 `4/5/9/11`，`ja` 恒跳过。全模块只有此处出现“字节读后紧跟字比较”，同语义
判定在 `0000:65A5` 写法正确；`DS:01A9h` 只被 `0000:FFF6` 写入一次立即数 `2FCH`，实模式下
高字节不可能为 `0`。

`stableRemake` 同时绘制两层，并按设计意图修复该比较，使标记层真正叠加在扫描带内的敌方
占用格上。判定条件按原生语义保留差异：扫描层要求码 `1..波宽`（码 `0` 不画），标记层要求
码 `0..波宽`。逐级资源、波宽、tile 码、绘制次数、`2` tick 等待、`(范围图最大值−1)+波宽`
轮数、固定图形等待与提交时点全部不变；本决定只影响画面，不改变伤害、经验、PRNG、存档
语义或规则身份。

`legacyStrict` 可只画扫描层以复现发行版的实际画面。Mod 若替换逐级资源、波宽或 tile 码，
必须改变内容身份。

### REMAKE-050：第 4 关力场脉冲按共用电波恢复两层

`1000:6D4C` 不是落雷技术专用例程。第 4 关行为 12 的力场脉冲 `1000:6D40` 调用同一个电波，
只是把范围图换成 `1000:6E88` 反转后的“结界取反”图：反转把每个非安全格写成 `1`，
`1000:6E25` 取最大值减一得阈值起点 `0`，`1000:6CF8` 写波宽 `11` 与标记码 `12/13`。
资源、波宽和标记码与究极落雷完全一致，`MAGIC/26` 的 13 帧同样满足 `帧数 = 波宽 + 2`。

因此 `REMAKE-049` 的两层结论直接适用，且在本关的画面后果更大：A 层由
`0000:97DC` 以 `DS:5390h/5392h` 相机原点遍历 `10×7` 窗口，对窗口内每个非安全格写
`范围值 − 阈值`，**不分阵营、不分是否有单位**。所以原版表现是「除葛蒂拉斯结界外，
当前画面上的每一格都逐帧爆闪」，前 11 次绘制走帧 `0..10`，后 11 次空白，合计 44 tick。
复刻版此前只画了 B 层的意图（受击单位上的帧 `11/12`），完全漏掉 A 层。

`[OF]` 本关 B 层还多一条独立的失效原因：行为 12 链路（`1000:1BBE`、`0000:633D`、
`1000:6CF8..6EDF`、`0000:6599`、`0000:97DC`、`0000:64A1`、`0000:63CF`）没有任何指令写
`1000:6E46` 读取的 `DS:1EF6h`，该值只是上一次攻击或技术遗留的陈旧阵营；而扣血扫描
`1000:6EA9` 在 `1000:6EC4` 硬编码 `cmp al,1`。

`stableRemake` 与 `REMAKE-049` 一致地同时绘制两层：A 层按当前视口内的非安全格绘制
`帧 = 码 − 1`，B 层按设计意图取扣血过滤的 side 1，叠加在受影响单位格上。绘制次数、
`2` tick 等待、44 tick 固定等待、`floor(currentLife / 2)` 结算、死亡离场、提交时点、
PRNG 与存档语义全部不变；本决定只影响画面。

`legacyStrict` 可只画 A 层以复现发行版的实际画面。Mod 若替换资源、波宽、标记码或
范围反转规则，必须改变内容身份。

### REMAKE-051：胜利条件原文改由模块 29 `DS:1273` 关卡表取值

`[OF]` 逐关“勝利條件／失敗條件”面板的 SAY 记录不是从关卡号推导的，而是模块 29
`DS:1273-131E` 的 42 项关卡键值表（数据签名
`5fae677f…0340e`，见 `story-presentations.json` 的
`globalReachabilityAudit.tables.alternate`，消费者是 `DS:80B5` 选择器在文件偏移
`77443` 的 `AX` 写入，未命中返回 `65535`）。同一审计中的 `DS:30BA-3169` 表提供逐关标题，
键 16 指向 SAY/0134“龍塔第三層”。目标表的实际取值为：

| 关卡 | 记录 | 关卡 | 记录 | 关卡 | 记录 |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 0 | 82 | 7 | 109 | 14 | 83 |
| 1 | 98 | 8 | 162 | 15 | 84 |
| 2 | 172 | 9 | 110 | 16 | 85 |
| 3 | 106 | 10 | 111 | 17 | 86 |
| 4 | 107 | 11 | 113 | 18 | 87 |
| 5 | 108 | 12 | 114 | 19 | 88 |
| 6 | 160 | 13 | 159 | 20 | 89 |

该表逐项与机器胜负条件一致：键 1 的 SAY/0098“娜米”对应 stage 1 的 side 2 槽 16 娜米，
键 3 的 SAY/0106“梅蒂／黛西／希蜜”对应 stage 3 的槽 17 梅蒂与双保护槽，键 4 的
SAY/0107 对应 stage 4 的葛蒂拉斯护送，键 5 的 SAY/0108“丁塔琪及萊茵”对应 stage 5 的
双首领任一，键 9/11 的 SAY/0110/0113 对应多莉与蘇蘭達护送，键 13/14/15/16 的
SAY/0159/0083/0084/0085 对应瑪西爾／芳／蘭／莎。

`stableRemake` 以该表为胜利条件原文的唯一真值。内部第 16 关据此显示
“擊敗『莎』／『妮雅』戰敗”，与原文语义一致，本关不需要额外的目标文字修正决策。

本决定同时推翻了此前按 `82 + 关卡号` 推导原文的做法，以及由此得出的“原版目标文字与
机器条件冲突”前提。用户于 2026-08-10 授权按本条更正第 1–15 关，实施记录见 M31。更正后：

- 第 1、2、3 关此前直接显示了错误记录的首领名（芳／蘭／莎），而机器首领分别是
  娜米（side 2 槽 16、肖像 `D/20`）、萊莉（槽 18、`D/19`）、梅蒂（槽 17、`D/16`）。
  这三关的目标文字、棋盘棋子姓名与右栏肖像现已全部改由 `enemyActors` 派生，不再手写；
- 第 4–15 关显示的是复刻自撰文案，与机器条件本就一致，只有取证与理由需要更正。
  `REMAKE-017`、`REMAKE-023`、`REMAKE-027`、`REMAKE-028`、`REMAKE-030`、`REMAKE-031`、
  `REMAKE-041`、`REMAKE-042`、`REMAKE-043`、`REMAKE-046`、`REMAKE-047`、`REMAKE-048`
  的“原文冲突”前提不成立，各关合同已改写为“原文与机器条件一致”，显示文案保留；
- `REMAKE-039` 只部分失效：第 9 关真实原文 SAY/0110 的护送对象与双保护都正确，但仍
  未提到全灭备选胜利，因此该决定继续有效，只缩小到补上该条备选；
- 十五个生成器现都从 `story-presentations.json` 的 `DS:1273` 表校验自己的记录号，
  `tests/unit/objective-records.test.ts` 逐关比对记录号、目标文字与棋子身份。

机器胜负条件、PRNG、存档格式与规则身份均未改变；受影响关卡的生成内容身份随取值更正
重新计算，存档不保存逐关内容身份，因此既有 v38 存档继续可读，保存版本不变。

`legacyStrict` 可逐字显示表中记录的原文。Mod 若替换关卡目标文本记录，必须改变内容身份。

### REMAKE-052：WD 等价路径使用可保存 PRNG，专家规划不预看随机

`[OF]` 女帝与龍的 AI 池固定使用 `WD`。动作在距离 10 内选敌军，从目标向施法者回溯
合法前驱；存在多个等价前驱时，原版用 PIT 余数决定方向。路径每格只处理与所选目标同
阵营的单位，饱和扣除最多 90；防魔完全阻挡且不被清除，动作固定经验 0。表现使用
`MAGIC/19` 十帧，无直接 VOC，等待 `(路径格数+10)×20` native tick。原版 `.TST` 不保存
PIT 相位，因此读档后无法单凭存档重放同一等价路径。

`stableRemake` 规定：专家 AI 枚举目标时使用固定前驱顺序计算规范路径，不消费也不窥视
模拟 PRNG；目标确定并正式准备动作后，只有实际遇到多个等价前驱时才从版本化、可保存的
模拟 PRNG 选择。准备结果冻结最终路径、受影响单位、生命与 PRNG 后态，提示、镜头和动画
不得重算。这样相同存档与语义操作可以确定重放，同时不让专家评分通过随机预看获得隐藏
信息。

本决定不覆盖既有 `REMAKE-013`：冰封主目标仍非法，路径覆盖的冰封单位完整跳过且保留
防魔。规划与提交使用同一距离、地形不可达门和目标阵营过滤；它们只在等价前驱选择的
随机时点不同。

`legacyStrict` 可使用原版 AI 与 PIT 方向余数，但这种模式必须明确标记为不能从原版
`.TST` 保证未来路径重放。Mod 若改变前驱顺序、随机源、选择距离、阻挡地形、单格伤害、
防魔、冰封或经验语义，必须改变规则身份。

### REMAKE-053：龍系 Boss 的混乱／毒免疫覆盖同槽普通命中

`[OF]` `LA/混亂` 与 `IP/施毒` 的技术包装器在完整表现后检查目标职业，`1P/龍`、`2P/頭`、
`3P/手` 均跳过状态写入。但普通攻击附加状态处理器 `0000:92DC` 直接把邪劍／叢林命中的
`+0Eh/+14h` 状态槽写成 `8003h`，没有调用技术包装器，也没有职业免疫判断。因此发行版
妖龍可以被邪劍普通命中混乱，也可以被叢林普通命中施毒；后者在完整回合边界把当前生命
减半，是 2400 生命妖龍会异常快速失血的主要捷径。职业数据与通用难度播种仍确认本关
难度 0 妖龍应为 1001 经验、2400 最大／当前生命，不应凭体感改写为更高数值。

试玩截图同时证明 Web 的真实叙事替换路径曾把妖龍生成为 160／2400：通用动态增援按数字
槽查找 side 1 战役 roster，敌方槽 28 因而碰撞到无关的我方槽 28 并继承其 160 当前生命。
这是复刻实现错误而非原版规则或 `stableRemake` 决策；修复后 side 2 不再读取 side 1 roster，
真实部署→替换流程以 2400／2400 登场。

用户于 2026-08-11 试玩后明确要求龍系 Boss 不应被混乱，并反馈其失血过快。`stableRemake`
据此把 `1P/2P/3P` 的 `LA/IP` 免疫解释为职业级状态免疫：邪劍与叢林主动普通攻击仍结算
原有物理伤害、随机、反击、经验和行动位，但不向这三类目标写混乱或毒。这样保留原始
Boss 生命与战斗公式，同时关闭两条同槽绕过路径。

`legacyStrict` 可保留 `0000:92DC` 的直接写入漏洞。Mod 若改变免疫职业、状态集合或让免疫
同时阻挡普通物理伤害，必须改变规则身份。

### REMAKE-054：琴斯的剧情棋子固定显示为魔祭師

`[OF]` 场景 20 的即时胜利事件只向棋盘写 side 1 槽 7，没有在该指令中另写职业；显示职业
因此取决于当时槽 7 的战役状态。与此相邻的明确角色证据中，场景 42 与场景 22 的模板都把
同一槽 7／肖像 14／琴斯写为职业记录 3“魔祭師”。Web 的第 20 关实现却把胜利登场职业
硬编码为记录 0“士兵”，并从同号战役 roster 继承了士兵当前生命，造成“士兵／琴斯”及
160／305 的混合职业状态。

用户于 2026-08-11 试玩后明确指出琴斯登场职业不应为士兵。`stableRemake` 因此把剧情中的
琴斯身份统一为记录 3“魔祭師”：第 20 关仍按原事件生成 side 1 槽 7、使用肖像 14、从
`(40,16)` 移到 `(33,15)`，但职业、地图棋子、HUD 和派生属性都从记录 3 重建；经验保持 0，
当前生命按该职业首行回满至 305。此处只修正第 20 关剧情棋子，不提前把仍不可部署的槽 7
写入玩家 roster，也不改变 stage 21 冻结边界。

用户于 2026-08-12 继续指出：琴斯在第 23 关首次成为部署候选后仍显示士兵。复核 B/0045
确认 stage 22 的 side 1 稀疏职业表已把槽 7 写为记录 3，而 B/0047 首次把槽 7 纳入普通部署；
因此 `stableRemake` 在进入 stage 23 时，把尚为默认士兵／经验 0 的槽 7 建立为魔祭師、经验
0、生命 305 的 campaign entry baseline。已有非默认职业或后续成长不被入口基线覆盖；v48
的 stage 23 以后旧档若仍保存错误士兵职业，则迁移为魔祭師并保留已损生命量。此修正改变
stage 23 内容身份与存档内容版本，但不改变胜负、PRNG 或 stage 22 的临时剧情单位边界。

`legacyStrict` 可继续逐字继承原事件到达时的 `ME_DATA[7]`，包括异常或未初始化的士兵状态。
Mod 若替换琴斯的剧情职业、经验或派生属性，必须改变内容身份。

### REMAKE-055：第 21 关作为确定性的非交互侦察过场

`[OF]` 场景 21 的 B/0043 开局双方为空且没有部署格。round 1 处理器先把妮雅、希蜜、
葛蒂拉斯、蘇蘭達四个战役槽写入森林入口，播放 SAY/0043，再按固定顺序用 `FM` 预算 50
移动到村庄并播放 SAY/0044。side 2 始终为空，因此事件完成后即时胜利；该场景属于跳过
普通胜利／保存提示的特例，999/1000 都直接进入 module 27 / stage 22，1000 不重播过场。
原版剧情路径器的 PIT 选择不会被 `.TST` 保存，但四个终点、执行顺序与最终路由不依赖
具体等价路径。

用户于 2026-08-11 确认第 20 关测试完成并授权推进第 21 关。`stableRemake` 将本场建模为
可序列化的战役事件链：四人继承 campaign roster 的职业、经验、生命与状态；每段脚本
移动使用确定性规范路径，不消费战斗 PRNG；对话、镜头、逐格速度、减少动态或跳过只改变
表现等待。SAY/0044 完成后直接提交 stage 22 路由，不开放部署、玩家／AI 回合、普通胜利
反馈或第二次存档提示。这样保留原版可见顺序和终点，同时确保存档与自动回放不依赖 PIT。

`legacyStrict` 可恢复原生 `FM` 的 PIT 等价路径选择，但必须明确其不能由原版 `.TST`
保证逐格重放。Mod 若改变四人身份、写入／移动顺序、起终点、是否可交互或完成路由，必须
改变内容身份；若改变随机源、胜负或保存语义，还必须改变规则身份。

两条表现细节一并登记。其一，round 1 的首个操作是 `focusPortraitResource 46`，因此整段
写入的右栏属于妮雅，不随最后写入的蘇蘭達漂移。其二，槽图字节的 `80h` 行动位是本阶段
已行动标记（见 `turn-action-system.md`），但事件取证只记录了写入的槽号，没有记录该位；
由于本场永不进入任何阵营阶段，行动位没有可观察语义，`stableRemake` 让四名斥候以未行动
状态登场，不显示已行動徽章。这只是本场的表现选择，不改变其他场景的写入默认值，也不
构成对该位取值的证据主张。

### REMAKE-056：第 22 关保持确定性的首回合伏击与单一妖龍目标

`[OF]` 场景 22 的 B/0045 在部署前只有固定妮雅与十八个部署格，side 2 为空。round 1
处理器依次写入并移动女帝／琴斯，播放 SAY/0076–77，写入葛蒂拉斯后播放 SAY/0078，
写入妖龍后播放 SAY/0079，再清除两名临时我方并写入四名魔祭師。最终六名敌军的槽、职业
与位置固定；本关不进入完整回合特殊生成或击杀替补／形态转换。机器胜利只检查槽 28
妖龍离场，失败只检查妮雅离场，SAY/0090 与机器条件一致。

用户于 2026-08-11 确认第 21 关测试通过并授权推进第 22 关。`stableRemake` 将部署后的
round 1 编译为确定性战役事件链：两段 `FM` 使用规范路径、不消费战斗 PRNG；剧情单位
只存在于当前棋盘且不覆盖 campaign roster；六名敌军按原版难度经验重建并在玩家取得
控制时保持未行动。琴斯写入保留“聚焦 `(33,29)`、实际写 `(32,29)`”的原生错位；槽
40–43 不逐个聚焦，而在正常战斗刷新时一同显现。跳过剧情只压缩表现等待，不改变部署、
敌军、当前生命、胜负或保存语义。

玩家回合继续使用全战役统一专家 AI、龍职业状态免疫及现有动作规则；本决定不新增职业
能力。胜利严格绑定槽 28，不能因其余五敌仍在场而阻止结算。普通胜利反馈、保存提示、
失败重试与默认 `module25/stage23` 路由保持既有规则；保存身份升级为 v44，并只允许在
首回合剧情完成后的稳定玩家阶段写战中档。

`legacyStrict` 可恢复原生 `FM` 的 PIT 等价路径及逐帧 VGA 刷新时序。Mod 若修正琴斯
聚焦错位、改变伏兵写入顺序、敌军总量、首领目标或把临时剧情单位写入战役 roster，必须
改变内容身份；改变胜负或 PRNG 语义还必须改变规则身份。

### REMAKE-057：全景致死保留底层棋子并衔接地图死亡链

`[OF]` 原版地图普通攻击的致死路径使用 `MAGIC/12` 十五个描述符，前六步保留棋子，
第七步开始清格；原版全景路径则先完成阈值受击，再运行 `frame 2 / E/11` 的二十四子步
死亡流并瞬时关闭窗口。Web 模拟层会在表现开始前原子提交战果和死亡移除，因此表现层必须
另持有只读参战快照，不能让模拟提交时点泄漏为玩家可见的提前消失。

用户于 2026-08-11 指出：开启全景后，致死目标在全景开窗前已经从底层地图消失，窗口关闭
后也没有地图死亡动画。`stableRemake` 据此规定，全部普通全景致死遵守三段可见顺序：

1. 从攻击提交到全部 `full*` 阶段结束，底层地图以入场快照保留攻防双方及同组死亡实体；
2. 全景内仍完整播放实际伤害对应的受击段与原生 `frame 2 / E/11` 死亡段；
3. 全景层关闭后，从直接受击实体开始播放完整 `MAGIC/12` 地图死亡链；共享生命组再按既定
   创建顺序串行播放其余实体，第六描述符完成后才逐实体清格。

转职、胜负、自动阶段和后续单位行动必须等待地图死亡链完成。参战快照不重新加入占用、
目标、ZOC 或胜负判断；表现开关与速度不改变伤害、经验、状态、PRNG、规则身份或存档。
`legacyStrict` 可在全景原生死亡流后直接返回最终地图状态而不追加 `MAGIC/12`。Mod 若改变
全景与地图死亡的组合、串行顺序或清格边界，属于表现语义覆盖，不得反向改写模拟结果。

### REMAKE-058：第 23 关保持精确到达区与固定二十一名守军

`[OF]` 场景 23 的 B/0047 固定妮雅并提供十四个开放格，side 2 开局已有二十一名静态
守军。round 1 处理器只播放 SAY/0046，不写入、替换或移动单位；本关不进入动态棋盘目录、
完整回合特殊生成或击杀替补／形态转换。机器胜利检查妮雅的线性格号 `y×50+x` 是否在
`0..524`，失败检查妮雅是否离场且优先；SAY/0091 与机器条件一致。

用户于 2026-08-11 确认第 22 关测试通过并授权推进第 23 关。`stableRemake` 保持精确线性
格号语义，不把出口近似为矩形，也不要求击破任何守军；二十一名敌军全部存活时，妮雅进入
格 524 仍立即胜利。行为 1 继续作为守卫硬约束，行为 0/2 进入已接受的共享专家追击规划；
行动规划不消费表现随机。模块 25 的 SAY/0045 完成后才进入部署，失败直接返回部署且不重播
关前剧情。保存身份升级为 v45，并严格保存二十一名静态敌军职业与完整 PRNG 状态。

原版场景 22 胜利默认连续进入模块 25／场景 23；SAY/0045 以 `BK` 开始且全段没有
`PP`，因此显示的是场景 22 退出时帧缓冲。Web 对该跨关画面与存档时点的处理由
`REMAKE-059` 单独定义；Mod 若改变出口区、敌军总量、行为硬约束或重试语义，必须改变内容身份，
改变胜负或 PRNG 语义还必须改变规则身份。

### REMAKE-059：SAY/0045 作为第 22 关关后对话并在对话后存档

`[OF]` 原版顺序是场景 22 普通胜利反馈／存档，再进入模块 25／场景 23 播放
SAY/0045。该脚本的 `BK` 保留场景 22 帧缓冲，台词又直接处理妖龍战后的女帝、
希蜜伤势和全军整顿，叙事上属于第 22 关战后。

用户于 2026-08-12 明确决定把该段对话归入第 22 关关后，并把存档提示移到对话播放完成之后，
以使叙事归属、背景与关卡事件链一致。`stableRemake` 固定顺序为：第 22 关目标达成 →
SAY/0045 十五个检查点（直接显示当前 stage 22 战场画布）→胜利反馈 → 存档提示 →
stage 23 部署。stage 23 不再登记 SAY/0045 关前事件，失败重试也不会重播。

本决定有意与原版存档时点不同，但不改变任何战斗结算、成长、目标或 PRNG。存档升级为 v46／
`stage-23-death-valley-breakthrough-2`；v45 的 stage 23 战中档移除旧关前事件标记，v44/v45 的
stage 23 完成边界按“已播放 SAY/0045”迁移，避免兼容存档重复播放。内容身份同步变更，
规则身份与完整 PRNG 语义不变。

### REMAKE-060：共享专家追击按完整可移动通路代价选择目标

`[OF]` 原版普通 AI 的远追与落点选择保留多处几何距离、固定方向表和逐行为专用流程；这些
事实继续由 `reverse/notes/ai-decision-system.md` 记录。`REMAKE-033/037` 已明确让
`stableRemake` 的敌军、独立友军 NPC 与“自由行动”改用共享专家规划器，因此其追击目标
选择属于复刻产品规则，不反向声称为原版行为。

用户于 2026-08-12 试玩后指出 NPC 看起来按画面几何距离而非可移动路径距离选择目标。代码
复核确认：没有即时交战动作时，专家规划器把全部敌方四邻格直接按曼哈顿距离排序；不可
通行格也会压低“原点距离”，所以近处隔墙目标不仅可能盖过通路更短的远目标，还可能令
单位原地待命。

`stableRemake` 据此改为从当前位置对全部合法敌方交战邻格执行确定性的完整通路搜索：职业
地形进入代价、不可通行格、敌方阻挡、友军穿越但不可落点、敌方控制区终止传播以及军团
路径约束都先参与搜索；不可达邻格排除，累计移动代价最低者胜出，同值按线性格号稳定
打破。行动沿选中通路走到本回合预算内最后一个合法落点，效用中的“目标推进”使用本次
实际减少的通路移动代价。搜索与评分不读取或消费 PRNG。

用户在初次优化试玩后继续指出两类同阵营拥堵：后排近战会因前排占满唯一交战格而待命；
已经近身的前排即使能换位攻击，也会因自身路径较短而持续堵住关键路口。用户确认按两阶段
方案继续纳入 `REMAKE-060`。第一层真实空闲交战通路完全不存在时，搜索可把同阵营占据的
合法交战格作为“虚拟前线”播种第二张代价图；它只用于让后排严格缩短队列距离，仍禁止
落脚，且不能压过任何真实空闲通路。

对于已经近身且不是确定击杀的普通攻击者，规划器先锁定既有评分选出的目标，只在同目标、
同战术结果且不增加暴露的替代攻击落点间做单行动反事实：临时投影攻击者换位，统计同军团
未行动普通近战新获得的真实交战通路数，以及已有通路累计缩短代价；净收益为正时，该收益
先于地形防御与攻击者自身路径长度打破平局。反事实不预留后续行动、不写入战斗状态；当前
攻击提交后，既有逐行动动态调度重新读取实际占格。确定击杀、额外暴露、守卫、防区、显式
路线和不同军团目标都不会被让路收益覆盖。

剧情路线、护送路线、路线脉冲、守卫、跟随主将和防区教义仍是显式硬约束，不改用本追击
选择器；`legacyStrict` 可保留原版逐行为几何流程。保存身份升级为 v47／
`expert-path-distance-ai-1`，v46 原样迁移；已提交行动、棋盘、回合、状态和完整 PRNG 不变，
读档后尚未提交的自动行动按新规则重算。Mod 若改变通路代价、目标格集合、占格／控制区
语义、虚拟前线、让路候选边界或平局顺序，必须改变规则身份。

### REMAKE-061：第 24 关保留线性城堡到达区并直接路由第 26 关

场景 24 的机器胜利条件不是矩形区域或全灭：妮雅槽 0 的线性格号 `y×50+x` 进入
`0..1030` 即胜，妮雅离场失败且失败优先。B/0049 的二十二名守军是整场固定总量；初始
模板、逐关处理器、动态棋盘目录、完整回合特殊生成链及击杀替补／形态转换链均没有后续
生成。原版 999/1000 又都从场景 24 直接进入模块 27／场景 26，内部场景 25 没有标题键，
不能在 Web 战役中自行补造一关。

`stableRemake` 因而保留精确 `0..1030` 目的地区，常驻投影共享目的地标记，并明确提示
无需全灭。行为 1 的十二名守军保持哨戒，行为 2 的十名守军使用既有专家追击；不新增
难度兵、增援或 PRNG 消费。部署后播放 SAY/0047，现场胜利后、普通胜利反馈与存档提示前
播放 SAY/0048；失败直接返回 1–15 人部署。完成路由以稳定语义 ID `stage-26` 保存，并显示
SAY/0143“遭遇碧娜維姬”。保存身份升级为 v48／`stage-24-castle-approach-1`，v47 原样
迁移；随后 `REMAKE-054` 的琴斯首次入队职业修正把当前保存身份升级为 v49／
`stage-23-campaign-class-baseline-1`，并迁移 v48。表现设置只能改变等待与绘制，不能改变
胜负、敌军、AI、随机序列或存档语义。

### REMAKE-062：半龍戰士傳送复现原版直连技術并补一项可见子菜单

`BAT-068` 推翻了此前“`1N/半龍戰士` 是普通行动职业、技術为空”的结论。原版行动菜单归属
由 `0000:6770/67D2/67EF` 三条内联职业链决定，技術链共 14 项，末位就是 `1N`；它不在
DS:`41CEh` 分层表里，而是由 `0000:702D` 在查表前直接跳 `0000:7475`。因此它没有动作码、
没有子菜单、没有阶段和等级门槛，单位一出现就能用。

`stableRemake` 因而按原版机制实现，而不是沿用对阵场此前那条实验室专用扩展：

- 范围用固定种子 `200` 与传播模式 `0`，即每步扣 1、只拒绝地形规则 `99`、传播时完全不读
  阵营图，因此可越过敌我占格。半龍戰士的移动 profile 只把地形槽 `0/17` 记为 `99`，在
  `50×50` 网格上最大曼哈顿距离 98 远小于 `200-1`，所以有效范围是“全图内与起点连通且
  地形规则不为 `99` 的格”，不是无条件的任意格；被 `99` 地形完全封闭的区域仍不可达。
- 落点必须范围图非零且当前为空，与普通移動确认路径同一套校验；不能停在占格上。
- 表现复用普通移動的前驱回溯路径与逐格动画，不是瞬间闪现；路径可以经过占格。
- 提交只移动行动者并立刻消费行动，不提供普通移動之后的“攻擊”追加菜单；无伤害、无经验、
  不消费模拟 PRNG。
- 敌方与友军自动单位不使用该技術：原版 `1N` 属普通 AI 分发且无任何 `1N` 判定。

`[DD]` 原版按下“技術”会直接进入目标选择，没有任何名称提示。复刻保留对阵场此前引入的
二级菜单做法并推广到全部表面：按“技術”后先显示唯一一项“傳送”，选中才进入目标态，取消
回到行动菜单。该项只增加一次可取消的确认，不改变范围、落点规则、行动消耗、结算顺序或
随机序列。原版没有为这项技術留下任何名称，“傳送”因此是复刻新增文本而非原版正字。

`[DD]` 对阵场此前的实验室专用扩展动作（含“瞬移”标签、蓝环／金环八帧调试表现和
“无条件全图”范围）随本决策撤销，改为与战役共用同一条正式规则和移動表现。

### REMAKE-063：内部第 26 关保留敌方阶段尾双次纵列下推

`[OF]` B/0053 的正常 side 2 AI 全部完成后，`1000:24A5/24B4/2508` 无条件调用同一段
纵列效果两次。每次都从当前棋盘按线性格号选第一名 side 1，先把其格号减 `50`，再持续
减 `50` 直到小于 `684`，保留该列作为演出与扫描原点；第二次必须读取第一次已经提交的
棋盘，不能复用第一次选择或把两次合成一个最终位移。

`[OF]` 每次先完整播放 `MAGIC/21` 十三描述符×15 tick、`MAGIC/14` 四描述符×15 tick，
再播放二十六描述符×5 tick 的纵向扫掠，共固定 `385` native tick。演出结束后才从原点下方
第 17 格向上扫描到第 1 格；每名 side 1 依次尝试向下 `3/2/1` 行的首个空格，无空格则
留在原位。原版只复制棋盘的阵营／单位槽映射，不改生命、状态、acted 或 PRNG。没有 side 1
时该次没有演出或位移。五条生成通道另确认本关只有开局八名敌军，没有增援、替补或形态转换。

`stableRemake` 以通用“敌方阶段尾：准备 → 完整表现 → 原子提交”边界复现以上顺序；正常
敌方行动造成的胜负也延后到两次尾阶段之后再进入终局判断，以保持原完整回合顺序。表现
速度、减少动态和声音只能缩短墙钟等待，不能删减 43 次绘制、提前提交坐标、改变第二次重选
或消费模拟随机。碧娜維姬槽 1 离场胜利、妮雅槽 0 离场失败且失败优先；失败返回 4–22 人
部署，胜利播放 SAY/0050 后保存到 `stage-27`“趕回瓦爾克麗城”。保存身份升级为 v50／
`stage-26-column-push-1`，v49 原样迁移；内部 stage 25 仍不构造运行时。

### REMAKE-064：内部第 27 关保留混合控制军团与四段线性回城目标

`[OF]` B/0055 同时放置七名行为 2 城防友军、三名行为 0 工兵、行为 0 妮雅与二十八名
可选战役角色。行为 2 单位由 side 1 自动队列行动，不受玩家集团命令；行为 0 固定单位与
部署候选由玩家行动。side 2 开局只有五名行为 0 叛军。五条生成通道均无 stage 27 入口，
所以五敌就是整场总量。

`[OF]` 本关胜利不是矩形或全灭：妮雅线性格号必须落入 `0..575`、`616..625`、
`666..675`、`716..725` 任一闭区间；妮雅离场失败且失败优先。round 1 播 SAY/0051，
现场胜利 999 播 SAY/0052；999/1000 路由 stage 28，1000 不重播。

`stableRemake` 用显式军团登记控制权，不在控制器按行为号分支：妮雅、三名工兵与已选候选
组成玩家“回城队”，七名城防军组成独立友军军团并复用 `REMAKE-037/060` 的共享专家 AI，
五名叛军组成敌方专家追击军团。地图按四段机器区间常驻投影目的地，并明确提示无需全灭；
不新增难度兵、增援或生成 PRNG。失败返回 11–31 人部署，胜利在 SAY/0052 后保存到
`stage-28`“保衛瓦爾克麗城”。保存身份升级为 v51／`stage-27-valkyrie-return-1`，v50
原样迁移。表现与输入设置不得改变控制权、行动队列、目标范围、胜负、随机序列或存档语义。

`[OF]` 部署名单与地图固定单位不是同一集合。B/0055 的名单标志只包含槽
`0–21,25–31`；其中仅妮雅槽 0 已在地图上，故名单显示妮雅固定项和二十八名候选。七名
城防军与三名工兵的名单标志均为 0：它们继续按模板坐标固定参战，但不出现在出击页面。

### REMAKE-065：专家 AI 增加断路接近与法系施法落点

`[DD]` 用户于 2026-08-12 试玩内部第 27 关后指出两个共享专家 AI 问题：完整接战通路被
海洋等不可通行带彻底切断时，单位会原地待命；法系在没有当前有效技术目标时会回退到主动
近战。这是 `stableRemake` 的产品规则修正，不反向声称为原版行为；原版逐行为流程仍由
`reverse/notes/ai-decision-system.md` 记录。

`REMAKE-060` 的完整可移动通路仍是首选。只有真实空闲交战通路与友军占据的虚拟前线都无法
把原点纳入代价图时，规划器才在本回合合法可达、可停留且满足军团路径约束的格中启用断路
兜底：必须严格缩短到经过军团过滤后最近敌方的曼哈顿距离，先选距离最小，再选暴露较少、
职业地形防御较高、路径较短和线性格号较小者。该分支不把几何距离伪装成通路代价，不记录
`隊列推進`，也不读取或消费 PRNG；若本回合没有任何严格接近格，仍安全待命。

法系边界使用职业目录的 AI 分派而不是“是否拥有技術菜单”：`aiClassDispatch=technique` 的
施法职业不得生成主动普通近战候选；巨龍騎士虽然同属 technique 分派，但其职业技术是近身
龍踏，明确保留普通攻击比较。半龍戰士与工程師本来就是 ordinary 分派，也不受本条影响。
固定守卫与防区教义在没有有效技术时原地待命；剧情路线、护送、跟随和路线脉冲继续优先，
不得被后述默认追击兜底覆盖。

非固定守卫的法系若当前没有合法且有收益的技术，先对本回合每个合法移动落点做只读预演：
落点必须让单位在下一回合从该格至少能对敌方施放一项有效技术，或对当前确有治疗收益的
友军施放治疗／回復。候选先按本职业地形防御最高，再按与预演目标的曼哈顿距离最远，然后
按暴露较少、路径较短与格号较小稳定打破；提交只包含本回合移动，预演动作／目标只进入
决策追踪，不预留单位、不承诺下回合动作。若没有这样的施法落点，单位回到进入法系规划
前的既有策略；只有默认专家追击才使用禁用普通攻击的共享追击移动及上述断路接近兜底，
固定守卫、防区、剧情路线、护送、跟随和路线脉冲仍各自遵守原有合同。

敌军、独立友军 NPC、玩家“自由行动”和竞技场共用以上规则；四档难度与表现设置不改变
结果。保存身份升级为 v52／`expert-approach-caster-positioning-ai-1`，v51 原样迁移；已
提交行动、棋盘、回合、状态和完整 PRNG 不变，读档后尚未提交的自动行动按新规则重算。

### REMAKE-066：近战／远程职责与魔弓兵优先级

`[DD]` 用户于 2026-08-13 确认以主流战棋的“职责、威胁、收益”分层继续优化共享专家
AI，并补充魔弓兵的专属顺序。本条修正 `REMAKE-065` 把法系施法落点只按地形／距离比较
所留下的目标切换与贴身风险，但不反向声称为原版行为。

职业职责是独立于原版动作菜单和 AI 分派的 `stableRemake` 战术字段，必须覆盖当前全部
39 个职业：

- 近战 25 职业：`soldier`、`magic-sword-warrior`、`jungle-warrior`、
  `great-axe-warrior`、`half-dragon-warrior`、`magic-armor-warrior`、`land-knight`、
  `demon-dragon-knight`、`flying-dragon-knight`、`beast-knight`、`bone-knight`、
  `swift-dragon-knight`、`great-dragon-knight`、`cavalry`、`pegasus-warrior`、
  `water-warrior`、`divine-sword-warrior`、`warrior`、`steel-armor-warrior`、
  `evil-sword-warrior`、`engineer`、`empress`、`dragon`、`head`、`hand`；
- 远程 14 职业：`magic-priest`、`prayer-guide`、`curse-master`、`magician`、
  `magic-guide`、`evil-mage`、`magic-archer`、`archer`、`crossbow`、`sister`、
  `monk`、`priest`、`wizard`、`magic-master`。

固定守卫、防区、剧情路线、护送、跟随与路线脉冲等显式策略仍先限定合法行动范围；职责层
不能把它们一刀切为追击。近战职业继续使用完整接战通路、虚拟前线排队与断路接近。远程
职业不生成主动普通近战；存在合法射击／技术时先比较实际战术收益，同等收益才比较下一
敌方阶段可建立近战接触的敌人数、预计近战伤害、暴露、职业地形防御、有效射距、路径与
稳定格号。近战威胁必须从候选落点投影行动者，并按敌方移动力、地形进入代价、阻挡和控制区
精确计算，不能只用曼哈顿半径代替。

法系只移动预演先固定一个动作与一个目标，再在该同一意图的合法落点间选择；不得让不同
落点各自改选目标后再把距离混在一起比较。先排除能被更多近战敌军在下一阶段接触的位置；
若所有位置均有接触风险，再选预计近战伤害最低者；随后按职业地形防御最高、距该固定预演
目标最远、暴露较少、路径较短和格号较小排序。只有确实存在下回合有效攻击或治疗时才使用
这套落点排序；否则按该单位原有显式策略行动，默认追击则前往职业最大有效射程形成的外圈。
纯支援职业以最接近敌军的友军前线为支援锚点，不单独贴向敌军。

魔弓兵在显式策略允许的候选范围内使用更严格的顺序：任何行动后落点若与任一敌军正交相邻
（包括当前被冻结／无法行动者），就不能成为移动／射击候选；剩余合法线路先以实际可扣除
生命总量最大者为首选，即使另一线路
能击杀一名仅剩少量生命的单位也不得覆盖更高总伤害；总伤害相同才回到上述远程通用的战术
收益、安全、地形、最远有效射距和稳定次序。线路和站位比较保持确定性，不读取或推进 PRNG。

敌军、独立友军 NPC、玩家“自由行动”和竞技场共享职责层。保存身份升级为 v53／
`class-role-ranged-tactics-ai-1`；v52 与 v51 原样迁移，尚未提交的自动行动读档后重算。

### REMAKE-067：内部第 27 关城防军首回合警戒与非保护目标边界

`[OF]` B/0055 只为城防槽 `22/41/44/43/45/42/40` 写逐槽行为 2；原版没有复刻军团
合同中的军团或主将字段，七人也没有任何特殊失败引用。
模块 29 场景 27 的失败表只把 side 1 槽 0 传给通用消失检查 `1000:26C3`，因此槽 22
死亡、任一城防友军死亡或七名城防军全灭都不会令关卡失败；只有妮雅离场失败，且仍保持
失败优先。`stableRemake` 的“瓦爾克麗城防军”只是控制权／AI 归属，不登记主将，也不从
军团存活数派生额外目标。

`[OF]` 七人在玩家向身份上同样没有主将。十名固定棋盘单位（含三名工兵）的 side-1 角色
描述符肖像字节都是 `FFh`，`0000:51B9` 因此按 UNIT-014 的职业回退同时覆写肖像记录
DS:`3192h` 与单位名指针 DS:`3190h`（改指职业名 DS:`31BBh`），stage 27 的战场界面显示
“职业／同名职业”加通用职业肖像；槽 22 是“巨斧戰士／巨斧戰士”＋肖像 `57`。槽 22 描述符
里的“愛莉歐拉”是全表唯一“有姓名无肖像”的条目，但本关名单标志为 0，未进入模块 27
直接显示描述符姓名的部署名单路径。`stableRemake` 与 `legacyStrict` 都不得把它登记为
stage 27 战场单位名，也不得为这十人生成专属肖像；stage 29 的原生名单／战场差异另由
`REMAKE-069` 保留。

`[OF]` 敌方总量仍为开局五名。B/0055 静态模板、`1000:4E9E` 逐关处理器、动态棋盘目录、
完整回合特殊生成链与场景 30 击杀替补／形态转换链均没有 stage 27 生成入口；round 1
处理器只播放 SAY/0051，现场 999 只播放 SAY/0052。跨回合、击杀、城防军死亡与难度变化
都不得新增敌军。

`[DD]` 用户于 2026-08-13 要求优化本关左上七名城防友军的开局节奏。`stableRemake` 第 1
回合把这七人的行为 2 临时解释为原地警戒：允许从当前格执行合法攻击／射击／技术，但行动
路径终点必须保持原格，不能主动移动。自第 2 回合起恢复行为 2 的正常共享专家追击；该覆盖
只属于本关城防军，不改变五名叛军、玩家“自由行动”、其他关卡 NPC 或 `legacyStrict`。

保存身份升级为 v54／`stage-27-first-round-sentry-1`，v53 原样迁移；回合号已在战中档中
保存，因此 round 1 读档仍警戒，round 2 及以后读档仍追击，不新增可变 AI 状态或 PRNG 消耗。

### REMAKE-068：内部第 28 关保留十七敌全灭战与三段剧情

`[OF]` 场景 28“保衛瓦爾克麗城”使用 B/0057：妮雅槽 0 固定，槽 `1–21,25–31` 为
二十八候选，三十四开放格，最多二十九人；没有名单外固定棋盘单位。side 2 开局固定
十七名行为 0 敌军，职业为二妖龍騎士、四魔劍戰士、四邪劍戰士、三魔法師、二弩兵与
二飛馬戰士。机器胜利条件是 side 2 全灭，失败条件是妮雅离场且失败优先；SAY/0095 与该
条件一致，不存在首领或提前胜利目标。

`[OF]` 模块 25 在部署前以 BK/22 播放 SAY/0053 十四检查点；round 1 播 SAY/0054 五
检查点；现场胜利 999 播 SAY/0055 八检查点。`1000:4EDE` 除两段战场对白与默认路由外
没有棋盘或运行状态操作。初始模板、逐关处理器、动态棋盘目录、完整回合特殊生成链与
stage 30 击杀替补／形态转换链均无本关生成入口，故叙事中的“四面包围”不能外推为模板外
敌军，跨回合、击杀和难度变化也不新增或替换单位。

`[SR]` 本关所有已部署 side 1 归玩家军团；十七名行为 0 敌军复用
`REMAKE-033/060/065/066` 的共享专家 AI，不新增逐关专用策略。失败或主动撤退按
`REMAKE-016` 重走 SAY/0053 与部署，尝试内已取得的成长保留。现场胜利依原顺序完成
SAY/0055、普通反馈与保存；保存身份升级为 v55／`stage-28-valkyrie-defense-1`，v54
原样迁移。999/1000 均保存／路由到 `stage-29`，1000 不重播胜利对白；stage 29 只作为
冻结完成档边界，未获独立授权前不得载入其剧情、部署或战斗运行时。

`[DD]` 用户于 2026-08-13 确认内部第 27 关测试通过并要求继续推进下一关，因此 M52/M53
只对上述 stage 28 合同解除冻结。该授权不新增职业能力、模板外敌军、首领目标或后续关卡
语义，也不解除 stage 29 及后续内容的冻结。

### REMAKE-069：内部第 29 关保留十五敌全灭战与单一关前剧情

`[OF]` 场景 29“騎士城堡前”使用 B/0059：妮雅槽 0 固定，名单标志为槽
`0–22,25–31` 共三十项；十四开放格使二十九候选中最多十四人可与妮雅共同出场，合计
1–15 人。模块 27 构造部署名单时只读取非零名单标志，绘制条目又直接读取角色描述符姓名，
因此槽 22 名单卡显示“愛莉歐拉”且可正常选择；进入模块 29 后才因该描述符肖像为 `FFh`
执行 UNIT-014 的职业回退，战场肖像取当前职业的通用记录、单位名改为当前职业名。
`legacyStrict` 保留这项有原生路径支持的名单／战场显示差异；`stableRemake` 后续显示选择
由用户试玩反馈新增的 `REMAKE-070` 覆盖。

`[OF]` side 2 开局固定十五名：槽 4 艾西柯羅为妖龍騎士，另有五邪法師、四迅龍騎士与
五魔弓兵；五名魔弓兵逐槽行为为 2，其余十人行为为 0。机器目标仍是 side 2 全灭，失败
条件是妮雅离场且失败优先；SAY/0096 与机器条件一致，叙事点名艾西柯羅不构成首领目标。
模块 25 只在部署前以 BK/23 播 SAY/0056 七检查点，场景 29 没有逐关处理器，因此没有
开战、回合或现场胜利专属对白／状态写入。初始模板、处理器缺席、动态棋盘目录、完整回合
特殊生成链与 stage 30 击杀替补／形态转换链均无本关生成入口，跨回合、击杀和难度变化
都不新增或替换敌军。

`[SR]` 本关全部已部署 side 1 归玩家军团；十五名敌军保留原始行为元数据，并复用
`REMAKE-033/060/065/066` 的共享专家 AI，不新增逐关专用策略或军团／主将语义。失败或
主动撤退按 `REMAKE-016` 重走 SAY/0056 与部署，尝试内成长保留。现场全灭直接进入普通
胜利反馈与五槽保存；保存身份升级为 v56／`stage-29-knight-castle-front-1`，v55 原样迁移。
999/1000 均默认路由 `stage-30`，1000 不重播表现；stage 30 只作为冻结完成档边界，
SAY/0057 属于下一关的模块 25 关前剧情，不得误接为本关胜利对白。

`[DD]` 原版共享焦点以妮雅 `(41,26)` 和偏移 `(-4,-3)` 推导镜头原点 `(37,23)`；Web 地图
只允许 10×7 视口在已绘制内容边界内移动，本关 x 原点上限为 36，因此复刻初始镜头统一
钳制为 `(36,23)`。该钳制只改变一格表现取景，不改变模拟、部署格或存档语义；控制器、
Phaser 滚动与点击换算必须共享同一个合法原点，不能只让渲染器隐式钳制。

`[DD]` 用户于 2026-08-13 确认玩家口径第 27 关（内部 stage 28）测试通过并要求继续推进
下一关，因此 M54/M55 只对上述内部 stage 29 合同解除冻结。该授权不新增职业能力、
艾西柯羅首领目标、模板外敌军或 stage 30 运行时语义，也不解除 stage 30 及后续内容。

### REMAKE-070：愛莉歐拉入场后保留角色名并使用当前职业通用肖像

`[OF]` BAT-072 已确认原版模块分流：stage 29 部署名单直接显示槽 22 描述符姓名
“愛莉歐拉”，进入模块 29 后 `FFh` 使战场姓名与肖像都回退到当前职业。该原版事实不改写，
`legacyStrict` 仍显示“当前职业／同名职业”与职业通用肖像。

`[DD]` 用户于 2026-08-13 试玩 stage 29 后明确指出，愛莉歐拉出场后右栏只显示
“巨斧戰士／巨斧戰士”，角色姓名没有保留下来。`stableRemake` 因此把姓名来源与肖像来源
拆开：部署名单、战场右栏、行动反馈、全景状态和地形参照均持续显示角色名“愛莉歐拉”，
职业字段仍显示当前职业；因原版没有她的专属肖像记录，肖像继续跟随当前职业的 side 1
通用记录。以当前必经职业为例，右栏应显示“巨斧戰士／愛莉歐拉”与通用肖像 57。

该修正不赋予主将、保护目标、专属数值或独立行动规则，不反向改写 stage 27：stage 27
槽 22 的名单标志为 0，其已接受战场身份仍按原版职业回退。保存身份升级为 v57／
`stage-29-eliola-display-name-1`；合法 v56 档原样迁移，stage 29 战中档若含已部署槽 22，
迁移时补上具名／职业肖像身份而不改变职业、生命、经验、坐标、行动位或 PRNG。

## 授权边界

用户已分别授权第 0–4 关的有界实现，`REMAKE-012` 已授权用于第 1 关 AI，
`REMAKE-013` 已授权用于当前冰雪／冰封闭环，`REMAKE-016` 已授权用于所有已开放关卡的
失败／撤退重试边界，`REMAKE-017–021` 已授权用于第 3 关目标、授职身份、回复反馈、
救援 AI 与集团命令，`REMAKE-022` 已授权把已验证的军团控制权、策略与目标能力提取为
通用模拟层合同；用户于 2026-08-04 授权 M06 第 4 关实现，`REMAKE-023` 已进入实现基线；
同日试玩反馈确认应修正标题映射，并要求检查已转职葛蒂拉斯的继承语义，`REMAKE-024`
因此适用于已开放的第 1、2、4 关；用户又明确授权逐项实现全部玩家技术，`REMAKE-025`
适用于该系统例外中的工兵构筑，`REMAKE-026` 适用于同一例外中 `FM` 的确定性 AI 修复。
用户于 2026-08-08 要求在复核全职业、全技能和测试场景后开始推进第 5 关，`REMAKE-027`
已进入 M07/M08 并由用户接受；用户随后确认第 5 关测试成功、要求继续推进下一关，
`REMAKE-028` 因而进入 M09/M10 第 6 关有界实现。用户在第 6 关流程测试后明确要求统一调整
剧情对话输入，`REMAKE-029` 适用于全部已开放关卡。用户于 2026-08-09 接受第 6 关并要求
继续推进第 7 关，`REMAKE-030` 因而进入 M11/M12 有界实现；用户同日确认第 7 关测试成功
并要求继续推进第 8 关，`REMAKE-031` 因而进入 M13/M14 有界实现；用户随后确认第 8 关
测试成功，并要求以 `REMAKE-032` 修复 SAY 157 的原版漏播。用户随后明确授权
`REMAKE-033`：全部已开放关卡和测试表面的 side 2 常规战斗默认使用专家 AI，且不增加
AI 难度选项。用户随后明确授权 `REMAKE-034` 的专家射手落点、魔弓线路期望、冰雪实际候选
末位排程、纯冰雪残军门禁和全员冰封自动阶段。用户随后授权 `REMAKE-035`，将玩家魔弓
改为主目标后指定完整箭道，并让专家 AI 直接选择最高价值确定线路。用户随后授权
`REMAKE-036`，恢复冰雪最外圈向值 0 格外推，并把有效命中巫師设为仅次于确定击杀的
专家策略。用户随后授权 `REMAKE-037`，让友军 NPC 与“自由行动”默认共享同一专家
规划器，并保留防区、路线与跟随等显式策略；再以 `REMAKE-038` 将第 8 关八名 side 1
全部改为玩家控制，不设置 NPC 友军。用户确认第 8 关验收完成并要求推进下一关后，
`REMAKE-039/040` 进入 M15/M16 第 9 关有界范围；用户接受第 9 关并要求继续推进后，
`REMAKE-041` 进入 M17/M18 内部第 11 关“拯救蘇蘭達”有界范围。用户于 2026-08-10
接受本关并要求继续推进后，`REMAKE-042` 进入 M19/M20 内部第 10 关“飛船上遭遇敵人”
有界范围。用户同日接受本关、登记未来难度待办并要求继续推进，`REMAKE-043` 因而进入
M21/M22 内部第 12 关“落入沼澤”有界范围；本关试玩发现共享生命的其他分身提前扣血后，
用户明确以 `REMAKE-044` 要求地图／全景表现结束后再统一投影；随后确认右侧角色详情提前显示
最终状态可作为复刻版对原版的 bug 修复，`REMAKE-045` 因而适用于全部已开放关卡的普通全景
战斗。用户随后接受内部第 12 关并要求继续推进，`REMAKE-046` 因而进入 M23/M24 内部
第 13 关“龍塔外”有界范围；用户接受本关并要求继续推进后，`REMAKE-047` 进入 M25/M26
内部第 14 关“龍塔第一層”有界范围；用户接受本关并要求继续推进后，`REMAKE-048` 进入
M27/M28 内部第 15 关“龍塔第二層”有界范围。用户于 2026-08-10 接受内部第 15 关并要求
继续推进后，M29/M30 把内部第 16 关“龍塔第三層”作为有界例外解除冻结；本关取证过程中
发现胜利条件原文表读错，`REMAKE-051` 因而确立正确取值并只应用于内部第 16 关，早期
关卡的更正随后由用户单独授权并在 M31 实施。用户于 2026-08-11 确认第 16 关测试完成并
要求推进第 17 关，M32/M33 因而把“龍塔第四層”作为有界例外解除冻结；其机器槽 11 倩
目标直接沿用 `REMAKE-051` 的正确表。用户同日确认第 17 关测试完成并要求推进第 18 关，
M34/M35 因而把“龍塔第五層”作为有界例外解除冻结；其机器槽 12 麗目标同样直接沿用
`REMAKE-051` 的正确表。用户随后确认第 18 关测试通过并要求继续推进下一关，M36/M37
因而把“龍塔第六層”作为有界例外解除冻结；其机器槽 13 愛目标继续直接沿用
`REMAKE-051` 的正确表。用户随后确认第 19 关测试通过并要求继续推进下一关，M38/M39
因而把“龍塔頂部”作为有界例外解除冻结；`REMAKE-052` 用于妖龍 WD 的确定重放边界。
用户于 2026-08-11 试玩后要求修正妖龍贴图、免疫混乱并复核异常快速失血；透明蒙版按原版
资源纠正，`REMAKE-053` 则获授权把 `龍／頭／手` 的混乱／毒职业免疫覆盖到普通命中。
用户随后指出琴斯胜利登场时错误显示为士兵，`REMAKE-054` 因而获授权把该剧情棋子统一为
场景 42/22 已明确使用的记录 3“魔祭師”，并按同一职业重建派生属性。用户随后确认第 20
关测试完成并要求推进第 21 关，M40/M41 因而把“焦土森林村莊外”作为有界非交互过场解除
冻结，`REMAKE-055` 用于其确定路径、无玩家回合与直接 stage 22 路由边界。用户随后确认
第 21 关测试通过并要求推进第 22 关，M42/M43 因而把“焦土森林村莊中”作为有界部署战
解除冻结，`REMAKE-056` 用于其首回合伏击、原生聚焦错位、固定六敌与单一妖龍目标。
用户随后指出普通全景致死会在开窗前提前删除棋盘目标，且关闭全景后漏播地图死亡动画；
`REMAKE-057` 因而适用于全部已开放关卡与测试表面的普通全景致死表现。
用户随后确认第 22 关测试通过并要求推进第 23 关，M44/M45 因而把“死亡之谷中”作为有界
到达目标战解除冻结，`REMAKE-058` 用于精确线性出口、固定二十一敌、无增援与失败回部署。
用户于 2026-08-12 进一步授权 `REMAKE-059`，把 SAY/0045 改为第 22 关关后对话并把存档移到对话后。
用户同日要求检查并优化 NPC 以画面几何距离代替可移动路径距离的问题，`REMAKE-060` 因而
适用于全部已开放关卡、竞技场、独立友军 NPC 与“自由行动”的共享专家追击；显式剧情／
军团路线保持既有合同。
用户同日确认第 23 关测试通过并要求推进第 24 关，M46/M47 因而把“死亡之谷城堡前”作为
有界到达目标战解除冻结，`REMAKE-061` 用于精确线性城堡区、固定二十二敌、无增援、胜利
对白与直接 `stage-26` 路由。用户随后确认第 24 关测试通过并要求继续推进第 25 个可玩关卡，
M48/M49 因而把原版内部第 26 关“遭遇碧娜維姬”作为有界首领战解除冻结；`REMAKE-063`
用于固定八敌、无增援、敌方阶段尾双次纵列下推、SAY/0049/0050 与 `stage-27` 路由。
原版内部场景 25 仍不得自行补造。用户随后确认第 25 个可玩关卡测试通过并要求推进下一关，
M50/M51 因而把内部第 27 关“趕回瓦爾克麗城”作为有界到达目标战解除冻结；`REMAKE-064`
用于七名独立城防友军、五名固定叛军、无增援、四段线性目的地、SAY/0051/0052 与
`stage-28` 路由。该授权当时不解冻 stage 28；其后续范围现由 M52/M53 独立授权。
用户在本关初步试玩后继续要求：隔海等无完整通路时自动单位仍应尽量接近；法系不得主动
近战，非固定守卫且当前无有效技术时应优先前往下回合可攻击／治疗的位置，并按地形、最远
施法距离排序。`REMAKE-065` 因而适用于全部已开放关卡、竞技场、独立友军 NPC 与“自由
行动”的共享专家 AI；该决定本身不解冻 stage 28，其后续范围现由 M52/M53 独立授权。
用户随后确认把全部职业显式分为近战／远程职责，并要求远程职业按精确近战威胁维持安全
射程；法系施法预演必须绑定同一动作与目标后再按安全、地形和最远距离选位；魔弓兵必须先
排除贴近任一敌军的落点，再最大化实际范围总伤害，伤害相同才用远程通用规则。`REMAKE-066`
适用于同一共享专家 AI 表面，保存身份升级为 v53；该决定本身不解冻 stage 28，其后续范围
现由 M52/M53 独立授权。
用户确认上述 AI 测试成功后，要求本关左上七名城防友军第 1 回合原地警戒、第 2 回合起恢复
正常追击，并要求复核原版城防主将／失败条件和敌方增援。机器证据确认城防军无主将字段且
不属于保护目标，七人全灭不失败；五条生成通道继续确认全场固定五敌。`REMAKE-067` 只调整
本关首回合城防节奏并移除复刻误加的城防主将标记；该决定本身不解冻 stage 28。
用户于 2026-08-13 确认内部第 27 关测试通过并要求继续推进下一关，M52/M53 因而把内部
第 28 关“保衛瓦爾克麗城”作为有界全灭战解除冻结；`REMAKE-068` 用于 B/0057 十七名
固定敌军、五通道无增援、SAY/0053/0054/0055、v55/v54 与 `stage-29` 冻结路由。用户本次
确认该关测试通过并再次要求继续推进下一关，M54/M55 因而把内部第 29 关“騎士城堡前”
作为新的有界全灭战解除冻结；`REMAKE-069` 用于 B/0059 三十项名单、十五名固定敌军、
五通道无增援、单一 SAY/0056 与 `stage-30` 冻结路由，`REMAKE-070` 再按用户试玩把
愛莉歐拉的稳定复刻身份改为角色名＋当前职业通用肖像并升级 v57/v56。stage 30 及后续内容仍
不得自行实现。
用户于 2026-08-10 质疑落雷取证并要求重新逆向后，确认共同收尾只覆盖效果范围内敌军，并发现
电波实为两层；用户随即明确选择“两层都画、并按设计意图修复标记层”，`REMAKE-049` 因而适用于
全部已开放关卡与测试表面的四级落雷。用户同日指出第 4 关力场脉冲应在结界外的全部地格出现
究极落雷爆闪，并要求重新逆向后复现，`REMAKE-050` 因而适用于第 4 关的 `route-pulse` 表现。
用户于 2026-08-12 要求逆向查验半龍戰士的技術，随后在证据确认原版确有该技術后明确授权
更新文档并解冻玩法，且指定按原版机制实现、只保留对阵场“技術 → 傳送”二级菜单这一项
优化。`REMAKE-062` 因而适用于全部已开放关卡、竞技场与全职业对阵场上的玩家半龍戰士；
它不解冻其他职业能力、敌方半龍戰士 AI、内部第 26 关之外的后续内容或 `.TST` 兼容语义。
`legacyStrict` 开关或未被当前切片消费的系统仍须独立授权；后续扩大范围须
先闭合对应 GDD 合同并取得明确授权。
