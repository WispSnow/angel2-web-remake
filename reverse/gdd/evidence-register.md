# GDD 证据登记表

| ID | 结论 | 等级 | 证据 | 下一验证 |
| --- | --- | --- | --- | --- |
| RES-001 | 14 个索引容器共有 1,282 个有效记录，已全部拆出 | C | 各容器索引表、提取 manifest、SHA-256 | 无 |
| CMP-001 | 内嵌压缩流为 stored/LH7，输出长度由流头给出 | C | 原生解压器、LHA 解码结果、2,249 条严格长度校验 | 标注主模块解压器的原始符号 |
| DBG-001 | `GO.EXE` 附带 TLINK 3.10 调试区，含 239 个带地址原始符号 | C | MZ 声明尾 `55C0h`、`52FBh` 头、固定 9 字节符号表、名称池，自写解析器与 TDUMP 交叉校验 | 继续解析模块/源码行表 |
| LDR-001 | `GO.EXE` 通过 `LOAD_V→UPK/OVER→RUNTO` 动态构造并执行运行模块 | C | 原始符号、反汇编调用链、既有运行时交接断点 | 捕获更多状态对应的运行模块 |
| MOD-001 | `UN.SWF` 含 10 个可重构运行模块，共 12 个 LH7 块、256,983 字节第一层装载映像 | C | 头记录、`UPK/OVER/RUNTO` 访问方式、入口地址 | 无 |
| MOD-002 | 加载器初始状态为 `WK_EXE=23`，首个运行模块由记录 23/24 组成，入口 `0602:000E` | C | `046A:004A` 初值、`LOAD_V` 读法、23 号头与 24 号解压块 | 首模块出站见 TITLE-001/003；其余外围状态继续追踪 |
| MOD-003 | 10 个第一层映像均为 LZEXE 0.91，二次解包得到 628,256 字节业务代码和 2,651 条 MZ 重定位 | C | 10/10 入口匹配 29 字节 LZEXE 0.91 签名；`unlzexe` 全部成功；恢复 MZ/relocation/load image；Ghidra 发现 2,819 个函数 | 建立完整状态图和模块角色表 |
| TITLE-001 | GO 以 `WK_EXE=23` 进入模块 23 并循环执行运行模块，`WK_EXE=0` 时清理退出；模块 23 向父块导出 `WK_EXE/CONTINU/LV_HARD` | C | GO `1000:0000`、模块 23 `0000:0000–010Ch/0351h`；25 段签名；`title-flow.json` | 无；密码等外围状态另行追踪 |
| TITLE-002 | 标题菜单默认“遊戲開始”，另一项为“繼續遊戲”；两项热区为 `(504,75/99,96,20)`，键盘在 0/1 间循环，指针悬停更新当前项 | C | 模块 23 `0480h/19F2h/1A47h/1AFDh/1B24h`；DS:`0BAAh/0BC6h`；`title-flow.json` | 无；完整表现见 PRES-008 |
| TITLE-003 | 新游戏四项难度依次为 0“過關斬將”、1“勢均力敵”、2“困難重重”、3“無法無天”，默认 0；确认写 `WK_EXE=25,CONTINU='N',LV_HARD=0..3`，取消返回标题 | C | 模块 23 `059Fh/1B7Eh/1BD3h/1C23h..1CDFh`、DS:`0D00h/0D32h`；模块 29 难度消费者；`title-flow.json` | 无；完整表现见 PRES-008 |
| TITLE-004 | 继续游戏扫描 `WAR0.TST..WAR4.TST`，五槽默认 0 且循环；空槽可高亮但不可确认，取消返回 `X`，有效槽返回 ASCII `0..4` 并写 `WK_EXE=29/CONTINU=槽字符` | C | 模块 23 `1455h/14E5h/16C7h/183Dh/3C70h..3D0Fh`、DS:`0ABFh`；`title-flow.json` | 覆盖/损坏存档的完整 UI 反馈 |
| TITLE-005 | 标题流程直接绑定 23 个资源记录；`A/23` 的 228 项 Big5 码表与 `A/24` 的 228 个 `16×15` 字形逐项对应。模块 23 的资源索引 8 是 `MUSIC`、9 才是 `NUM` | C | 模块 23 入口与 `0611h/0CE2h/1034h/12E4h/1455h` 的资源读取；A/23、A/24 长度/终止符断言；`title-flow.json/title-presentations.json` | 无；表现见 PRES-008 |
| IMG-001 | 常规图像由四个位平面组成，第五路可作透明遮罩；资源流 `0/1/2/3` 原生对应颜色索引 bit `3/2/1/0` | C | 目录边界与四路同构；模块 21/25/29 三份绘图例程中指针槽 `+0/+4/+8/+0Ch` 与 VGA Map Mask `08h/04h/02h/01h` 的逐项配对；`plane-order.json`；妮雅/希蜜/战场实机截图视觉核验 | 对全部异常记录分类 |
| PAL-001 | 原作运行时按场景写入 16 色 VGA DAC 调色板；gameplay、intro、password 及标题逐场景表均已有原生绑定 | C | 模块 21 `05A8h/22C4h` 与 DS:`03ACh`；模块 23/25/29 的 DAC 写入、调色板指针和数据表；专题 manifest | 未绑定的归档资源仍以无损位平面为母版，不臆测唯一显示调色板 |
| AUD-001 | 71 条音频为 Creative Voice，可完整转 WAV | C | 文件签名、FFprobe、71/71 转换成功 | 识别每条用途 |
| AUD-002 | 59 条 Softstar RIX OPL 均可渲染；`MUSIC/2..13、18..39` 是 17 组“奇数短入场→偶数长循环”的战斗曲对，并由玩家/敌方两张逐关表选曲。第 0 关玩家阶段为 `7→6`、敌方阶段为 `5→4`；`MUSIC/29` 是曲对 `28/29` 的入场兼模块 27 交接预载，不是第 0 关整场曲。`MUSIC/0、1、14、40` 已分别绑定密码、标题、滚动开场、繁荣结局 | C | `55AAh`、59/59 渲染；模块 29 `DS:1D98/1E46/19F6`、`1000:32D1–338E/3796–37E7`；模块 27 `0000:00DC–00F7`；第 0 关 `7→6` 实机；`music-catalog.json` | `MUSIC/15–17` 的发布流程用途待实机标注 |
| AUD-003 | `SET.TXT` 只消费前三字节；发布值 `Y13` 表示启用 Sound Blaster 数字采样/VOC、基址 `220h`、IRQ 7，尾随五字节忽略。九个模块加载器/两表一致；RIX 音乐与四个战斗分类开关独立，`0220h` 直提 VOC、`0224h` 先过战斗音效门 | C | 模块 `21/23/25/27/29/33/35/44/46` 九份 93 字节加载器与查找表；模块 23 `91F2h/9315h/937Bh/93A4h/94BFh/954Eh/9590h` 七段 DSP/PIC 签名；`set-config.json` | 无；IRQ 10/11 仅保留原表元数据，不模拟为可靠的 Web 硬件路径 |
| SAY-001 | `SAY/NUM/CHA` 有 176 组对齐记录：93 条含正式命令，83 条为名字、提示或其他文本/标签表；语料共有 17 种命令码，但 `CW` 不在任一发布版分发器中；69/116/117/118 无发布版生产者 | C | 全语料 5,591 动作/4,057 命令动作；模块 25 固定关卡表；模块 29 `DS:80B5` 的 75 次立即写、2 次查表写、9 次读取及 `DS:30BA/1273` 映射表；`dialogue/*.json` semanticVersion 2；`story-presentations.json.globalReachabilityAudit` | 无；四条脚本完整保留为归档内容但不接入忠实流程 |
| SAY-002 | 模块 25/29 共享 `WU/KY/ME/ED/HU/\\/CU/BK/WD/CD/HD/W-/PP/PU/PD`；模块 29 另支持 `DL`。`BK` 备份 framebuffer，`W-` 载入 `A/18`，`ME` 仅保存编号，`CW` 为 no-op；模块 29 的 Big5 逐字声按字符码模 15 选择 `MAGIC/57..71` | C | 模块 25 `0736h/07C7h`、模块 29 `023Eh/BE14h/BEC3h/C9B9h/EA04h`；34 段代码/12 段数据签名；`story-presentations.json` | 无；标称 tick 时长见 SYS-004 |
| FONT-001 | `NUM/SAY/CHA` 的 176 组包含 9,399 个 `16×15` Big5 对应字形 | C | 全记录编码顺序、30 字节步长、逐条长度校验、176 张图集 | 无 |
| FONT-002 | `A/B/UN` 另有 9 组、1,769 个无头 `16×15` 单色字形 | C | 30 字节步长、big-endian 行位图、可读图集、末行留白与笔画密度 | 查找各组的字符索引/分隔表 |
| FONT-003 | `A/0023` 是 `A/0024` 的精确 Big5 码表：228 个双字节码、单个 `0000` 终止，与 228 个 30 字节位图一一配对 | C | 模块 23 同时加载两记录；长度、终止符、标题/难度字符覆盖断言；`title-flow.json` | 无 |
| UI-001 | 行动菜单的七种状态及 `M/A/S/C/H/N/X` 动作码已恢复；移动后 `結束` 仅结束当前单位动作，`返悔` 恢复位置/行动位 | C | 模块 29 DS:`3DD8h..3E4Eh/3FAAh` 菜单表；`5651h/5721h/6849h..6B6Ah`；`input-ui.json` | 完整动作表现时间轴另列 BAT-026 |
| UI-002 | 集体命令为 `全部休息/跟隨主將/自由行動/全面徹退`；系统菜单为游戏功能/胜利条件/读档/存档/离开，Q/Y 非胜负支路分别绑定退出和主动撤退；F1–F4、Esc、Tab、E、M 快捷键已闭合 | C | DS:`3F3Eh/41B6h/F6AAh/F6B8h/F6BBh/F6DBh/F6E4h..F6E7h`；`6CCAh..6DEAh/B941h/BA6Eh..BAAEh`；`input-ui.json` | 无；完整表现归 BAT-026 |
| UI-003 | 战斗单位详情在非空焦点停留四 tick 且格号变化时刷新；右栏精确几何、动态 `D` 肖像、职业／单位名、五行原文/数值、双比例条、`A/17` 八状态紧密装箱、回合框及第 37 关 side 2 的九字段 `?????` 隐藏均已闭合 | C | 模块 29 `7C27h/8492h/851Ah/86C9h..8CE9h`，DS:`5DD4h..5FD5h/6006h..6018h`；18 段代码/4 段数据；`hud-presentations.json` | DS:`3192h`→全部导出 `D` 记录映射单独保留；范围与地图特效见 PRES-011 |
| UI-004 | 右侧面板有 12 个鼠标热区、17 项动作分发和三类设置面板；方格/滚动/肖像/战斗动画可直接切换并分别用 `A/6` 的 `24/25、26/27、30/31、20/21` 局部帧同步改变桌面物件，音效四独立、游戏功能五独立、音乐五选一；DS:`7FADh` 的发布版行为是不可达 no-op | C/U | DS:`7FF0h/806Ah/10E1h..11F8h`；`B78Ch..BAAEh`；战术桌重绘 `B7C6h/B7F1h/B812h/B833h/B854h/B875h/B896h`；DS:`7FB1h/7FADh` 全部直接引用审计；`input-ui.json` | C：发布版可达性、帧/坐标与副作用；U：遗留开关的原设计名称 |
| SYS-001 | 模块 29 在本进程第一次战斗模块出站时以 `MIMA_PASS={count,savedNext}` 暂存原下一模块并改走模块 21；模块 21 通过后恢复原目标，后续不再重复 | C | 模块 29 `0000:009Ch/1000:06B9h`、模块 21 `00F0h/0428h`、GO 父块 `+1Eh→0312h`；17 段签名；`password-flow.json` | nextStage=6 后置覆盖已记录为原生边界；`MIMA_NUM` 在此链中无消费者 |
| SYS-002 | 密码为三次 28 项图册问答；模块 21 专用 DAC 表下六个可见按钮依次为红/黄/蓝/绿/紫/白，对应内部答案码 `2/1/0/3/4/5`；回答码写 DOS 向量 0/1/3，确认时按 28×`{x,y,answerCode}` 表校验；可整组重输，任一错误显示重启提示并锁死 | C | 模块 21 `0502h/05A8h/068Ch..0793h/0928h/0B4Bh/0BEBh/13C6h/22C4h`、DS:`02C6h/03ACh/0426h`、`C/0032` 图像记录 1–7/20–23、`UN/0041–0042`；`password-flow.json` | 提示前 DOS/BIOS 向量兼容检查依环境保留，不作为 Web IVT 模拟要求 |
| SYS-003 | `PLAY.COM` 的 INT 21h 钩子把模块 21 `CS:0BEB` 的 PIT 挑战器改为固定索引 16；该项答案码 2 对应默认最左的红色按钮，直接运行 `GO.EXE` 则保留随机挑战 | C | `PLAY.COM` 0118h 规则与 013Ch 钩子、原/替换八字节逐字匹配模块 21、模块 21 首次 INT 21h 位于 000Ch | 无 |
| SYS-004 | 发布程序的 native tick 标称频率为 `1193182/11932 = 99.998491 Hz`，每 tick `10.000151 ms`：九个发布运行模块都先挂模块本地 INT 08h 计数器，再由同构内嵌驱动把 PIT0 mode 3 除数设为 11932；驱动每次 IRQ0 链回旧处理器，故本地计数器每次加一（模块 29 为 `DS:F5B3`）。Web 忠实层可用 10 ms 逻辑 tick，并保持全部整数等待计数 | C | 模块 21/23/25/27/29/33/35/44/46 的启动 FFh 请求、PIT 常量、INT 08h 安装/链回、计数生产器和等待消费者全量审计；模块 29 另有 10 段定址签名；`native-timing.json` | 物理晶振误差、模拟器调度、VGA 回扫和音频设备漂移只作宿主观测，不改变标称 tick |
| INPUT-001 | 启动时检测 DOS 鼠标驱动并把可用性记为 `MOUSE_USE='Y'/'N'` | C | 原始函数 `MUS_SET` 在 `0000:0079` 调用 INT 33h AX=0，并写 `046A:0100` | 运行模块的语义消费见 INPUT-003/004 |
| INPUT-002 | `AG2.JS3` 是 Joymouse Setup 3.00 的 78 字节输入配置，不是游戏进度；低七位是 128 项内建键表索引，高位是逐动作重复开关。当前 Keyboard 4 Way 的两组 12 项相同，A/B/C/D/E/F/Start/Select 为 Space/Enter/F1/M/E/Tab/Esc/No Use，方向为数字键盘 8/2/4/6；12/12 均与游戏语义消费者吻合 | C | `JS3.EXE` `494Fh/4A96h` 互逆读写、`3453h/3E05h` 重复位切换、`36ECh/407Bh` 掩码/键名显示、`4D9Ah/4DFFh` 驱动打包；`14E5:08B3h` 查表与 `08D3h` 128 条记录；8 段签名；`AG2-JS3.json` | 未选用的 Analog/Gravis/鼠标轮询与 DOS `E0` 细节只作兼容取证，不阻塞 Web 语义输入 |
| INPUT-003 | 模块 27/29 各装 INT `09h` 处理器，从端口 `60h` 读 Set-1 扫描码，按下/释放写 `1/2`；两份各 14 项发布版表完全一致并映射上下左右、主操作、次操作，键盘和鼠标进入同一动作层 | C | 模块 27 `1CAAh/1CC4h/1D27h`、DS:`2410h/2567h`；模块 29 `D2E0h/D2FAh/D35Dh`、DS:`F552h/F6A9h`；40 段输入/UI 签名；`input-ui.json` | 当前 Joymouse 适配见 INPUT-002；低层扩展键兼容不改变语义规则 |
| INPUT-004 | 部署为三列五行名单、三页签和结束，共 19 个热区/五列循环键盘导航；战场为 `10×7`、`40×44` 格，当前焦点格使用调色板 0 的 `40×44` 双像素底框叠加调色板 15 的 `39×43` 单像素框；数字键盘 7/9/1/3 补充斜向移动，鼠标内部/四边滚动与主/次操作规则已恢复 | C | 模块 27 `0C60h..0FC8h/1884h/18B2h`、DS:`0E40h/0ED8h`；模块 29 `54AAh/54D1h/8330h`、DS:`5B29h/5C35h/F6F0h/F6F2h/F6F8h/F6FAh`；`input-ui.json` | 表现时间轴见 UI-003 |
| UNIT-001 | `DATA` 有 39 条，每条为五组七字段 | C | 文件长度与全表边界 | 跟踪每个字段访问点 |
| UNIT-002 | `DATA` 每条的五组是固定等级行；末字段为等级；模块 29 当前属性从前三行按累计经验阈值选择，之后转入独立成长表，不消费第四/第五行 | C | 模块 29 `5256h/52BCh` 与 `1000:8E5Bh/8E85h/8E62h`；原始值序列和转职边界 | 只在发现其他消费者时继续解释第四/第五行的设计用途 |
| UNIT-003 | `修改.txt` 的兵种表来自同一版 `DATA`，但经验列被统一除以 50 | C | 39×5×7 全表程序化比对：`field1..6` 各 195/195 相同，`field0` 195/195 满足 `floor(original/50)` | 无；禁止把攻略经验值写回原版表 |
| UNIT-004 | `field0..6` 依次为经验阈值、攻击、防御、生命、移动、保留字段、等级；`field5` 会被保存/显示，但两套战斗属性应用器都跳过 | C/U | 模块 29 `5256h/52BCh`、模块 33 `09DCh/0B6Ah` 的独立逐偏移访问；全表 I/O 和数值显示保留 `+0Ah` | C：复刻不得赋予其战斗效果；U：原设计意图仍未知 |
| UNIT-005 | 模块 29 两套描述符分别对应 side 1/side 2，并确认全部 39 个记录号—Big5 显示名；钢甲战士为 `1C/0C` 阵营短码差异 | C | `5087h` 设置 DS:`31C9h=1/2`；`5230h` 据此选 DS:`320Dh/325Dh`；`5355h` 返回名称；`22B8h/22DDh` 固定用 set1 短码选择 MAP profile；四段签名 | 原版层保留 side 2 钢甲的 `0C` 职业分派/成长与 `1C` MAP profile 组合 |
| UNIT-006 | `修改.txt` 有八处名称差异：四处“帥/師”，另有魔祭師、飛馬戰士及邪法師/魔法師对调 | C | 原生描述符 Big5 字节、双表一致性、机器提取结果 | 无；外部名称不得覆盖原生名称 |
| UNIT-007 | 原生消费者确认兵种记录存在 `35 + 4` 边界：模块 33 只装入前 35 条，模块 29 战斗/存档状态读写全部 39 条；记录 36“龍”在场景 20/22 的动态激活也已闭合 | C | 模块 33 `090Eh` 的 `CX=23h`；模块 29 `1310h/1395h` 的 `CX=27h`；每条均五次复制七个 word；`1000:4A5Bh/4C88h` | 无 |
| UNIT-008 | 记录 35–38 的原生短码/名称为 `0P女帝、1P龍、2P頭、3P手`；场景 30 含活动女帝，场景 37 含活动的頭与两只手 | C | 双套原生描述符；`B/0061`与`B/0075`逐槽模板；模块 29 `510Ch` 的职业值读取 | 三部位行动与效果见 BAT-016/BAT-035 |
| UNIT-009 | 场景 30 的死亡单位处理分支按职业记录号确定性递增；达到难度上限后把记录 35“女帝”写入玩家职业槽 23，并在原战场格登记 side 1/slot 23 | C | 模块 29 `9733h/97B2h` 对场景号、side 2、死亡状态与 `LV_HARD` 的检查，以及 `56DAh`、两张 50×50 图的直接写入 | 叙事意义可由剧情文本补充，但不影响规则实现 |
| UNIT-010 | 场景 37 的第二只手使用内部职业值 39，描述符查找钳制到记录 38，并非缺失的第 40 条 `DATA` | C | `B/0075` slot 55；模块 29 `510Ch` 保留原值后将查找副本 `>38` 钳制为 38 | 无 |
| UNIT-011 | 场景 37 的頭/双手有独立状态与当前生命、统一覆盖为攻 100/防 10/最大生命 10000、禁止移动，死亡只清当前格 | C | side 2 状态指针表槽 54–56=`5176h/518Eh/51A6h`；模块 29 `502Ah/510Ch/8B60h/690Bh/96C2h/6ABCh` | 无；行动与效果见 BAT-016/BAT-035 |
| UNIT-012 | 普通经验是累计值；第三行后按 23 项专属表或默认值反复跨阈值，增加攻击、最大生命和成长行计数，不增加防御，也不读取 `DATA` 第四/第五行 | C | 模块 29 `5256h`；`1000:8E5Bh/8E85h/8E62h`；23 项哨兵表；机器导出 `combat-formulas.json` | 命名成长行计数的 UI 表述 |
| UNIT-013 | 39 项合并兵种目录已把原版数值、双阵营短码、转职、成长、玩家行动、AI 分派、普通附加状态、射击/技术与两类 `MAP` profile 对齐；攻略只作为已验证派生来源 | C | `angel2-unit-catalog.mjs` 硬断言 39/39 名称与分派、195/195 经验变换、1170/1170 其他值、31/31 边、12/12 顺序和两类 39/39 profile；`unit-catalog.json/csv` | 无；目录不把 `field5` 或攻略经验值提升为原版规则 |
| PROMO-001 | 普通转职图有 31 条原生边，覆盖士兵的四大分支及后续职业 | C | 模块 29 DS:`06C3h` 的 35 指针表；候选为“目标记录+1”，`99` 终止；31 条边及每组顺序与攻略完全一致 | 无 |
| PROMO-002 | 31 条边中 30 条目标首行等级与来源转职等级相同；弓兵 7→弩兵 8 是唯一例外，而且原生生产链明确允许在来源等级 7 标记处提交该候选 | C | `DATA.SWF` 31/31 边逐值比较；`029Ah` 全 side-1 棋盘扫描；`02B7h` 唯一资格调用；`09D8h/045Bh/0693h/0744h` 唯一候选、菜单与提交链不读取/比较目标首行等级；`promotion-table.json.levelBoundaryAudit` | 无；实机只可校准呈现体验，不再承担规则确认 |
| PROMO-003 | 经验达到转职目标后进入显式的新职业选择/授予流程；至少有两套角色措辞 | C | 模块 29 三段 Big5 原文；`ANGEL2_PROMOTION_DIALOGUE` `0487h` 按角色槽 `002Eh` 分支；水神愛西斯授职文字 | 命名角色槽 `002Eh` |
| PROMO-004 | 原生短码族按 31 条原生边形成完整层级，`N/P` 为普通表之外的特殊族 | C | 原生边表、两套短码/名称描述符及 `A→B/C/I/D→E/F/G/H/J/K/L` 全覆盖 | 无 |
| PROMO-005 | 转职只对玩家 side 1 触发，要求成长/等级行计数大于 3 且候选表非 `99` | C | 模块 29 `ANGEL2_CHECK_PROMOTION_TRIGGER` `02B7h` | 继续命名 `31BDh` 的 UI 表述 |
| PROMO-006 | 转职菜单不可取消；提交只写目标职业记录并把经验清零，不写当前生命或立即重算攻、防、最大生命、移动与状态 | C | `0693h` 按 `99` 建项；完整 `0744h` 只写 `56DAh` 与单位结构 `+02h=0`，随后恢复画面；三段代码签名 | 后续常规装载按“新职业+零经验”派生；不得实现为转职瞬间治疗 |
| MOVE-001 | `MAP` 与 `DATA` 一一对应为 39 条；每条物理保存两个 24-word 窗口，但运行时 profile 步长为 23 words，第 24 字是下一对象的重叠字 | C | `39 × 2 × 24 × u16` 读写循环；DS:`1F1C/26BA` 两表指针严格相差 `2Eh`；记录 38 末字 `4130h` 与 DS:`26BA` 短码 `0A` 同址 | 无；导入器须保留物理窗口但只消费 23 个逻辑槽 |
| MOVE-002 | 第一张逻辑 profile 是移动规则：`1..5` 是进入目标格的加权代价；玩家模式 `M` 与 AI 邻格试移拒绝 `98/99`；Y/A/FY/FA 还把攻击邻格/敌军格标为 2/1 | C | `1000:3BB0h..3EADh/40EEh..416Ch/3EC6h/4186h/690Bh`、`0000:7336h/939Eh`；47 段签名导出 | 逻辑槽以数字 ID 为规范；AI 消费见 BAT-037/038 |
| MOVE-003 | 第二张逻辑 profile 是地形防御百分比，按 `floor(有效防御×百分比/100)` 加入普通伤害链 | C | `0000:946Ah → 1000:41A1h` 明确查 DS:`26BA`；`93F2h/942Eh` 乘后除 100；`map-rules.json` | 命名逻辑槽 `0..22` |
| MOVE-004 | 玩家范围图保存剩余范围；每边执行 `candidate=current-stepCost`，候选须正且大于旧值；四邻接正反扫描至稳定，最终落点须空闲；模式变量 43 个生产点已穷举，模式 `1` 无生产者，十个 `FM` 生产点全属场景 `0/1/6/20/21/22/42` 剧情移动 | C | `0000:7310h/7336h`；`1000:39D4h/3A5Ah/3A6Ch/3A8Ch/3B13h/3B9Dh..3D49h/690Bh`；近回调 `139D0h+01CDh=13B9Dh`；`map-rules.json` `producerAudit` | 模式 `1` 仅保留不可达兼容算法；全部可达范围模式已绑定 |
| MOVE-005 | 普通玩家职业使用加权模式 `M`，可经空格/友军格传播并拒绝 `98/99`；`0N/水戰士` 使用统一代价 1、传播时忽略占格的模式 `0`，两者最终均只能停在空格 | C | `0000:7336h` 的短码分支；`1000:3A0Dh/3BB0h/3C46h/690Bh`；记录 26 profile | 实机确认水戰士越过占格时的界面表现 |
| MOVE-006 | 44 个场景的原始 token 同时直索引同号 `40×44` B 图块，并经逐关 128-word 描述表与 `UN/0056` 第一页解析为逻辑槽；128 个 token 全部合法，使用 21/23 槽 | C | `0000:4D65h/47EAh/7E2Ah`、`1000:3EC6h/946Ah/55DBh/8995h/0FBBh`；44 模板、36 张唯一描述表；`terrain-token-map.json` | 命名逻辑槽；槽 17/22 在现有模板无引用 |
| MOVE-007 | `UN/0056` 第二页是地形小地图颜色页；非零 token 经描述偏移取得 VGA 索引并画成 `3×3` 色块，完整地图为 `150×150` | C | `0000:2530h/285Dh/2884h/28A4h/D050h`；坐标 `(485,161)`、50×50 循环和 3 像素步长；44 张小地图 | 无 |
| MOVE-008 | 小地图占用层只处理 side 图的 1、2、`FFh`：黑色 `4×4` 外框加颜色 10、11、15 的 `2×2` 内芯；`FFh` 是未填入的玩家部署格 | C | `28DDh/2904h/2924h` 与 DS:`18A3/18AD/18B7/18C1`；模块 27 `1907h/1884h/0D61h`；44 模板计数；44 张占用叠加图 | 无 |
| MOVE-009 | 当前 10×7 视口在小地图上使用黑色 `30×21` 外接边框，鼠标预览使用同尺寸白框；点击预览中心会重新夹取战场视口 | C | `259Bh/25EDh/263Bh/2681h/26DEh/2744h/279Fh/7D2Dh`；17 段代码和三组矩形数据签名；`minimap-rules.json` | 无 |
| MOVE-010 | DS:`2E7D` 的六个战斗读取点均用于规则、颜色、地形判定/改变或攻击表现，没有字形、字符串或 HUD 名称消费者 | C/U | Ghidra 全引用审计；六段原生签名；`terrain-name-audit.json` | C：原生运行时无已发现名称绑定；U：编辑器友好标签只能推断，不得冒充原版槽名 |
| BAT-001 | `B` 记录 0–87 是 44 个偶/奇数据对 | C | 偶数四路 29,184 字节；奇数恒 8,506 字节 | 无 |
| BAT-002 | 模块 27 有 44 项模板选择表：场景 `0..38` 使用 `B` 奇数记录 `1..77`，39–43 使用 `1,49,65,85,87`；每条均为固定九段布局，其中 DS:`01A7` 是原始地形 token 图、首段是 128 个地形描述偏移、双方逐槽数组是 AI 行为，最后数组是部署名单标志 | C | `0718h` 的 DS:`08CA` 44-word 表、九段固定复制；行为经 DS:`5644h/3BFDh→3BF6h→0D3Fh` 进入 AI；名单经模块 27 `08CBh` 消费；44 模板严格校验 | 无 |
| BAT-003 | 偶数流每个位平面的前 28,160 字节含 128 个 `40×44` 图块；token 直接索引同号图块，原生视口为 `10×7` 格；末尾 1,024 字节是全零填充 | C | `4E6Bh/7E2Ah/82A4h/47EAh/3960h/427Ah/7DEFh/7DC0h`；44 张 `2000×2200` 全图无越界；176/176 尾部全零且在最大寻址外 | 无 |
| BAT-004 | side 1 模板职业为稀疏覆写，0 保留战役职业；模块 27 将合并结果写为下一场 `JUST.TST` | C | 模块 27 `082Dh` 非零合并与 `0FF8h` 序列化链 | 无 |
| BAT-005 | 模块 29 两张各 42 项的失败/胜利条件表已 84/84 分类；13 个处理器覆盖保护单/多槽、击破单/双槽、全灭及六种区域到达规则 | C | `257Ch/2583h/258Eh` 分发链；全部处理器反汇编；机器导出 `battle-objectives.json` | 将剧情角色名映射到目标槽号 |
| BAT-006 | 场景 39–41 有模板入口但无胜负表项；42/43 同时有模板和表项；49 仅有表项而无模块 27 模板映射 | C/U | 模块 27 的 44-word 表与模块 29 两张 42 项表逐项集合比较 | U：命名这些非标准入口的实际流程 |
| BAT-007 | 奇数记录 79/81/83 不被模块 27 选择表使用，十个运行模块的全部 `B.SWF` 读取生产点也无法选择它们；压缩 payload 与解码模板分别逐字节等于 21/49/65。偶数 78/80/82 仍由模块 29 的场景 39–41 路径读取并分别复制 20/48/64 | C | 十模块中每个唯一索引读取器及其全部 near caller；模块 27 的 44-word 表、模块 29 `currentStage×2`、模块 46 常量 88；六组压缩/解码 SHA-256；`b-record-audit.json` | 无独有素材或规则缺口；保留副本的制作历史不可恢复但不影响复刻 |
| BAT-008 | 普通先攻伤害为 `max(0,攻-防-地形防御)+r1+r2`，`r1/r2=4..7`；地形防御为有效防御乘地形百分比后向下取整 | C | 模块 29 `93F2h/942Eh/946Ah/9490h/94E3h/95D2h`；41 段战斗/状态签名及 39 段 MAP/范围/地形签名 | 逻辑槽以数字 ID 为规范；原版无可见名称绑定 |
| BAT-009 | 反击基础伤害是相反攻防差的非负值除 2，无普通随机加成；巨斧攻击压制反击，防守者逐单位禁用字节非零也禁止反击，兽骨骑士另有 PIT 低位特例 | C/U | 模块 29 `927Ah/92C5h/94E3h/A17Bh` | 命名逐单位禁用字节；实测兽骨骑士分支概率 |
| BAT-010 | 当前生命位于状态 `+00h`；普通伤害的地图动画逐点扣血且受 `+0Ch` bit15 阻止，全屏动画一次饱和扣血且已恢复路径不检查该位；两路由 DS:`1119h` bit0 的发布版战斗动画开关选择，普通伤害链随后扫描并移除零生命单位 | C | 模块 29 `783Dh/9135h/91C5h/96C2h/A096h/A0C6h/A0F6h/A10Ch/A122h`；`combat-presentations.json/input-ui.json` | 原版差异可达且应由忠实层保留；毒伤走 BAT-033 独立边界 |
| BAT-011 | 状态 `+02h` 是累计经验；先攻存活奖为目标等级+4..7，反击存活奖减半，击杀均为 35 项职业基础表+4..7 | C | 模块 29 `9161h/91F1h`、`1000:8CDDh`、DS:`784Eh`；机器导出 35 项表 | 追踪特殊记录 35–38 是否存在独立击杀奖励入口 |
| BAT-012 | 普通攻击链无命中/闪避/暴击分支；魔剑/兽骑分别施加防御/攻击 -20，邪剑/丛林分别写入已由技术菜单命名为混乱/施毒的 `8003h` 状态槽 | C | 模块 29 `8C2Dh/92DCh`；`LA/IP` 与同一 `8D22h/8D30h` 写入器；完整普通攻击、技术与状态调用链 | 不得外推普通命中规则到射击/技术；状态生命周期见 BAT-032..034 |
| BAT-013 | 单位槽图每格低 7 位是槽号，高位 `80h` 是该格本阵营阶段已行动；重置器按阵营扫描 2,500 格清高位 | C | 模块 29 `1000:22C9h/147Eh/14A6h`；攻击/射击/技术/休息/AI 多条独立 OR `80h` 路径 | 无；保存/模板必须原样保留高位 |
| BAT-014 | 标准战斗为玩家 side 1 → 我方自动单位 → side 2 AI 的阵营阶段制；移动不消耗终结动作，攻击/射击/技术/休息会消耗。`全部休息` 可让所有剩余手动单位恢复最大生命 15% 并变为已行动；跟随/自由行动可把余下单位交给 AI | C | 模块 29 `48FEh/55D3h/6CCAh..6DEAh/719Bh/75E4h/7528h/7A8Ch/9123h`、`1000:1542h/1595h/41BCh/5D12h` | 无；键位见 INPUT-003/UI-002 |
| BAT-015 | AI 按 39 项短码表逐类反复扫描；随机位只延后候选，每个符合条件的单位格最终提交一次行动，胜负触发可提前中断 | C | 模块 29 `1000:1595h/15F4h/1681h/2032h`；DS:`1036h` 39 项表；机器导出 `turn-actions.json` | 行为与落点见 BAT-036..038；继续命名完整表现时间轴 |
| BAT-016 | 场景 37 頭、左手、右手各有独立行动格、模板行为 0 和独立单位状态；三者存活时每个敌方阶段各提交一次专用行动，共三次 | C | `B/0075` 槽 54–56；AI 表 `2P/3P`；调度禁用检查与模块 29 `1000:17A7h/17EEh/2032h`；23 段行动签名 | 效果见 BAT-035；四项完整表现见 PRES-006 |
| BAT-017 | 玩家原生菜单将普通、射击、技术分为三类；射击成功点为 `719Bh`，技术提交/36 项分发点为 `75E4h`/DS:`52A2h`，技术提交链没有 MP/消耗品字段 | C | `6849h/688Dh/68E4h/719Bh/75E4h`；36 条 `{短码,处理器,范围}`；36 段签名；`technique-rules.json` | 继续审计是否有剧情脚本另行限制次数；玩家战斗入口无资源成本 |
| BAT-018 | 弓兵/弩兵/魔弓兵射击 seed 为 5/8/6；模式 2 每步扣 1、结果必须大于 0，再清起点与四邻，故实际距离为 `2..4/2..7/2..5`。玩家前两者伤害为 30..49/70..89，魔弓目标为 50..68、其他合资格线格为 25..34；目标为迅龍騎士时有 PIT bit 0 射击闪避 | C | `716Dh/719Bh/722Bh/7264h`、`1000:3FA8h/4014h`、范围模式 2、`V` 线形效果；AI `1000:1F7Bh` 同条件但表现入口不同；范围见 PRES-011，数值/动画见 PRES-005 | 仅 PIT 实机统计待校准；核心范围、输入与射击表现已闭合 |
| BAT-019 | DS:`41CEh` 定义 13 个职业的三阶段技术菜单，原版 Big5 项名与顺序已全部解析；DS:`52A2h` 有 36 项分发，所有玩家菜单短码均可命中 | C | 原生表解析、13 组逐阶段断言、36 项分发表及 FFFF 哨兵；`angel2-technique-rules.mjs` | 无；新增/Mod 菜单必须与原版层分离 |
| BAT-020 | 落雷为中心高伤的菱形固定伤害并消耗防魔；炎暴为按最大生命比例且有上限的单体伤害；冰雪不扣血而按固定方向优先级击退 | C | `CB0Bh..CD6Ch` 处理器；`1000:7166h/736Dh/6771h/69FAh`；防魔与 Boss 短码分支 | 实机只用于验证表现时序，不再改变核心公式 |
| BAT-021 | 单体治疗按 24/36/48% 最大生命恢复，基础经验仅在比例经验商非零时加入、外层随机仍结算；范围回復按范围值固定治疗，总实际治疗低于 50 时经验为 0 | C | `CDB5h..CE1Dh`；`1000:5CA0h/5DA4h/6EE0h/788Ah/791Eh/79A8h`；表现见 PRES-006 | 无；完整治疗/回復帧序已闭合 |
| BAT-022 | 状态技术与单位结构槽完成绑定：攻/防提升 `+08/+0A`、防魔 `+0C`、混乱 `+0E`、攻/防下降 `+10/+12`、施毒 `+14`、禁咒 `+16`；破邪清相应最高位 | C | `CE1Eh..CFB4h` 与 `1000:8CC1h..8D7Dh`；技术目标验证 `8D94h` | 生命周期和特殊边界见 BAT-032..034 |
| BAT-023 | 祈祷逐我方单位做约半数命中，再等概率恢复 5..14、加经验 5..14、攻击提升或防御提升；施法者统一经验为 0 | C | `CAE1h`、`1000:591Ch/5928h` 及四结果分支 | 确认 PIT 实机采样统计只影响概率分布，不影响值域/分支 |
| BAT-024 | 铁板/障碍从目标起横向覆写五个连续 token；每关源 token 分别从原始地图 `(16,25)/(16,26)` 复制，44 关源值已导出 | C | `CAE7h/CAF9h`、`1000:7C92h/7CB4h`、模板载入源格；`technique-rules.json` | 实机确认边界目标光标如何限制五格越界 |
| BAT-025 | 龙踏/男踏/女踏以目标中心三步模式 0 区域与当前 10×7 视口的并集为几何区，只伤所选目标阵营；每单位独立造成 10..19/15..29/20..39，零生命正常移除，固定经验 5 | C | `CD6Dh/CD85h/CD9Dh`、`628Eh/63CFh`、`1000:11BCh/1350h/4264h/73B6h..7460h`；36 段签名 | 传入的 20 不是伤害；PIT 精确统计分布只影响区间内频率 |
| BAT-026 | 落雷、炎暴、冰雪、治疗、回復、踏地与状态已连接到 `MAGIC/E/UN` 原生记录；祈祷确认为程序绘图且无归档资源，工兵确认为直接地形写入；V 线形效果见 PRES-005，五个主家族见 PRES-006，其余技术见 PRES-007。`1V/2V` 无玩家菜单、AI 池或直接调用者，`3V` 唯一直接生产者是魔弓射击 `72D3h` | C | 各表现处理器的资源调用；13 套玩家菜单、14 套 AI 池、三处理器 direct-call 全扫描；`technique-rules.json/ai-rules.json/shooting-presentations.json/technique-presentations.json/remaining-technique-presentations.json` | 无资源/同步/隐藏 V 入口缺口；低层字段原设计名只作可选考古，标称 tick 时长见 SYS-004 |
| BAT-027 | 39 个原生职业短码被 AI 分成 20 普通、3 射击、12 技术、2 女帝/龍、2 頭/手五组；技术组状态位可强制走普通流程 | C | `1000:1681h/16BCh/1728h/1745h/1795h/17A7h`；五组集合严格分割 39 个描述符短码 | 命名 DS:`31B5h` bit15 的原版状态/UI 名称 |
| BAT-028 | side 2 与 side 1 自动 AI 分别使用 `Y/FY` 与 `A/FA`；`CM/CY` 按所选目标阵营限制一点击伤；模式 2 同时用于 AI 射击；`FM` 只用于已绑定的剧情脚本移动，模式 `1` 无生产者 | C | `1000:14D8h/1519h/4264h/434Dh..4CFDh`、`0000:644Dh`、范围传播器与射击入口；43 生产点审计 | 无；AI 技能池中的同字节短码 `FM` 是另一套动作码问题，归 BAT-031 |
| BAT-029 | AI 生命低于 20% 休息，20%..39% 依行为值和四邻敌军尝试防御撤退，40% 起继续；撤退取无邻敌空格中的最高地形防御格 | C | `1000:2233h/2291h/2468h/1D67h/0C83h/0CD1h/0CF0h`；54 段 AI 签名 | 无；行为与落点见 BAT-036..038 |
| BAT-030 | AI 射击按最低有效防御、同值最低生命选目标；弓兵 30..49，弩兵 50..89，魔弓目标 50..58、其他合资格线格 25..29；后两者与玩家值域不同 | C | `1000:1F11h/1F3Fh/1F7Bh/1FB6h/2016h/0B91h`；`ai-rules.json`；完整表现见 PRES-005 | 无；仅 PIT 采样分布与光标反馈可由实机校准 |
| BAT-031 | DS:`0D9Eh` 有 14 组三级 AI 技能池，DS:`0EE8h` 有 33 项动作参数；敌方取最低防御/生命，我方取最大缺血；`SM/FM` 是无法命中参数表的原生孤项 | C/T | 全表解析与断言；`1000:1DA2h..1EFEh/3FE6h/3FFDh/4065h`；`SM/FM` 缺项和忽略失败返回的控制流 | C：孤项与危险控制流；T：`SM→OJ` 原意；实机观察可见异常，不得覆盖原始字节 |
| BAT-032 | 八个状态槽在完整玩家+自动+敌方回合后统一 tick 一次；bit15 状态按 `8003→8002→8001→0000` 递减，所有施加器直接覆盖并刷新计数 | C | `0000:4DCDh`；`1000:35DCh/35F2h/8CC1h..8D4Ch`；双方 58 指针×八槽循环；41 段签名 | 无；实现测试必须按完整回合而非阵营阶段 tick |
| BAT-033 | 施毒在状态递减前令 `currentLife=floor(currentLife/2)`；毒伤降到 0 不调用死亡移除，单位仍留在棋盘、计入目标并可被选择/休息 | C | `1000:3633h/35DCh` 无 `96C2h` 调用；`55D3h` 选择器无生命检查；目标只扫棋盘图；AI 0% 生命进入休息 | 原版层保留；“毒可致死”只能作为显式 Mod/修复规则 |
| BAT-034 | 混乱 `+0Eh` bit15 在 AI 装载时把行为覆写为 `FFh`；普通职业只做最高防御安全撤退，射击/技术/女帝职业只做 PIT 选格移动，随后消耗动作且不攻击；玩家选择器不直接禁止手动操作 | C | `1000:2300h` 以 `SI=31A5h+8` 间接读取；`18A1h/192Ch/19DDh/1A68h/1D67h/212Dh`；`0000:55D3h` | 旧直接地址审计漏掉寄存器算址，空效果结论已撤销；实机只校准表现/PIT 分布 |
| BAT-035 | 场景 37 頭从高級回復开始与高級冰雪交替；双手共享从究級落雷开始的落雷/炎暴开关，两手同存时各一次；四项直接复用玩家处理器、公式与 PRES-006 表现 | C | AI 代码段基址 `11470h`、零初值 `CS:037D/0405`；`1000:17A7h/17EEh`；`CDD9h/CD2Fh/CB62h/CC0Ch`；`turn-actions.json/technique-presentations.json` | 多目标时手部选择有低格偏置；四项帧/音效已闭合 |
| BAT-036 | 44 模板的 672 个活动实例仅用行为 `0,1,2,3..8,11,12`；行为按关卡单位槽配置；3/4、5/6、7/8 是领队/跟随组，9/10 仅有代码，FF 仅由混乱产生 | C | `B.SWF` 双方 75-word 表；DS:`5644h/3BFDh→3BF6h→0D3Fh`；`1BD9h/0D83h/0D9Ch`；逐实例分布断言 | 无；不得将行为并入兵种或 `DATA.field5` |
| BAT-037 | 近战与距离 2 射击落点都只最大化候选格地形防御、同分后者胜；近战可移动后攻击，普通射手只有实际换格才射击，原点最佳反而失败 | C | `1000:070Ah/0748h/078Bh/07A7h/083Ah/0897h/0993h/09BCh/09E2h/1D0Bh/2044h/2081h`；54 段 AI 签名 | 忠实层保留射手“原点最佳不射击”怪癖 |
| BAT-038 | 默认远追只检查 `enemyCell+50`；行为 2 以硬编码模式 A 门控后只追逐；行为 12 仅场景 0/4/9 有路线，其他场景仍绕过职业行动 | C | `1000:20DBh/0AE1h/0B09h/0B64h/2193h/1AF8h/1B56h/1BBEh`；行为 12 模板分布；`ai-rules.json` | 行为 12 的场景 4/9 额外机制见 BAT-052 |
| BAT-039 | 阵营图 `FFh` 是未填入的玩家部署格；44 个映射入口中 33 个进入部署 UI、11 个直接写 `JUST`，共 423 格 | C | 模块 27 `056Eh/1907h/1884h`；44 模板完整值域与逐格导出；`battle-lifecycle.json` 的 22 段签名 | 无 |
| BAT-040 | 部署名单来自 75 个场景标志的非零项；模板初始 side 1 单位固定不可撤，其余可填入/撤回 `FFh`；未填标记在模块 29 载入后清空；19 个热区、分页导航和前后 `FFh` 循环已恢复 | C | 模块 27 `08CBh/0898h/0C60h..0FC8h/0D61h/1884h/18B2h`；DS:`0E40h/0ED8h`；模块 29 `1000:543Bh`；`input-ui.json` | 精确像素时序归 UI-003 |
| BAT-041 | 新战 `N` 分支载入 `JUST`，除场景 3/8/11 外以 `LV_HARD+1` 次把 side 2 经验推进到下一阈值加 1，再重建双方、回满生命并调用 `4DCDh` 开启第 1 回合；编号 `WAR` 用 `3788h` 恢复阶段且不重复播种。第 0 关六名 side 1 最终同为等级 1/经验 0/攻 39/防 21/生命 160/移动 4。标准循环为玩家→我方自动→敌方 AI→完整回合 tick；Q/Y 分别为退出/主动撤退；Caps+J/数字键盘 `*` 是 DS:`132Fh` 在发布版关闭的开发调试支路 | C | `4BD9h/48FEh/4A24h/4ABEh/4DCDh/536Bh/537Bh/538Bh/539Bh/540Dh`、`B941h/BA8Ch/BAAEh`、`1000:8BD1h/5386h/3788h/41BCh/1542h/14A6h`、DS:`132Fh/F6CDh/F6E0h`；22 段生命周期签名；`battle-lifecycle.json/input-ui.json` | 其他发行版若有角色专属初值，须以对应二进制或存档另立版本证据；调试门不可作为正常玩法 |
| BAT-042 | 普通胜利默认 `nextModule=25,nextStage=current+1`；失败明确 `nextModule=27,nextStage=current`，故重试会回同关部署而非战场内复活 | C | `1000:42A8h` 默认写入；`02E4h/0627h` 胜利提示；`0321h/057Fh` 失败提示与转移；模块 29 主入口回传 | 38 个处理器的覆盖见 BAT-045/046/048/049；无处理器场景保留默认值 |
| BAT-043 | DS:`2F83` 的 999 是当前即时胜利；`WAR` 元数据把 999 序列化为 1000，载入 1000 后做逐关事件的结算分支但不重复询问存档 | C | `4A6Eh/4A74h/4A79h`；`1000:88B9h` 的 999→1000 写缓冲；`4BD9h/48FEh` 的载入终局分支 | 无 |
| BAT-044 | `1000:42A8h` 是回合与结算共用的逐关战斗事件分发器，不是纯战后函数；在 `4DF5h` 与 `4A74h` 调用，共有 38 个唯一场景守卫入口 | C | 分发器 128 字节完整签名；38 条 near call、完整函数边界、每个处理器首条 DS:`2E77h` 比较和逐函数 SHA；`stage-events.json` | 控制流目录已闭合；特殊表现见 PRES-012 |
| BAT-045 | 场景 21 第 1 回合加入 side 1 槽 `0,1,24,8`、运行 SAY 43/44 并脚本移动，随后因 side 2 为空立即胜利并直达模块 27/场景 22；场景 22 第 1 回合临时加入女帝/魔祭師、运行 SAY 76–79，最终留下六个敌人，只有槽 28 是胜利目标 | C | `1000:4B72h/4C88h` 完整函数签名；`51E5h/7DF7h/BAB8h`；B 记录 43/45；目标表；六条剧情记录交叉；`stage-events.json` | 槽与剧情姓名的全战役角色映射另行闭合；保留场景 22 聚焦 `05CBh` 却写 `05CAh` 的原版怪癖 |
| BAT-046 | 场景 42 的空 side 2 模板立即胜利；999 移动槽 0/24、运行 SAY 11/18/20、对槽 7 播放完整 4L 落雷、清除槽 7/23 并运行 SAY 19，1000 只恢复下一场 6；清格表示穿过传送门 | C | `1000:457Fh/6084h` 完整函数签名；B 记录 85；场景 42 目标；SAY 11/18/19/20；`stage-events.json`；4L 304-tick 表现见 PRES-006，对话窗见 PRES-009，逐格移动见 PRES-012 | 无；PIT 等价路径选择和 VGA 墙钟差异按 PRES-012 作为忠实层参数保留 |
| BAT-047 | 普通战前顺序是模块 25 剧情→27 部署→29 战斗；场景 6 特例是 27 部署→25 的剧情记录 14→29 战斗。模块 23 新游戏进入 25、继续游戏进入 29；模块 25/27/29 的后续出站均已闭合 | C | 模块 23 `0480h/059Fh/1455h`；模块 25 `05F2h/145Fh`、DS:`0E16h/0F88h`；模块 25/27/29 主入口返回片段；`title-flow.json/stage-events.json` | 场景 38 的重复 MAGIC 后项 74 原生不可达，保持为原版异常 |
| BAT-048 | 场景 37 第 1 回合运行 SAY 81，999/1000 改走内部场景 49；模块 25 剧情 70→模块 33→35→`module27/stage38`。场景 38 第 1 回合运行 SAY 164，999 运行 SAY 165 后转模块 46、1000 只恢复该跳转；模块 46 正常路径进入制作人员表与永久终幕，不会返回追加战部署 | C | `1000:515Ch/5197h`；模块 33 `00C7h/02E0h`、35 `00ADh/02E0h`、46 `00FEh/03CEh/0725h`；B 记录 75/77；目标表；SAY 70/81/164/165；共享提取器合计 38 段签名；`stage-events.json` | 场景 38 的重复 MAGIC 后项 74 与模块 46 `0101h–0128h` 的重玩写入都保持不可达 |
| BAT-049 | 38/38 个逐关处理器的回合/结算条件、72 个 SAY 记录、直接状态修改和 999/1000 路由均已分类；动态棋盘关为 0/1/6/11/20/21/22/42，14–19 从第 6 回合起把 75 个敌方逐槽 AI 行为清零，场景 8 写 SAY 157 却不调用渲染器 | C | `1000:4328h–51E5h` 全段及 38 个逐函数 SHA；`5319h/533Eh/536Ah`；DS:`2E5Ch`；特殊 far/near call 计数；72 份剧情 JSON；`stage-events.json`；剧情解释见 PRES-009，特殊棋盘表现见 PRES-012 | 无；直接移动、聚焦、写入可见边界和特殊覆盖均已闭合 |
| BAT-050 | 场景 30 首回合把 side 2 槽 27 从记录 35 改为 0；每次击破按记录号 `+1` 重建；四个 `LV_HARD` 标签依次对应记录 `0..7/0..15/0..23/0..31`，末次转为 side 1 槽 23“女帝”；“無法無天”还令全部 side 2 基础/当前攻防和最大生命增加 `floor(value/2)` | C | `1000:4F1Eh/8BD1h`；`0000:536Bh/537Bh/540Dh/9733h/97B2h`；模块 23 原生难度表；`title-flow.json/stage-events.json`；上下文短句见 PRES-012 | 无；每次形态转换前的短句及最终转换顺序已闭合 |
| BAT-051 | `0P/女帝` 与 `1P/龍` 的 `WD` 沿目标→施法者路径反向增长；每格只伤与所选目标同阵营的单位，最多逐点扣 90。防魔完全阻挡且本次不被清除，死亡在全动画后统一移除 | C | `0000:62F2h/6438h/644Dh/783Dh/CCD2h`、`1000:660Eh..673Fh/7E09h`；21 段专项代码、3 段数据签名；`wd-stage26.json` | 原版玩家可见名称未恢复，只保留内部码 `WD`；不影响规则 |
| BAT-052 | 行为 12 场景 4 每次路线移动尝试后以 seed 3 建立近身安全区，用 `MAGIC/26` 做 44-tick 演出并令区外 side 1 单位生命减半；场景 9 低于格 934 写胜利 999，却以 VGA 复制留下的 `DI=3450` 进入无界 PIT 路径器，可折回后再播放 `E/14` 移动或超过 100 项覆写代码段 | C/U | `0000:633Dh/63CFh/D4EEh`、`1000:1AF8h/1B56h/1BBEh/240Fh/3A8Ch/6CF8h..6EDEh/7E09h..7F21h/7F4Ah/7F72h/828Fh`；26 段代码、3 段数据签名；四条确定性参考轨迹；`behavior12-effects.json` | C：场景 4 目标/公式/资源/时序及场景 9 陈旧 DI、相邻零缓冲、PIT 排序和越界机制；U：实际 PIT 轨迹、动态棋盘结果及代码段覆写后的可见表现 |
| PRES-001 | 模块 33 的战后名册固定展示妮雅至爱欧里雅 22 张战绩卡，第 23 项爱莉欧拉以 `FFh` 画像终止；显示姓名、兵种、等级、生命、攻击、防御与 `KILL_ALL[序号]`“战绩”，每卡最多 400 tick、任一动作可提前翻页 | C | 模块 33 `02FAh/03B2h/042Ch/0492h/0526h/084Ah/090Eh..0B6Ah/0F34h`；DS:`0556h` 58 项描述符与 `02E1h..033Bh` 标签；`A/9,D/<画像>,C/随机,UN/6–8`；专题合计 22 段签名与 `ending-presentations.json` | 无；`KILL_ALL` 只按可见语义命名“战绩”，不外推为击杀数 |
| PRES-002 | 模块 35 依前 23 槽的骑兵/战士/法师计数、`SAVE_NUM>100` 与否、75 项战绩总和是否大于 100 选择四段结局中的 8 段文字和 `UN/0..20` 插画；骑兵赢并列，战士赢与法师的并列，法师必须严格最大 | C | 模块 35 `02FAh/0454h/0470h/049Bh/0529h/069Eh/072Fh..084Fh`；8 段内嵌 Big5 文本；模块 29 `1000:88ADh–88B4h` 的存档次数生产者与 `9252h–925Dh` 的战绩生产者；`MUSIC/40,UN/49`；`ending-presentations.json` | 无；阈值是严格 `>100`，不得写成大于等于 |
| PRES-003 | 模块 46 用 `B/88` 的 20 个职务帧和 `C/33` 的 22 个姓名帧组成七页不可跳过的制作人员表，共执行八次 400 步 CRTC 转场；随后 `UN/54` 五帧永久循环且无退出输入，调用后的 `module27/stage38` 写入不可达 | C/S | C：模块 46 `03CEh/0433h/0467h..06DDh/06F6h/0725h/0736h/078Eh/07BAh/07E8h/0DC6h`、资源帧与控制流签名、`ending-presentations.json`；S：22 个姓名由原生 PNG 人工转录，`C/33` 第 2 帧的部件形态并由作曲者罗马字资料交叉验证为“蘇泓漳” | 无；逐帧 PNG 仍是文字权威证据 |
| PRES-004 | 普通攻击由 `DS:1119` bit0 选择地图/全屏表现；地图受击为 `UN/62` 九次绘制和两次 `E/38` 请求，死亡为 `MAGIC/12` 清格前 6、清格后 9 个描述符；全屏以职业记录选择 `M_00/Y_00` direct/+50 图形和两套五槽 `E` 表。第 0 关士兵/骑兵在伤害 `<=10` 时使用 `frame 3 + V2/E0` 格挡，在 `>10` 时使用 `frame 1 + V1/E2` 常规受击；零生命仍先执行该阈值段，再执行 `frame 2 + E/11` 死亡段。魔劍戰士 1/51 显式重映射到 `Y_00/41、42` | C/U | C：模块 29 `0220h/0254h/9135h/91C5h/926Bh..9296h/9852h/9FC4h/A01Bh/A17Bh/A1E8h/A23Eh/A2E4h/A377h/A52Fh/A6B5h/A77Fh/A7A4h/B683h/B6BDh/B725h..B78Ah/1000:6ABCh/6B58h`；25 段代码、3 段数据签名；39 职业、158 图形记录、37 音效记录；`combat-presentations.json`；第 0 关实机录像音轨互相关 | C：资源号、帧/step 表、受击阈值与两阶段死亡顺序、native tick（标称 10.000151 ms）、等待计数和命令同步；U：全屏渲染子步/VGA 同步的宿主时长、其余命令字段的原设计名称 |
| PRES-005 | 三类射击始终走棋盘表现且不读取普通攻击的全屏开关；弓/弩共用 `UN/60` 的“空→八帧→空”10 次绘制、每次 6 tick，动画后才逐点扣血；魔弓以 `MAGIC/83` 配 `UN/60` 沿目标到射手路径反向增长，再做八步收尾，总等待 `(路径格数+8)×20` tick。无防魔时目标受两次 `floor(base/2)`、其余合资格线格一次；有防魔时增长半伤先被阻挡并清盾，所以选中目标只受第二次半伤、其他盾保护线格受 0。迅龍骑士闪避在玩家侧用无声 `UN/60` 60 tick，AI 侧用 `UN/62+E/38` 90 tick/两次请求，均不扣血 | C/U | C：模块 29 玩家 `719Bh/722Bh/7264h/CCA4h`、AI `1000:1F7Bh/1FB6h`、公共射击 `1000:747Ch`、线形链 `1000:64A6h/64F5h/6532h/659Bh/7E09h/7E3Bh/7DECh` 与 `0000:642Bh/64A1h/783Dh/5491h`；19 段代码、3 段数据签名；`UN/60、UN/62、MAGIC/83、E/38`；`shooting-presentations.json` | C：入口、资源、逐帧 tile code、伤害/防魔/等待同步、标称 tick 时长和玩家/AI 不对称；U：末尾 `82h` 高位渲染角色的原设计名称 |
| PRES-006 | 落雷、炎暴、冰雪、治疗、回復共 18 项动作直接使用棋盘效果描述符；固定等待分别为 L `414/257/348/304`、F `70/120/195/290`、C `120/180/240/300`、H `275/215/235`、I 共用 `255` tick。五类都在完整图形链后才开始扣血、防魔消费、位移或治疗 | C/U | C：模块 29 五家族入口及公共链 `1000:5BFEh..6FFEh/7166h/788Ah..79A8h`、`0000:8D18h..8ECDh/0220h/0224h/6425h/642Bh/64A1h/64B7h/64D6h`；46 段代码、9 段数据签名；25 图形、8 VOC、10 联系图；`technique-presentations.json` | C：资源、描述符、VOC 请求点、`0220h` 直提与 `0224h` 战斗音效门、固定等待、标称 tick 时长与规则同步；U：低层字段原设计名 |
| PRES-007 | 剩余 15 项技术表现已闭合：三种踏地各做 33 次程序双帧绘制、4 次 `MAGIC/82` 请求和 13 个显式 tick 后才伤害；九种状态/净化固定等待为 `165/300/300/150/165/165/290/250/225`，完整表现后才免疫/写状态；祈祷逐合资格单位程序绘图、显示结果、写规则并最多停留 60 tick；工兵无独立动画/音效而直接写五个地形字节 | C/U | C：模块 29 `CD6Dh..CD9Dh/CE1Eh..CFC6h/CAE1h..CB0Ah`、`1000:11BCh..1477h/591Ch..5BFDh/74FEh..7CD5h`；50 段代码、7 段数据签名；15 图形、5 VOC、10 联系图；`remaining-technique-presentations.json` | C：资源、程序坐标/描述符、音效、等待、标称 tick 时长与规则同步；U：低层绘图原语原设计名与 PIT 采样分布 |
| PRES-008 | 模块 23 的标题前 Logo、滚动开场、标题两套素材、难度和继续槽表现均已闭合：Logo 最多 300 tick；开场含 17 行文字、6 次背景切换、1,773 轮/7,092 tick；标题按 40-tick 上部揭示、最多 40-tick 停留、启动 `MUSIC/1`、80-tick 下部揭示，201 个 8-tick 空闲循环后重播完整开场；继续界面用 `A/6` 执行 135 次构造绘制且无归档音效/显式等待 | C/U | C：模块 23 `0611h..13EAh/1455h..1CDFh/20A0h/22B9h/31B4h..31E0h/40F0h`；31 段代码、5 段数据签名；19 图形、2 RIX、97 复原图、4 联系图；`title-presentations.json` | C：资源、原生调色板、绘制/翻页/揭示顺序、RIX 起止、固定等待、标称 tick 时长与空闲重播；U：VGA retrace 宿主时长、低层绘图原语原设计名、主机音频漂移 |
| PRES-009 | 模块 25/29 剧情表现已闭合：两套解释器共享 15 条有效命令，模块 29 另有 `DL`；上下窗首次展开 11 步、关闭 12 步，ASCII/Big5 步幅 8/16、每字 8 tick、换行 20；`PP` 绘 `BK/<id>` 于 `(160,80)`，`HU/HD` 载入 `D/<id>`，`W-` 载入 `A/18`，模块 29 的 `ED` 恢复战场。模块 29 的“說話”开关预载 `MAGIC/57..71`，每个非排除标点 Big5 字按字符码模 15 选声并等待播放；66 项肖像元数据与 68 个肖像主帧已恢复；SAY 74 利用 D/67 元数据查找失败后沿用 D/56“龍王”的布局/名字完成变色；69/116/117/118 已确认为无生产者归档 | C/U | C：模块 25 `05F2h/0736h..145Fh/1A18h/2B2Ch/391Ah`，模块 29 `023Eh/4F41h/BAB8h..CAC3h/D3B6h/EA04h/1000:2E77h`；34 段代码、12 段数据；97 张调色板正确图、4 联系图；`story-presentations.json` | C：命令、几何、资源、原生调色板、RIX/VOC、等待、标称 tick 时长、恢复和全发布版选择器；U：低层 VGA 拷贝原语的原设计名 |
| PRES-010 | 部署错误与普通战斗终局反馈已闭合：三条可达部署错误在 `(2,328,636,20)` 底条立即绘制，仅新主操作关闭；普通胜利/撤退/失败/退出复用 `A/18` 与 `D/45/46`，保留 8-tick 字形、`MAGIC/57..71` 逐字声、确认菜单和状态路由。胜利五槽存档直接写入且无覆盖二次确认；失败只接受新主操作并回同关部署；`DS:059F` 固定短句无发布版入口。模块 27 的 `DS:0005` 已通过共享父描述符导入链确认为“按鍵”开关 | C | 模块 27 `0301h/041Eh/052Eh/0E63h..0ED2h/1A1Ah/271Ch`、DS:`0F1Bh..0F7Bh`、`UN/39/40`；模块 29 `0054h/023Eh/02E4h..0693h/08DDh/3326h..3889h/4A74h/5651h/6DB8h/B9D2h/C9B9h/EA04h/1000:03D1h/0520h`、DS:`059Fh..06A1h/1B97h..1C1Fh/3F2Ch/4475h`；30 段代码、6 段数据、16 VOC；同一 `MF` 父描述符 `+12h` 的四字节等序导入；全 near/far/pointer 入口扫描；`feedback-presentations.json` | 无 |
| PRES-011 | DS:`01A9h` 的 `50×50` 范围图同时驱动表现与输入：非零格完整绘地形，零值格四个位平面逐行与 `11h/44h` 相与只保留 25% 像素；所有手动选格先要求同一字节非零，再检查动作目标。普通攻击只标四邻敌军、单目标自动锁定；移动要求空格；射击 seed 5/8/6 并清自身/四邻；技术/工兵走专用验证；退出填全 1。DS:`58A5h` 则是描述符投影的独立 `10×7` 地图特效 sprite 层 | C/U | C：模块 29 `388Ah..47E9h/7097h..77CCh/785Eh/7C00h/7E2Ah..7F2Ch/8273h..82CCh`、`1000:39D4h..40CCh/84E0h..8595h`，DS:`58A5h..58EAh`；34 段代码/1 段数据；B/0 实际地形核验；`range-presentations.json` | C：范围生产、零/非零像素、动作输入门、恢复和图层边界；U：动态描述符 `0205h` 与高位码 `81h..83h` 的原设计名称 |
| PRES-012 | 特殊逐关表现已闭合：`51E5h` 只改棋盘内存而不刷新，`834Ah` 以钳制视口逐步聚焦并在终点重绘高亮；`7DF7h` 构造最多 100 格的目标→源路径，等价邻格按 PIT mod 3 排序，每格复制占用、重绘视口/单位/小地图/指针并执行 VGA 翻页，同时异步播放 5000 Hz、1.261 秒的 `E/14`。场景 `0/1/6/11/20/21/22/30/42` 已逐操作编排；场景 30 的 `22h` 是“我．．．我好難過．．．／頭好痛啊！”上下文短句并在每次形态转换前重播，场景 42 继承 PRES-006 的 4L 304-tick 表现 | C/U | C：模块 29 `0000:0249h/48B8h/7C50h/7C5Fh/7C8Bh/C97Ah/C97Eh`、`1000:51E5h/533Eh/7DF7h..849Ah`；21 段代码、2 段数据签名；DS:`84BBh[22h]→8762h`；`E/14` SHA-256 `f16bdce41ef0dc30b0dc93429322ea94fb33c64503776ef99bc58cd3088c343e`；`stage-presentations.json` | C：九关调用顺序、首次可见边界、路径构造、资源/声音请求、重绘、翻页与标称 tick 时长；U：PIT 等价路径的采样分布、VGA 回扫宿主时长 |
| PRES-013 | `WD` 使用 `MAGIC/19` 十帧及 `1..10,82h` 十一个描述符，无直接 VOC，等待 `(路径格数+10)×20` tick；第 26 关敌方阶段尾每次先以 `MAGIC/21` 13 个描述符、`MAGIC/14` 4 个描述符和 26 次纵列扫过合计 385 tick，再将首个 side 1 单位所在列扫描带内单位向下推 1–3 格，固定执行两次且无伤害/状态 | C/U | C：模块 29 `0000:6392h/64ACh/64B7h/6548h/CC3Bh/CCD2h`、`1000:24A5h..254Eh/660Eh..673Fh/7CD8h..7DEAh`；21 段代码、3 段数据签名；`MAGIC/14,19,21` 共 51 帧、3 张联系图；`wd-stage26.json` | C：资源、描述符、等待、标称 tick 时长、结算与两图移动；U：`WD` 等价路径 PIT 分布及玩家可见原名 |
| SAVE-001 | `WAR0.TST` 至 `WAR4.TST` 是五个编号存档槽文件；战斗侧栏与系统菜单均可进入编号读/写选择器 | C | 模块 `3C70h` 循环变更文件名数字；`3CE2h` 逐个只读打开并读取 50 字节；`BA18h/BA22h/B941h` | 槽位视觉布局与覆盖提示的完整时序 |
| SAVE-002 | 槽位列表从头部 `12h/14h/16h/18h/1Eh` 读取“職業/等級/經驗值/儲存次數/難度”；职业码查表为职业名，三项数值右对齐，难度使用四个原版标签 | C | 模块 23 DS:`0A35h..0A79h`、`17ADh..195Fh/3C9Ch`；模块 29 DS:`1B97h..1C1Fh`、`3410h..36AFh/1000:8882h`；`title-flow.json`、`save-state-semantics.json` | 无 |
| SAVE-003 | `.TST` 首个 `u16` 是压缩流绝对结束偏移；保存器写出该值再加 18 字节 | C | 模块 29 `8AA2h` 保存压缩目的偏移、`8A54h` 写首字、`87F2h` 以 `CX=首字+12h` 写文件；六样本吻合 | 尾随 18 字节按原样保存，不赋予校验语义 |
| SAVE-004 | `JUST.TST` 是模块 27 在部署完成后为下一场重新生成、由模块 29 在 `CONTINU='N'` 时使用的开战模板；偏移 7,952 是地形描述表，双方 75-word 数组是逐槽 AI 行为；残余 `FFh` 在开战前清零 | C | 模块 23 新游戏写 `CONTINU='N'`；模块 27 `0FF8h` 创建/序列化/XOR/RLE；模块 29 `4BD9h/5386h/543Bh/55DBh` 载入链；固定解码 8,358 字节 | 无格式级未知；特殊关动态覆写归场景逻辑 |
| SAVE-005 | `.TST` 使用文件偏移 `02h..11h` 的 16 字节循环 XOR 和 `C0h` 高位标记 RLE；写端只覆写 15 个 key 字节，代码段游标不是随机源，第 16 字节是缓冲残留 | C | 写端 `8A54h/8AA2h` 与读端 `108Fh/10C1h`、`560Bh/563Dh`；key 游标 `8B3Eh/122Eh`；六文件完整声明流严格解码，第 16 字节含 `55h/A8h` | 无 |
| SAVE-006 | 五个 `WAR` 均解为 11,972 字节；双方 1,440 字节块各是 60×24 字节单位动态状态，含生命、累计经验、两个无规则消费者兼容字和八个状态；772 字节尾段含双方 AI 行为、设置、经验奖励计数、逐槽行动状态、重叠残留，以及通用菜单颜色属性和原点；DS:`111Dh` 是隐藏 tick 等待门，DS:`1165h` 无消费者 | C/U | `5087h/510Ch/540Dh`、`35F2h`；DS:`31B9h/45D0h/4648h/464Ah/4BECh/4C64h/4C66h`；`29A2h/D3B6h/5C71h/5C88h/8EE6h`；读写链；五样本；`save-state-semantics.json` 16 段代码/1 段数据签名 | C：全部运行时行为和兼容策略；U：`04h/06h/1165h` 的原源代码名称，不阻塞实现 |
| SAVE-007 | `WAR` 解压偏移 0/2/4/6 依次为视口左上 X/Y、战场光标/焦点 X/Y；五个样本可重构真实小地图视口 | C | DS:`5390/5392/538C/538E`；`2681h/7CE2h/7CF1h/7D34h/7D6Dh`；五张存档态小地图 | 无 |
| SAVE-008 | 保存器把 `SAVE_NUM` 先加一再写“儲存次數”；保存前的 side-1 槽-0 预载只刷新 `DS:318E` 经验，不调用 `51ECh` 重算 `DS:319D/31BD`，所以“職業/等級”可来自先前工作单位而“經驗值”来自 side 1 槽 0 | C | 模块 29 `0000:5083h..5299h`、`1000:8882h..88CAh`；五个样本的头部经验与解压体 side-1 槽-0 累计经验逐一相等；`save-state-semantics.json` | 严格兼容保留；改良模式如修正须标为偏离原版 |
| SAVE-009 | `.TST` 无 magic、版本、校验和或旧版迁移；标题只处理打开失败，读写器不检查短读/写入结果，XOR 多处理 50 字节，RLE 不检查源/目标边界。损坏行为是未定义内存访问；安全 Web 导入必须结构硬拒绝，不模拟内存破坏 | C | 模块 23 `3C70h..3D1Fh`；模块 27 `0FF8h/1144h/1192h`；模块 29 `0DD0h/108Fh/10C1h/5386h/560Bh/563Dh/87F2h/8A54h/8AA2h`；21 段签名、六样本、12 项损坏语料；`save-robustness.json` | 尾随 18 字节只作写器指纹；现代版本/校验信息放在 legacy payload 外层 |
| SAVE-010 | `WAR` RLE 原生目标为 11,964，末游程把五样本写到 11,966/11,967 后停并忽略 5–7 个压缩字节；差异只在 256 字节无消费者填充。`.TST` 不保存 PIT/RNG 状态或输入日志，不能单独确定重放未来随机分支 | C | `1000:10C1h` 目标常量与六样本逐命令模拟；`0000:95D2h`、`1000:2016h/5BE6h/7460h` 的 PIT/未序列化累加器；`save-robustness.json` | Web 重放需扩展 PRNG 状态/调用序号、语义动作日志、规则与 Mod/内容哈希 |
