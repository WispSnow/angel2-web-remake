# GDD 证据登记表

> 肖像补充核查：原生通用表 C（`DS:0DC0h..0DE4h`）明确让 `0N/水戰士` 敌我两侧共用 `D/51`、`1N/半龍戰士` 敌我两侧共用 `D/64`，对应布局字节也完全相同；这两类没有独立敌方头像资源。该说明补充 `PRES-009` 的肖像资源边界，不新增证据行。

> 场景 37 肖像补充核查：side 2 槽 54–56 的角色描述符肖像均为 `FFh`，`2P/3P` 经
> `0000:51B9` 通用身份回退后都指向 `D/51` 水戰士；原版没有頭／手专属 `D` 肖像。
> 碧娜維姬的具名敌方描述符另为槽 1／`D/8`。该说明细化 `BAT-080/UNIT-014`，不把
> `stableRemake` 的身份肖像修正写成原版事实，也不新增证据行。

> 完整回合特殊生成链勘误（2026-08-31）：BAT-056 已确认发布版分支为 stage `4/11/27`。
> BAT-071/072/074/075/076/077/079 等较早逐关行中的“只有／只覆盖 stage 11”应统一解读为
> “只有 stage 4/11/27，且不含该行关卡”；这些关卡的无增援结论不变。stage 27 的旧无增援
> 结论则由 BAT-069 正式推翻。

| ID | 结论 | 等级 | 证据 | 下一验证 |
| --- | --- | --- | --- | --- |
| RES-001 | 14 个索引容器共有 1,282 个有效记录，已全部拆出 | C | 各容器索引表、提取 manifest、SHA-256 | 无 |
| CMP-001 | 内嵌压缩流为 stored/LH7，输出长度由流头给出 | C | 原生解压器、LHA 解码结果、2,249 条严格长度校验 | 标注主模块解压器的原始符号 |
| DBG-001 | `GO.EXE` 附带 TLINK 3.10 调试区，含 239 个带地址原始符号 | C | MZ 声明尾 `55C0h`、`52FBh` 头、固定 9 字节符号表、名称池，自写解析器与 TDUMP 交叉校验 | 继续解析模块/源码行表 |
| LDR-001 | `GO.EXE` 通过 `LOAD_V→UPK/OVER→RUNTO` 动态构造并执行运行模块 | C | 原始符号、反汇编调用链、既有运行时交接断点 | 捕获更多状态对应的运行模块 |
| MOD-001 | `UN.SWF` 含 10 个可重构运行模块，共 12 个 LH7 块、256,983 字节第一层装载映像 | C | 头记录、`UPK/OVER/RUNTO` 访问方式、入口地址 | 无 |
| MOD-002 | 加载器初始状态为 `WK_EXE=23`，首个运行模块由记录 23/24 组成，入口 `0602:000E` | C | `046A:004A` 初值、`LOAD_V` 读法、23 号头与 24 号解压块 | 首模块出站见 TITLE-001/003；其余外围状态继续追踪 |
| MOD-003 | 10 个第一层映像均为 LZEXE 0.91，二次解包得到 628,256 字节业务代码和 2,651 条 MZ 重定位 | C | 10/10 入口匹配 29 字节 LZEXE 0.91 签名；`unlzexe` 全部成功；恢复 MZ/relocation/load image；Ghidra 发现 2,819 个函数 | 建立完整状态图和模块角色表 |
| TITLE-001 | GO 以 `WK_EXE=23` 进入模块 23 并循环执行运行模块，`WK_EXE=0` 时清理退出；模块 23 向父块导出 `WK_EXE/CONTINU/LV_HARD` | C | GO `1000:0000`、模块 23 `0000:0000–010Ch/0351h`；25 段签名；`title-flow.json` | 无；密码等外围状态另行追踪 |
| TITLE-002 | 标题菜单默认“遊戲開始”，另一项为“繼續遊戲”；两项热区为 `(504,75/99,96,20)`，键盘在 0/1 间循环，指针悬停更新当前项 | C | 模块 23 `0480h/19F2h/1A47h/1AFDh/1B24h`；DS:`0BAAh/0BC6h`；`title-flow.json` | 无；完整表现见 PRES-008 |
| TITLE-003 | 新游戏四项难度依次为 0“過關斬將”、1“勢均力敵”、2“困難重重”、3“無法無天”，默认 0；确认写 `WK_EXE=25,CONTINU='N',LV_HARD=0..3`，取消返回标题 | C | 模块 23 `059Fh/1B7Eh/1BD3h/1C23h..1CDFh`、DS:`0D00h/0D32h`；模块 29 难度消费者；`title-flow.json` | 无；完整表现见 PRES-008 |
| TITLE-004 | 继续游戏扫描 `WAR0.TST..WAR4.TST`，五槽默认 0 且循环；空槽可高亮但不可确认，取消返回 `X`，有效槽返回 ASCII `0..4` 并写 `WK_EXE=29/CONTINU=槽字符` | C | 模块 23 `1455h/14E5h/16C7h/183Dh/3C70h..3D0Fh`、DS:`0ABFh`；`title-flow.json` | 覆盖/损坏存档的完整 UI 反馈 |
| TITLE-005 | 标题流程直接绑定 23 个资源记录；`A/23` 的 228 项 Big5 码表与 `A/24` 的 228 个 `16×15` 字形逐项对应。模块 23 的资源索引 8 是 `MUSIC`、9 才是 `NUM` | C | 模块 23 入口与 `0611h/0CE2h/1034h/12E4h/1455h` 的资源读取；A/23、A/24 长度/终止符断言；`title-flow.json/title-presentations.json` | 无；表现见 PRES-008 |
| IMG-001 | 常规图像由四个位平面组成，第五路可作透明遮罩；资源流 `0/1/2/3` 原生对应颜色索引 bit `3/2/1/0` | C | 目录边界与四路同构；模块 21/25/29 三份绘图例程中指针槽 `+0/+4/+8/+0Ch` 与 VGA Map Mask `08h/04h/02h/01h` 的逐项配对；`plane-order.json`；妮雅/希蜜/战场实机截图视觉核验 | 对全部异常记录分类 |
| PAL-001 | 原作运行时按场景写入 16 色 VGA DAC 调色板；gameplay、intro、password 及标题逐场景表均已有原生绑定 | C | 模块 21 `05A8h/22C4h` 与 DS:`03ACh`；模块 23/25/29 的 DAC 写入、调色板指针和数据表；专题 manifest | 未绑定的归档资源仍以无损位平面为母版，不臆测唯一显示调色板 |
| AUD-001 | 71 条音频为 Creative Voice，可完整转 WAV | C | 文件签名、FFprobe、71/71 转换成功 | 识别每条用途 |
| AUD-002 | 59 条 Softstar RIX OPL 均可渲染；`MUSIC/2..13、18..39` 是 17 组“奇数短入场→偶数长循环”的战斗曲对，并由玩家/敌方两张逐关表选曲。第 0 关玩家阶段为 `7→6`、敌方阶段为 `5→4`；`MUSIC/29` 只是曲对 `28/29` 的入场段，模块 27 `0000:00DC–00F7` 的 `1Dh/19h` 是写给父接口偏移 8 的下一模块号（29／25），不是 `MUSIC` 记录。`MUSIC/0、1、14、40` 已分别绑定密码、标题、滚动开场、繁荣结局 | C | `55AAh`、59/59 渲染；模块 29 `DS:1D98/1E46/19F6`、`1000:32D1–338E/3796–37E7`；模块 27 `0000:03C0` 写入器与十个模块同形式的下一模块号写入；第 0 关 `7→6` 实机；`music-catalog.json` | `MUSIC/15` 的发布流程用途待实机标注 |
| AUD-004 | 出场准备（名单）界面有自己的循环音乐，不沿用关前剧情或上一关战斗曲：模块 27 按当前场景 `cmp ax,5`／`ja` 选 `MUSIC/16`（场景 `0..5`）或 `MUSIC/17`（场景 `6` 起），以驱动命令 1／模式 1 单曲无限循环；`DS:0FF6` 为 0 的关卡跳过整段交互名单，因而不播放。模块 25 在剧情结束时以命令 0 关闭驱动，模块 27 以命令 `FFh` 重新初始化，故这是硬性模块边界而非续播 | C | 模块 27 `0000:0584` 名单门、`0000:06A1` 选曲分支（容器索引 8）、`0000:0591` RIX 提交、`0000:003B`／`0000:0104` 驱动初始化与关闭；模块 25 `0000:00D3` 关闭；`music-catalog.json.deploymentScreen` | 无；`MUSIC/15` 与 `MUSIC/16` 原始 RIX 字节相同，听感相同不能反推 `MUSIC/15` 的场景绑定 |
| AUD-003 | `SET.TXT` 只消费前三字节；发布值 `Y13` 表示启用 Sound Blaster 数字采样/VOC、基址 `220h`、IRQ 7，尾随五字节忽略。九个模块加载器/两表一致；RIX 音乐与四个战斗分类开关独立，`0220h` 直提 VOC、`0224h` 先过战斗音效门 | C | 模块 `21/23/25/27/29/33/35/44/46` 九份 93 字节加载器与查找表；模块 23 `91F2h/9315h/937Bh/93A4h/94BFh/954Eh/9590h` 七段 DSP/PIC 签名；`set-config.json` | 无；IRQ 10/11 仅保留原表元数据，不模拟为可靠的 Web 硬件路径 |
| SAY-001 | `SAY/NUM/CHA` 有 176 组对齐记录：93 条含正式命令，83 条为名字、提示或其他文本/标签表；语料共有 17 种命令码，但 `CW` 不在任一发布版分发器中；69/116/117/118 无发布版生产者 | C | 全语料 5,591 动作/4,057 命令动作；模块 25 固定关卡表；模块 29 `DS:80B5` 的 75 次立即写、2 次查表写、9 次读取及 `DS:30BA/1273` 映射表；`dialogue/*.json` semanticVersion 2；`story-presentations.json.globalReachabilityAudit` | 无；四条脚本完整保留为归档内容但不接入忠实流程 |
| SAY-002 | 模块 25/29 共享 `WU/KY/ME/ED/HU/\\/CU/BK/WD/CD/HD/W-/PP/PU/PD`；模块 29 另支持 `DL`。`BK` 备份 framebuffer，`W-` 载入 `A/18`，`ME` 仅保存编号，`CW` 为 no-op；模块 29 的 Big5 逐字声按字符码模 15 选择 `MAGIC/57..71` | C | 模块 25 `0736h/07C7h`、模块 29 `023Eh/BE14h/BEC3h/C9B9h/EA04h`；34 段代码/12 段数据签名；`story-presentations.json` | 无；标称 tick 时长见 SYS-004 |
| FONT-001 | `NUM/SAY/CHA` 的 176 组包含 9,399 个 `16×15` Big5 对应字形 | C | 全记录编码顺序、30 字节步长、逐条长度校验、176 张图集 | 无 |
| FONT-002 | `A/B/UN` 另有 9 组、1,769 个无头 `16×15` 单色字形 | C | 30 字节步长、big-endian 行位图、可读图集、末行留白与笔画密度 | 查找各组的字符索引/分隔表 |
| FONT-003 | `A/0023` 是 `A/0024` 的精确 Big5 码表：228 个双字节码、单个 `0000` 终止，与 228 个 30 字节位图一一配对 | C | 模块 23 同时加载两记录；长度、终止符、标题/难度字符覆盖断言；`title-flow.json` | 无 |
| UI-001 | 行动菜单的七种状态及 `M/A/S/C/H/N/X` 动作码已恢复；移动后 `結束` 仅结束当前单位动作，`返悔` 恢复位置/行动位 | C | 模块 29 DS:`3DD8h..3E4Eh/3FAAh` 菜单表；`5651h/5721h/6849h..6B6Ah`；`input-ui.json` | 完整动作表现时间轴另列 BAT-026 |
| UI-002 | 集体命令为 `全部休息/跟隨主將/自由行動/全面徹退`；前三项分别先显示选择器 `1Fh/21h/20h` 的原文战斗对白，窗口完成后才批量休息或进入我方 AI；`跟隨主將` 把当前游标格写入临时凝聚格并对行动位幂等 `OR 80h`，入口不检查原行动位或行动禁用值，故已行动单位仍可当锚点且不会再行动；系统菜单为游戏功能/胜利条件/读档/存档/离开，Q/Y 非胜负支路分别绑定退出和主动撤退；F1–F4、Esc、Tab、E、M 快捷键已闭合 | C | DS:`3F3Eh/41B6h/84F9h..84FDh/86E4h/8716h/873Ch/F6AAh/F6B8h/F6BBh/F6DBh/F6E4h..F6E7h`；`6CCAh..6DEAh/B941h/BA6Eh..BAAEh/C97Ah`；`0000:6D21..6D6E`；`input-ui.json` semanticVersion 7 | 无；完整表现归 BAT-026 |
| UI-003 | 战斗单位详情在非空焦点停留四 tick 且格号变化时刷新；右栏精确几何、`A/6` frame 0–14 的上框／肖像框／主体分隔／小地图框／回合框拼装、动态 `D` 肖像、无条件分开绘制的职业／单位名（通用单位仍为“士兵／士兵”）、五行原文/数值、双比例条、`A/17` 八状态紧密装箱及第 37 关 side 2 的九字段 `?????` 隐藏均已闭合 | C | 模块 29 `2A1Ah..2A8Ah/7C27h/8492h/851Ah/86C9h..8CE9h`，DS:`5DD4h..5FD5h/6006h..6018h`；19 段代码/4 段数据；`hud-presentations.json` | DS:`3192h`→全部导出 `D` 记录映射单独保留；范围与地图特效见 PRES-011 |
| UI-004 | 右侧面板有 12 个鼠标热区、17 项动作分发和三类设置面板；方格/滚动/肖像/战斗动画可直接切换并分别用 `A/6` 的 `24/25、26/27、30/31、20/21` 局部帧同步改变桌面物件；战术小地图复用 `A/6` frame 6/7/9/10 的原生边饰；音效四独立、游戏功能五独立、音乐五选一；DS:`7FADh` 的发布版行为是不可达 no-op | C/U | DS:`7FF0h/806Ah/10E1h..11F8h`；`2A1Ah..2A8Ah/B78Ch..BAAEh`；战术桌重绘 `B7C6h/B7F1h/B812h/B833h/B854h/B875h/B896h`；DS:`7FB1h/7FADh` 全部直接引用审计；`input-ui.json` | C：发布版可达性、帧/坐标与副作用；U：遗留开关的原设计名称 |
| UI-005 | 原版鼠标使用 `A/1` frame `0..4`：普通/菜单为橙色手指，战场四边依次切换白色上、下、左、右箭头；四角纵向优先。方向图标独立于“地圖捲動”开关；开关关闭时悬停不滚动，但箭头处按下主操作仍移动一格并在按住期间重复，松开停止。未发现移动/攻击/禁用专用光标 | C | `A/0001` frame `0..4` 位图、尺寸与透明索引；模块 29 `1000:34B1..3559` 的方向槽 `0/1/2/3/4`、横向后纵向写入顺序、DS:`111Bh` bit 0／主操作 DS:`F590h` 的 OR 门；`input-ui.json` | 无；网页只需以原生像素光标投影该五态，不把棋盘焦点框误作鼠标图标 |
| UI-006 | 发布版“ＡＩ對話”默认开启；side 1 自动单位与 side 2 敌军技术在效果前都以行动者肖像显示动作组情境对白并自动关闭。选择器 `0Ah..17h` 的 14 条原文覆盖 33/33 个 AI 参数行；`SM/FM/OJ` 无可证实参数行对白。效果前只重绘当前视口并写入目标格，没有调用目标居中原语 | C | 模块 29 DS:`111Ch/84BBh[0Ah..17h]/85CAh..8653h`；`1000:1E51..1EDE/254Fh`、`0000:7BFCh/C97Ah`；`ai-rules.json` 57 段代码／7 段数据签名 | Web 自动对焦若采用，必须登记为 `[DD]`，不能冒充原版行为；缺失参数行不得按名称补猜 |
| SYS-001 | 模块 29 在本进程第一次战斗模块出站时以 `MIMA_PASS={count,savedNext}` 暂存原下一模块并改走模块 21；模块 21 通过后恢复原目标，后续不再重复 | C | 模块 29 `0000:009Ch/1000:06B9h`、模块 21 `00F0h/0428h`、GO 父块 `+1Eh→0312h`；17 段签名；`password-flow.json` | nextStage=6 后置覆盖已记录为原生边界；`MIMA_NUM` 在此链中无消费者 |
| SYS-002 | 密码为三次 28 项图册问答；模块 21 专用 DAC 表下六个可见按钮依次为红/黄/蓝/绿/紫/白，对应内部答案码 `2/1/0/3/4/5`；回答码写 DOS 向量 0/1/3，确认时按 28×`{x,y,answerCode}` 表校验；可整组重输，任一错误显示重启提示并锁死 | C | 模块 21 `0502h/05A8h/068Ch..0793h/0928h/0B4Bh/0BEBh/13C6h/22C4h`、DS:`02C6h/03ACh/0426h`、`C/0032` 图像记录 1–7/20–23、`UN/0041–0042`；`password-flow.json` | 提示前 DOS/BIOS 向量兼容检查依环境保留，不作为 Web IVT 模拟要求 |
| SYS-003 | `PLAY.COM` 的 INT 21h 钩子把模块 21 `CS:0BEB` 的 PIT 挑战器改为固定索引 16；该项答案码 2 对应默认最左的红色按钮，直接运行 `GO.EXE` 则保留随机挑战 | C | `PLAY.COM` 0118h 规则与 013Ch 钩子、原/替换八字节逐字匹配模块 21、模块 21 首次 INT 21h 位于 000Ch | 无 |
| SYS-004 | 发布程序的 native tick 标称频率为 `1193182/11932 = 99.998491 Hz`，每 tick `10.000151 ms`：九个发布运行模块都先挂模块本地 INT 08h 计数器，再由同构内嵌驱动把 PIT0 mode 3 除数设为 11932；驱动每次 IRQ0 链回旧处理器，故本地计数器每次加一（模块 29 为 `DS:F5B3`）。Web 忠实层可用 10 ms 逻辑 tick，并保持全部整数等待计数 | C | 模块 21/23/25/27/29/33/35/44/46 的启动 FFh 请求、PIT 常量、INT 08h 安装/链回、计数生产器和等待消费者全量审计；模块 29 另有 10 段定址签名；`native-timing.json` | 物理晶振误差、模拟器调度、VGA 回扫和音频设备漂移只作宿主观测，不改变标称 tick |
| INPUT-001 | 启动时检测 DOS 鼠标驱动并把可用性记为 `MOUSE_USE='Y'/'N'` | C | 原始函数 `MUS_SET` 在 `0000:0079` 调用 INT 33h AX=0，并写 `046A:0100` | 运行模块的语义消费见 INPUT-003/004 |
| INPUT-002 | `AG2.JS3` 是 Joymouse Setup 3.00 的 78 字节输入配置，不是游戏进度；低七位是 128 项内建键表索引，高位是逐动作重复开关。当前 Keyboard 4 Way 的两组 12 项相同，A/B/C/D/E/F/Start/Select 为 Space/Enter/F1/M/E/Tab/Esc/No Use，方向为数字键盘 8/2/4/6；12/12 均与游戏语义消费者吻合 | C | `JS3.EXE` `494Fh/4A96h` 互逆读写、`3453h/3E05h` 重复位切换、`36ECh/407Bh` 掩码/键名显示、`4D9Ah/4DFFh` 驱动打包；`14E5:08B3h` 查表与 `08D3h` 128 条记录；8 段签名；`AG2-JS3.json` | 未选用的 Analog/Gravis/鼠标轮询与 DOS `E0` 细节只作兼容取证，不阻塞 Web 语义输入 |
| INPUT-003 | 模块 27/29 各装 INT `09h` 处理器，从端口 `60h` 读 Set-1 扫描码，按下/释放写 `1/2`；两份各 14 项发布版表完全一致并映射上下左右、主操作、次操作，键盘和鼠标进入同一动作层 | C | 模块 27 `1CAAh/1CC4h/1D27h`、DS:`2410h/2567h`；模块 29 `D2E0h/D2FAh/D35Dh`、DS:`F552h/F6A9h`；55 段输入/UI/部署表现签名；`input-ui.json` | 当前 Joymouse 适配见 INPUT-002；低层扩展键兼容不改变语义规则 |
| INPUT-004 | 部署为三列五行名单、三页签和结束，共 19 个热区/五列循环键盘导航；战场为 `10×7`、`40×44` 格，当前焦点格使用调色板 0 的 `40×44` 双像素底框叠加调色板 15 的 `39×43` 单像素框；数字键盘 7/9/1/3 补充斜向移动，鼠标内部/四边滚动与主/次操作规则已恢复 | C | 模块 27 `0C60h..0FC8h/1884h/18B2h`、DS:`0E40h/0ED8h`；模块 29 `54AAh/54D1h/8330h`、DS:`5B29h/5C35h/F6F0h/F6F2h/F6F8h/F6FAh`；`input-ui.json` | 表现时间轴见 UI-003 |
| UI-007 | 地图棋子的标记层只有三个原生对象且共用 DS:`5D48h` 演出门：逐单位行动禁用值画 `A/1` frame 10；棋盘格 `80h` 行动位画 frame 9 的 `E`；未行动且阵营标量为 1、side 1 逐槽 AI 行为值非零时画 frame 13 的 `N`。`E`／`N` 落在同一个 `(格 x, 格 y+1Ch)` 的 `16×14` 槽位并互斥，敌方走不到 `N` 分支；棋子上没有任何色环或悬浮描边 | C | 模块 29 `0000:81C4/81FA/8202/822B/822F/8234/823B/8243/825B`、`0000:5087/510C`、`0000:62F2/633D/6392/63EE/641F`；DS:`5D48h/31B7h/31C9h/3BF6h`、side 1 行为数组 DS:`3BFDh`；`A/0001` frame `9/10/13` 位图 | 无；逐槽行为值的来源见 BAT-002/BAT-036 |
| UI-008 | 部署页是 640×350 黑底上的程序化原生构图，不存在整张名单框纹理：三列五行复合项位于 `x=8/152/296,y=35+60n`，`130×50` 外框、`48×48` 图形框与 `80×24` 职业条均由颜色 `0/15/7` 嵌套矩形组成，已部署态的图形框／职业条换为 `0/15/8`；`A/0002` 的 `40×43` 职业图画于相对 `(3,2)`，原版点阵姓名／职业分别画于 `(72,3)/(56,27)`。三页签在 `(440,35/65/95)`、结束在 `(540,35)`。右下另有 `(440,125)` 的 `200×200` 部署专用全图：每格 `4×4`，side 1／side 2／`FFh` 的 `2×2` 内芯分别为颜色 9／11／15，当前 `FFh` 黑白反相；错误条为 `(2,328,636,20)` | C | 模块 27 `05C2h/08EAh..0C5Eh/0E81h/165Bh..17EEh`，DS:`19BCh..19F6h/0F5Eh..0F7Bh`；`A/0002`；`input-ui.json` semanticVersion 7 | 原版每四 tick 反相一次；Web 为避免高频闪烁保留较慢周期时必须标作 `[DD]`，不能改写原版事实 |
| UNIT-001 | `DATA` 有 39 条，每条为五组七字段 | C | 文件长度与全表边界 | 跟踪每个字段访问点 |
| UNIT-002 | `DATA` 每条的五组是固定等级行；末字段为等级；模块 29 当前属性从前三行按累计经验阈值选择，之后转入独立成长表，不消费第四/第五行 | C | 模块 29 `5256h/52BCh` 与 `1000:8E5Bh/8E85h/8E62h`；原始值序列和转职边界 | 只在发现其他消费者时继续解释第四/第五行的设计用途 |
| UNIT-003 | `修改.txt` 的兵种表来自同一版 `DATA`，但经验列被统一除以 50 | C | 39×5×7 全表程序化比对：`field1..6` 各 195/195 相同，`field0` 195/195 满足 `floor(original/50)` | 无；禁止把攻略经验值写回原版表 |
| UNIT-004 | `field0..6` 依次为经验阈值、攻击、防御、生命、移动、保留字段、等级；`field5` 会被保存/显示，但两套战斗属性应用器都跳过 | C/U | 模块 29 `5256h/52BCh`、模块 33 `09DCh/0B6Ah` 的独立逐偏移访问；全表 I/O 和数值显示保留 `+0Ah` | C：复刻不得赋予其战斗效果；U：原设计意图仍未知 |
| UNIT-005 | 模块 29 两套描述符分别对应 side 1/side 2，并确认全部 39 个记录号—Big5 显示名；钢甲战士为 `1C/0C` 阵营短码差异 | C | `5087h` 设置 DS:`31C9h=1/2`；`5230h` 据此选 DS:`320Dh/325Dh`；`5355h` 返回名称；`22B8h/22DDh` 固定用 set1 短码选择 MAP profile；四段签名 | 原版层保留 side 2 钢甲的 `0C` 职业分派/成长与 `1C` MAP profile 组合 |
| UNIT-006 | `修改.txt` 有八处名称差异：四处“帥/師”，另有魔祭師、飛馬戰士及邪法師/魔法師对调 | C | 原生描述符 Big5 字节、双表一致性、机器提取结果 | 无；外部名称不得覆盖原生名称 |
| UNIT-007 | 原生消费者确认兵种记录存在 `35 + 4` 边界：模块 33 只装入前 35 条，模块 29 战斗/存档状态读写全部 39 条；记录 36“龍”在场景 20/22 的动态激活也已闭合 | C | 模块 33 `090Eh` 的 `CX=23h`；模块 29 `1310h/1395h` 的 `CX=27h`；每条均五次复制七个 word；`1000:4A5Bh/4C88h` | 无 |
| UNIT-008 | 记录 35–38 的原生短码/名称为 `0P女帝、1P龍、2P頭、3P手`；场景 30 含活动女帝，场景 37 含活动的頭与两只手 | C | 双套原生描述符；`B/0061`与`B/0075`逐槽模板；模块 29 `510Ch` 的职业值读取 | 三部位行动与效果见 BAT-016/BAT-035 |
| UNIT-009 | 场景 30 的死亡单位处理分支按职业记录号确定性递增；达到难度上限后把记录 35“女帝”写入玩家职业槽 23，并在原战场格登记 side 1/slot 23 | C | 模块 29 `9733h/97B2h` 对场景号、side 2、死亡状态与 `LV_HARD` 的检查，以及 `56DAh`、两张 50×50 图的直接写入 | 叙事意义可由剧情文本补充，但不影响规则实现 |
| UNIT-010 | 场景 37 的第二只手使用内部职业值 39，描述符查找钳制到记录 38，并非缺失的第 40 条 `DATA` | C | `B/0075` slot 55；模块 29 `510Ch` 保留原值后将查找副本 `>38` 钳制为 38 | 无 |
| UNIT-011 | 场景 37 的頭/双手有独立状态与当前生命、统一覆盖为攻 100/防 10/最大生命 10000、禁止移动，死亡只清当前格 | C | side 2 状态指针表槽 54–56=`5176h/518Eh/51A6h`；模块 29 `502Ah/510Ch/8B60h/690Bh/96C2h/6ABCh` | 无；行动与效果见 BAT-016/BAT-035 |
| UNIT-012 | 普通经验是累计值；第三行后按 23 项专属表或默认值反复跨阈值，增加攻击、最大生命和成长行计数，不增加防御，也不读取 `DATA` 第四/第五行 | C | 模块 29 `5256h`；`1000:8E5Bh/8E85h/8E62h`；23 项哨兵表；机器导出 `combat-formulas.json` | 命名成长行计数的 UI 表述 |
| UNIT-013 | 39 项合并兵种目录已把原版数值、双阵营短码、转职、成长、玩家行动、AI 分派、普通附加状态、射击/技术与两类 `MAP` profile 对齐；攻略只作为已验证派生来源 | C | `angel2-unit-catalog.mjs` 硬断言 39/39 名称与分派、195/195 经验变换、1170/1170 其他值、31/31 边、12/12 顺序和两类 39/39 profile；`unit-catalog.json/csv` | 无；目录不把 `field5` 或攻略经验值提升为原版规则 |
| UNIT-014 | 模块 29 的 side 1 / side 2 各有 60 项角色显示描述符表；第 5 关 side 2 槽 25、26 分别为“汀塔琪”/肖像 3 与“萊茵”/肖像 2，身份与士兵职业分开解析 | C | `5087h` 阵营分支；DS:`362C-36A3`、`32AD-3324` 双表签名；`campaign-roster.json` 的 `actors/enemyActors` 全表机器提取与槽 15/25/26 硬断言 | 通用 `FFh` 职业视觉回退规则不变；关卡内容不得手抄首领姓名或肖像 |
| UNIT-015 | 通用无名友军的战役身份是槽号，且跨关接续：`ME_DATA/ME_EXP` 按槽保存职业与累计经验，胜利后经 copyback 写回。side-1 描述符槽 34–59 带占位名 `xxxx12..xxxx37`（`xxxx(槽号 − 22)`），其中只有 40–47、50–54、56–58 在 44 张 B 模板里实际作为 side 1 出场。槽 40–43 连续用于场景 0／1／2（场景 2 另加 44／45），场景 3 换成 45–47、50–54，40–44 在场景 8／11 被覆写为騎兵、场景 27 被覆写为魔劍戰士等，场景 30 槽 40 再以继承身份出场；场景 39–43 的 B 记录映射回 `1/49/65/85/87`，因此不是这些槽的另一次出场 | C | GO `ME_DATA/ME_EXP` 与模块 29 `victory_prompt_and_copyback`（见 SAVE-011）；`campaign-roster.json` 的 side-1 `actors` 全表；`battle-templates.json` 全 44 张模板的 `activeUnitInstances`／`deployment.eligibleUnitSlots`／`stageToBRecordMap` 程序化枚举 | 无；复刻的玩家向逐槽编号是 `REMAKE-107` 的产品决定，不得写回原版事实 |
| PROMO-001 | 普通转职图有 31 条原生边，覆盖士兵的四大分支及后续职业 | C | 模块 29 DS:`06C3h` 的 35 指针表；候选为“目标记录+1”，`99` 终止；31 条边及每组顺序与攻略完全一致 | 无 |
| PROMO-002 | 31 条边中 30 条目标首行 `field6` 与来源 `DATA` 第四行相同，弓兵 7／弩兵 8 是唯一表格不对齐；这两个字段不参与转职资格或提交。资格读取派生成长行 `>3`，12 个来源双方均按默认 `+100` 从第三行进入第 4 成长行，但只有 side 1 可转职 | C | `DATA.SWF` 31/31 边逐值比较；`5256h/1000:8E5Bh..8E85h` 前三行与第三行后成长；`029Ah` 全 side-1 棋盘扫描；`02B7h` 唯一资格调用；`09D8h/045Bh/0693h/0744h` 唯一候选、菜单与提交链；`promotion-table.json.dataRow4LevelAlignmentAudit`、`combat-formulas.json#experience.cumulativeProgression` | 无；实机只可校准呈现体验，不再承担规则确认 |
| PROMO-003 | 转职菜单前有两套自动逐字叙事：妮雅只自问职业；其他队友先请求主将授职，妮雅再以水神愛西斯之名回应，且上下窗口同时保留 | C | 模块 29 `0487h` 对当前角色记录 `002Eh` 的完整分支；DS:`050Dh/053Dh/056Dh` 三段 Big5 原文；两条路径直接调用 `08DDh` 且无确认输入；`promotion-table.json.runtimeEvidence.promotionDialogue` | 无 |
| PROMO-004 | 原生短码族按 31 条原生边形成完整层级，`N/P` 为普通表之外的特殊族 | C | 原生边表、两套短码/名称描述符及 `A→B/C/I/D→E/F/G/H/J/K/L` 全覆盖 | 无 |
| PROMO-005 | 转职只对玩家 side 1 触发，要求成长/等级行计数大于 3 且候选表非 `99` | C | 模块 29 `ANGEL2_CHECK_PROMOTION_TRIGGER` `02B7h` | 继续命名 `31BDh` 的 UI 表述 |
| PROMO-006 | 转职菜单不可取消；提交只写目标职业记录并把经验清零，不写当前生命或立即重算攻、防、最大生命、移动与状态 | C | `0693h` 按 `99` 建项；完整 `0744h` 只写 `56DAh` 与单位结构 `+02h=0`，随后恢复画面；三段代码签名 | 后续常规装载按“新职业+零经验”派生；不得实现为转职瞬间治疗 |
| PROMO-007 | 转职候选画面保留战场且无文字：`A/0006` 组成 `(160,110) 332×126` 固定面板／阴影，`A/0002` 职业图从 `(184,134)` 起按 56 px 单行排列；`48×52` 框内普通色为 palette 1、选中色为 palette 12 | C | 模块 29 `03A0h/045Bh/0693h/0721h/0794h/4AE1h/4E9Bh`；DS:`04D4h/04DEh/0697h/06A1h/06ABh/078Dh`；`promotion-table.json.runtimeEvidence.promotionMenuPresentation` 的五段代码签名与矩形硬断言 | Web 可在鼠标悬浮／键盘聚焦时补充详情，但默认选择画面不得重新变成文字卡片 |
| MOVE-001 | `MAP` 与 `DATA` 一一对应为 39 条；每条物理保存两个 24-word 窗口，但运行时 profile 步长为 23 words，第 24 字是下一对象的重叠字 | C | `39 × 2 × 24 × u16` 读写循环；DS:`1F1C/26BA` 两表指针严格相差 `2Eh`；记录 38 末字 `4130h` 与 DS:`26BA` 短码 `0A` 同址 | 无；导入器须保留物理窗口但只消费 23 个逻辑槽 |
| MOVE-002 | 第一张逻辑 profile 是移动规则：`1..5` 是进入目标格的加权代价；玩家模式 `M` 与 AI 邻格试移拒绝 `98/99`；Y/A/FY/FA 还把攻击邻格/敌军格标为 2/1 | C | `1000:3BB0h..3EADh/40EEh..416Ch/3EC6h/4186h/690Bh`、`0000:7336h/939Eh`；47 段签名导出 | 逻辑槽以数字 ID 为规范；AI 消费见 BAT-037/038 |
| MOVE-003 | 第二张逻辑 profile 是地形防御百分比，按 `floor(有效防御×百分比/100)` 加入普通伤害链 | C | `0000:946Ah → 1000:41A1h` 明确查 DS:`26BA`；`93F2h/942Eh` 乘后除 100；`map-rules.json` | 命名逻辑槽 `0..22` |
| MOVE-004 | 玩家范围图保存剩余范围；每边执行 `candidate=current-stepCost`，候选须正且大于旧值；四邻接正反扫描至稳定，最终落点须空闲；模式变量 43 个生产点已穷举，模式 `1` 无生产者，十个 `FM` 生产点全属场景 `0/1/6/20/21/22/42` 剧情移动 | C | `0000:7310h/7336h`；`1000:39D4h/3A5Ah/3A6Ch/3A8Ch/3B13h/3B9Dh..3D49h/690Bh`；近回调 `139D0h+01CDh=13B9Dh`；`map-rules.json` `producerAudit` | 模式 `1` 仅保留不可达兼容算法；全部可达范围模式已绑定 |
| MOVE-005 | 普通玩家职业使用加权模式 `M`，可经空格/友军格传播并拒绝 `98/99`；`0N/水戰士` 使用统一代价 1、传播时忽略占格的模式 `0`，两者最终均只能停在空格 | C | `0000:7336h` 的短码分支；`1000:3A0Dh/3BB0h/3C46h/690Bh`；记录 26 profile | 实机确认水戰士越过占格时的界面表现 |
| MOVE-006 | 44 个场景的原始 token 同时直索引同号 `40×44` B 图块，并经逐关 128-word 描述表与 `UN/0056` 第一页解析为逻辑槽；128 个 token 全部合法，使用 21/23 槽 | C | `0000:4D65h/47EAh/7E2Ah`、`1000:3EC6h/946Ah/55DBh/8995h/0FBBh`；44 模板、36 张唯一描述表；`terrain-token-map.json` | 命名逻辑槽；槽 17/22 在现有模板无引用 |
| MOVE-007 | `UN/0056` 第二页是地形小地图颜色页；非零 token 经描述偏移取得 VGA 索引并画成 `3×3` 色块，完整地图为 `150×150` | C | `0000:2530h/285Dh/2884h/28A4h/D050h`；坐标 `(485,161)`、50×50 循环和 3 像素步长；44 张小地图 | 无 |
| MOVE-008 | 战斗模块 29 的小地图占用层只处理 side 图的 1、2、`FFh`：黑色 `4×4` 外框加颜色 10、11、15 的 `2×2` 内芯；`FFh` 是未填入的玩家部署格。模块 27 的部署专用全图另用颜色 9、11、15，见 UI-008 | C | `28DDh/2904h/2924h` 与 DS:`18A3/18AD/18B7/18C1`；模块 27 `1907h/1884h/0D61h`；44 模板计数；44 张占用叠加图 | 无 |
| MOVE-009 | 当前 10×7 视口在小地图上使用黑色 `30×21` 外接边框，鼠标预览使用同尺寸白框；点击预览中心会重新夹取战场视口 | C | `259Bh/25EDh/263Bh/2681h/26DEh/2744h/279Fh/7D2Dh`；17 段代码和三组矩形数据签名；`minimap-rules.json` | 无 |
| MOVE-010 | DS:`2E7D` 的六个战斗读取点均用于规则、颜色、地形判定/改变或攻击表现，没有字形、字符串或 HUD 名称消费者 | C/U | Ghidra 全引用审计；六段原生签名；`terrain-name-audit.json` | C：原生运行时无已发现名称绑定；U：编辑器友好标签只能推断，不得冒充原版槽名 |
| BAT-001 | `B` 记录 0–87 是 44 个偶/奇数据对 | C | 偶数四路 29,184 字节；奇数恒 8,506 字节 | 无 |
| BAT-002 | 模块 27 有 44 项模板选择表：场景 `0..38` 使用 `B` 奇数记录 `1..77`，39–43 使用 `1,49,65,85,87`；每条均为固定九段布局，其中 DS:`01A7` 是原始地形 token 图、首段是 128 个地形描述偏移、双方逐槽数组是 AI 行为，最后数组是部署名单标志 | C | `0718h` 的 DS:`08CA` 44-word 表、九段固定复制；行为经 DS:`5644h/3BFDh→3BF6h→0D3Fh` 进入 AI；名单经模块 27 `08CBh` 消费；44 模板严格校验 | 无 |
| BAT-003 | 偶数流每个位平面的前 28,160 字节含 128 个 `40×44` 图块；token 直接索引同号图块，原生视口为 `10×7` 格；末尾 1,024 字节是全零填充 | C | `4E6Bh/7E2Ah/82A4h/47EAh/3960h/427Ah/7DEFh/7DC0h`；44 张 `2000×2200` 全图无越界；176/176 尾部全零且在最大寻址外 | 无 |
| BAT-004 | side 1 模板职业为稀疏覆写，0 保留战役职业；模块 27 将合并结果写为下一场 `JUST.TST` | C | 模块 27 `082Dh` 非零合并与 `0FF8h` 序列化链 | 无 |
| BAT-005 | 模块 29 两张各 42 项的失败/胜利条件表已 84/84 分类；13 个处理器覆盖保护单/多槽、击破单/双槽、全灭及六种区域到达规则 | C | `257Ch/2583h/258Eh` 分发链；全部处理器反汇编；机器导出 `battle-objectives.json` | 将剧情角色名映射到目标槽号 |
| BAT-006 | 场景 39–41 有模板入口但无胜负表项；42/43 同时有模板和表项；49 仅有表项而无模块 27 模板映射 | C/U | 模块 27 的 44-word 表与模块 29 两张 42 项表逐项集合比较 | U：命名这些非标准入口的实际流程 |
| BAT-007 | 奇数记录 79/81/83 不被模块 27 选择表使用，十个运行模块的全部 `B.SWF` 读取生产点也无法选择它们；压缩 payload 与解码模板分别逐字节等于 21/49/65。偶数 78/80/82 仍由模块 29 的场景 39–41 路径读取并分别复制 20/48/64 | C | 十模块中每个唯一索引读取器及其全部 near caller；模块 27 的 44-word 表、模块 29 `currentStage×2`、模块 46 常量 88；六组压缩/解码 SHA-256；`b-record-audit.json` | 无独有素材或规则缺口；保留副本的制作历史不可恢复但不影响复刻 |
| BAT-008 | 普通先攻伤害为 `max(0,攻-防-地形防御)+r1+r2`，`r1/r2=4..7`；地形防御为有效防御乘地形百分比后向下取整 | C | 模块 29 `93F2h/942Eh/946Ah/9490h/94E3h/95D2h`；41 段战斗/状态签名及 39 段 MAP/范围/地形签名 | 逻辑槽以数字 ID 为规范；原版无可见名称绑定 |
| BAT-009 | 反击基础伤害是相反攻防差的非负值除 2，无普通随机加成；巨斧攻击压制反击，防守者逐单位禁用字节非零也禁止反击，兽骨骑士另有 PIT 低位特例 | C/U | 模块 29 `927Ah/92C5h/94E3h/A17Bh` | 命名逐单位禁用字节；实测兽骨骑士分支概率 |
| BAT-010 | 当前生命位于状态 `+00h`；普通伤害的地图动画逐点扣血且受 `+0Ch` bit15 阻止，全屏动画一次饱和扣血且已恢复路径不检查该位；两路由 DS:`1119h` bit0 的发布版战斗动画开关选择，普通伤害链随后扫描并移除零生命单位 | C | 模块 29 `783Dh/9135h/91C5h/96C2h/A096h/A0C6h/A0F6h/A10Ch/A122h`；`combat-presentations.json/input-ui.json` | 原版差异可达且应由忠实层保留；毒伤走 BAT-033 独立边界 |
| BAT-011 | 状态 `+02h` 是累计经验；先攻存活奖为目标等级+4..7，反击存活奖向下除二，先攻／反击击杀均为完整的 35 项职业基础表+4..7；每次实际反击即使伤害为 0 也独立取奖。地图路径先取先攻奖再取反击奖，全屏路径反序 | C | 模块 29 `9161h/91F1h/927Ah/9296h`、`1000:8CDDh`、DS:`784Eh`；机器导出 35 项表 | 追踪特殊记录 35–38 是否存在独立击杀奖励入口 |
| BAT-012 | 普通攻击链无命中/闪避/暴击分支；魔剑/兽骑分别施加防御/攻击 -20，邪剑/丛林分别写入已由技术菜单命名为混乱/施毒的 `8003h` 状态槽 | C | 模块 29 `8C2Dh/92DCh`；`LA/IP` 与同一 `8D22h/8D30h` 写入器；完整普通攻击、技术与状态调用链 | 不得外推普通命中规则到射击/技术；状态生命周期见 BAT-032..034 |
| BAT-013 | 单位槽图每格低 7 位是槽号，高位 `80h` 是该格本阵营阶段已行动；重置器按阵营扫描 2,500 格清高位 | C | 模块 29 `1000:22C9h/147Eh/14A6h`；攻击/射击/技术/休息/AI 多条独立 OR `80h` 路径 | 无；保存/模板必须原样保留高位 |
| BAT-014 | 标准战斗为玩家 side 1 → 我方自动单位 → side 2 AI 的阵营阶段制；移动不消耗终结动作，攻击/射击/技术/休息会消耗。单个玩家／AI 休息先播放 `MAGIC/0` 五帧治疗公共尾效（每帧 15 tick、无声音），再恢复最大生命 15% 并提交；`全部休息` 可让所有剩余手动单位恢复并变为已行动，但其批量路径不逐人播放；跟随/自由行动可把余下单位交给 AI | C | 模块 29 `48FEh/55D3h/6CCAh..6DEAh/719Bh/75E4h/7528h/7A8Ch/9123h`、`1000:1542h/1595h/2213h/41BCh/5D12h`；DS:`60E0h..6127h`；`turn-actions.json` | 无；键位见 INPUT-003/UI-002 |
| BAT-015 | AI 按 39 项短码表逐类反复扫描；随机位只延后候选，每个符合条件的单位格最终提交一次行动，胜负触发可提前中断 | C | 模块 29 `1000:1595h/15F4h/1681h/2032h`；DS:`1036h` 39 项表；机器导出 `turn-actions.json` | 行为与落点见 BAT-036..038；继续命名完整表现时间轴 |
| BAT-016 | 场景 37 頭、左手、右手各有独立行动格、模板行为 0 和独立单位状态；三者存活时每个敌方阶段各提交一次专用行动，共三次 | C | `B/0075` 槽 54–56；AI 表 `2P/3P`；调度禁用检查与模块 29 `1000:17A7h/17EEh/2032h`；23 段行动签名 | 效果见 BAT-035；四项完整表现见 PRES-006 |
| BAT-017 | 玩家原生菜单将普通、射击、技术分为三类，归属由 `6770h/67D2h/67EFh` 三条内联职业链决定，分别为 18／3／14 个职业；射击成功点为 `719Bh`，技术提交/36 项分发点为 `75E4h`/DS:`52A2h`，技术提交链没有 MP/消耗品字段 | C | `6770h/67D2h/67EFh` 职业链；`6849h/688Dh/68E4h/719Bh/75E4h`；36 条 `{短码,处理器,范围}`；56 段签名；`technique-rules.json#actionMenuClassifier` | 继续审计是否有剧情脚本另行限制次数；玩家战斗入口无资源成本 |
| BAT-018 | 弓兵/弩兵/魔弓兵射击 seed 为 5/8/6；模式 2 每步扣 1、结果必须大于 0，再清起点与四邻，故实际距离为 `2..4/2..7/2..5`。玩家前两者伤害为 30..49/70..89，魔弓目标为 50..68、其他合资格线格为 25..34；目标为迅龍騎士时有 PIT bit 0 射击闪避 | C | `716Dh/719Bh/722Bh/7264h`、`1000:3FA8h/4014h`、范围模式 2、`V` 线形效果；AI `1000:1F7Bh` 同条件但表现入口不同；范围见 PRES-011，数值/动画见 PRES-005 | 仅 PIT 实机统计待校准；核心范围、输入与射击表现已闭合 |
| BAT-019 | DS:`41CEh` 定义 13 个职业的三阶段技术菜单，原版 Big5 项名与顺序已全部解析；DS:`52A2h` 有 36 项分发，所有玩家菜单短码均可命中。分层表只覆盖技術职业链 14 项中的 13 项，缺口即 BAT-068 的 `1N` | C | 原生表解析、13 组逐阶段断言、技術链减分层表差集恰为 `1N` 的断言、36 项分发表及 FFFF 哨兵；`angel2-technique-rules.mjs` | 无；新增/Mod 菜单必须与原版层分离 |
| BAT-020 | 落雷为中心高伤的菱形固定伤害并消耗防魔；内部效果器先汇总击杀奖励，四层外包装器再无条件追加 `8..9/10..11/12..14/15..17`，因此无击杀或全部伤害被挡时仍有经验且消费一次层级随机。炎暴为按最大生命比例且有上限的单体伤害；冰雪不可指定目标格，玩家入口把施法者当前格直接写为效果中心，再按固定方向优先级做无伤击退；原版目的范围值不要求非零，最外圈值 `1` 可推到范围外值 `0` | C | `CB0Bh/CB28h/CB45h/CB62h` 外包装器；`1000:7166h` 内部效果与死亡扫描；`D00Ah` 随机；玩家提交 `72FFh` 与经验写入 `1000:8CDDh`；冰雪玩家分支 `75FAh..7620h/76B0h`、`1000:6771h/6974h..6983h/69FAh`；防魔与 Boss 短码分支 | 分发表第三字 `2/3/3/4` 在玩家冰雪路径中不作为可选距离消费；实机只校准 PIT 值域内分布与表现时序 |
| BAT-021 | 单体治疗按 24/36/48% 最大生命恢复，基础经验仅在比例经验商非零时加入、外层随机仍结算；范围回復按范围值固定治疗，总实际治疗低于 50 时经验为 0 | C | `CDB5h..CE1Dh`；`1000:5CA0h/5DA4h/6EE0h/788Ah/791Eh/79A8h`；表现见 PRES-006 | 无；完整治疗/回復帧序已闭合 |
| BAT-022 | 状态技术与单位结构槽完成绑定：攻/防提升 `+08/+0A`、防魔 `+0C`、混乱 `+0E`、攻/防下降 `+10/+12`、施毒 `+14`、禁咒 `+16`；破邪清相应最高位 | C | `CE1Eh..CFB4h` 与 `1000:8CC1h..8D7Dh`；技术目标验证 `8D94h` | 生命周期和特殊边界见 BAT-032..034 |
| BAT-023 | 祈祷逐我方单位做约半数命中，再等概率恢复 5..14、加经验 5..14、攻击提升或防御提升；施法者统一经验为 0 | C | `CAE1h`、`1000:591Ch/5928h` 及四结果分支 | 确认 PIT 实机采样统计只影响概率分布，不影响值域/分支 |
| BAT-024 | 铁板/障碍以 seed 5、模式 `M` 选择空格并移动工兵，再按 `+50/-50/+1/-1` 把非零逻辑地形槽四邻写成逐关 base token，中心不改且无经验；`CAE7h/CAF9h` 的横向五字节写入器虽在分发表中，但正常玩家分支绕过且工兵 AI 无技术池生产者。地形字节写入在整条玩家路径上只有 `77CDh` 一处：`772Dh` 先用共享行走器 `17DEh:0029h` 走完路径（该函数只读范围图 DS:`01A9h`，不碰地形图 DS:`01A7h`），再对终点的 `±50/±1` 各调用一次 `77CDh`；终点格本身只在 `77A7h..77B5h` 被 `OR AL,80h` 写占位图 DS:`0022h`，那是所有技术共用的“已行动”标记，不是地形写入。因此构筑最多改 4 格，路径与落点都不铺 | C | 玩家分支 `761Ah..762Fh/76C0h/772Dh/77CDh`，验证器 `785Eh`（范围图非零）与 `7880h`（占位图为空）；共享行走器 `17DEh:0029h/0142h`；潜伏分发表 `CAE7h/CAF9h → 1000:7C92h/7CB4h`；模板载入源格；`range-presentations.json/technique-rules.json` | 实机只需校准地图边界附近的线性邻格可见异常：`77CDh` 对 `bx±50/±1` 没有任何边界检查，边缘构筑在原版会跨行或越出 2500 格平面，`REMAKE-025` 以几何边界取代；发布路径与正常格规则已闭合 |
| BAT-025 | 龙踏/男踏/女踏以目标中心三步模式 0 区域与当前 10×7 视口的并集为几何区，只伤所选目标阵营；每单位独立造成 10..19/15..29/20..39，零生命正常移除，固定经验 5 | C | `CD6Dh/CD85h/CD9Dh`、`628Eh/63CFh`、`1000:11BCh/1350h/4264h/73B6h..7460h`；36 段签名 | 传入的 20 不是伤害；PIT 精确统计分布只影响区间内频率 |
| BAT-026 | 落雷、炎暴、冰雪、治疗、回復、踏地与状态已连接到 `MAGIC/E/UN` 原生记录；祈祷确认为程序绘图且无归档资源，工兵确认为直接地形写入；V 线形效果见 PRES-005，五个主家族见 PRES-006，其余技术见 PRES-007。`1V/2V` 无玩家菜单、AI 池或直接调用者，`3V` 唯一直接生产者是魔弓射击 `72D3h` | C | 各表现处理器的资源调用；13 套玩家菜单、14 套 AI 池、三处理器 direct-call 全扫描；`technique-rules.json/ai-rules.json/shooting-presentations.json/technique-presentations.json/remaining-technique-presentations.json` | 无资源/同步/隐藏 V 入口缺口；低层字段原设计名只作可选考古，标称 tick 时长见 SYS-004 |
| BAT-027 | 39 个原生职业短码被 AI 分成 20 普通、3 射击、12 技术、2 女帝/龍、2 頭/手五组；技术组状态位可强制走普通流程 | C | `1000:1681h/16BCh/1728h/1745h/1795h/17A7h`；五组集合严格分割 39 个描述符短码 | 命名 DS:`31B5h` bit15 的原版状态/UI 名称 |
| BAT-028 | side 2 与 side 1 自动 AI 分别使用 `Y/FY` 与 `A/FA`；`CM/CY` 按所选目标阵营限制一点击伤；模式 2 同时用于 AI 射击；`FM` 只用于已绑定的剧情脚本移动，模式 `1` 无生产者 | C | `1000:14D8h/1519h/4264h/434Dh..4CFDh`、`0000:644Dh`、范围传播器与射击入口；43 生产点审计 | 无；AI 技能池中的同字节短码 `FM` 是另一套动作码问题，归 BAT-031 |
| BAT-029 | AI 生命低于 20% 休息，20%..39% 依行为值和四邻敌军尝试防御撤退，40% 起继续；撤退取无邻敌空格中的最高地形防御格 | C | `1000:2233h/2291h/2468h/1D67h/0C83h/0CD1h/0CF0h`；54 段 AI 签名 | 无；行为与落点见 BAT-036..038 |
| BAT-030 | AI 射击按最低有效防御、同值最低生命选目标；弓兵 30..49，弩兵 50..89，魔弓目标 50..58、其他合资格线格 25..29；后两者与玩家值域不同 | C | `1000:1F11h/1F3Fh/1F7Bh/1FB6h/2016h/0B91h`；`ai-rules.json`；完整表现见 PRES-005 | 无；仅 PIT 采样分布与光标反馈可由实机校准 |
| BAT-031 | DS:`0D9Eh` 有 14 组三级 AI 技能池，DS:`0EE8h` 有 33 项动作参数；成长行选层后以 PIT 拒绝采样抽一个池项，只为该动作找目标，无目标不重抽。敌对动作取最低有效防御、同值最低生命；友方动作取最大缺血；`SM/FM` 是无法命中参数表的原生孤项 | C/T | 全表解析与断言；`1000:1DA2h..1EFEh/3FE6h/3FFDh/4065h`；`SM/FM` 缺项和忽略失败返回的控制流 | C：抽池／目标顺序、孤项与危险控制流；T：`SM→OJ` 原意；实机观察可见异常，不得覆盖原始字节 |
| BAT-032 | 八个状态槽在完整玩家+自动+敌方回合后统一 tick 一次；bit15 状态按 `8003→8002→8001→0000` 递减，所有施加器直接覆盖并刷新计数 | C | `0000:4DCDh`；`1000:35DCh/35F2h/8CC1h..8D4Ch`；双方 58 指针×八槽循环；41 段签名 | 无；实现测试必须按完整回合而非阵营阶段 tick |
| BAT-033 | 施毒在状态递减前令 `currentLife=floor(currentLife/2)`；毒可把生命写为 0，但写成 0 的当下不触发死亡，单位暂时仍在棋盘、计入目标并可被选择／休息；后续任一伤害收尾调用全棋盘死亡扫描时才会移除 | C | `1000:3633h/35DCh` 完整函数签名；`0000:96C2h` 全模块 direct call 只有 `63D2h/9156h/91E6h/92A6h` 四处且均不在毒链，far call 为零；`55D3h` 选择器无生命检查；目标只扫棋盘图；AI 0% 生命进入休息 | 原版层保留毒 tick 与后续死亡扫描的时点差；“毒写 0 时立即致死”只能作为显式 Mod／修复规则 |
| BAT-034 | 混乱 `+0Eh` bit15 在 AI 装载时把行为覆写为 `FFh`；普通职业只做最高防御安全撤退，射击/技术/女帝职业只做 PIT 选格移动，随后消耗动作且不攻击；玩家点选混乱单位时，`0000:66F4` 用同一算址重读该字，先播上下文台词 `1Ch`「我的頭好昏，無法思考．」再远调 `1000:1506` 单独跑同一套 AI，命令菜单不打开 | C | `1000:2300h` 与 `0000:66F4h` 同以 `SI=31A5h+8` 间接读取；`18A1h/192Ch/19DDh/1A68h/1D67h/212Dh`；`0000:55D3h`；`0000:C97Eh`；`DS:84BBh[1Ch]→86ABh`；`ai-rules.json` 的 `rules.playerConfusionRoute` | 旧直接地址审计漏掉寄存器算址，空效果与“手动单位不受影响”两个结论均已撤销；实机只校准表现/PIT 分布 |
| BAT-035 | 场景 37 頭从高級回復开始与高級冰雪交替；双手共享从究級落雷开始的落雷/炎暴开关，两手同存时各一次；四项直接复用玩家处理器、公式与 PRES-006 表现 | C | AI 代码段基址 `11470h`、零初值 `CS:037D/0405`；`1000:17A7h/17EEh`；`CDD9h/CD2Fh/CB62h/CC0Ch`；`turn-actions.json/technique-presentations.json` | 多目标时手部选择有低格偏置；四项帧/音效已闭合 |
| BAT-036 | 44 模板的 672 个活动实例仅用行为 `0,1,2,3..8,11,12`；行为按关卡单位槽配置；3/4、5/6、7/8 是领队/跟随组，9/10 仅有代码，FF 仅由混乱产生 | C | `B.SWF` 双方 75-word 表；DS:`5644h/3BFDh→3BF6h→0D3Fh`；`1BD9h/0D83h/0D9Ch`；逐实例分布断言 | 无；不得将行为并入兵种或 `DATA.field5` |
| BAT-037 | 近战与距离 2 射击落点都只最大化候选格地形防御、同分后者胜；近战装载的目标生命／攻／防／脚下地形均为死输入，其方向顺序为 `+50,-1,+1,-50`。近战可移动后攻击，普通射手只有实际换格才射击，原点最佳反而失败 | C | `1000:070Ah/0748h/078Bh/07A7h/083Ah/0897h/0993h/09BCh/09E2h/1D0Bh/2044h/2081h`；54 段 AI 签名 | 忠实层保留射手“原点最佳不射击”怪癖 |
| BAT-038 | 默认远追只检查 `enemyCell+50`；行为 2 以硬编码模式 A 门控后只追逐；行为 12 仅场景 0/4/9 有路线，其他场景仍绕过职业行动。行为 12 的落点是两阶段选择而非可达格评分：先以模式 0、seed 50 建**忽略占格**的探路图，由 `1000:7E09/7E3B` 从锚点爬回行动者写满 `CS:028B` 的 100 word 路线表（接受条件 `邻格>=当前最优`，同值由 PIT `mod 3` 轮换表的后扫描方向覆盖），再以本阶段 `Y/A` 与覆写移动值重建真实移动图；`1000:828F` 取路线上第一个范围非零格，并按「本格 → `+50` → `-50` → `+1` → `-1`」用 `1000:8329` 取第一个范围非零且阵营图为空的落点，失败才沿路线前进一格重试。挡路的我方棋子只使敌方停到其旁边（右优先），不会被绕开 | C | `1000:20DBh/0AE1h/0B09h/0B64h/2193h/1AF8h/1B56h/1BBEh/7E09h/7E3Bh/7E5Ch/7ECBh/7EFBh/828Fh/82BAh/8329h`；行为 12 模板分布；`ai-rules.json` | 行为 12 的场景 0/4/9 额外机制见 BAT-052 |
| BAT-039 | 阵营图 `FFh` 是未填入的玩家部署格；44 个映射入口中 33 个进入部署 UI、11 个直接写 `JUST`，共 423 格 | C | 模块 27 `056Eh/1907h/1884h`；44 模板完整值域与逐格导出；`battle-lifecycle.json` 的 23 段签名 | 无 |
| BAT-040 | 部署名单来自 75 个场景标志的非零项；模板初始 side 1 单位固定不可撤，其余可填入/撤回 `FFh`；未填标记在模块 29 载入后清空；19 个热区、分页导航和前后 `FFh` 循环已恢复 | C | 模块 27 `08CBh/0898h/0C60h..0FC8h/0D61h/1884h/18B2h`；DS:`0E40h/0ED8h`；模块 29 `1000:543Bh`；`input-ui.json` | 精确像素时序归 UI-003 |
| BAT-041 | 模块 27 新战预处理把职业 0、具名描述符且经验不高于 299 的 side 1 槽写为 299；第 0 关妮雅/希蜜因此最终为等级 3/经验 299/攻 45/防 27/生命 180，四名 `FFh` 描述符通用士兵仍为等级 1/经验 0/攻 39/防 21/生命 160，移动均为 4。模块 29 新战 `N` 分支载入 `JUST`，除场景 3/8/11 外以 `LV_HARD+1` 次把 side 2 经验推进到下一阈值加 1，再重建双方、回满生命并调用 `4DCDh` 开启第 1 回合；编号 `WAR` 用 `3788h` 恢复阶段且不重复新战修正。标准循环为玩家→我方自动→敌方 AI→完整回合 tick；Q/Y 分别为退出/主动撤退；Caps+J/数字键盘 `*` 是 DS:`132Fh` 在发布版关闭的开发调试支路 | C | 模块 27 `0493h`；模块 29 `4BD9h/48FEh/4A24h/4ABEh/4DCDh/536Bh/537Bh/538Bh/539Bh/540Dh`、`B941h/BA8Ch/BAAEh`、`1000:8BD1h/5386h/3788h/41BCh/1542h/14A6h`、DS:`132Fh/F6CDh/F6E0h`；23 段生命周期签名；原版 HUD 实机交叉核验；`battle-lifecycle.json/input-ui.json` | 调试门不可作为正常玩法 |
| BAT-042 | 普通胜利默认 `nextModule=25,nextStage=current+1`；失败明确 `nextModule=27,nextStage=current`，故重试会回同关部署而非战场内复活 | C | `1000:42A8h` 默认写入；`02E4h/0627h` 胜利提示；`0321h/057Fh` 失败提示与转移；模块 29 主入口回传 | 38 个处理器的覆盖见 BAT-045/046/048/049；无处理器场景保留默认值 |
| BAT-043 | DS:`2F83` 的 999 是当前即时胜利；`WAR` 元数据把 999 序列化为 1000，载入 1000 后做逐关事件的结算分支但不重复询问存档 | C | `4A6Eh/4A74h/4A79h`；`1000:88B9h` 的 999→1000 写缓冲；`4BD9h/48FEh` 的载入终局分支 | 无 |
| BAT-044 | `1000:42A8h` 是回合与结算共用的逐关战斗事件分发器，不是纯战后函数；在 `4DF5h` 与 `4A74h` 调用，共有 38 个唯一场景守卫入口 | C | 分发器 128 字节完整签名；38 条 near call、完整函数边界、每个处理器首条 DS:`2E77h` 比较和逐函数 SHA；`stage-events.json` | 控制流目录已闭合；特殊表现见 PRES-012 |
| BAT-045 | 场景 21 第 1 回合加入 side 1 槽 `0,1,24,8`、运行 SAY 43/44 并脚本移动，随后因 side 2 为空立即胜利并直达模块 27/场景 22；场景 22 第 1 回合临时加入女帝/魔祭師、运行 SAY 76–79，最终留下六个敌人，只有槽 28 是胜利目标 | C | `1000:4B72h/4C88h` 完整函数签名；`51E5h/7DF7h/BAB8h`；B 记录 43/45；目标表；六条剧情记录交叉；`stage-events.json` | 槽与剧情姓名的全战役角色映射另行闭合；保留场景 22 聚焦 `05CBh` 却写 `05CAh` 的原版怪癖 |
| BAT-046 | 场景 42 的空 side 2 模板立即胜利；999 移动槽 0/24、运行 SAY 11/18/20、对槽 7 播放完整 4L 落雷、清除槽 7/23 并运行 SAY 19，1000 只恢复下一场 6；清格表示穿过传送门 | C | `1000:457Fh/6084h` 完整函数签名；B 记录 85；场景 42 目标；SAY 11/18/19/20；`stage-events.json`；4L 304-tick 表现见 PRES-006，对话窗见 PRES-009，逐格移动见 PRES-012 | 无；PIT 等价路径选择和 VGA 墙钟差异按 PRES-012 作为忠实层参数保留 |
| BAT-047 | 普通战前顺序是模块 25 剧情→27 部署→29 战斗；场景 6 特例是 27 部署→25 的剧情记录 14→29 战斗。模块 23 新游戏进入 25、继续游戏进入 29；模块 25/27/29 的后续出站均已闭合 | C | 模块 23 `0480h/059Fh/1455h`；模块 25 `05F2h/145Fh`、DS:`0E16h/0F88h`；模块 25/27/29 主入口返回片段；`title-flow.json/stage-events.json` | 场景 38 的重复 MAGIC 后项 74 原生不可达，保持为原版异常 |
| BAT-048 | 场景 37 第 1 回合运行 SAY 81，999/1000 改走内部场景 49；模块 25 剧情 70→模块 33→35→`module27/stage38`。场景 38 第 1 回合运行 SAY 164，999 运行 SAY 165 后转模块 46、1000 只恢复该跳转；模块 46 正常路径进入制作人员表与永久终幕，不会返回追加战部署 | C | `1000:515Ch/5197h`；模块 33 `00C7h/02E0h`、35 `00ADh/02E0h`、46 `00FEh/03CEh/0725h`；B 记录 75/77；目标表；SAY 70/81/164/165；共享提取器合计 38 段签名；`stage-events.json` | 场景 38 的重复 MAGIC 后项 74 与模块 46 `0101h–0128h` 的重玩写入都保持不可达 |
| BAT-049 | 38/38 个逐关处理器的回合/结算条件、72 个 SAY 记录、直接状态修改和 999/1000 路由均已分类；动态棋盘关为 0/1/6/11/20/21/22/42，14–19 从第 6 回合起把 75 个敌方逐槽 AI 行为清零，场景 8 写 SAY 157 却不调用渲染器 | C | `1000:4328h–51E5h` 全段及 38 个逐函数 SHA；`5319h/533Eh/536Ah`；DS:`2E5Ch`；特殊 far/near call 计数；72 份剧情 JSON；`stage-events.json`；剧情解释见 PRES-009，特殊棋盘表现见 PRES-012 | 无；直接移动、聚焦、写入可见边界和特殊覆盖均已闭合 |
| BAT-050 | 场景 30 首回合把 side 2 槽 27 从记录 35 改为 0；每次击破按记录号 `+1` 重建；四个 `LV_HARD` 标签依次对应记录 `0..7/0..15/0..23/0..31`，末次转为 side 1 槽 23“女帝”；“無法無天”还令全部 side 2 基础/当前攻防和最大生命增加 `floor(value/2)` | C | `1000:4F1Eh/8BD1h`；`0000:536Bh/537Bh/540Dh/9733h/97B2h`；模块 23 原生难度表；`title-flow.json/stage-events.json`；上下文短句见 PRES-012 | 无；每次形态转换前的短句及最终转换顺序已闭合 |
| BAT-051 | `0P/女帝` 与 `1P/龍` 的 `WD` 沿目标→施法者路径反向增长；每格只伤与所选目标同阵营的单位，最多逐点扣 90。防魔完全阻挡且本次不被清除，死亡在全动画后统一移除 | C | `0000:62F2h/6438h/644Dh/783Dh/CCD2h`、`1000:660Eh..673Fh/7E09h`；21 段专项代码、3 段数据签名；`wd-stage26.json` | 原版玩家可见名称未恢复，只保留内部码 `WD`；不影响规则 |
| BAT-052 | 行为 12 场景 0 的撤离由 AI 行动收尾 `1000:1876`（`1000:16AD` 无条件调用）判定：场景为 0 且行动结束后格号严格大于 2271 `(21,45)` 时清零槽位图与阵营图，故撤离区是第 45 行 `x>=22`、第 46 行全部与楼梯三格共 20 个可通行格，而非仅楼梯三格；玩家 `0000:7310` 移动不经过该收尾。场景 4 每次路线移动尝试后以 seed 3 建立近身安全区，用 `MAGIC/26` 格码 12/13 对区外 side 1 单位播放 11 次命中绘制及 11 次空白尾段（共 44 tick），再令其生命减半；场景 9 低于格 934 写胜利 999，却以 VGA 复制留下的 `DI=3450` 进入无界 PIT 路径器，可折回后再播放 `E/14` 移动或超过 100 项覆写代码段 | C/U | `0000:633Dh/63CFh/D4EEh`、`1000:16ADh/1876h/1AF8h/1B56h/1BBEh/240Fh/3A8Ch/6CF8h..6EDEh/7E09h..7F21h/7F4Ah/7F72h/828Fh/8329h`；33 段代码、3 段数据签名；场景 0 撤离阈值立即数与 20 个可通行撤离格断言；四条确定性参考轨迹；`behavior12-effects.json` | C：场景 0 撤离阈值与区域、场景 4 目标/公式/资源/时序及场景 9 陈旧 DI、相邻零缓冲、PIT 排序和越界机制；U：实际 PIT 轨迹、动态棋盘结果及代码段覆写后的可见表现 |
| BAT-053 | 骑士／战士十一种普通转职末端职业已完成专属分支审计；九种有确认机制，妖龍骑士与魔鎧战士在普通攻击、射击、技术、玩家菜单和 AI 分发中无额外专属分支。`0F/飛龍騎士` 在玩家普通攻击后存活时以 `floor(当前移动力/2)`、模式 `M` 进入“移動／放棄”追加移动，不能再次攻击；AI 普通分发无该调用。妖龍 `DATA` 三行攻防为 `87/45、93/48、99/51`，魔鎧为 `76/60、78/72、80/84`；两者均由静态数据决定，不存在动态攻防交换；原生转职来源的攻防也都增加。迅龍闪避与獸骨强反击的 PIT 低位候选在 UI 中标为约 50%。2026-08-16 以全模块穷举扫描独立复核该否定结论：攻略流传的“妖龍牺牲防御换攻击／魔鎧牺牲攻击换防御”不是隐藏特性，只是 `DATA` 明面数值分布——魔鎧以 `+12`/行 的全表最陡防御成长拿到 3 级防御 84（35 个常规记录最高），代价是战士线最低攻击 `76→80` 与 4 阶近战最低攻击步长 `+1/590`，且防御在职业内 3 级后永久固定，该优势不随后期成长扩大；妖龍连该描述都不成立，同线 `0F` 攻击更高（92 对 87）且防御更低（44 对 45），`2E` 同为 87 攻击但防御 48／生命 400 更高并另有反击特性，`1F` 的实际特征是骑兵线第二高攻击步长 `+3/540` 与骑兵线最低移动 9。地形防御 profile 上 `1H` 与 `1G/2G/2E/0E/3E` 逐槽相同，`1F` 与 `0F` 逐槽相同（飞行档，约为地面系一半），两者均无个体差异 | C/U | 模块 29 `0000:69BCh..6A53h/70D2h/7109h`、`67AA/67C3`；模块 29/33 `5256/52BCh/8E5Bh..8E85h` 属性与成长消费者；`input-ui.json#class0FExtraMove`；`module29-skill-entry-context.log`；职业短码、普通战斗、状态、射击、技术菜单和 AI 全分发审计；`native-unit-table-access.md`、`unit-catalog.json`；2026-08-16 复核补充：对 `lzexe-modules`／`runtime-modules`／`JS3.UNPACKED.EXE` 全部模块按 39 个短码做完整 8086 立即数操作数分类扫描，七项已知普通攻击链特性 7/7 阳性对照命中（`0000:92C5/A1A7`、`9543`、`932D`、`92EE/92F3/92F8/92FD`），`1F/1H` 的全部立即数命中仅三类且均无特殊性——`0000:6770..67CD` 与 `1000:16BC..1723` 两条全近战派发链同落 `0000:6849`／`1000:18A1`、`1000:8D94` 属技能码命名空间、模块 0025/0037 的 4 字节分组指针表与同线队友共用同一指针；职业记录号 DS:`318Ch` 的 `cmp` 立即数命中数为 0，5 处读取全为查表索引；有效攻防链 `0000:52BC`（照抄 `DATA` 行）、`1000:8BD1`（难度 3 且 side 2 各 `+floor(x/2)`）、`1000:8C2D..8C89`（仅状态位 ±20）无任何职业项，直接在计算点排除动态攻防交换 | C：分支集合、静态攻防行、成长公式、菜单、玩家调用边界与无独立分支结论；U：迅龍／獸骨 PIT 的实机频率分布。短码 `1F/1H` 与技能码 `1F`＝初級炎暴／`1H`＝初級治療 同形，重查时不得把 `1000:8D94` 技能链误判为职业特性；静态扫描的唯一盲区是寄存器对寄存器短码比较，该形式只能比较两个动态职业码，无法编码写死的职业特性，不构成缺口。复刻不得为这两个职业新增特性分支 |
| BAT-054 | `0N/水戰士` 作为原始防守者承受普通近战且存活时，每次在上／下／左／右首个空且规则非 `98/99` 的相邻格新增一个同阵营、同单位槽分裂体，总数上限 4；不使用随机数。反击受伤、射击、技术和逐关伤害不触发。分裂格共享单位槽的生命、经验与状态；共享生命归零时死亡扫描清除全部分裂格；无独立动画或声音 | C | 模块 29 `0000:927Ah/9296h/931Bh/9372h/939Eh/96C2h`；三段新增代码签名；`combat-formulas.json#waterWarriorSplit` | 无 |
| BAT-055 | 内部场景路由为 `9→11→10`；场景 11 标题 SAY/0128 是“拯救蘇蘭達”。B/0023 初始固定 9 对 1；第 1 回合运行 SAY 24/25/26 后清格 126 的 side 1 槽 9，多莉以剧情职责离场；槽 8 蘇蘭達进入格 `0..279` 胜利、离场失败，清除当前敌军不构成胜利；999 运行 SAY 27，999/1000 均进入模块 25/场景 10且 1000 不重播 | C | B/0023 SHA `c14643db…292a`；场景 11 目标表；`1000:477Ch..47FAh` 处理器 SHA `f3371536…23ee`；SAY 24–27/93/128/129；`battle-templates.json/battle-objectives.json/battle-lifecycle.json/stage-events.json` | SAY/0093 的碧娜維姬／妮雅文本与机器条件冲突；逐回合增援见 BAT-056，复刻显示政策见 REMAKE-041 |
| BAT-056 | side 1 全部行动结束后、side 2 AI 前，每个完整回合调用一次特殊生成链，发布版有三个场景分支。场景 4 在回合 `<20` 时递增初值为 0 的 DS:`2E58`，每逢第 4 次即回合 `4/8/12/16` 清零计数，并分别尝试格 `0049h (23,1)`、`004Dh (27,1)`；每个空格从当前不在场的最小 side 2 槽 `30..39` 生成一名士兵，故每波最多两人、四波最多八人。场景 11 每轮从格 `0980h (32,48)` 向前找首个空格，加入当前不在场的最小 side 2 槽 `40..79`；每轮一名、总量无限、同时动态在场上限 40。场景 27 从第 5 回合起每轮尝试格 `0823h (33,41)`，空格时从槽 `30..39` 依次生成飛馬戰士／半龍戰士／妖龍騎士／飛龍騎士／飛馬戰士／飛馬戰士／巨斧戰士／妖龍騎士／飛馬戰士／飛龍騎士；同时上限 10、死亡槽可复用且总量无限。三支路新单位都在随后同轮 side 2 阶段行动且不消费 PRNG；精确生成格被占用时场景 4／27 只跳过该格或该轮。场景 11 槽 40–45 为 `騎兵/飛馬戰士/騎兵/飛馬戰士/騎兵/半龍戰士`，46–79 为士兵，仅槽 44/45 行为 2；场景 4 候选槽职业／行为全为 0，场景 27 候选槽行为全为 0。场景 10 不进入本链，其 `1000:475C` 处理器又是 `route only` 且事件/特殊调用均为空，所以 B/0021 的五名追兵就是整场敌军总量 | C | `0000:4E03h..4E25h`、`1000:5207h/5211h/5240h/525Fh/5277h/52B8h/52F9h` 八段完整签名；DS:`2E58h` 静态初值；`1000:475Ch..477Ch` 场景 10 处理器；B/0009/B/0021/B/0023/B/0055 的 side 2 阵容、职业／行为数组及相邻 side 1 数组；`stage-events.json#module29BattleRuntime.fullRoundSpecials/handlerBehaviorCatalog`、`battle-templates.json` | 槽 75–79 的职业／行为从紧邻 side 1 数组前五项读得 0，是原版内存布局语义；Web 以生成内容显式固化，不复制越界读取。旧版只展开 5240 分支，导致 stage 4 Web 漏增援，并把 stage 27 误报为无增援 |
| BAT-057 | 场景 12“落入沼澤”使用 B/0025：妮雅槽 0 固定 `(23,20)`，十九候选、8 格、最多 9 人；五个敌方根槽 `40/41/42/44/43` 均为水戰士，位于东侧 `(39,17/20/23/26/28)`。逐关处理器只在 round 1 播 SAY 30、999 播 SAY 31，无棋盘动作或特殊调用；本关不在动态棋盘表与完整回合特殊生成链，故无关卡增援。水戰士职业级近战受击分裂仍可使每根最多四体且共享生命，但不新增根生命池。全灭 side 2 胜利、妮雅离场失败，999/1000 路由模块 25/场景 13，1000 不重播 | C | B/0025 SHA `8b97b5d6…f63e`；`1000:47FAh..483Ah` SHA `fa93cb29…008`；SAY 29–31/95/130/131；`battle-templates.json/battle-objectives.json/battle-lifecycle.json/stage-events.json/music-catalog.json` | SAY/0095 错写“攻击瓦尔克丽城”；复刻显示政策见 REMAKE-043。须在逐关增援审计中把职业分裂与关卡增援分开 |
| BAT-058 | 场景 13“龍塔外”使用 B/0027：妮雅槽 0 固定 `(36,37)`，二十一候选、11 格、最多 12 人；槽 10/11 以非零模板职业 26 引入水戰士瑪琳／摩莉娜。开局九名敌军由槽 24 神劍戰士瑪西爾领队，另含飛馬戰士、陸戰騎士、魔術士、魔導師、鋼甲戰士、騎兵、弓兵、僧侶各一。机器只检查槽 24 离场胜利、妮雅离场失败。处理器为纯路由、无事件／特殊调用，本关不在动态棋盘或完整回合特殊链，故无任何增援；999/1000 路由模块 27/场景 14，1000 不重播 | C | B/0027 SHA `c510d86e…a53a`；`1000:48A9h..48CFh` SHA `c991a2d6…2342`；SAY 32/96/131/132；`battle-templates.json/battle-objectives.json/battle-lifecycle.json/stage-events.json/music-catalog.json/technique-rules.json` | SAY/0096 错写全灭；复刻显示政策见 REMAKE-046。逐关推进必须继续显式审计增援，即使结论为无 |
| BAT-059 | 逐关“勝利條件／失敗條件”原文来自模块 29 `DS:1273-131E` 的 42 项关卡键值表，逐关标题来自 `DS:30BA-3169`；两表均由 `DS:80B5` 选择器消费，目标表未命中返回 `65535`。目标表取值为场景 `0→82、1→98、2→172、3→106、4→107、5→108、6→160、7→109、8→162、9→110、10→111、11→113、12→114、13→159、14→83、15→84、16→85、17→86、18→87、19→88、20→89`，逐项指向的目标对象与机器胜负条件一致（键 1 娜米＝槽 16、键 3 梅蒂＋黛西／希蜜双保护、键 4 葛蒂拉斯护送、键 5 汀塔琪／萊茵＝槽 25/26、键 13/14/15/16 瑪西爾／芳／蘭／莎），因此此前认定的“原文写了不存在的首领”整类冲突并不存在。唯一残留的原文冲突是键 5 的 SAY/0108 写“打敗「丁塔琪」**及**「萊茵」”，而胜利处理器 `CS:0084` 扫描槽 25 得 `'Y'` 即 `ret`、根本不检查槽 26，实为“任一”；该原版文案错误按 `REMAKE-027`／`REMAKE-089` 单独登记。场景 16 使用 B/0033：妮雅槽 0 固定 `(25,31)`，二十一候选、9 格、最多 10 人；side 2 十三人，槽 10 莎为半龍戰士，与两名弓兵、一名魔術士、五名鋼甲戰士在 `(23..27,12..14)` 结阵且行为均为 1，另有四名神劍戰士以行为 0 位于 `(15/18/32/35,27)`。机器只检查槽 10 离场胜利、妮雅离场失败；处理器 `1000:4957` 只在 round 1 播 SAY 35、第 6 回合起每个活动回合清零 75 个 side 2 逐槽行为，不生成棋盘单位；本关不在动态棋盘表、完整回合特殊生成链或击杀替补／形态转换链，故无任何增援。999/1000 路由模块 27/场景 17，1000 不重播 | C | 模块 29 数据签名 `DS:1273-131E` SHA `5fae677f…0340e`、`DS:30BA-3169`；`story-presentations.json#globalReachabilityAudit.tables`；B/0033 SHA `0888e3ad…da8b`；`1000:4957h..499Bh` SHA `7fcad09e…dbc127`；SAY 35/85/134/135；`battle-templates.json/battle-objectives.json/battle-lifecycle.json/stage-events.json/music-catalog.json/technique-rules.json` | 本行推翻此前按 `82 + 场景号` 推导原文的做法；受影响的复刻显示决策与更正范围见 REMAKE-051 |
| BAT-060 | 场景 17“龍塔第四層”使用 B/0035：妮雅槽 0 固定 `(25,24)`，二十一候选、9 格、最多 10 人；side 2 十二人，槽 11 倩为半龍戰士、槽 43 为魔術士且行为均为 1，另有僧侶、两名神劍戰士、两名巨斧戰士、祭司与四名鋼甲戰士。机器只检查倩离场胜利、妮雅离场失败，SAY/0086 与条件一致。处理器 `1000:499B` 在 round 1 播 SAY/0036，第 6 回合起持续清零 75 个 side 2 逐槽行为；不生成单位。本关不在动态棋盘目录、完整回合特殊生成链或击杀替补／形态转换链，故无增援；999/1000 路由模块 27/场景 18，1000 不重播 | C | B/0035 SHA `9f5c7381…c900`；`1000:499Bh..49DFh` SHA `02b758…34a4`；SAY 36/86/135/136；`battle-templates.json/battle-objectives.json/battle-lifecycle.json/stage-events.json/music-catalog.json/technique-rules.json` | 无；复刻规则沿用 REMAKE-051，增援五路审计必须保留 |
| BAT-061 | 场景 18“龍塔第五層”使用 B/0037：妮雅槽 0 固定 `(25,33)`，二十一候选、7 格、最多 8 人；side 2 十六人，槽 12 麗为半龍戰士，另有僧侶、四名弓兵、魔弓兵、弩兵、两名鋼甲戰士与六名神劍戰士。前十敌行为 1，六名神劍戰士行为 0。机器只检查麗离场胜利、妮雅离场失败，目标表键 18 的 SAY/0087 与条件一致。处理器 `1000:49DF` 在 round 1 播 SAY/0037，第 6 回合起持续清零 75 个 side 2 逐槽行为；不生成单位。本关不在动态棋盘目录、完整回合特殊生成链或击杀替补／形态转换链，故无增援；999/1000 路由模块 27/场景 19，1000 不重播 | C | B/0037 SHA `1ff30a9e…f757`；`1000:49DFh..4A23h` SHA `35ceb04b…bd8`；SAY 37/87/136/137；`battle-templates.json/battle-objectives.json/battle-lifecycle.json/stage-events.json/music-catalog.json/technique-rules.json` | 无；复刻规则沿用 REMAKE-051，增援五路审计必须保留 |
| BAT-062 | 场景 19“龍塔第六層”使用 B/0039：妮雅槽 0 固定 `(25,33)`，二十一候选、9 格、最多 10 人；side 2 二十一人，槽 13 愛为半龍戰士，另有两名戰士、四名神劍戰士、七名鋼甲戰士、祭司、僧侶、魔術士与四名巨斧戰士。十三敌行为 1，另八敌行为为 3/4/5/6。机器只检查愛离场胜利、妮雅离场失败，目标表键 19 的 SAY/0088 与条件一致。处理器 `1000:4A23` 在 round 1 播 SAY/0038，第 6 回合起持续清零 75 个 side 2 逐槽行为；不生成单位。SAY/0038 四个检查点中肖像 10 是蘇蘭達。本关不在动态棋盘目录、完整回合特殊生成链或击杀替补／形态转换链，故无增援；999/1000 路由模块 25/场景 20，1000 不重播 | C | B/0039 SHA `cd33d9ca…c17`；`1000:4A23h..4A5Bh` SHA `99a91ddd…e41`；SAY 38/88/137/138；`battle-templates.json/battle-objectives.json/battle-lifecycle.json/stage-events.json/music-catalog.json/technique-rules.json/campaign-roster.json` | 无；复刻规则沿用 REMAKE-051，增援五路审计及肖像说话人归属必须保留 |
| BAT-063 | 场景 20“龍塔頂部”使用 B/0041：守護者槽 32、妮雅槽 0、葛蒂拉斯槽 24 固定出场，二十候选、14 格、最多 17 人；开局 side 2 是 X=`25/34`、Y=`14..21` 的十六名半龍戰士叙事阵形。round 1 处理器依次播 SAY/0040、把守護者由 `(28,14)` 移至 `(28,17)`、播 SAY/0041、清除全部 side 2、在 `(29,16)` 生成槽 28／职业 36 妖龍并播 SAY/0071。此后完整回合特殊链与击杀替补／形态转换链都无本关入口，故动态棋盘目录中的 stage 20 只表示这次整体替换，没有后续增援。机器只检查妖龍离场胜利、妮雅离场失败，目标表键 20 的 SAY/0089 与条件一致。999 时从 `(40,16)` 生成琴斯槽 7并移动到 `(33,15)`，播放 SAY/0072–75；999/1000 路由模块 25/场景 21，1000 不重播 | C | B/0041 SHA `83cc30af…953e`；`1000:4A5Bh` 处理器 SHA `b79b7cca…53a`；SAY 39–41/71–75/89/138/139；`battle-templates.json/battle-objectives.json/battle-lifecycle.json/stage-events.json/music-catalog.json/technique-rules.json/campaign-roster.json/wd-stage26.json` | WD 规则／表现已有 BAT-051/PRES-013；复刻确定重放选择见 REMAKE-052。增援五路审计必须把一次性叙事替换与后续增援分开 |
| BAT-064 | 场景 21“焦土森林村莊外”使用 B/0043：开局双方活动数与部署格均为 0。模块 25 先播 SAY/0042；round 1 处理器按槽 `0/1/24/8` 在 `(42,25)/(43,24)/(41,24)/(40,26)` 写入妮雅、希蜜、葛蒂拉斯、蘇蘭達，职业来自模板稀疏覆盖前已导入的战役状态；播放 SAY/0043 后依同序用 `FM` 预算 50 移到 `(28,41)/(30,42)/(28,40)/(27,42)`，再播 SAY/0044。side 2 始终为空，事件完成后即时胜利；本场跳过普通胜利／保存提示，999/1000 均路由模块 27/场景 22且 1000 不重播。动态棋盘目录包含 stage 21 只因上述四人写入／移动；完整回合特殊生成链与击杀替补／形态转换链无本关入口 | C | B/0043 SHA `ab200d36…666c`；`1000:4B72h` 处理器 SHA `77f24db0…e2b7`；SAY 42–44/139/140；`battle-templates.json/battle-objectives.json/battle-lifecycle.json/stage-events.json/stage-presentations.json/music-catalog.json/technique-rules.json/campaign-roster.json` | 无；复刻须保持非交互过场、战役职业继承、四人顺序与直接 stage 22 路由，不能把本场误作普通可玩战斗 |
| BAT-065 | 场景 22“焦土森林村莊中”使用 B/0045：固定妮雅、27 名候选、18 个部署格、最多 19 人，槽 25–31 的稀疏职业覆盖均为记录 8“半龍戰士”。stage 21 直达模块 27，故无模块 25 剧情；round 1 临时加入女帝槽 23与琴斯槽 7并移动，琴斯聚焦 `05CBh` 却写 `05CAh`，随后播放 SAY 76–79，留下 side 2 槽 `2,28,40,41,42,43`。槽 2 是葛蒂拉斯／魔祭師，槽 28 是妖龍／龍且是唯一胜利目标；两名临时我方在玩家取得控制前清除。动态棋盘目录只登记本次伏击，完整回合特殊生成与场景 30 击杀替补／形态转换均无本关入口，故累计敌军固定六名。999/1000 保留默认模块 25/场景 23，1000 不重播 | C | B/0045 SHA `3a4ed8fd…c8d5b`；`1000:4C88h` 处理器 SHA `1e63fecf…9006`；SAY 76–79/90/140/141；`battle-templates.json/battle-objectives.json/battle-lifecycle.json/stage-events.json/stage-presentations.json/story-presentations.json/music-catalog.json/technique-rules.json/campaign-roster.json` | 无；复刻须保持先部署、35 个检查点、原生聚焦错位、六敌固定总量与单一妖龍目标，不能改成全灭战 |
| BAT-066 | 场景 22 的普通胜利反馈／存档后默认路由模块 25／场景 23；SAY/0045 以 `BK` 开始、全段没有 `PP`，因此十五个检查点继续显示场景 22 退出时帧缓冲，具体视口随当时相机而变。随后使用 B/0047：妮雅槽 0 固定 `(25,38)`，槽 `1–21,25–31` 共 28 名候选、14 格、最多 15 人。开局 side 2 二十一名静态守军：三半龍戰士、三飛龍騎士、两迅龍騎士、两魔弓兵、七連弩兵、两神劍戰士、两鋼甲戰士；九敌行为 1，其余行为 0/2。round 1 处理器 `1000:4DB5` 只播放 SAY/0046 四个检查点，不写入单位。本关不在动态棋盘目录、完整回合特殊生成链或场景 30 击杀替补／形态转换链，故无增援。妮雅线性格号 `y×50+x` 进入 `0..524` 胜利、妮雅离场失败且失败优先；SAY/0091 与机器条件一致。999/1000 路由模块 27/场景 24，1000 不重播 | C | B/0047 SHA `1b19094f…f045`；`1000:4DB5h` 处理器 SHA `d4964d7e…c9f1`；SAY 45/46/91/141/142；`battle-templates.json/battle-objectives.json/battle-lifecycle.json/stage-events.json/story-presentations.json/music-catalog.json/technique-rules.json/campaign-roster.json` | 无；复刻须区分原版 SAY/0045 的跨关帧缓冲事实与 Web 流程决策，并保留线性 `0..524` 到达区、固定二十一敌、无增援及“无需全灭”目标，不能矩形化或全灭化 |
| BAT-067 | 场景 24“死亡之谷城堡前”使用 B/0049：妮雅槽 0 固定 `(27,39)`，槽 `1–21,25–31` 共 28 名候选、14 格、最多 15 人。开局 side 2 二十二名静态守军：一獸骨騎士、十一連弩兵、三半龍戰士、三鋼甲戰士、三魔龍騎士与一叢林戰士；十二敌行为 1，其余十敌行为 2。round 1 处理器 `1000:4DF6` 只播放 SAY/0047 四个检查点；现场胜利 999 播放 SAY/0048 九个检查点，均不写入单位。本关不在动态棋盘目录、完整回合特殊生成链或场景 30 击杀替补／形态转换链，故无增援。妮雅线性格号 `y×50+x` 进入 `0..1030` 胜利、妮雅离场失败且失败优先；目标表键 24 的 SAY/0092 与机器条件一致。999/1000 直接路由模块 27/场景 26，跳过无标题键的内部场景 25，1000 不重播 | C | B/0049 SHA `f60f4765…aa7`；`1000:4DF6h` 处理器 SHA `c86b8d1c…1544`；SAY 47/48/92/142/143；`battle-templates.json/battle-objectives.json/battle-lifecycle.json/stage-events.json/story-presentations.json/music-catalog.json/technique-rules.json/campaign-roster.json` | 无；复刻须保留线性 `0..1030` 到达区、固定二十二敌、无增援、胜利对白与直接跳过场景 25 的路由，不能矩形化、全灭化或虚构第 25 关内容 |
| BAT-068 | `1N/半龍戰士` 是技術职业链第 14 项，但没有 DS:`41CEh` 分层条目。`702Dh` 在任何查表前判定职业码并跳 `7475h`：seed 固定 `200`、传播模式 `'0'`（`1000:3BB0h` 每步扣 1、只拒地形 `99`、不读阵营图），落点校验 `785Eh/7880h` 与普通移動确认路径 `7379h` 逐字节相同，表现复用移動的 `17DE:0029/0142`，`74EDh` 移动后立刻置行动位且不追加攻擊菜单，无伤害无经验。无动作码、无子菜单、无阶段与等级门槛。AI 侧 `1N` 属 `1000:171Fh` 普通链且无任何 `1N` 判定，故原版只有玩家半龍戰士可用；发布内容仅场景 22 把 side 1 槽 25–31 覆写为该职业 | C | 模块 29 `6FFDh/702Dh/7054h/7475h/74C9h/74D7h/74EDh`、`7318h/7336h/7379h`、`1000:3BB0h/3C5Ch/3C70h`；13 段新增签名；`technique-rules.json#techniqueMenu.directHandlerClasses` | 无；复刻不得据此给敌方半龍戰士开放该技術，也不得改成无视地形 `99` 的无条件全图传送 |
| BAT-069 | 场景 27“趕回瓦爾克麗城”使用 B/0055：side 1 固定七名行为 2 城防友军（槽 22/41/44/43/45/42/40）、三名行为 0 工兵（57/56/58）与妮雅槽 0，另有槽 `1–21,25–31` 二十八候选、二十开放格、最多三十一人。固定职业覆写为巨斧戰士／魔祭師／魔術士／咒術師／槽 45 当前职业／祈導師／魔劍戰士、三工兵；side 2 开局五名行为 0 静态叛军：魔劍戰士、魔祭師、魔弓兵、魔鎧戰士、咒術師。七名城防友军只有逐槽行为，没有原版军团／主将字段；失败分发表只保护 side 1 槽 0，故槽 22 死亡或七名城防军全灭均不失败。十名固定棋盘单位的 side-1 角色描述符肖像字节都是 `FFh`，按 UNIT-014 的回退规则由 `0000:51B9` 同时覆写肖像记录与单位名指针，因此本关棋盘/HUD 中七名城防军与三名工兵一律显示“职业／同名职业”加通用职业肖像（槽 22 为巨斧戰士＋肖像 57），没有任何一人具备独立姓名或肖像；槽 22 描述符 DS:`37C6h` 的“愛莉歐拉”肖像同为 `FFh`，但其名单标志在本关为 0，故不进入模块 27 名单显示路径。round 1 播 SAY/0051 六检查点，现场胜利 999 播 SAY/0052 十检查点；模块 25 无关前记录。独立于逐关处理器，完整回合特殊链 `1000:525F` 从第 5 回合起每轮尝试在 `0823h (33,41)` 生成当前不在场的最小 side 2 槽 `30..39`；十槽职业记录依次为 `23/8/14/15/23/23/7/14/23/15`，行为均为 0，格被占用时跳过该轮，死亡槽可复用、当轮可行动且无 PRNG。妮雅线性格号落入 `0..575`、`616..625`、`666..675`、`716..725` 任一区间即胜，妮雅离场失败且失败优先；目标表键 27 的 SAY/0094 与条件一致。999/1000 路由模块 25／stage 28，1000 不重播 | C | B/0055 SHA `73fca304…8a24`；stage 27 处理器 SHA `57ab5ceb…bc39`；`1000:5207/525F/5277/52F9`；SAY 51/52/94/144/145；`battle-templates.json/battle-objectives.json/battle-lifecycle.json/stage-events.json/story-presentations.json/music-catalog.json/campaign-roster.json`；`0000:5087/510C/51EC/51B9` 与 `campaign-roster.json#displayResolution` 的槽 22 肖像 `FFh` | 复刻须保留四段线性到达区、七名非保护目标独立友军、五名固定敌军与第 5 回合起的单格逐轮增援，不能把城防军改成玩家单位、矩形化或全灭化目标；原版姓名／肖像同时回退的事实必须保留，`stableRemake` 的槽 22 姓名显示例外另由 `REMAKE-120` 授权，仍不得虚构专属肖像。当前 Web stage 27 合同／实现仍错误标记“无增援”，留待单独授权修复 |
| BAT-070 | 场景 27 的部署名单与固定棋盘单位是两个集合：B/0055 最后一张 75-word 名单标志仅在槽 `0–21,25–31` 非零，共 29 项；十一名 side 1 固定棋盘单位中只有妮雅槽 0 同时属于名单。七名城防友军槽 `22,41,44,43,45,42,40` 与三名工兵槽 `57,56,58` 的标志均为 0，因此固定参战但不占三页名单项目。页面初始仍有十一名实际参战者；名单内初始已选固定项只有妮雅 | C | B/0055 最后 150 字节与 side/unit 图交叉；模块 27 `08CBh/09B8h/0A1Bh/0D61h`；`battle-templates.json#stage=27.deployment.fixedRosterUnitSlots/fixedBoardOnlyUnitSlots`、`battle-lifecycle.json` | 无；复刻部署页必须按名单标志投影，不能把 NPC 或玩家控制的地图固定工兵因“已在场”而拼入名单 |
| BAT-071 | 场景 28“保衛瓦爾克麗城”使用 B/0057：妮雅槽 0 固定 `(28,24)`，名单标志覆盖槽 `0–21,25–31` 共二十九项，三十四开放格，最多二十九名 side 1；没有名单外固定单位。side 2 开局固定十七名且逐槽行为全为 0：二妖龍騎士、四魔劍戰士、四邪劍戰士、三魔法師、二弩兵、二飛馬戰士。模块 25 先以 BK/22 播 SAY/0053 十四检查点；round 1 处理器 `1000:4EDE` 只播 SAY/0054 五检查点，现场胜利 999 只播 SAY/0055 八检查点，均不写棋盘或其他运行状态。本关不在动态棋盘目录 `0/1/6/11/20/21/22/42`，完整回合特殊生成链只有 stage 11 分支，击杀替补／形态转换只在 stage 30 进入，故跨回合、击杀与难度变化均无增援、替补或特殊逐关机制。机器检查 side 2 全灭胜利、妮雅离场失败且失败优先；目标表键 28 的 SAY/0095“打敗攻擊瓦爾克麗城的敵人／妮雅戰敗”与机器条件一致。玩家音乐 `11→10`、敌方 `27→26`；999/1000 保留默认模块 25／stage 29 路由，1000 不重播，下一标题 SAY/0146“騎士城堡前” | C | B/0057 SHA `957c9681…1512`；stage 28 处理器 SHA `1cccbd6a…d128`；SAY 53/54/55/95/145/146；`battle-templates.json/battle-objectives.json/battle-lifecycle.json/stage-events.json/story-presentations.json/music-catalog.json` | 无；复刻须保持二十九项名单、三十四开放格、固定十七敌、全灭目标、三段剧情顺序与无增援结论，不得从叙事“四面包围”补造模板外敌军或首领目标 |
| BAT-072 | 场景 29“騎士城堡前”使用 B/0059：妮雅槽 0 固定 `(41,26)`，名单标志覆盖槽 `0–22,25–31` 共三十项，十四开放格，最多十五名 side 1；没有名单外固定单位。模块 27 只按非零名单标志压紧条目，绘制时直接读取角色描述符的 `$` 姓名；槽 22 因此在部署名单显示“愛莉歐拉”且可正常选择，肖像 `FFh` 不会过滤该项。提交部署进入模块 29 后，`0000:51B9` 才按当前职业回退肖像并把战场单位名改为职业名，形成原版明确的“部署名单名／战场职业名”差异。side 2 开局固定十五名：艾西柯羅槽 4 为妖龍騎士，另有五邪法師、四迅龍騎士与五魔弓兵；五名魔弓兵行为 2，其余行为 0。模块 25 在部署前以 BK/23 播 SAY/0056 七检查点；场景 29 明确没有逐关处理器，因此没有 round 1、普通回合或现场胜利专属剧情／状态写入。B/0059 静态模板、缺席处理器、动态棋盘目录 `0/1/6/11/20/21/22/42`、只覆盖 stage 11 的完整回合特殊生成链，以及只在 stage 30 进入的击杀替补／形态转换链共同证明本关无增援、替补或形态转换。机器检查 side 2 全灭胜利、妮雅离场失败且失败优先；目标表键 29 的 SAY/0096“打敗所有的敵人／妮雅戰敗”与机器条件一致，艾西柯羅不是单独首领目标。关前音乐 `MAGIC/77`，玩家音乐 `35→34`、敌方 `13→12`；999/1000 均保留默认模块 25／stage 30 路由，1000 不重播表现，下一标题 SAY/0147“治癒維斯塔女帝” | C | B/0059 SHA `0149c70f…9751`；SAY 56/96/146/147；BK/23；`battle-templates.json/battle-objectives.json/battle-lifecycle.json/stage-events.json/story-presentations.json/music-catalog.json/campaign-roster.json`；模块 27 `08CBh/09B8h/0A1Bh/0B39h/0D61h`；模块 29 `0000:5087/510C/51EC/51B9` | 无；复刻须保持三十项名单、槽 22 的名单／战场双层显示、十五名固定敌军、全灭目标、单一关前剧情及无逐关处理器／无增援结论，不得把叙事点名的艾西柯羅改成单首领胜利条件 |
| BAT-073 | 场景 30“治癒維斯塔女帝”使用 B/0061 且没有交互部署：side 1 固定槽 40 `(30,19)`、琴斯槽 7 `(26,25)`、妮雅槽 0 `(28,25)`，职业从战役状态继承；side 2 只有槽 27 `(28,17)` 的记录 35 女帝“維絲塔”，行为 0。模块 25 以 `MAGIC/78`、BK/23 播 SAY/0057 十七检查点；round 1 处理器 `1000:4F1E` 播 SAY/0058 十八检查点、聚焦槽 27，播放 selector `22h`／DS:`8762h`“我．．．我好難過．．．／頭好痛啊！”，再把该槽从女帝重建为记录 0 士兵。此后 `0000:9733/97B2` 在每次槽 27 当前形态被击败时先清经验、以槽 27 角色描述符的固定 D/41 女帝肖像重播同一句，再按难度上限 8／16／24／32 依记录 `0..7/15/23/31` 原地满生命重建下一形态；序列不读 PRNG。`C97Ah` 从 `502Ah` 载入的 DS:`3192h` 取当前单位画像，但连续变形只改 DS:`55AEh[27]` 职业记录；side 2 槽 27 描述符画像是 41 而非 `FFh`，故 `51B9h` 不进入职业通用画像回退。最终形态后不再生成敌方，而把同格改写为 side 1 槽 23、记录 35 女帝。静态模板、逐关处理器、动态棋盘目录与完整回合特殊生成链均没有第二条生成通道，故上述击杀替补／形态转换是唯一动态单位链，不存在额外增援。机器以 side 2 槽 27 不在场胜利、side 1 槽 0 不在场失败且失败优先；目标表键 30 的 SAY/0097“把女帝「維絲塔」打醒／妮雅戰敗”一致。现场胜利播 SAY/0059 六检查点；玩家音乐 `29→28`、敌方 `5→4`；999/1000 路由模块 25／stage 31，1000 不重播，下一标题 SAY/0148“前往斯德林海峽” | C | B/0061 SHA `bc65d12b…f9c7`；stage 30 处理器 `1000:4F1E`；模块 29 `0000:502A/5087/510C/51B9/51EC/537B/9733/97B2/C97A`；DS:`3192h/55AEh[27]/8762h`；side 2 槽 27 描述符；SAY 57/58/59/97/147/148；BK/23；`battle-templates.json/battle-objectives.json/battle-lifecycle.json/stage-events.json/stage-presentations.json/story-presentations.json/music-catalog.json/campaign-roster.json` | 无；复刻须保持无部署固定三人、四档确定形态数、固定 D/41 女帝肖像台词、经验清零、最终女帝归队与唯一动态链，不得把连续形态误报为额外增援或随机职业 |
| BAT-074 | 场景 31“前往斯德林海峽”使用 B/0063：名单标志覆盖槽 `0–21,25–31` 共二十九项；固定槽 4/3/2/1/0 分别在 `(25,12)/(22,14)/(27,14)/(25,15)/(26,33)`，槽 `5–21,25–31` 为二十四候选，十二开放格位于 `(21/26/29,23/25/27)` 与 `(22,31)/(26,31)/(29,32)`，故最多十七名 side 1。side 2 开局固定十五名且行为全为 0：槽 5 菲伊魯茵与槽 55/42/56/43 共五妖龍騎士，槽 51/54/53/52/50 共五半龍戰士，槽 49/46 两獸骨騎士、槽 48 骨騎士、槽 47/45 两迅龍騎士。模块 25 以 `MAGIC/79`、BK/23 播 SAY/0060 十四检查点；round 1 处理器 `1000:4F9D` 只播 SAY/0061 八检查点，现场 999 只播 SAY/0062 两检查点，均不写棋盘。B/0063 静态模板、逐关处理器、动态棋盘目录 `0/1/6/11/20/21/22/42`、只覆盖 stage 11 的完整回合特殊生成链，以及只在 stage 30 进入的击杀替补／形态转换链共同证明本关无增援、替补、形态转换或难度追加敌军。机器检查 side 2 全灭胜利、side 1 槽 0 妮雅离场失败且失败优先；目标表键 31 的 SAY/0099“打敗所有的敵人／妮雅戰敗”一致，菲伊魯茵不是单独首领目标。玩家音乐 `11→10`、敌方 `37→36`；999/1000 路由模块 27／stage 32，1000 不重播，下一标题 SAY/0149“斯德林海峽” | C | B/0063 SHA `1e40d2ed…be8da`；stage 31 处理器 `1000:4F9D` SHA `dd09…a4a`；SAY 60/61/62/99/148/149；BK/23；`battle-templates.json/battle-objectives.json/battle-lifecycle.json/stage-events.json/story-presentations.json/music-catalog.json/campaign-roster.json` | 无；复刻须保持二十九项名单、五名固定角色、十二开放格、十五名固定敌军、全灭目标、三段剧情与五通道无增援结论，不得按叙事把菲伊魯茵改成单首领目标或补造海峡增援 |
| BAT-075 | 场景 32“斯德林海峽”使用 B/0065：名单标志覆盖槽 `0–21,25–31` 共二十九项；妮雅槽 0 固定在 `(26,28)`，槽 `1–21,25–31` 为二十八候选，十五开放格为 `(20,23)/(33,23)/(20,26)/(33,26)/(22,28)/(24,28)/(28,28)/(30,28)/(23–29,43)`，故最多十六名 side 1。side 2 开局固定十八名且行为全为 0：槽 5 菲伊魯茵、槽 6 芙瑪羅妮与飛龍、獸骨、骨、迅龍、大斧、邪劍、魔劍、魔鎧、魔祭、祈禱、邪法、咒術、巫師、魔法、魔導等十六名联军。模块 25 没有本关关前剧情三元组，表内 `MAGIC/72` 无可达消费者；round 1 处理器 `1000:4FF1` 只播 SAY/0063 六检查点，现场 999 只播 SAY/0064 四检查点，均不写棋盘。SAY/0063 的“援兵”已经全部存在于 B/0065 静态开局模板；逐关处理器、动态棋盘目录 `0/1/6/11/20/21/22/42`、只覆盖 stage 11 的完整回合特殊生成链，以及只在 stage 30 进入的击杀替补／形态转换链共同证明本关无运行时增援、替补、形态转换或难度追加敌军。机器检查 side 2 全灭胜利、side 1 槽 0 妮雅离场失败且失败优先；目标表键 32 的 SAY/0100“打敗所有的敵人／妮雅戰敗”一致。玩家音乐 `39→38`、敌方 `13→12`；999/1000 路由模块 27／stage 33，1000 不重播，下一标题 SAY/0150“拉那洛城外” | C | B/0065 SHA `f4ab3da4…fc4`；stage 32 处理器 `1000:4FF1` SHA `9d7c97da…1549`；SAY 63/64/100/149/150；`battle-templates.json/battle-objectives.json/battle-lifecycle.json/stage-events.json/story-presentations.json/music-catalog.json/campaign-roster.json` | 无；复刻须保持直接部署、二十九项名单、妮雅固定、十五开放格、十八名固定敌军、全灭目标、两段剧情与五通道无动态增援结论，不得把叙事“援兵”实现成延迟波次或把任一四骑士改成单首领目标 |
| BAT-076 | 场景 33“拉那洛城外”使用 B/0067：名单标志覆盖槽 `0–21,25–31` 共二十九项；妮雅槽 0 固定在 `(27,44)`，槽 `1–21,25–31` 为二十八候选，九个开放格为 `(19/21/23/25/27/29/31/33/35,42)`，故最多十名 side 1。side 2 开局固定二十九名：二妖龍騎士、六巨斧戰士、四獸騎士、二迅龍騎士、四邪法師、二巫師、二祈導師、二魔法師与五魔鎧戰士；其中迅龍騎士槽 23／24 经模块 29 敌方角色描述符表 `DS:32AD` 分别解析为阿莉絲／瑪西爾与画像 D/30／D/31，其余二十七名按职业回退。十五名法师／魔鎧为行为 1 哨戒，六名巨斧／獸騎士为行为 2，其余八名（含阿莉絲／瑪西爾）为行为 0。模块 25 没有本关关前剧情三元组，表内 `MAGIC/73` 无可达消费者；round 1 处理器 `1000:5045` 只播 SAY/0065 六检查点，现场 999 没有专属剧情，999/1000 只路由模块 27／stage 34。SAY/0065 只说明四骑士最后一人仍在城内，并未否定城外两名具名守军；静态模板、逐关处理器、动态棋盘目录 `0/1/6/11/20/21/22/42`、只覆盖 stage 11 的完整回合特殊生成链，以及只在 stage 30 进入的击杀替补／形态转换链共同证明本关无增援、替补、形态转换或难度追加敌军。机器检查 side 2 全灭胜利、side 1 槽 0 妮雅离场失败且失败优先；目标表键 33 的 SAY/0101“打敗所有的敵人／妮雅戰敗”一致。玩家音乐 `19→18`、敌方 `27→26`；1000 不重播，下一标题 SAY/0151“拉那洛城內” | C | B/0067 SHA `02dd9e67…d56`；stage 33 处理器 `1000:5045` SHA `57ec9302…f29`；模块 29 敌方描述符指针表 `DS:32AD`；SAY 65/101/150/151；`battle-templates.json/battle-objectives.json/battle-lifecycle.json/stage-events.json/story-presentations.json/music-catalog.json/campaign-roster.json` | 无；复刻须保持直接部署、二十九项名单、妮雅固定、九开放格、槽 23 阿莉絲 D/30、槽 24 瑪西爾 D/31 与二十七名职业回退静态守军、15 哨戒＋14 追击、全灭目标、单一开场剧情、无胜利 SAY 与五通道无动态增援结论，不得把城内的蕾娜吉芙提前加入本关或改成单首领目标 |
| BAT-077 | 场景 34“拉那洛城內”使用 B/0069：名单标志覆盖槽 `0–21,25–31` 共二十九项；妮雅槽 0 固定在 `(30,21)`，槽 `1–21,25–31` 为二十八候选，十个开放格为 `(16..20,21)` 与 `(31..35,21)`，故最多十一名 side 1。side 2 开局固定十九名且行为全为 0：槽 6 芙瑪羅妮／巨龍騎士、槽 7 蕾娜吉芙／邪劍戰士，另有三祈導師、四邪劍戰士、三邪法師、三魔法師、二神劍戰士、一魔鎧戰士与一魔劍戰士；逐关处理器没有清除或替换槽 6，故两名具名敌军均按模板参战。模块 25 没有本关关前剧情三元组，表内 `MAGIC/74` 无可达消费者；round 1 处理器 `1000:5086` 只播 SAY/0066 四检查点，现场 999 没有专属剧情，999/1000 只路由模块 27／stage 35。静态模板、逐关处理器、动态棋盘目录 `0/1/6/11/20/21/22/42`、只覆盖 stage 11 的完整回合特殊生成链，以及只在 stage 30 进入的击杀替补／形态转换链共同证明本关无增援、替补、形态转换或难度追加敌军。机器检查 side 2 全灭胜利、side 1 槽 0 妮雅离场失败且失败优先；目标表键 34 的 SAY/0102“打敗所有的敵人／妮雅戰敗”一致，芙瑪羅妮与蕾娜吉芙都不是单独首领目标。玩家音乐 `3→2`、敌方 `21→20`；1000 不重播，下一标题 SAY/0152“時空異變” | C | B/0069 SHA `5bc313ef…2889`；stage 34 处理器 `1000:5086` SHA `df4ac578…cdfd`；SAY 66/102/151/152；`battle-templates.json/battle-objectives.json/battle-lifecycle.json/stage-events.json/story-presentations.json/music-catalog.json/campaign-roster.json` | 无；复刻须保持直接部署、二十九项名单、妮雅固定、十开放格、十九名静态敌军、全员追击、槽 6 芙瑪羅妮与槽 7 蕾娜吉芙共存、全灭目标、单一开场剧情、无胜利 SAY 与五通道无动态增援结论，不得静默修正模板具名槽或改成单首领目标 |
| BAT-078 | 场景 35“時空異變”使用 B/0071：无部署标记，`scenarioUnitFlags` 全零；side 1 槽 `5/3/0/8/4/2/1/18/7` 共九人均为名单外固定棋盘角色，职业继承战役，妮雅槽 0 位于 `(32,10)`。side 2 开局固定十名：二陸戰騎士与魔鎧戰士、半龍戰士、魔劍戰士、邪法師、妖龍騎士、魔祭師、巨斧戰士、魔術士各一，逐槽行为全为 12。处理器没有写路线目标；行为 12 的普通目标选择返回 0，故十人只消耗行动，不移动、不普攻、不施放技术。模块 25 没有本关关前剧情三元组，`MAGIC/75` 无可达消费者；round 1 处理器 `1000:50C7` 播 SAY/0067 十三检查点，现场 999 播 SAY/0068 四检查点，999/1000 路由模块 27／stage 36。静态模板、逐关处理器、动态棋盘目录、完整回合特殊生成链与击杀替补／形态转换链共同证明无运行时增援；SAY/0067 所称“衝出來”的十人已全部存在于初始模板。机器检查 side 2 全灭胜利、side 1 槽 0 妮雅离场失败且失败优先；目标表键 35 的 SAY/0103 一致。玩家音乐 `35→34`、敌方 `13→12`；1000 不重播，下一标题 SAY/0153“異世界的碧娜維姬” | C | B/0071 SHA `33d0556b…8122`；stage 35 处理器 `1000:50C7` SHA `3b2f0036…1e47`；SAY 67/68/103/152/153；`battle-templates.json/battle-objectives.json/battle-lifecycle.json/stage-events.json/story-presentations.json/music-catalog.json/campaign-roster.json`；`behavior12-stage-effects.md`、`ai-decision-system.md` | 无；复刻须保持固定九对十、名单标志与棋盘固定单位分离、全员行为 12 无路线待命、全灭目标、开场／胜利剧情与五通道无动态增援结论，不得把行为 12 自动解释成共享专家追击 |
| BAT-079 | 场景 36“異世界的碧娜維姬”使用 B/0073：名单标志覆盖槽 `0–21,25–31` 共二十九项；妮雅槽 0 固定在 `(24,27)`，槽 `1–21,25–31` 为二十八候选，二十七开放格位于 y=25/26 的 x=`21..27`、y=27 的 x=`21/22/23/25/26/27` 与 y=28 的 x=`21..27`，故最多二十八名 side 1。side 2 开局固定三十名：槽 1 碧娜維姬／巫師位于 `(23,13)` 且行为 1，另有五巫師、两魔法師、两妖龍騎士、两飛龍騎士、两邪法師、两魔劍戰士、一祈導師、一咒術師、两巨斧戰士、两魔術士、一魔導師、一魔祭師、两邪劍戰士、两魔鎧戰士与两獸骨騎士；五名行为 0、二十四名行为 2。模块 25 没有本关关前剧情三元组，表内 `MAGIC/76` 无可达消费者；round 1 处理器 `1000:511B` 只播 SAY/0080 十检查点，现场 999 没有专属剧情，999/1000 只路由模块 27／stage 37。静态模板、逐关处理器、动态棋盘目录 `0/1/6/11/20/21/22/42`、只覆盖 stage 11 的完整回合特殊生成链，以及只在 stage 30 进入的击杀替补／形态转换链共同证明本关无增援、替补、形态转换或难度追加敌军。机器只检查 side 2 槽 1 碧娜維姬离场胜利、side 1 槽 0 妮雅离场失败且失败优先，其余二十九敌无需离场；目标表键 36 的 SAY/0104“打敗「碧娜維姬」／「妮雅」戰敗”一致。玩家音乐 `19→18`、敌方 `13→12`；1000 不重播，下一标题 SAY/0154“究極女神” | C | B/0073 SHA `8dfe4efa…c13`；stage 36 处理器 `1000:511B` SHA `1a1728a2…458d`；SAY 80/104/153/154；`battle-templates.json/battle-objectives.json/battle-lifecycle.json/stage-events.json/story-presentations.json/music-catalog.json/campaign-roster.json`；`ai-decision-system.md` | 无；复刻须保持直接部署、二十九项名单、妮雅固定、二十七开放格、三十名静态敌军、碧娜維姬行为 1 哨戒、其余行为 0/2 追击、单首领目标、无胜利 SAY 与五通道无动态增援结论，不得改成全灭战 |
| BAT-080 | 场景 37“究極女神”使用 B/0075：名单标志覆盖槽 `0–21,25–31` 共二十九项；妮雅槽 0 固定在 `(23,17)`，其余二十八槽为候选，二十六开放格位于 y=15/16 的 x=`19..27` 与 y=17 的 x=`19/20/21/22/24/25/26/27`，故最多二十七名 side 1。side 2 只有槽 56 頭 `(23,11)`、槽 54 手 `(22,12)`、槽 55 手 `(24,12)` 三个静态部位；槽 54/55 的静态职业／描述符存在回退边界，但专属链均把两者作为手。三部位普通难度攻 100、防 10、生命 10,000、移动记录 1，最高难度攻 150、防 15、生命 15,000；专属链不移动并按頭→左手→右手行动。頭独立从高級回復开始与高級冰雪交替；两手共享状态，从究級落雷开始与究級炎暴交替。手部目标按 side 1 棋盘顺序逐项调用 `randomBelow(5)`，结果 0 时选中，无命中则重新扫描。頭／手免疫冰雪的位移／行动禁用、`LA` 混乱与 `IP` 施毒；`SA/SD/SN` 仍写攻击下降、防御下降与禁咒，其中攻防各降 20。`SN` 只免疫 `1P` 龍，`2P/3P` 頭／手虽显示并递减禁咒状态，但专属分发 `1000:17A7` 不读取普通技术流程 `1000:19DD` 的禁咒门禁，所以四种专属行动不受影响。HUD 隐藏九个数值字段但仍绘生命／经验槽。模块 25 没有关前剧情三元组，`MAGIC/77` 无可达消费者；round 1 处理器 `1000:515C` 只播 SAY/0081 七检查点，现场 999 无专属胜利 SAY，999/1000 路由模块 25／stage 49。五条生成通道共同证明无增援、替补、形态转换或难度追加单位。机器要求全部 side 2 离场胜利、妮雅离场失败且失败优先；目标表 SAY/0105 仍写“打敗碧娜維姬”，与机器三部位全灭条件冲突。玩家音乐 `33→32`、敌方 `5→4` | C | B/0075 SHA `43629082…abe`；stage 37 处理器 `1000:515C` SHA `62678f37…be3`；SAY 81/105/154；`battle-templates.json/battle-objectives.json/battle-lifecycle.json/stage-events.json/music-catalog.json/technique-rules.json/campaign-roster.json`；`special-unit-behavior.md`、`remaining-technique-presentations.md`、`ai-decision-system.md` | 无；复刻须保持二十九项名单、妮雅固定、二十六开放格、三静态部位、专属数值／行动顺序／术法切换／随机目标、原版状态／控制矩阵、九字段隐藏、全 side 2 目标、无胜利 SAY、五通道无动态增援与 stage 49 原生结局链入口；不能按 SAY/0105 误改成单部位目标，也不能跳过冻结的结局链直达 stage 38 |
| BAT-081 | 场景 38“異世界”使用 B/0077：44 名静态 side 2 中，活动槽 `2..7,15..24` 共十六项的敌方角色描述符画像不是 `FFh`，战场 HUD 分别显示葛蒂拉斯、庫安梅伊、艾西柯羅、菲伊魯茵、芙瑪羅妮、蕾娜吉芙、哈釘、娜米、梅蒂、萊莉、西艾蕾、克諾絲、麗蘭特、菲尼雅、阿莉絲、瑪西爾及各自 `D/0,12,6,25,11,24,15,20,16,19,5,4,28,29,30,31` 肖像。其余活动槽恰为 `30..57` 共二十八项，描述符画像均为 `FFh`，模块 29 按当前职业同时回退单位名与职业肖像；不能把全体 44 人统一显示成职业身份，也不能为这二十八项虚构角色身份 | C | B/0077 的 side 2 unit-slot/class map；模块 29 敌方 60 项角色描述符指针表 DS:`32ADh`、`0000:5087/510C/51EC/51B9`；`battle-templates.json#stage=38.activeUnitInstances`、`campaign-roster.json#displayResolution.enemyActors/rule`、`unit-descriptors.json` | 无；复刻生成内容须逐槽解析描述符，保留十六名历代角色与二十八项原版职业回退边界 |
| PRES-001 | 模块 33 的战后名册固定展示妮雅至爱欧里雅 22 张战绩卡，第 23 项爱莉欧拉以 `FFh` 画像终止；显示姓名、兵种、等级、生命、攻击、防御与 `KILL_ALL[序号]`“战绩”，并以当前职业记录直接读取 `M_00/frame 0` 左侧普通全景战斗图形；每卡最多 400 tick、任一动作可提前翻页 | C | 模块 33 `02FAh/03B2h/042Ch/0492h/0526h/06C2h..0849h/084Ah/090Eh..0B6Ah/0F34h`；DS:`0556h` 58 项描述符与 `02E1h..033Bh` 标签；`A/9,D/<画像>,C/随机,M_00/<当前职业>,UN/6–8`；专题合计 23 段签名与 `ending-presentations.json` | 无；`KILL_ALL` 只按可见语义命名“战绩”，不外推为击杀数；模块 33 不走普通战斗的魔劍 `Y_00/41` 重映射 |
| PRES-002 | 模块 35 依前 23 槽的骑兵/战士/法师计数、`SAVE_NUM>100` 与否、75 项战绩总和是否大于 100 选择四段结局中的 8 段文字和 `UN/0..20` 插画；骑兵赢并列，战士赢与法师的并列，法师必须严格最大 | C | 模块 35 `02FAh/0454h/0470h/049Bh/0529h/069Eh/072Fh..084Fh`；8 段内嵌 Big5 文本；模块 29 `1000:88ADh–88B4h` 的存档次数生产者与 `9252h–9260h` 的战绩生产者（只在击杀分支 `921Ch` 内可达，镜像反击 `9161h` 无该尾巴）；`MUSIC/40,UN/49`；`ending-presentations.json` | 无；阈值是严格 `>100`，不得写成大于等于 |
| PRES-003 | 模块 46 用 `B/88` 的 20 个职务帧和 `C/33` 的 22 个姓名帧组成七页不可跳过的制作人员表，共执行八次 400 步 CRTC 转场；随后 `UN/54` 五帧永久循环且无退出输入，调用后的 `module27/stage38` 写入不可达 | C/S | C：模块 46 `03CEh/0433h/0467h..06DDh/06F6h/0725h/0736h/078Eh/07BAh/07E8h/0DC6h`、资源帧与控制流签名、`ending-presentations.json`；S：22 个姓名由原生 PNG 人工转录，`C/33` 第 2 帧的部件形态并由作曲者罗马字资料交叉验证为“蘇泓漳” | 无；逐帧 PNG 仍是文字权威证据 |
| PRES-004 | 普通攻击由 `DS:1119` bit0 选择地图/全屏表现；地图受击为 `UN/62` 九次绘制和两次 `E/38` 请求，死亡为 `MAGIC/12` 清格前 6、清格后 9 个描述符；全屏以职业记录选择 `M_00/Y_00` direct/+50 图形和两套五槽 `E` 表。左右合成器共用 `channelX-xAnchor / channelY-height+yOffset`；`A2E4/A377` 都将出手者初始化为 `(250,135)`，对手按出手侧从 `(650,135)` 或 `(-150,135)` 入场，反击重新初始化人物通道而不继承先攻落点。第 0 关士兵/骑兵在伤害 `<=10` 时使用 `frame 3 + V2/E0` 格挡，在 `>10` 时使用 `frame 1 + V1/E2` 常规受击；零生命仍先执行该阈值段，再执行 `frame 2 + E/11` 死亡段。记录 18 迅龍騎士两侧 `direct frame 3` 的纵向表均为 `-16`，合成器原样相加且共享格挡流无反向位移，所以发布版格挡位图悬空 16 像素。全屏窗底两条生命条各宽 210 像素，每点一像素并每 210 点按红→蓝→绿→浅红换层；命中时一次更新，状态框生命数字冻结。魔劍戰士 1/51 显式重映射到 `Y_00/41、42` | C/U | C：模块 29 `0220h/0254h/9135h/91C5h/926Bh..9296h/9852h/9E28h/9E8Ch/9EEDh/9FC4h/A01Bh/A17Bh/A1E8h/A23Eh/A2E4h/A377h/A52Fh/A6B5h/A77Fh/A7A4h/B0FFh/B29Bh/B683h/B6BDh/B725h..B78Ah/1000:6ABCh/6B58h`、DS:`8824h/A9DFh/D539h`；28 段代码、5 段数据签名；39 职业、158 图形记录、37 音效记录；`combat-presentations.json`；第 0 关实机录像逐帧/音轨互相关 | C：资源号、帧/step 表、统一合成坐标、先攻／反击初始化、生命条几何/分层/更新时间、受击阈值与两阶段死亡顺序、迅龍格挡纵向落点、native tick（标称 10.000151 ms）、等待计数和命令同步；U：全屏渲染子步/VGA 同步的宿主时长、其余命令字段的原设计名称 |
| PRES-005 | 三类射击始终走棋盘表现且不读取普通攻击的全屏开关；弓/弩共用 `UN/60` 的“空→八帧→空”10 次绘制、每次 6 tick，动画后才逐点扣血；魔弓以 `MAGIC/83` 配 `UN/60` 沿目标到射手路径反向增长，再做八步收尾，总等待 `(路径格数+8)×20` tick。无防魔时目标受两次 `floor(base/2)`、其余合资格线格一次；有防魔时增长半伤先被阻挡并清盾，所以选中目标只受第二次半伤、其他盾保护线格受 0。迅龍骑士闪避在玩家侧用无声 `UN/60` 60 tick，AI 侧用 `UN/62+E/38` 90 tick/两次请求，均不扣血 | C/U | C：模块 29 玩家 `719Bh/722Bh/7264h/CCA4h`、AI `1000:1F7Bh/1FB6h`、公共射击 `1000:747Ch`、线形链 `1000:64A6h/64F5h/6532h/659Bh/7E09h/7E3Bh/7DECh` 与 `0000:642Bh/64A1h/783Dh/5491h`；19 段代码、3 段数据签名；`UN/60、UN/62、MAGIC/83、E/38`；`shooting-presentations.json` | C：入口、资源、逐帧 tile code、伤害/防魔/等待同步、标称 tick 时长和玩家/AI 不对称；U：末尾 `82h` 高位渲染角色的原设计名称 |
| PRES-006 | 落雷、炎暴、冰雪、治疗、回復共 18 项动作直接使用棋盘效果描述符；固定等待分别为 L `414/257/348/304`、F `70/120/195/290`、C `120/180/240/300`、H `275/215/235`、I 共用 `255` tick。落雷双相波纹在 A／B 每次绘制后都把范围阈值减 1，让不同范围值的占用格逐 draw 错相进入波带；究级炎暴在 `MAGIC/28` 八项后先载入 `MAGIC/29`，再执行四项直画与五次上移火柱；冰雪从施法者中心由内向外逐圈播放，每圈六帧；五类都在完整图形链后才开始扣血、防魔消费、位移或治疗。炎暴逐点扣血，落雷 `1000:736D` 则对每个目标一次饱和减去范围伤害 | C/U | C：模块 29 五家族入口及公共链 `1000:5BFEh..6FFEh/7166h/736Dh/788Ah..79A8h`、`0000:8D18h..8ECDh/0220h/0224h/6425h/642Bh/64A1h/64B7h/64D6h`；49 段代码、9 段数据签名；25 图形、8 VOC、10 联系图；`technique-presentations.json` | C：资源、描述符、VOC 请求点、`0220h` 直提与 `0224h` 战斗音效门、固定等待、伤害更新时间、标称 tick 时长与规则同步；U：低层字段原设计名 |
| PRES-007 | 剩余 15 项技术表现已闭合：三种踏地各做 33 次程序双帧绘制、4 次 `MAGIC/82` 请求和 13 个显式 tick 后才伤害；九种状态/净化固定等待为 `165/300/300/150/165/165/290/250/225`，完整表现后才免疫/写状态；祈祷逐合资格单位程序绘图、显示结果、写规则并最多停留 60 tick；工兵无独立动画/音效，普通移动演出结束后直接写最多四个邻格地形字节（休眠分发表另有五字节横向写入器，正常入口到不了，详见 BAT-024） | C/U | C：模块 29 `CD6Dh..CD9Dh/CE1Eh..CFC6h/CAE1h..CB0Ah`、`1000:11BCh..1477h/591Ch..5BFDh/74FEh..7CD5h`；50 段代码、7 段数据签名；15 图形、5 VOC、10 联系图；`remaining-technique-presentations.json` | C：资源、程序坐标/描述符、音效、等待、标称 tick 时长与规则同步；U：低层绘图原语原设计名与 PIT 采样分布 |
| PRES-008 | 模块 23 的标题前 Logo、滚动开场、标题两套素材、难度和继续槽表现均已闭合：Logo 最多 300 tick；开场含 17 行文字、6 次背景切换、1,773 轮/7,092 tick；标题按 40-tick 上部揭示、最多 40-tick 停留、启动 `MUSIC/1`、80-tick 下部揭示，201 个 8-tick 空闲循环后重播完整开场；继续界面用 `A/6` 执行 135 次构造绘制且无归档音效/显式等待 | C/U | C：模块 23 `0611h..13EAh/1455h..1CDFh/20A0h/22B9h/31B4h..31E0h/40F0h`；31 段代码、5 段数据签名；19 图形、2 RIX、97 复原图、4 联系图；`title-presentations.json` | C：资源、原生调色板、绘制/翻页/揭示顺序、RIX 起止、固定等待、标称 tick 时长与空闲重播；U：VGA retrace 宿主时长、低层绘图原语原设计名、主机音频漂移 |
| PRES-009 | 模块 25/29 剧情表现已闭合：两套解释器共享 15 条有效命令，模块 29 另有 `DL`；上下窗首次展开 11 步、关闭 12 步。`A/18` frame 0–2 构成肖像框，frame 3–11 依 11 步展开拼成 `400×86` 文字窗；每步先由 `0000:1032` 用 `0000:1B50` 把两条 `56×100` 边条（`leftOuter-17`／`rightInner+7`）从背景带还原到合成页，再画贴图，最后只发布这两条边条到可见页，因此窗顶不会累积每步的转角顶饰；模块 25 窗锚点为 `(153,2)/(97,260)`，模块 29 为 `(153,10)/(97,250)`，文字内缩 `(12,12)`。ASCII/Big5 步幅 8/16、每字 8 tick、换行 20；`PP` 绘 `BK/<id>` 于 `(160,80)`，`HU/HD` 载入 `D/<id>`，`W-` 载入 `A/18`，模块 29 的 `ED` 恢复战场。模块 29 的“說話”开关预载 `MAGIC/57..71`，每个非排除标点 Big5 字按字符码模 15 选声并等待播放；66 项肖像元数据与 68 个肖像主帧已恢复；SAY 74 利用 D/67 元数据查找失败后沿用 D/56“龍王”的布局/名字完成变色；69/116/117/118 已确认为无生产者归档 | C/U | C：模块 25 `05F2h/0736h..145Fh/1A18h/2B2Ch/391Ah`，模块 29 `023Eh/4F41h/BAB8h..CAC3h/D3B6h/EA04h/1000:2E77h`；34 段代码、12 段数据；97 张调色板正确图、4 联系图；`story-presentations.json` | C：命令、几何、资源、原生调色板、RIX/VOC、等待、标称 tick 时长、恢复和全发布版选择器；U：低层 VGA 拷贝原语的原设计名（其描述符布局与边条语义已闭合） |
| PRES-010 | 部署错误与普通战斗终局反馈已闭合：三条可达部署错误在 `(2,328,636,20)` 底条立即绘制，仅新主操作关闭；普通胜利/撤退/失败/退出复用 `A/18` 与 `D/45/46`，保留 8-tick 字形、`MAGIC/57..71` 逐字声、确认菜单和状态路由。胜利五槽存档直接写入且无覆盖二次确认；失败只接受新主操作并回同关部署；`DS:059F` 固定短句无发布版入口。模块 27 的 `DS:0005` 已通过共享父描述符导入链确认为“按鍵”开关 | C | 模块 27 `0301h/041Eh/052Eh/0E63h..0ED2h/1A1Ah/271Ch`、DS:`0F1Bh..0F7Bh`、`UN/39/40`；模块 29 `0054h/023Eh/02E4h..0693h/08DDh/3326h..3889h/4A74h/5651h/6DB8h/B9D2h/C9B9h/EA04h/1000:03D1h/0520h`、DS:`059Fh..06A1h/1B97h..1C1Fh/3F2Ch/4475h`；30 段代码、6 段数据、16 VOC；同一 `MF` 父描述符 `+12h` 的四字节等序导入；全 near/far/pointer 入口扫描；`feedback-presentations.json` | 无 |
| PRES-011 | DS:`01A9h` 的 `50×50` 范围图同时驱动表现与输入：非零格完整绘地形，零值格四个位平面逐行与 `11h/44h` 相与只保留 25% 像素；所有手动选格先要求同一字节非零，再检查动作目标。普通攻击只标四邻敌军、单目标自动锁定；移动要求空格；射击 seed 5/8/6 并清自身/四邻；技术/工兵走专用验证；退出填全 1。DS:`58A5h` 则是描述符投影的独立 `10×7` 地图特效 sprite 层 | C/U | C：模块 29 `388Ah..47E9h/7097h..77CCh/785Eh/7C00h/7E2Ah..7F2Ch/8273h..82CCh`、`1000:39D4h..40CCh/84E0h..8595h`，DS:`58A5h..58EAh`；34 段代码/1 段数据；B/0 实际地形核验；`range-presentations.json` | C：范围生产、零/非零像素、动作输入门、恢复和图层边界；U：动态描述符 `0205h` 与高位码 `81h..83h` 的原设计名称 |
| PRES-012 | 特殊逐关表现已闭合：`51E5h` 只改棋盘内存而不刷新，`834Ah` 以钳制视口逐步聚焦并在终点重绘高亮；`7DF7h` 构造最多 100 格的目标→源路径，等价邻格按 PIT mod 3 排序，每格复制占用、重绘视口/单位/小地图/指针并执行 VGA 翻页，同时异步播放 5000 Hz、1.261 秒的 `E/14`。场景 `0/1/6/11/20/21/22/30/42` 已逐操作编排；场景 30 的 `22h` 是“我．．．我好難過．．．／頭好痛啊！”上下文短句并在每次形态转换前重播，场景 42 继承 PRES-006 的 4L 304-tick 表现 | C/U | C：模块 29 `0000:0249h/48B8h/7C50h/7C5Fh/7C8Bh/C97Ah/C97Eh`、`1000:51E5h/533Eh/7DF7h..849Ah`；21 段代码、2 段数据签名；DS:`84BBh[22h]→8762h`；`E/14` SHA-256 `f16bdce41ef0dc30b0dc93429322ea94fb33c64503776ef99bc58cd3088c343e`；`stage-presentations.json` | C：九关调用顺序、首次可见边界、路径构造、资源/声音请求、重绘、翻页与标称 tick 时长；U：PIT 等价路径的采样分布、VGA 回扫宿主时长 |
| PRES-013 | `WD` 使用 `MAGIC/19` 十帧及 `1..10,82h` 十一个描述符，无直接 VOC，等待 `(路径格数+10)×20` tick；第 26 关敌方阶段尾每次先以 `MAGIC/21` 13 个描述符、`MAGIC/14` 4 个描述符和 26 次纵列扫过合计 385 tick，再将首个 side 1 单位所在列扫描带内单位向下推 1–3 格，固定执行两次且无伤害/状态 | C/U | C：模块 29 `0000:6392h/64ACh/64B7h/6548h/CC3Bh/CCD2h`、`1000:24A5h..254Eh/660Eh..673Fh/7CD8h..7DEAh`；21 段代码、3 段数据签名；`MAGIC/14,19,21` 共 51 帧、3 张联系图；`wd-stage26.json` | C：资源、描述符、等待、标称 tick 时长、结算与两图移动；U：`WD` 等价路径 PIT 分布及玩家可见原名 |
| PRES-014 | 记录 36「龍」、37「頭」、38「手」有完整的普通全屏表现，但只在 side 2：三条 side 1 块 `DS:BD3B/BD93/BDEB` 的 `w1..w11` 是 2 字节递增地址且全部落在零区，`w4/w5` 的 step-count 表长度为 0；三条 side 2 块 `DS:E887/E951/EA87` 与普通职业同构，含有效 step-count（`5,5,12,9` / `4,9,5,8,6,3,1,1,1` / `6,10,4,4,4,4,4,4,4`）、击后表、五槽音效（`2,0,14,6,0` / `2,0,14,57,4` ×2）、逐帧锚点和六个命令指针，且复用共享表 `DS:87F6/8812/8824`。右侧图形 `Y_00/36..38`（各 4 帧）与 `Y_00/86..88`（2/7/2 帧）全部存在；`Y_00/87` 的 7 帧与 37 攻击流的帧上界 6 精确吻合。只有 side 2 有数据的原因是编队：龍只在场景 20/22 的 side 2 槽 28，頭与两只手只在场景 37 的 side 2 槽 54/55/56。`E/57` 的唯一引用方即 37/38 的 side 2 五槽 | C/U | C：模块 29 DS:`BD3B/BD93/BDEB/E887/E951/EA87/E8BF/E9AB/EACD/E969/E977/EA9F/EAA3`；`combat-presentations.json` 的 `side2OnlyRecords`；`battle-templates.json` 的 `unusualClassOccurrences`；`Y_00/36..38、86..88` 渲染帧 | U：反击（`mainLeftOrAttacker` +50 流）可达性目前由“逐单位禁用字节为 0”推得，标记 `[H]`，未实机复验。2026-08-01 更正：此前 `not-applicable-original` 的结论只检查了 side 1，属错误 |
| PRES-015 | 全景战场背景每次普通攻击重选一次，不是每关常量：`0000:90D8` 写入攻守格后先调 `0000:95F8`，按当前关卡 DS:`2E77` 线性扫描 DS:`78DC` 的 42 项 `{stage, record}` 表（步长 4、`FFFF` 结束，未命中时游标回到表头，因此未登记关卡沿用表首 `C/5`），把 `C.SWF` 记录写入 DS:`77B2`；除关卡 `23/24/6` 外再调 `0000:962E`，以受击格 DS:`77C1` 读 DS:`01A7` 的原始地形 token，经 DS:`2E7D` 与 `UN/0056` 第一页（段指针 DS:`0026`）解析逻辑地形槽后改写记录：槽 1→`C/18`、2→`C/16`、3→`C/17`、5→`C/20`、6→`C/21`、7/12/8→`C/19`（`9696` 另比较 DS:`2E77`，关卡 10 改为 `C/28`）、9→`C/29`。`9676` 第二次比较槽 8 并指向 `C/29`，但槽 8 已在 `9667` 命中，该分支不可达。选择只做一次，反击复用同一背景；`9881` 以 `bx=1` 从 `C.SWF` 装载。`C/0..31` 均为 448×148，其中 `C/0,3,5,7,9,11` 是同一幅走廊按墙面调色板索引 6/4/7/9/3/2 出的六种配色，`C/19` 与 `C/29` 在容器内逐字节相同，`C/17` 与 `C/27` 各多解出一行、被定长拷贝丢弃 | C/U | C：模块 29 `0000:90D8h/95F8h/962Eh/964Eh/9681h..96C0h/9852h/9881h`、DS:`78DCh..7984h/77B2h/77C1h/2E77h/2E7Dh/01A7h/0026h`；3 段代码签名与逐条跳转目标核对；`reverse/decoded/C` 32 条记录尺寸；`full-combat-backgrounds.generated.ts` | C：表、比较链、不可达分支、装载容器与素材。2026-08-15 更正：此前 `ordinary-combat-presentations.md` 记为“第 0 关背景取自 `C.SWF/0`”属错误，原生表值为 `C/5`；U：`C/1,2,4,10,13,26,30` 等未被本链引用的记录只由模块 33 战绩卡随机装饰引用 |
| PRES-016 | `E/14` 是所有棋盘移动共用的走路声，不是逐关脚本移动专属，同时闭合了「移動」开关 `DS:10EC` 的唯一消费者。`0000:0249` 是 `test byte [10ECh],1` 的门控 VOC 包装器，全模块只被共用播放器 `1000:7F72` 的 `1000:7F9A` 调用；`7F72` 先查 `DS:77AF=='N'` 则跳 `1000:805B` 静默返回，否则以 `BX=3/CX=0Eh` 载入 `E/14` 并在第一格之前一次性异步提交，不逐格重播。`DS:77AF` 由建路函数 `1000:7E09` 在 `cmp si,di` 相等（起点==终点）时写 `'N'`、否则写 `'Y'`，聚焦原语 `1000:830F` 也直接写 `'N'`。进入 `7F72` 的业务已穷举为五条：玩家 `移動`（`0000:738F/7394`）、`1N/半龍戰士` 直连技術（`0000:74DF/74E4`）、工兵構築 `772Dh`（`0000:7770/7775`）、双方 AI 移动（`1000:1B1D..2202` 十对）与逐关脚本移动（`1000:7DF7` 13 处）。取消移动没有反向走路，因此原版不存在返悔走路声 | C | 模块 29 `0000:0249h/0254h/7310h/738Fh/7394h/7475h/74DFh/74E4h/772Dh/7770h/7775h`、`1000:7E09h/7E27h/7E31h/7F12h/7F22h/7F4Ah/7F72h..7FA0h/805Bh/830Fh`；`DS:10ECh` 全模块唯一 bit-0 读取点；按线性地址枚举的 `9A` 远调用审计；`E/14` SHA-256 `f16bdce41ef0dc30b0dc93429322ea94fb33c64503776ef99bc58cd3088c343e`；复算命令见 `movement-terrain-rules.md` | 无；本行细化 PRES-012 对 `E/14` 的绑定范围。2026-08-17 更正：此前只把 `E/14` 记为逐关脚本移动的声音，普通玩家/AI 移动被漏记 |
| SAVE-001 | `WAR0.TST` 至 `WAR4.TST` 是五个编号存档槽文件；战斗侧栏与系统菜单均可进入编号读/写选择器 | C | 模块 `3C70h` 循环变更文件名数字；`3CE2h` 逐个只读打开并读取 50 字节；`BA18h/BA22h/B941h` | 槽位视觉布局与覆盖提示的完整时序 |
| SAVE-002 | 槽位列表从头部 `12h/14h/16h/18h/1Eh` 读取“職業/等級/經驗值/儲存次數/難度”；职业码查表为职业名，三项数值右对齐，难度使用四个原版标签 | C | 模块 23 DS:`0A35h..0A79h`、`17ADh..195Fh/3C9Ch`；模块 29 DS:`1B97h..1C1Fh`、`3410h..36AFh/1000:8882h`；`title-flow.json`、`save-state-semantics.json` | 无 |
| SAVE-003 | `.TST` 首个 `u16` 是压缩流绝对结束偏移；保存器写出该值再加 18 字节 | C | 模块 29 `8AA2h` 保存压缩目的偏移、`8A54h` 写首字、`87F2h` 以 `CX=首字+12h` 写文件；六样本吻合 | 尾随 18 字节按原样保存，不赋予校验语义 |
| SAVE-004 | `JUST.TST` 是模块 27 在部署完成后为下一场重新生成、由模块 29 在 `CONTINU='N'` 时使用的开战模板；偏移 7,952 是地形描述表，双方 75-word 数组是逐槽 AI 行为；残余 `FFh` 在开战前清零 | C | 模块 23 新游戏写 `CONTINU='N'`；模块 27 `0FF8h` 创建/序列化/XOR/RLE；模块 29 `4BD9h/5386h/543Bh/55DBh` 载入链；固定解码 8,358 字节 | 无格式级未知；特殊关动态覆写归场景逻辑 |
| SAVE-005 | `.TST` 使用文件偏移 `02h..11h` 的 16 字节循环 XOR 和 `C0h` 高位标记 RLE；写端只覆写 15 个 key 字节，代码段游标不是随机源，第 16 字节是缓冲残留 | C | 写端 `8A54h/8AA2h` 与读端 `108Fh/10C1h`、`560Bh/563Dh`；key 游标 `8B3Eh/122Eh`；六文件完整声明流严格解码，第 16 字节含 `55h/A8h` | 无 |
| SAVE-006 | 五个 `WAR` 均解为 11,972 字节；双方 1,440 字节块各是 60×24 字节单位动态状态，含生命、累计经验、两个无规则消费者兼容字和八个状态；772 字节尾段含双方 AI 行为、设置、经验奖励计数、逐槽行动状态、重叠残留，以及通用菜单颜色属性和原点；DS:`111Dh` 是隐藏 tick 等待门，DS:`1165h` 无消费者 | C/U | `5087h/510Ch/540Dh`、`35F2h`；DS:`31B9h/45D0h/4648h/464Ah/4BECh/4C64h/4C66h`；`29A2h/D3B6h/5C71h/5C88h/8EE6h`；读写链；五样本；`save-state-semantics.json` 16 段代码/1 段数据签名 | C：全部运行时行为和兼容策略；U：`04h/06h/1165h` 的原源代码名称，不阻塞实现 |
| SAVE-007 | `WAR` 解压偏移 0/2/4/6 依次为视口左上 X/Y、战场光标/焦点 X/Y；五个样本可重构真实小地图视口 | C | DS:`5390/5392/538C/538E`；`2681h/7CE2h/7CF1h/7D34h/7D6Dh`；五张存档态小地图 | 无 |
| SAVE-008 | 保存器把 `SAVE_NUM` 先加一再写“儲存次數”；保存前的 side-1 槽-0 预载只刷新 `DS:318E` 经验，不调用 `51ECh` 重算 `DS:319D/31BD`，所以“職業/等級”可来自先前工作单位而“經驗值”来自 side 1 槽 0 | C | 模块 29 `0000:5083h..5299h`、`1000:8882h..88CAh`；五个样本的头部经验与解压体 side-1 槽-0 累计经验逐一相等；`save-state-semantics.json` | 严格兼容保留；改良模式如修正须标为偏离原版 |
| SAVE-009 | `.TST` 无 magic、版本、校验和或旧版迁移；标题只处理打开失败，读写器不检查短读/写入结果，XOR 多处理 50 字节，RLE 不检查源/目标边界。损坏行为是未定义内存访问；安全 Web 导入必须结构硬拒绝，不模拟内存破坏 | C | 模块 23 `3C70h..3D1Fh`；模块 27 `0FF8h/1144h/1192h`；模块 29 `0DD0h/108Fh/10C1h/5386h/560Bh/563Dh/87F2h/8A54h/8AA2h`；21 段签名、六样本、12 项损坏语料；`save-robustness.json` | 尾随 18 字节只作写器指纹；现代版本/校验信息放在 legacy payload 外层 |
| SAVE-010 | `WAR` RLE 原生目标为 11,964，末游程把五样本写到 11,966/11,967 后停并忽略 5–7 个压缩字节；差异只在 256 字节无消费者填充。`.TST` 不保存 PIT/RNG 状态或输入日志，不能单独确定重放未来随机分支 | C | `1000:10C1h` 目标常量与六样本逐命令模拟；`0000:95D2h`、`1000:2016h/5BE6h/7460h` 的 PIT/未序列化累加器；`save-robustness.json` | Web 重放需扩展 PRNG 状态/调用序号、语义动作日志、规则与 Mod/内容哈希 |
| SAVE-011 | 失败与主动撤退都回 `module 27 / currentStage` 并重建同关模板，但不恢复入关前的 `ME_DATA/ME_EXP`；本次尝试写入的经验、升级与转职成为下一次同关部署输入，可据此无限练级 | C/S | 模块 29 `053Bh/057Fh/6DC1h/4AADh` 的同关出站；模块 27 side-1 战役职业导入与 `0493h` 开战预处理；GO `ME_DATA/ME_EXP`；用户 2026-08-04 原版流程复验 | `stableRemake` 的回滚快照必须登记为规则偏离，不能写回原版事实 |
