# 第一阶段用户评审清单

日期：2026-08-08
对应 GDD：Draft 0.47
状态：证据已就绪，`implementationFrozen=true`

## 审计结论

第一阶段的“全部可提取资源 + 原版 GDD 复原”已经达到证据评审条件：28/28 原始文件完成哈希与分类，14 个索引容器的 1,282 个有效记录全部拆出，2,249 个内嵌流完成解码；标题、剧情、战斗、HUD、存档、兵种/转职、AI、逐关事件和终局均有机器规格。

在工作区根目录运行 `node reverse/tools/angel2-phase1-verify.mjs` 可一次复核原文件哈希、全部 JSON、Markdown 本地链接、资源覆盖、兵种/转职、九模块计时和证据审计；当前验收结果为 `phase_1_evidence_baseline_verified`。

`phase1-residual-audit.json` 对证据登记做了全量检查：

| 项目 | 数量 | 结论 |
| --- | ---: | --- |
| 证据登记总项 | 139 | 全部已分类 |
| 纯 C | 120 | 原生代码、数据或资源直接确认 |
| C/U、C/T、C/S | 19 | 全部是不阻塞确定性规则的保留边界 |
| 实现必需未知 | 0 | 无需猜测玩法规则即可进入下一阶段规划 |

19 项混合等级只包含：VGA/音频宿主时序与 PIT 分支采样分布（含迅龍闪避／獸骨反击的实机频率）、低层/原设计名称、一项制作人员姓名人工转录，以及 `SM/FM`、行为 12 场景 9 两类原生未定义行为。四条无生产者 SAY、固定倒下短句、弓兵 7→弩兵 8、模块 27“按鍵”门和标称 tick 均已离开保留项。剩余边界不能被伪造为确定规则，也不要求 Web 普通模式模拟内存破坏。

## 原版怪癖与复刻决策

原版事实不因修正版选择而改变。2026-07-15 的首轮用户评审已形成以下决策；完整合同见 [`web-remake-rule-decisions.md`](web-remake-rule-decisions.md)。

| # | 原版事实与解释 | `stableRemake` 决策 |
| ---: | --- | --- |
| 1 | 转职提交函数只改职业并清经验，下一次常规派生随即更新职业属性；玩家可见结果本来就是转职后属性更新 | 已确认：合并为一次原子重算，保留当前生命，不治疗 |
| 2 | 普通射手 AI 若当前格是距离 2 候选中的最佳格，会把“不需要移动”误判为重定位失败并跳过射击；行为 1 另有原地射击入口 | 已确认：当前格有效时直接原地射击 |
| 3 | 远追只检查敌人下方 `enemyCell+50`，行为 2 又固定用 side 1 模式 `A` 作预检查 | 已确认：考察四邻并使用行动方阵营语义 |
| 4 | 原版毒为 `floor(生命/2)`，可产生 0 生命但不移除的单位 | 已确认：改为 `max(1,floor(生命/2))`，毒不致死 |
| 5 | 普通伤害的地图路径错误读取防魔，全屏路径不读取，表现开关因而改变规则 | 已确认：普通物理伤害一律忽略防魔，只有明确魔法效果读取 |
| 6 | 魔弓目标受两次半伤；防魔挡第一次并随后被消耗 | 已确认：保留 |
| 7 | `.TST` 不保存 PIT/随机未来，因此不是确定性重放点 | 已确认：Web 存档保存版本化 PRNG 状态；原版存档导入后进入稳定 Web 随机流 |
| 8 | `field5`、动态记录 `04h/06h` 等字段没有已知规则消费者 | 已确认：原样往返，不赋予默认战斗效果 |

## 原生未定义行为的安全边界

这两项的错误机制已经确认，但不存在唯一正确的“原版确定结果”：

- 行为 12 场景 9：胜利 999 后沿用 `DI=3450`，PIT 路径可能折回并额外移动，也可能超过 100 项覆写代码段。未来安全默认建议直接设置胜利并消耗动作；内存游走只可进入诊断模式。
- AI `SM/FM`：技能池引用不存在的参数行，查找失败又被忽略，执行器可能读取零值或残留字段。未来安全默认需要作为明确兼容决策记录，不能把 `SM→OJ` 猜测写成原版事实。

损坏 `.TST` 的原版无界 RLE/XOR 同样不应模拟；Web 导入器应按已记录的结构边界拒绝危险输入。

## 门槛签署

八项原版怪癖的 `stableRemake` 决策均已确认。四位平面颜色顺序和 password 专用调色板已在 Draft 0.46 修正，骑士／战士末端职业专属分支在 Draft 0.47 闭合。仍请按以下三项完成 Draft 0.47 门槛评审：

1. 是否接受 `original-gdd.md` 作为“原版事实层”的当前基线；如不接受，请指出需要继续取证的系统或结论。
2. 是否接受上述 19 项混合证据均不阻塞下一阶段；其中 VGA/音频宿主表现与 PIT 分支分布可在以后通过 DOS 实机录像校准。
3. 是否同意结束第一阶段。只有明确同意后，才解除开发冻结并另立 Web 架构、兼容模式与 Mod 设计文档。

本清单不等于授权开发；在第 3 项得到明确同意前，下一轮仍只做证据修订、审阅或额外取证。
