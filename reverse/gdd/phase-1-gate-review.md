# 第一阶段门槛复核

日期：2026-08-08
对应 GDD：Draft 0.47

## 当前结论

“全部原始文件归类与可提取资源落盘”子目标已经达到：28 个原始文件均有哈希和类别，14 个索引容器的 1,282 个有效记录已全部拆出，2,249 个内嵌流已解码，`AG2.JS3` 也不再是未分类文件。机器覆盖表的 `unresolvedFiles` 现为空。

第一阶段证据现已达到用户评审条件，但在用户明确签署前仍不视为结束。完成性反证审计发现的 `WD`、第 26 关阶段尾效果、`SET.TXT`、行为 12 特殊分支和未选 `B` 模板均已闭合；Draft 0.46 修正四位平面流到颜色 bit 的全局顺序、全量重渲染素材并恢复 password 专用 DAC 表，Draft 0.47 又闭合骑士／战士十一种末端职业的专属分支及飛龍玩家攻击后移动。当前 139 项证据中 120 项纯 C、19 项混合边界，`implementationRequiredUnknowns=[]`。评审入口见 `phase-1-review-checklist.md`；全战役仍冻结于已有有界例外之外。

Draft 0.26 另已闭合普通攻击的地图受击/死亡逐帧时序、全屏职业图形/五槽音效选择和先攻/反击/死亡编排；普通攻击表现不再计入上述“其余音画”缺口。

Draft 0.27 另已闭合射击的棋盘帧序、魔弓线形尾迹与音效、迅龙闪避的玩家/AI 表现不对称，并纠正魔弓选中目标为两次半伤害。射击表现不再计入“其余音画”缺口。

Draft 0.28 另已闭合落雷、炎暴、冰雪、治疗与回復共 18 项动作：25 个图形记录、8 个 VOC、描述符顺序、声音请求点、固定 native tick 和规则同步边界均已有机器断言。五个主技术家族不再计入“其余音画”缺口；剩余技术表现收窄为踏地、状态施加/净化、祈祷和工兵构造。

Draft 0.29 已闭合上述剩余 15 项技术：踏地、九种状态/净化、祈祷、铁板与障碍共新增 50 段代码、7 段数据签名、15 个图形记录、5 个 VOC 和 10 张联系图。正常玩家菜单中的射击与技术现已全部离开“其余音画”缺口。

Draft 0.30 已闭合模块 23 标题专题：31 段代码、5 段数据、19 个图形记录、2 段 RIX、97 张调色板正确复原图和 4 张联系图覆盖标题前 Logo、17 行开场、六次背景切换、两套标题素材、1,608-tick 空闲重播及难度/继续界面。旧 `NUM/1,14` 绑定已纠正为 `MUSIC/1,14`。

Draft 0.31 已闭合模块 25/29 剧情专题；补充逐字声与全选择器证据后现为 34 段代码、12 段数据、97 张调色板正确资源图和 4 张联系图，覆盖两套解释器、16 条原生有效命令、上下窗几何与动画、肖像元数据、文字节奏、`MAGIC/57..71` 逐字声、等待、背景、剧情 RIX 和战场恢复。`CW` 已纠正为发布版 no-op；176 个 SAY 记录已分为 93 条命令脚本和 83 条文本/标签记录，69/116/117/118 已确认为无发布版生产者的归档脚本。

Draft 0.32 已闭合部署错误与普通战斗终局反馈：26 段代码、6 段数据覆盖三条可达部署错误、底部三层提示条、`UN/39+40` 字形、`A/18+D/45/46` 胜利/撤退/失败/退出窗口、逐字 VOC、确认菜单、五槽存档和状态路由。全 near/far/pointer 扫描又确认 `DS:059F` 短句是发布版不可达归档；当时保留的模块 27 本地声音门标签已在 Draft 0.45 由共享父描述符导入链闭合。

Draft 0.33 已闭合悬停单位详情 HUD：18 段代码、4 段数据覆盖四 tick 延迟与缓存、`640×350` 精确几何、动态 `D` 肖像、职业／单位名、五行原文/数值源、100 像素生命/经验竖条、`A/17` 八状态图标与剩余值、三字符回合框及第 37 关 side 2 的 `?????` 隐藏。DS:`3192h` 到全部导出 `D` 记录的映射仍独立保留，但不阻塞运行时动态索引的复刻；范围/命中叠层不再混入 HUD 结论。

Draft 0.34 已闭合战场范围与目标表现：34 段代码、1 段数据覆盖 DS:`01A9h` 的 `50×50` 临时图、A000/A800 两页逐格缓存、非零完整/零值 `11h/44h` 网点地形、同一字节的点击门、普通攻击/移动/射击/技术/工兵动作绑定和退出后的全 1 恢复。射击表值 `5/8/6` 已纠正为含起点 seed，实际曼哈顿距离是 `2..4/2..7/2..5`；DS:`58A5h` 已确认是描述符投影的独立地图特效 sprite 层，而非范围缓存。

Draft 0.35 已闭合特殊逐关事件表现：21 段代码、2 段数据覆盖 `51E5h` 的只写内存边界、`834Ah` 的视口聚焦、`7DF7h` 的最多 100 格脚本移动、PIT mod-3 等价路径排序、逐格 VGA 翻页和异步 `E/14`。场景 `0/1/6/11/20/21/22/30/42` 已逐操作编排；场景 30 的 `22h` 已纠正为每次形态转换重播的上下文短句，而非独立动画，场景 42 则完整继承既有 4L 时间轴。

Draft 0.36 已闭合范围模式与 `WAR` 保留状态：DS:`1F0Fh` 的 43 个生产点已机器化，模式 `1` 无生产者，十个 `FM` 生产点全部属于场景 `0/1/6/20/21/22/42`；动态记录 `04h/06h` 无规则消费者，DS:`111Dh` 是隐藏 tick 等待门，DS:`1165h` 无消费者，DS:`3DCAh` 为菜单颜色属性和原点。五个存档样本、原生初值和 12 段代码签名均已交叉验证。

Draft 0.37 已闭合五槽元数据：两模块原文一致为“職業/等級/經驗值/儲存次數/難度”，五列偏移、职业码查名、数字排版、难度标签、`SAVE_NUM` 先增后写和保存端快照怪癖均已机器化。五样本全部显示行已结构化，存档语义审计现为 16 段代码与 1 段数据签名。

Draft 0.38 已闭合当前 Joymouse 配置：恢复 128 项 Set-1/ASCII 键名表，确认绑定高位为逐动作重复开关，并以八段原生签名和模块 27/29 消费表验证 Keyboard 4 Way 的 12/12 项。未选用设备的 DOS 硬件轮询不再作为 Web 输入规则门槛。

Draft 0.39 已闭合存档可靠性与确定性边界：21 段代码签名、六个发布样本和 12 项合成损坏语料证明格式无版本/校验、I/O 与 RLE 无安全容错、XOR 多处理 50 字节、WAR 解码目标与完整填充相差 8 字节，以及 key 的代码段游标/残留来源。安全导入策略已分成结构硬拒绝、语义警告/拒绝与 Web 外层版本化；PIT 直接读取和三个未序列化累加器同时证明 `.TST` 不足以确定重放未来随机流。

Draft 0.40 已闭合 `WD` 与第 26 关阶段尾效果：21 段代码、3 段数据、`MAGIC/14,19,21` 共 51 帧与 3 张联系图确认同目标阵营路径 90 点伤害、防魔保留、`(路径格数+10)×20` tick，以及每敌方阶段两次、每次 385 tick 后的纵列 1–3 格下推。魔弓防魔也由 `783Dh→5491h→第二次半伤` 的顺序纠正。

Draft 0.41 已闭合 `SET.TXT`：九份同构加载器只消费 `Y13`，映射 Sound Blaster 数字音频启用、`220h/IRQ7`；七段 DSP/PIC 代码确认硬件身份。RIX 音乐、四个分类开关和 `0220h/0224h` 的请求边界已分开，尾随五字节与非法索引策略也已机器化。

Draft 0.42 已闭合行为 12 的确定部分：26 段代码、3 段数据与 `MAGIC/26` 证明场景 4 每次路线移动尝试后令 seed-3 安全区外 side 1 单位生命减半，固定演出 44 tick；场景 9 则确认胜利 999 后沿用 `DI=3450`，路径器会按 PIT 穿行相邻零缓冲，既可能折回后额外播放 `E/14` 移动，也可能超过 100 项覆写代码段。具体 PIT 轨迹与越界后的可见结果属于原生未定义行为，不作为确定性 Web 规则门槛。

Draft 0.43 已闭合未选 `B` 记录：十个运行模块中只有三条 `B.SWF` 读取生产路径；模块 27 的 44 项表排除奇数 79/81/83，模块 29 只产生偶数，模块 46 固定读取 88。三个未选奇数的压缩和解码内容分别精确复制 21/49/65，故没有独有素材、模板规则或隐藏关卡需要补做；偶数 78/80/82 的场景 39–41 图块读取另有明确保留。

Draft 0.44 已完成剩余证据审计：`1V/2V` 无玩家菜单、AI 池或 direct caller，`3V` 只由魔弓射击生产；地图/全屏普通伤害的防魔差异已提升为 C。机器文件对证据登记 137 项逐条分类，117 项纯 C，20 项混合等级全部属于时序校准、原设计名、不可达归档、人工转录、可选实机复核或原生未定义行为；实现必需未知为 0。

Draft 0.45 已继续消化上述混合项：攻略兵种/转职表与原生值全量对照，原生提交链确认弓兵 7 级转弩兵后为 8 级；终幕姓名纠正为“蘇泓漳”；69/116/117/118 与 `DS:059F` 分别由全生产者/入口扫描确认为不可达归档；模块 27/29 的同一 `MF +12h` 四字节导入证明 `DS:0005` 为“按鍵”。九个发布运行模块的 PIT0 除数 11932、INT 08h 链、本地计数器与等待消费者均闭合，native tick 标称为 10.000151 ms。机器审计现为 138 项、120 项纯 C、18 项混合边界。

Draft 0.46 已闭合颜色复原缺陷：模块 21/25/29 的资源槽 `+0/+4/+8/+0Ch` 均配对 VGA Map Mask `08h/04h/02h/01h`，故资源流 `0..3` 必须映射到颜色 bit `3..0`。8,725 张通用 PNG、标题/剧情专题、战场拼图与派生联系图均已重渲染；妮雅、希蜜和战场截图完成视觉核验。模块 21 DS:`03ACh` 的专用 DAC 表另将密码按钮纠正为红/黄/蓝/绿/紫/白。

Draft 0.47 已闭合骑士／战士普通转职树十一种末端职业的专属分支：九种确认机制、两种无独立分支；新增 `0F/飛龍騎士` 玩家普通攻击后半移动力追加移动的处理器、菜单与调用边界。迅龍／獸骨的 PIT 机器候选已确认，实机频率仍保留为非阻塞校准项。

## 逐项复核

| 门槛 | 状态 | 已完成 | 尚缺 |
| ---: | --- | --- | --- |
| 1. 28 个文件归类、manifest、哈希、重复命令 | 通过 | 28/28 哈希；14 容器；覆盖表；所有解析器可重复运行 | 无 |
| 2. 图像、字体、VOC、RIX、剧情记录完整提取 | 通过（提取） | 8,725 张通用预览已按流 `0..3`→bit `3..0` 重渲染，另有 password 专用色表 81 张、标题专题 97 张、剧情专题 97 张调色板正确复原图；44 张完整战场图；44 张地形小地图；44 张地形+占用图；5 张存档态小地图；普通受击/死亡 2 张、射击 1 张、技术 20 张、标题 4 张、剧情 4 张联系图与范围明暗 1 张核验图；11,168 个字形；71 VOC；59 RIX；176 组 SAY/NUM/CHA（93 命令脚本 + 83 文本/标签）；176 个 B 平面尾部均为零填充 | 无 |
| 3. 一场完整战斗状态流程 | 通过 | `FFh` 部署格、名单/固定单位、19 个部署热区、`JUST/WAR` 开战分支、第 1 回合、玩家→我方自动→敌方 AI、集体命令、状态 tick、胜负、主动撤退、`999/1000`、可选存档、下一关及失败回到同关部署均已闭合 | 无；特殊关覆盖归门槛 6/7 |
| 4. `DATA/MAP` 与核心公式 | 通过（复刻规则） | 39 条记录、side 1/2 描述符、31 条转职边与提交副作用、普通伤害/反击/生命/经验/累计成长；39 项权威合并目录与全量断言；玩家与 AI 射击；13 职业三阶段玩家菜单、36 项玩家分发；14 组 AI 池、33 项 AI 参数、逐槽行为、近战/射击落点、编队/固定路线/追逐；玩家主要技术；八槽状态；`MAP` 23 槽；全部可达范围模式；模式 `1` 无生产者 | 编辑器友好地形名只能另列推断标签，不属于原版规则缺口 |
| 5. `B.SWF` 模板与图形 | 通过 | 39 普通 + 5 特殊/替代映射；8,506 字节模板九段布局；双方逐槽数组是 AI 行为；场景标志是部署名单；128 个 token 已连接到 `40×44` 图块、逐关 `MAP` 逻辑槽和小地图颜色；地形/占用/视口小地图叠加已闭合；44 张完整战场图均无越界引用；B 平面尾部确认为零填充 | 无 |
| 6. `.TST` 与关卡目标 | 通过（复刻规则） | 六文件无损解码；`JUST` 生成/部署/载入链；五槽原文/字段/保存时机；`WAR` 战中角色与 `999/1000`；双方 60×24 字节单位动态状态；772 字节尾段；无版本/校验事实、原生 I/O/RLE 缺陷、安全导入与非确定重放边界；21 段可靠性签名、六样本、12 项损坏语料；84/84 目标表项；38/38 逐关处理器、72 个 SAY 引用、八个动态棋盘场景、七个其他状态场景、九个特殊表现时间轴、场景 30 多职业序列、stage-6 桥接、主结局→追加战路由、剧情解释与战后/终幕表现 | 兼容字原设计名称不可恢复，但行为和往返策略已闭合 |
| 7. 实现必需规则达到 C/S | 通过（机器复核） | 标题/新游戏/继续游戏及完整标题表现、一次性密码门、资源、剧情结构及完整解释器表现、职业/转职、模板/部署、普通单场首尾、目标、普通攻击数值与地图/全屏表现、敌我射击、`WD`、第 26/37 关特殊动作和全部玩家技术的数值/棋盘表现、八槽状态生命周期、AI 主链、全部可达范围模式、范围/目标明暗与地图特效分层、小地图、发布版实体键位、当前 Joymouse 12 项语义映射、战场光标、行动/集体/系统菜单、部署错误、普通胜负/撤退/退出反馈、侧栏、精确单位详情 HUD、`WAR` 兼容状态、骑士／战士末端职业专属分支、38/38 逐关处理器及特殊表现时间轴、战绩卡/条件结局/终幕、九模块标称 tick 与模块编排已有 C/S；139 项残余审计的实现必需未知为 0 | DOS 向量/PIC、VGA/音频宿主漂移、PIT 分支采样分布、未选用的遗留硬件轮询、地形显示名、不可恢复的兼容字原名及原生未定义行为的单次结果不作为确定性 Web 规则门槛 |
| 8. 用户评审结束第一阶段 | 未通过 | Draft 0.47 和评审清单已就绪 | 等待用户明确接受、要求修订或指定额外取证 |

## 当前下一步

1. 由用户评审 `original-gdd.md` 与 `phase-1-review-checklist.md`，决定接受、修订或追加取证。
2. 若追加取证，优先处理用户认为影响原版体验的项目；密码提示前 DOS 向量、VGA/音频宿主漂移、PIT 分支采样分布和不可恢复名称仍默认不作为 Web 规则门槛。
3. 只有用户明确同意结束第一阶段后，才另立开发计划；当前不开始 Phaser 或游戏代码。

在用户第一阶段评审完成前，继续维持 `implementationFrozen=true`。
