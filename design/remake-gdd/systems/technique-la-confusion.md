# `LA / 混亂` 系统规格

状态：`implemented`；原版玩家／AI 规则、持续状态、首领免疫、地图表现、确定性随机与
`REMAKE-013` 冰封例外已闭合，并通过完整自动与截图门禁；无实施必需 `[TBD]`。
2026-08-18 按用户试玩反馈复核 `0000:66F4`，撤销“手动单位不受混亂影响”的旧结论，
补上玩家点选路径与其上下文台词

负责人：Web 复刻实现

依赖：[`technique-implementation-sequence.md`](technique-implementation-sequence.md)、
[`shooting-and-technique-system.md`](../../../reverse/notes/shooting-and-technique-system.md)、
[`status-lifecycle.md`](../../../reverse/notes/status-lifecycle.md)、
[`remaining-technique-presentations.md`](../../../reverse/notes/remaining-technique-presentations.md)、
[`ai-decision-system.md`](../../../reverse/notes/ai-decision-system.md)、
[`technique-rules.json`](../../../reverse/parsed/native/technique-rules.json)、
[`ai-rules.json`](../../../reverse/parsed/native/ai-rules.json)、
[`remaining-technique-presentations.json`](../../../reverse/parsed/native/remaining-technique-presentations.json)、
[`web-remake-rule-decisions.md#remake-013冰封不可被攻治外圈不外推破邪可解除`](../../../reverse/gdd/web-remake-rule-decisions.md#remake-013冰封不可被攻治外圈不外推破邪可解除)、
[`web-remake-rule-decisions.md#remake-014敌方技术提示保留原文并对焦效果中心`](../../../reverse/gdd/web-remake-rule-decisions.md#remake-014敌方技术提示保留原文并对焦效果中心)

## 玩家目的

咒術師三层都可在五步内指定一名敌军，完整播放原版环绕鬼脸／星光后，把混亂计数重置
为 3。混亂不反转阵营，也不会让单位攻击友军：单位进入自动调度时，普通职业只尝试撤到
无相邻敌军且地形防御最高的可达空格；射击、技术、女帝／龍类只随机选一个可达格移动；
两类随后都消耗动作而不攻击、射击或施术。玩家点选被混亂的我方单位同样走这条路：单位
先用自己的肖像说出原文「我的頭好昏，無法思考．」，随后按同一套自动规则行动并消耗本
回合行动，命令菜单不会打开。龍、頭、手完整播放表现但免疫写入；施法固定取得
`14+random(0..3)` 经验。

## 证据与决策

- `[OF]` 原版菜单字节为 `LA / 混  亂 `，规范化显示“混亂”。咒術師第一层菜单为
  `1H/SA/LA`，第二层为 `1H/SA/LA/IP`，第三层为 `1H/SA/LA/IP/SN`，故 LA 三层均可用。
- `[OF]` 玩家分发处理器为 `0000:CEEB`，选择距离 5，目标组为敌方；成功时把目标
  `+0Eh` 状态字完整写为 `8003h`，重复施放刷新为 3，不叠加。
- `[OF]` `1P/龍`、`2P/頭`、`3P/手` 只免疫状态写入；免疫判断在表现返回后，故首领也
  必须完整播放 165 tick，并同样取得一次 `14+random(0..3)` 经验。
- `[OF]` 混亂 bit 15 在 AI 输入装载器 `1000:2300` 把逐槽行为覆写为 `FFh`。普通职业
  调用 `1000:1D67`，在正常移动范围内选无四邻敌军的空格并最大化地形防御，同值取后扫
  描格；射击、技术、女帝／龍类由 `1000:212D/216E` 按格号升序扫描非零移动范围格，取
  第一个 PIT bit 0 为 0 的格。两路都只移动／原地并消耗动作，不执行职业行动。
- `[OF]` 玩家选择器 `0000:55D3` 的四项判据（side 1、行动位未消耗、逐单位禁用值为 0、
  逐槽自动行为为 0）确实不读取 `+0Eh`，所以混亂单位仍能通过选择门；但被接受的格立即
  进入 `0000:66F4`，该处用与 AI 装载器 `1000:2300` 完全相同的 `SI=31A5h+8` 算址重读
  DS:`31ADh`。bit 15 为 0 时才落到正常手动流程 `0000:6733`；bit 15 为 1 时改为
  `mov ax,1Ch` 调 `0000:C97E` 播出上下文台词，再远调 `1000:1506` 把该单位单独送进
  `1000:2300` 装载与职业分派 `1000:1681`，返回后 `0000:560E` 清空选择。因此“手动单位不
  受混亂影响”是错误结论，已按机器证据撤销；没有阵营反转或友军攻击路径这一条不变。
- `[OF]` 该台词是 DS:`84BBh[1Ch]`→DS:`86ABh` 的「我的頭好昏，無法思考．」，使用当前
  单位肖像与所属阵营窗口（side 1 上窗、side 2 下窗），按逐字节奏自动关闭，没有确认菜单。
  它直接调用 `0000:C97E` 而不经过 `1000:254F`，所以不受“ＡＩ對話”开关 DS:`111Ch` 控制。
  机器结论见 `ai-rules.json` 的 `rules.playerConfusionRoute`。
- `[DD]` PIT bit 是影响棋盘结果的随机输入；Web 以版本化、可序列化模拟 PRNG 的逐次
  bit 抽样替代 PIT，保持“按格号升序逐格抽样，首个 0 命中”的调用次序。表现随机源、
  帧率与声音不得参与选格。
- `[OF]` 每个完整玩家＋我方自动＋敌方回合边界执行 `3→2→1→0`；破邪清除混亂。
- `[SR]` `REMAKE-013` 只禁止冰封单位行动以及被普通攻击、射击、单体伤害或治疗直接
  选中；混亂没有即时伤害，因此冰封敌军仍可成为目标并获得状态。其冻结阶段不进入 AI
  队列，完整轮边界照常消耗混亂计数；冰壳始终绘制在混亂演出之上。
- `[OF]` 表现入口 `1000:76D6` 使用 `MAGIC/44`：11 个 `3×2` 描述符都以目标
  `(-1,-1)` 为锚点，依次使用素材帧 `0..47`、`30..47`、`36..47`、`42..47` 的证据序列；
  每 draw 等待 15 tick，共 165 tick，无 VOC。状态、经验和行动位只能在表现后提交。
- `[OF]` AI 参数行为 22、敌方目标、距离 5、选择器 `1000:0A7B`，提示原文“混亂.”；
  咒術師三层 AI 池与玩家池同序。

## 触发与输入

- 三层咒術師都显示“混亂”；行动者必须未行动、未冻结且未被禁咒。
- 目标必须是距离 5 内存活敌军；冻结敌军和首领免疫目标合法，友军、死亡单位和距离 6
  不合法。
- 取消目标回技术菜单，再取消回动作菜单；不提交状态、经验、行动位或 PRNG。
- 确认时冻结 `{目标, 免疫结果, 状态前后值, 经验随机值, PRNG 前后状态}`；165 tick 表现
  期间不能重新准备结果。

## 有序规则

1. 验证咒術師职业、行动位、禁咒、敌方目标与距离 5；冻结只限制行动者；
2. 复制目标状态；若职业不是龍、頭、手，把 `confusion` 重置为 3，否则保持不变；
3. 不论免疫与否，恰好调用一次模拟 PRNG，准备 `14+random(0..3)` 经验；
4. 播放 `MAGIC/44` 十一个 `3×2` 描述符，每项完整保持 15 native ticks；
5. 165 tick 后原子提交目标状态、施法者经验和行动位；
6. 自动单位准备行动时，先于路线、军团 doctrine、低生命休息与职业池检查混亂：普通类
   选择防御撤退格，其他三类按升序候选逐格抽 PRNG bit；移动后或无格时都只消耗动作；
7. 玩家点选被混亂的我方单位时按同一顺序处理：先通过原版四项选择判据，再播放上下文
   台词，然后走第 6 条的同一套自动规则并消耗行动；不打开命令菜单、不进入移动或攻击
   选择。完整轮边界把 `confusion` 饱和减 1，破邪可立即清零。

## AI 使用

咒術師的原版层级池继续界定 LA 在哪些层级可选：第一层 `1H/SA/LA`，第二层
`1H/SA/LA/IP`，第三层 `1H/SA/LA/IP/SN`。`REMAKE-033/037` 之后池内不再抽签，选哪一项
以及选哪个目标由共享专家效用决定，也不再使用 LA 自己的原生选择器；完整规则见
[`expert-enemy-ai.md`](expert-enemy-ai.md#与逐技术规格的关系)。距离 5、首领免疫不预过滤、
显示“混亂.”以及共用结算与表现都不变。side 1 自动咒術師使用同一规划器，层级门禁分别生效。

LA 的控制估值 `100 + 威胁/2` 高于同池的 `SA`（`40 + 威胁/4`）与通常情况下的 `IP`
（`80 + 生命/4`），因此只要目标未混乱且不免疫混乱，咒術師就先出 LA；混亂已在目标身上时
LA 记为重复，让位给同层级的次优项。`legacyStrict` 可保留原版抽签与原生选择器。

## 规则集与 Mod

| 配置 | 行为 |
| --- | --- |
| `stableRemake` | 原版 FFh 两类 AI 覆写；游戏随机映射到可保存 PRNG；冻结可受状态但不行动 |
| `legacyStrict` | 行为算法相同；诊断层可展示原 PIT 来源，但运行时仍不能依赖宿主时钟改变存档结果 |
| 可开放 Mod 字段 | 职业层级、距离、计数、免疫表、经验、AI 池／类别／选格、帧序与等待；修改后必须改变规则身份 |

## 表现与可访问性

正式地图与 technique-lab 复用固化的 `MAGIC/44` 48 张原版素材及 11 个描述符。像素禁用
平滑，不添加声音。冰封目标的冰壳位于全部混亂帧上方。加速、减少动态和跳过演出只缩放
墙钟，不得改变 11 draw、165 tick、免疫结果、PRNG 次数或提交时点。

## 验收场景

1. 咒術師三层菜单都含 LA，选择距离为 5；其他职业不可用；
2. 普通、冻结和龍／頭／手敌军可选；友军、距离 6 与死亡单位不可选；
3. 普通目标重置混亂为 3；首领完整演出但状态不变；两者都取得 `14..17` 且一次 PRNG；
4. 165 tick 前状态、经验和行动位不提交；11 个 `3×2` 描述符与 15 tick 保持逐项一致；
5. 普通混亂 AI 只撤向无相邻敌人的最高地形防御格；无合法格原地耗动作且不攻击；
6. 射击／技术／女帝／龍类按升序候选和确定性随机只移动，不射击／施术；存读档后续一致；
7. 点选被混亂的我方单位：先出现「我的頭好昏，無法思考．」上下文台词，随后该单位按自动
   规则移动或原地停留并消耗行动，命令菜单始终不打开；“ＡＩ對話”关闭时台词照常出现；
   完整轮边界 `3→2→1→0`，破邪清零；
8. 冻结目标可被施加混亂但不进入行动队列，冰壳高于鬼脸／星光，计数照常递减；
9. 敌方 AI 层级池不压缩；目标未混乱且不免疫时，专家规划确定性选择 LA 并显示“混亂.”；
10. 正式竞技场与 technique-lab 的帧序、锚点、无声音、免疫与结果读数一致。

## 验证记录

2026-08-05 已完成：生成器固化 `MAGIC/44` 48 帧与 11 个描述符；单元测试覆盖距离／层级、
冻结可选、首领免疫、一次经验随机、完整轮计数、手动玩家例外、普通／技术类 FFh 自动
调度、确定性重放与警戒组不误触发。正式竞技场验证 165 tick 后才提交、11 次
`statusEffect`、零声音、目标状态、经验及混亂敌军下一行动只移动；technique-lab 验证
末帧保持、免疫读数与冰壳上层深度。实际运行 `pnpm test`（37 文件、456 项）、
`pnpm build`、`pnpm test:e2e`（197 项）、`node reverse/tools/angel2-phase1-verify.mjs`、
`git diff --check` 全部通过；人工查看 `arena-confusion-faces.png`、
`technique-lab-confusion.png` 与 `technique-lab-confusion-frozen.png`，未发现构图或层级回归。

2026-08-18 修正：用户试玩反馈原版点选混亂单位会直接乱走并先说话。复核模块 29 后确认
`0000:66F4` 是被漏掉的第二个混亂消费者，撤销上一轮“手动玩家例外”结论。提取器新增
`rules.playerConfusionRoute` 并固化 `0000:55D3`／`0000:66F4`／`1000:1506` 三段机器码与
`DS:84BB[1Ch]`／`DS:86AB` 两段数据；运行时新增上下文台词与点选后自动行动。实际运行
`angel2-ai-rules` 提取、`angel2-phase1-verify.mjs`、`pnpm docs:check`、
`status-presentations`／`ai-technique-dialogue` 单元测试与 `arena-ailments.spec.ts`
的点选混亂用例，并人工查看 `arena-confused-player-line.png`。
