# `4C / 究級冰雪` 系统规格

状态：`implemented`；规则、AI、原版表现与 `stableRemake` 冰封例外已闭合并通过完整门禁

负责人：Web 复刻实现

依赖：[`technique-implementation-sequence.md`](technique-implementation-sequence.md)、
[`technique-3c-advanced-ice.md`](technique-3c-advanced-ice.md)、
[`web-remake-rule-decisions.md#remake-009同一动作不因阵营或控制方式改变规则参数`](../../../reverse/gdd/web-remake-rule-decisions.md#remake-009同一动作不因阵营或控制方式改变规则参数)、
[`web-remake-rule-decisions.md#remake-013冰封不可被攻治外圈不外推破邪可解除`](../../../reverse/gdd/web-remake-rule-decisions.md#remake-013冰封不可被攻治外圈不外推破邪可解除)、
[`web-remake-rule-decisions.md#remake-014敌方技术提示保留原文并对焦效果中心`](../../../reverse/gdd/web-remake-rule-decisions.md#remake-014敌方技术提示保留原文并对焦效果中心)、
[`shooting-and-technique-system.md`](../../../reverse/notes/shooting-and-technique-system.md)、
[`technique-presentations.md`](../../../reverse/notes/technique-presentations.md)、
[`ai-decision-system.md`](../../../reverse/notes/ai-decision-system.md)、
[`technique-rules.json`](../../../reverse/parsed/native/technique-rules.json)、
[`technique-presentations.json`](../../../reverse/parsed/native/technique-presentations.json)、
[`ai-rules.json`](../../../reverse/parsed/native/ai-rules.json)

## 玩家目的

第三层巫師用一次行动以自身为中心释放究級冰雪。效果中心值 6，向外五圈依次扩散；未受
保护的敌军按原版方向尝试向外推一格并冰封。默认复刻中值 1 的最外圈只冰封、不被推出
范围；已经冰封的单位完全不刷新、不移动、不消费防魔。

## 证据与决策

- `[OF]` 巫師 `2L` 第一／二／三层玩家菜单和 AI 池分别只有 `2C/3C/4C`；`4C` 只属于
  第三层。玩家入口 `0000:75FA..7620/76B0` 绕过目标格选择，把行动者格直接写成中心，
  分发表选择字 4 不被这条路径消费。
- `[OF]` 效果使用 mode `0`、半径 6 的曼哈顿范围：中心值 6，距离 `1..5` 的圈值依次
  为 `5/4/3/2/1`。只处理敌军，不扣生命。
- `[OF]` 目标按行优先线性格升序处理；未免疫目标依次尝试下、上、左、右，取第一个空闲、
  可通行且范围值更低的邻格。前一单位的准备位移立即参与后一单位的占格判断。
- `[OF]` 原版已有行动禁用、防魔或职业为龍／頭／手的目标不位移；防魔目标结算后消费
  防魔。其他目标即使没有合法退格仍写行动禁用位。至少一名单位成功位移时只取一次
  `15 + randomBelow(3)`；无人位移时经验为 0 且 PRNG 不前进。
- `[SR]` 严格采用 `REMAKE-013`：已有冰封不叠加、不刷新、不延长，不移动且不消费防魔；
  目的格必须仍有正范围值，故最外圈不能移入值 0。新冰封持续到目标跳过紧接着的一次本
  阵营阶段，期间不能行动或被攻治，范围伤害／治疗也完整跳过；`TR/破邪` 可提前解除。
- `[OF]` AI 第三层巫師池只有 `4C`。原版 AI 先用当前位置距离 5 的敌军候选决定是否施放，
  但执行中心仍是巫師格；候选沿最低有效防御、最低生命、路径与线性格顺序破平。
- `[OF]` AI 成功使用组 12 原文“看我的冰魔法.”；Web 依 `REMAKE-014` 对焦准备结果中的
  施法者中心，而不是 AI 候选格。
- `[OF]` 四档冰雪共用 `MAGIC/10 + UN/50`。`4C` 依次只绘制距离中心 `1/2/3/4/5`
  的五圈；每圈六帧、每帧 10 native tick，且每圈开始请求一次 `UN/50`。总计 30 draw、
  5 次声音、300 tick；末帧等待结束后才提交规则结果并显示持久冰壳。

## 触发与输入

- 进入条件：巫師第三层、未行动、未冰封且未被禁咒；菜单只列“究級冰雪”，第一／二层
  分别保留 `2C/3C`，不得回退显示其他层级动作。
- 玩家确认技术即以行动者格准备，不进入目标选择；取消技术菜单只回动作菜单，不改变
  位置、状态、行动位、经验或 PRNG。
- 玩家、我方自动和敌方 AI 共用 `ice-4` action definition、准备器与原子提交器。AI 候选
  只决定是否施放和对白提示；送入准备器时必须省略 targetId。
- 确认时冻结中心、范围图、行优先接收者、顺序占格、位移／冰封／防魔结果和 PRNG 前后态；
  动画、镜头、音频、墙钟与资源完成顺序不能重算结果。

## 有序规则

1. 验证行动者为巫師第三层，未行动、未冰封、未禁咒；玩家中心只能等于行动者格；
2. AI 额外要求当前位置距离 5 内存在未冰封敌军候选，但候选不改变中心；
3. 以行动者格建立半径 6 范围图，按线性格升序收集范围内敌军；
4. 已冰封目标完整跳过且保留防魔；防魔目标不移动／冰封并准备清盾；龍／頭／手免疫；
5. 其余目标按下、上、左、右找第一格合法更低正值目的地；成功立即更新准备占格，失败
   仍准备冰封，最外圈只能冰封；
6. 位移数大于 0 时调用一次版本化 PRNG 得到 `15..17` 经验，否则经验 0、调用 0；
7. 播放五轮 `MAGIC/10` 六帧扩散，每轮开始请求 `UN/50`，模拟仍保持准备前状态；
8. 300 tick 与末帧保持完成后原子提交位置、防魔、冰封、经验、PRNG 和行动位；冰壳持续
   到自然解除或破邪提交。

## AI 使用

第三层巫師原版池只有 `4C`。规划器从当前位置以距离 5 查找未冰封敌军；多候选沿用原版
评分，但准备结果的 `target` 始终是巫師格。成功后对焦自身中心、显示组 12 原文、播放五圈
300 tick 表现并共同提交。第一／二层不使用 `4C`；无候选、冰封或禁咒时进入 ordinary AI，
且不消费位移经验随机。

## 规则集与 Mod

| 配置 | 行为 |
| --- | --- |
| `stableRemake` | 固定自身中心；AI 候选门 5；冰封不可攻治／叠加，最外圈不外推，破邪可解除 |
| `legacyStrict` | 固定自身中心；AI 候选门 5；最外圈可外推，冰封仍可受攻治且破邪不解冰 |
| 可开放 Mod 字段 | 职业层级、AI 候选距离、效果半径、位移顺序、经验、资源／声音／等待；修改后必须改变规则身份 |

## 表现与可访问性

正式地图和 technique-lab 共用六帧 `MAGIC/10`，但任一时点只画当前距离圈，前圈不残留；
不能缩放 `3C` 四圈画面或同时铺满菱形。五次 `UN/50` 受战斗音效开关控制。冰壳是独立
持续状态投影，后续范围演出不能暂时隐藏。加速、减少动态和静音只改变墙钟或声音，不能
改变 30 draw、五圈顺序、300 tick、位移、经验、PRNG 或提交时点。

## 验收场景

1. 三层巫師分别只显示／规划 `2C/3C/4C`；`4C` 仅第三层可用；
2. 距行动者 `1..5` 格目标的效果值为 `5/4/3/2/1`，生命不变，并按行优先和下上左右准备；
3. 内圈目标有合法更低正值邻格时外移，值 1 外圈只冰封；有任意位移只取一次 `15..17`，
   无人位移则经验／PRNG 均为 0；
4. 已冰封目标位置、解除时点、防魔和生命不变；防魔目标不冰封且清盾；龍／頭／手免疫；
5. 新冰封单位不能成为攻治目标，范围伤害／治疗覆盖时完整跳过，下一次本阵营阶段后解除，
   或由 `TR` 提前解除；
6. AI 第三层巫師在距离 5 有候选时选择 `4C`，显示组 12 原文并以自身为中心；其他层级或
   无候选时不使用 `4C`；
7. 正式玩家与 AI 依次只显示五个距离圈，30 draw、5 次 `UN/50`、300 tick 后原子提交，
   完成后冰壳持续可见；
8. technique-lab 可 seek 第五圈并推进阵营阶段解除冰壳；代表截图确认第五圈 20 格、像素
   边缘、单位层级和战场裁切正确，而不是四圈素材缩放。

## 验证记录

- `scripts/generate-stage1-actions.mjs` 锁定玩家自中心、AI 候选距离 5、半径 6、五圈
  `5/4/3/2/1`、经验 `15..17`、组 12、五轮 `MAGIC/10` 与 `UN/50`，生成正式
  `ice-4` 动作定义和 30 draw／300 tick 时间线。
- 模拟与竞技场单测覆盖巫師第三层菜单／双方 AI、自中心执行、五圈范围、行优先和下上左右
  位移、最外圈只冰封、任意位移单次 PRNG、无人位移零经验／零 PRNG，以及
  `REMAKE-013` 下既有冰封完整跳过并保留防魔；完整覆盖率为 37 文件／431 项测试通过。
- 正式竞技场玩家与 AI 覆盖组 12“看我的冰魔法.”、AI 距离 5 候选门、五次声音、30 次
  逐圈绘制和 300 tick 原子提交；玩家外圈与 AI 无位移场景分别确认外圈单位不被推出、
  冰封生效且经验／PRNG 不被虚增。
- 人工查看 `arena-ice-4-outer-ring.png`、`arena-ice-4-ai.png` 与
  `technique-lab-ice-4-outer-ring.png`：第五圈精确显示 20 格，AI 以自身为中心且原文正确，
  素材保持逐圈像素边缘和战场裁切，没有退化为四圈缩放或全菱形同时铺满。
- 2026-08-05 通过生产构建、第一阶段证据校验和 174／174 项完整 Chromium 回归；玩家、
  AI、实验室第五圈以及冰封例外均在同一完整回归中通过。
