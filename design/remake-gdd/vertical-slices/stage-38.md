# 隱藏關「異世界」

版本：Draft 0.1

狀態：`[OF]/[SR]/[DD] 已閉合；M74 已指定，M75 已驗證，等待用户试玩`

## 1. 範圍

本切片承接 stage 49 主線結局完成後的 `stage-38` 完成檔，實作原版最後追加戰、戰後
制作人員表與 `The End` 終端畫面。它是主線之後的獨立戰鬥，不修改戰績分支，也不把
模块 46 的不可達父状态写入当作可重玩路线。

## 2. 玩法合同

- `[OF]` 地图模板为 B/0077，固定两名我方单位：妮雅槽 0 `(29,21)`、希蜜槽 1
  `(30,21)`；18 个可部署格，名单标志槽为 `2..21,25..31`，最多 20 人。
- `[OF]` 初始敌方为 44 名静态单位，槽位、职业记录、地图坐标和 side 2 角色描述符均由
  机器生成内容固化。槽 `2..7,15..24` 共 16 名沿用历代角色的原版姓名与专属肖像；其余
  28 槽的描述符肖像为 `FFh`，按原版同时回退到职业名和职业肖像。没有回合增援、替补或
  形态转换。
- `[OF]` 44 名敌军随 B/0077 战场一起建立，不在悼词后动态生成。第 1 回合先按
  `focusPortraitResource 46` 聚焦妮雅槽 0 `(29,21)`，Web 镜头原点为 `(25,18)`；该视野
  内没有敌军。随后播放 SAY/0164，悼词后的诡异笑声只在叙事上揭示已经存在的敌军；现场
  胜利播放 SAY/0165，读取完成档时不重播。
- `[OF]` 胜利条件为 side 2 全灭；失败条件为妮雅槽 0 离场，失败优先。
- `[OF]` 玩家音乐为 `MUSIC/33 -> MUSIC/32`，敌方音乐为 `MUSIC/5 -> MUSIC/4`。
- `[OF]` 胜利后进入模块 46：七页制作人员表，共八次不可跳过的 400 步 CRTC 转场，随后
  `UN/54` 永久 `The End` 动画；制作人员表音乐为 `UN/55`。片尾没有点击、Enter、Space
  或退出输入，Web 的 `presentationFast`／测试加速只缩短等待，不改变八段顺序。
  Web 的 `640×350` 片尾窗口与其它关卡／结局页面一样在可用页面宽度内水平居中。
  其后写入 `module27/stage38` 的代码不可达，不实现为再次进入战斗。
- `[SR]` 敌方 44 人共用正式专家追击 AI；表现设置只能改变等待，不改变模拟、PRNG 或存档。
- `[DD]` 追加战完成档使用新的 `stage-39` 终端路由标识；它只用于恢复制作人员表／终幕，
  不产生伪造的可玩关卡存档。

## 3. 生成与验收

- 内容来源：`reverse/decoded/B/0077/00.raw`、`battle-templates.json`、`battle-objectives.json`、
  `battle-lifecycle.json`、`stage-events.json`、`music-catalog.json`、SAY/0164/0165、
  `B/0088`、`C/0033`、`UN/0054`、`UN/0055`、`ending-presentations.json`。
- 必须验证名单槽与固定棋盘槽分离、44 名敌军在 SAY/0164 前已建立、16 名历代角色的姓名／
  肖像与 28 名职业回退、开场聚焦妮雅、无增援、SAY 检查点、目标优先级、v73 存档
  恢复、普通入口部署／战斗／胜利、八次自动片尾转场、`UN/55` 音乐和模块 46 终端画面。
- 试玩不能把结局后不可达写入误认为隐藏关重玩入口。
