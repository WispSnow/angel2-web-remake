# 系统规格模板：`<系统名称>`

状态：Draft

负责人：`<待填>`

依赖：`<规则/内容>`

## 玩家目的

`<玩家为何使用此系统，它提供什么决策或反馈>`

## 证据与决策

- `[OF] <原版事实与链接>`
- `[SR] <默认修复与决策编号>`
- `[DD] <复刻新增决策>`
- `[TBD] <不得由实现猜测的问题>`

## 触发与输入

- 进入条件：
- 合法行动者：
- 合法目标：
- 取消边界：
- 提交时点：

## 有序规则

1. `<验证>`
2. `<随机/取整>`
3. `<结算>`
4. `<状态更新>`
5. `<后续消费者>`

## 状态变化与边界

| 情况 | 结果 | UI 反馈 |
| --- | --- | --- |
| 正常 |  |  |
| 无效目标 |  |  |
| 免疫/阻挡 |  |  |
| 致死/终局 |  |  |
| 保存/读取 |  |  |

## AI 使用

`<AI 是否使用同一合法性、数值和结算规则；目标选择有何不同>`

## 规则集与 Mod

| 配置 | 行为 |
| --- | --- |
| `stableRemake` |  |
| `legacyStrict` |  |
| 可开放 Mod 字段 |  |

## 表现与可访问性

`<必须播放/显示什么；哪些可以加速或关闭；如何不改变模拟>`

## 验收场景

1. Given `<初态>`，When `<动作>`，Then `<精确结果>`；
2. Given `<边界初态>`，When `<动作>`，Then `<精确结果>`；
3. 保存/读取与表现设置差异不会改变结果。
